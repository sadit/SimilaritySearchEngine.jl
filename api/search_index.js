var documenterSearchIndex = {"docs":
[{"category":"section","location":"server.html#SimilaritySearchServer-API","page":"Server API","text":"The service layer: the HTTP API, the job spool, the two command lines, and the supporting machinery for access tokens, telemetry and result cursors. It is a separate package, in server/ of this same repository, and it reaches the engine only through the public API documented under Engine API – the same surface any other caller has.\n\nProse first, if you are arriving here cold: the server manual explains how a dataset is held and how failures are reported, and the server tutorial walks a corpus from create to a query over HTTP.","title":"SimilaritySearchServer API"},{"category":"section","location":"server.html#Package-level-API","page":"Server API","text":"Configuration, the similarity-search and similarity-search-ctl command lines, and the handlers each subcommand dispatches to.","title":"Package-level API"},{"category":"section","location":"server.html#Server","page":"Server API","text":"The HTTP layer: AppState (one EmbeddedEngine per dataset), the endpoint handlers, and the mapping from the engine's typed errors to status codes.","title":"Server"},{"category":"section","location":"server.html#Jobs-and-Executors","page":"Server API","text":"The job spool – an atomic directory tree of .job.toml files – and the executor that dispatches each job as an independent Julia subprocess.","title":"Jobs and Executors"},{"category":"section","location":"server.html#Tokens,-Telemetry-and-Cursors","page":"Server API","text":"Access tokens, the op_log operation history (whose distance_evaluations figure comes from the engine's SearchStats), and server-side cursors for paging a materialized result set.","title":"Tokens, Telemetry and Cursors"},{"category":"method","location":"server.html#SimilaritySearchServer._command_fields-Tuple{ArgParse.ArgParseSettings, String}","page":"Server API","text":"_command_fields(settings::ArgParseSettings, cmd_name::String) -> Vector{ArgParse.ArgParseField}\n\nThe real, promptable arguments of subcommand cmd_name – every field of its own args_table.fields except an auto-added --help/-h flag, if one is present (kept as a guard even though empirically none of this app's subcommand settings currently carry one).\n\n\n\n\n\n","title":"SimilaritySearchServer._command_fields"},{"category":"method","location":"server.html#SimilaritySearchServer._command_menu_names-Tuple{ArgParse.ArgParseSettings}","page":"Server API","text":"_command_menu_names(settings::ArgParseSettings) -> Vector{String}\n\nThe ordered list of top-level subcommand names settings declares (declaration order, via its own args_table.fields, not args_table.subsettings's Dict – dict iteration order isn't guaranteed), excluding interactive (itself). serve never appears in either settings object this is called on (build_settings/build_ctl_settings) – it has its own genuinely separate ArgParseSettings (build_serve_settings, chunk 21) that this function is never called against at all, since run_interactive_serve has no command menu (a single-purpose binary with exactly one thing to configure has no business being one menu item among many, see this file's header note).\n\n\n\n\n\n","title":"SimilaritySearchServer._command_menu_names"},{"category":"method","location":"server.html#SimilaritySearchServer._convert_value-Tuple{ArgParse.ArgParseField, AbstractString}","page":"Server API","text":"_convert_value(f::ArgParse.ArgParseField, raw::AbstractString)\n\nConverts a raw string answer to the type parse_args itself would have produced for this field – f.arg_type is Int64/Float64 for --k/--min-k/--epsilon-style fields (confirmed by introspecting build_settings() directly) and Any (meaning: pass the string through as-is) for everything else. Booleans never reach here – see _prompt_field's own :store_true branch.\n\n\n\n\n\n","title":"SimilaritySearchServer._convert_value"},{"category":"method","location":"server.html#SimilaritySearchServer._ctl_poll_job-Tuple{String, String}","page":"Server API","text":"_ctl_poll_job(base_url, job_id; interval=0.3, max_attempts=600) -> Union{String, Nothing}\n\nPolls GET /api/v1/jobs/{job_id} until it reaches a terminal status (completed/ failed), returning that status – or nothing on a request failure or if it never reaches one within max_attempts. Backs ctl_dump/ctl_load: unlike the heavy compute job kinds (allknn/etc, which -ctl has no submit command for at all – only queue/result/block/resume/kill/gc against a job already submitted some other way), dump/load are meant to feel synchronous to the operator running them, so these two block and wait rather than just printing a job_id back.\n\n\n\n\n\n","title":"SimilaritySearchServer._ctl_poll_job"},{"category":"method","location":"server.html#SimilaritySearchServer._ctl_request-Tuple{Symbol, String}","page":"Server API","text":"_ctl_request(method::Symbol, url::String; body=nothing) -> Union{HTTP.Response, Nothing}\n\nShared error handling for every -ctl command: a non-2xx response prints the server's own JSON error body and returns nothing (so the caller can just check for that and return 1); a connection failure prints PLAN.md §1's own \"server not reachable\" wording rather than a raw stacktrace, also returning nothing.\n\n\n\n\n\n","title":"SimilaritySearchServer._ctl_request"},{"category":"method","location":"server.html#SimilaritySearchServer._dense_distance_stats-Tuple{Any}","page":"Server API","text":"_dense_distance_stats(handle) -> Union{Nothing, Dict}\n\nReal, sampled nearest-neighbor distance statistics (min/mean/max over up to the first 100 items' own nearest live neighbor) – the \"distance statistics ... to help people know about their data\" PLAN.md §1's describe note asks for. nothing if the index has fewer than 2 items (no pair to measure) or isn't dense.\n\n\n\n\n\n","title":"SimilaritySearchServer._dense_distance_stats"},{"category":"method","location":"server.html#SimilaritySearchServer._dense_index_kind_name-Tuple{SearchGraph}","page":"Server API","text":"_dense_index_kind_name(index) -> String\n\nBest-effort index_kind string for a dense index instance, matching the strings IndexEngine.create_engine/the HTTP API's index_type accept – used by execute_dump only when a dataset has no descriptor.json to read the original creation-time string back from (CLI-built datasets never write one, see the two-layout watch-out elsewhere in this file).\n\n\n\n\n\n","title":"SimilaritySearchServer._dense_index_kind_name"},{"category":"method","location":"server.html#SimilaritySearchServer._discover_project_ids-Tuple{String}","page":"Server API","text":"_discover_project_ids(workdir::String) -> Vector{String}\n\nScans both on-disk dataset layouts this app has (see _load_engine_for_job's docstring in cli_handlers.jl): <workdir>/datasets/* (HTTP-created) and <workdir>/* with a sibling CURRENT (a RocksDB project directory). This is the dynamic-pre-fill piece PLAN.md's Interactive Mode section requires – offering real, currently-valid ids instead of a blank field the operator has to remember or describe/list separately first.\n\n\n\n\n\n","title":"SimilaritySearchServer._discover_project_ids"},{"category":"method","location":"server.html#SimilaritySearchServer._format_ctl_invocation-Tuple{Vector{String}, AbstractDict, Vector{ArgParse.ArgParseField}}","page":"Server API","text":"_format_ctl_invocation(path, cmd_args, fields) -> String\n\n-ctl counterpart of _format_invocation: same \"omit a field still at its declared default\" logic, but the command name is a path (one segment for most commands, two for jobs <sub> – the only nested command tree either binary has) rather than always one name, and the printed prefix is the -ctl binary, not similarity-search.\n\n\n\n\n\n","title":"SimilaritySearchServer._format_ctl_invocation"},{"category":"method","location":"server.html#SimilaritySearchServer._format_invocation-Tuple{String, AbstractDict, Vector{ArgParse.ArgParseField}}","page":"Server API","text":"_format_invocation(cmd_name, cmd_args, fields) -> String\n\nRenders the exact non-interactive-equivalent CLI invocation for the confirm-before-run step, in fields' declared order, omitting a field whose collected value equals its declared default (so the printed command is the shortest one that reproduces the same cmd_args, exactly like a human tuning flags by hand would omit the defaults).\n\n\n\n\n\n","title":"SimilaritySearchServer._format_invocation"},{"category":"method","location":"server.html#SimilaritySearchServer._is_new_id_field-Tuple{String, String}","page":"Server API","text":"_is_new_id_field(cmd_name, dest_name) -> Bool\n\nWhether --dataset for this subcommand names a dataset to create (build's target, or load's restore target) rather than an existing dataset to select from the workdir. Only used to decide whether _discover_project_ids's menu applies – everywhere else, --dataset refers to something that should already exist.\n\n\n\n\n\n","title":"SimilaritySearchServer._is_new_id_field"},{"category":"method","location":"server.html#SimilaritySearchServer._live_items-Tuple{Any, Any}","page":"Server API","text":"_live_items(ds, engine) -> (all_ids::Vector{Int}, live::Vector{Tuple{Int,Dict}})\n\nScans ds's metadata column family once, returning every docid currently stored (`allids, needed to wipe stale rows during a rebuild) alongside the reconstructed raw item (declaredfieldsmerged with the decodedextraJSON -- seeSchema.MetadataRecord`) for every live (non-tombstoned) one, sorted by original docid.\n\n\n\n\n\n","title":"SimilaritySearchServer._live_items"},{"category":"method","location":"server.html#SimilaritySearchServer._open_handle-Tuple{String, String}","page":"Server API","text":"_open_handle(project_id, workdir; read_only=false) -> Union{Nothing, SSE.EmbeddedEngine}\n\nReopens a dataset by id, whichever of this codebase's two layouts it lives under – <workdir>/datasets/<id> for one created over HTTP, <workdir>/<id> for one built by the CLI. Prints an error and returns nothing when neither exists.\n\nThis replaced a snapshot file per dataset, written by build and by every HTTP append, and read by each command that operated on a whole dataset. The engine persists its own index, so that file was a second and older copy of state that already had a place to live, and a job that ran before a snapshot had been written failed with \"snapshot not found\". The functions that wrote and read it were removed on 2026-09-13, together with the JLD2 dependency.\n\n\n\n\n\n","title":"SimilaritySearchServer._open_handle"},{"category":"method","location":"server.html#SimilaritySearchServer._prompt_field-Tuple{String, ArgParse.ArgParseField, AbstractString}","page":"Server API","text":"_prompt_field(cmd_name, f, workdir) -> Any\n\nPrompts for one argument, choosing among four presentations, in this priority order:\n\nAn existing-dataset menu (_discover_project_ids) when f is a --dataset field that refers to something that should already exist (!_is_new_id_field) and the workdir actually has candidates – falls through to free text otherwise (empty workdir, or a dataset-creating field). Skipped entirely when allow_dataset_discovery=false (see run_interactive_ctl – a --dataset field there names something on a remote server, not a local workdir this process can readdir).\nA fixed-choice menu (CLI_CHOICES) when f.dest_name has one declared.\nA Yes/No menu for a boolean (:store_true action) field.\nFree text via readline, pre-filled as a shown default, not an editable inline field – accepted on a bare Enter, exactly PLAN.md's \"shows and accepts its default\" wording; REPL.TerminalMenus/the stdlib have no portable \"editable pre-filled text field\" primitive, so this is the honest, simplest thing that satisfies the actual requirement.\n\n\n\n\n\n","title":"SimilaritySearchServer._prompt_field"},{"category":"method","location":"server.html#SimilaritySearchServer._prompt_with_default-Tuple{String, AbstractString}","page":"Server API","text":"_prompt_with_default(label::String, current::AbstractString) -> String\n\nFree-text prompt pre-filled with current as the shown default (Enter accepts it, the same convention _prompt_field's free-text branch already established) – used by run_interactive_serve for its three plain host/port/workdir fields, whose \"current value\" comes from the loaded --config TOML file rather than a static ArgParseField default the way every other interactive prompt in this codebase works.\n\n\n\n\n\n","title":"SimilaritySearchServer._prompt_with_default"},{"category":"method","location":"server.html#SimilaritySearchServer.build_ctl_settings-Tuple{}","page":"Server API","text":"build_ctl_settings() -> ArgParse.ArgParseSettings\n\nThe similarity-search-ctl command line: the control-plane subcommands that talk to a running server over HTTP (list, stats, log, the token commands, and the jobs subtree). The counterpart of build_settings, which is the data plane against a working directory, and introspected by interactive.jl the same way.\n\n\n\n\n\n","title":"SimilaritySearchServer.build_ctl_settings"},{"category":"method","location":"server.html#SimilaritySearchServer.build_serve_settings-Tuple{}","page":"Server API","text":"build_serve_settings() -> ArgParseSettings\n\nsimilarity-search-serve's own settings object (PLAN.md §1, chunk 21) – a genuinely different, separate ArgParseSettings from build_settings()'s data-operations one, not a shared parser both binaries happen to use. serve is a thin, single-purpose binary (PLAN.md's own framing): it has exactly two commands, serve and interactive, and none of similarity-search's eleven data-operation subcommands (build through load) are reachable through it at all – similarity-search-server build ... is now an ArgParse \"unrecognized command\" error, not a working (if undocumented) escape hatch, same as how plain similarity-search serve is now the mirror-image error (serve isn't one of build_settings()'s commands either). --host/--port/--workdir are all optional (default = nothing) – an omitted flag means \"fall back to --config's TOML file, then a hardcoded literal,\" layered in main_serve/run_serve, not here (an ArgParseField default can only be one fixed value, not \"whatever the config file says\"). This is also what gives run_interactive_serve (interactive.jl) real fields to introspect at all – PLAN.md §1's Interactive Mode section's own prerequisite for serve's guided form.\n\n\n\n\n\n","title":"SimilaritySearchServer.build_serve_settings"},{"category":"method","location":"server.html#SimilaritySearchServer.build_settings-Tuple{}","page":"Server API","text":"build_settings() -> ArgParse.ArgParseSettings\n\nThe similarity-search command line: every data-plane subcommand (build, search, searchbatch, allknn, fft, neardup, hsp, closestpair, describe, rebuild, dump, load) with its options. Built fresh on each call rather than held in a constant, because interactive.jl introspects the same object to generate its guided forms.\n\n\n\n\n\n","title":"SimilaritySearchServer.build_settings"},{"category":"method","location":"server.html#SimilaritySearchServer.cli_exit_code-Tuple{EngineError}","page":"Server API","text":"cli_exit_code(e::SSE.EngineError) -> Cint\n\nThe CLI half of the error boundary (the HTTP half is Server.engine_error_response): an engine error becomes an exit code by its category, so a shell script can branch on why a command failed without reading its output.\n\n2  an invalid request -- wrong arguments, wrong kind of project. Retrying will not help.\n4  something named does not exist.\n5  a conflicting state -- a staged backlog, an untrained profile. The same command works\n   once the project is in a different state.\n70 a storage failure (sysexits' EX_SOFTWARE): stored data this version cannot read.\n\n1 stays what it always was here: this command's own refusal, decided before the engine was ever called (no such dataset directory, unreadable input file).\n\n\n\n\n\n","title":"SimilaritySearchServer.cli_exit_code"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_add_token-Tuple{String, Dict}","page":"Server API","text":"ctl_add_token(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl add-token [--user] [--permissions] [--expires-at] -> POST /api/v1/admin/tokens. --permissions is a comma-separated list on the CLI, split into a real array for the JSON body (\"\" becomes an empty permission list, matching the endpoint's own default).\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_add_token"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_del_token-Tuple{String, Dict}","page":"Server API","text":"ctl_del_token(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl del-token --token <token> -> DELETE /api/v1/admin/tokens/{token}.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_del_token"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_dump-Tuple{String, Dict}","page":"Server API","text":"ctl_dump(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl dump --dataset <id> -> submits a dump job (POST /api/v1/jobs/dump), blocks until it finishes, then prints its result – a JSON pointer (bundle_dir + the parsed manifest.json) rather than the bundle's raw file contents, since the bundle is a directory on the server's filesystem, not something meant to be streamed to the CLI's caller (see handle_get_job_result's isdir branch).\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_dump"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_block-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_block(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs block --job-id <id> -> POST /api/v1/jobs/{id}/block.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_block"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_gc-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_gc(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs gc [--retention-seconds] -> POST /api/v1/admin/jobs/gc.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_gc"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_kill-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_kill(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs kill --job-id <id> -> POST /api/v1/jobs/{id}/kill.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_kill"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_queue-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_queue(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs queue [--status] [--kind] -> GET /api/v1/jobs?status=&kind=.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_queue"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_result-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_result(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs result --job-id <id> [--output <path>] -> GET /api/v1/jobs/{id}/result. The result payload is an arbitrary raw file (e.g. a heavy job's JSONL output, not necessarily JSON despite the endpoint's Content-Type header) – --output writes the raw bytes rather than assuming a text/JSON shape.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_result"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_jobs_resume-Tuple{String, Dict}","page":"Server API","text":"ctl_jobs_resume(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl jobs resume --job-id <id> -> POST /api/v1/jobs/{id}/resume.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_jobs_resume"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_list-Tuple{String}","page":"Server API","text":"ctl_list(base_url::String) -> Int\n\nsimilarity-search-ctl list -> GET /api/v1/datasets.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_list"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_load-Tuple{String, Dict}","page":"Server API","text":"ctl_load(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl load --bundle <dir> --dataset <new-id> -> submits a load job (POST /api/v1/jobs/load), blocks until it finishes, then prints the new dataset's descriptor. Loading only writes the dataset to disk – it still needs an explicit POST /api/v1/admin/datasets/{id}/reload (not run automatically here) to bring it live on a running server, exactly like the CLI-driven dump/load + admin reload flow.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_load"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_log-Tuple{String, Dict}","page":"Server API","text":"ctl_log(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl log --dataset <id> [--offset] [--limit] -> GET /api/v1/datasets/{id}/log?offset=&limit= (PLAN.md §1's admin log command, chunk 17).\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_log"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_log_tokens-Tuple{String}","page":"Server API","text":"ctl_log_tokens(base_url::String) -> Int\n\nsimilarity-search-ctl log-tokens -> GET /api/v1/admin/tokens (PLAN.md §1's admin surface: an audit listing of every token, expired or not).\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_log_tokens"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_prune_tokens-Tuple{String}","page":"Server API","text":"ctl_prune_tokens(base_url::String) -> Int\n\nsimilarity-search-ctl prune-tokens -> POST /api/v1/admin/tokens/prune.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_prune_tokens"},{"category":"method","location":"server.html#SimilaritySearchServer.ctl_stats-Tuple{String, Dict}","page":"Server API","text":"ctl_stats(base_url::String, cmd_args::Dict) -> Int\n\nsimilarity-search-ctl stats --dataset <id> -> GET /api/v1/datasets/{id}. PLAN.md's stats (\"extract and compute statistics usage and costs\") maps directly onto this endpoint's existing doccount/tombstoneratio/calibrated-beamsearch-baseline payload – there is no separate, more detailed stats endpoint to call instead.\n\n\n\n\n\n","title":"SimilaritySearchServer.ctl_stats"},{"category":"method","location":"server.html#SimilaritySearchServer.dispatch_command-Tuple{String, AbstractDict}","page":"Server API","text":"dispatch_command(cmd_name::String, cmd_args::AbstractDict) -> Int\n\nRuns one of the data-operation subcommands (build through load) given its already-assembled cmd_args (the same Dict{String,Any} shape parse_commandline produces for that subcommand). Shared by main's direct non-interactive dispatch and run_interactive's confirm-then-run step (interactive.jl), so there is exactly one command-name → execute_* mapping regardless of how the arguments were collected.\n\n\n\n\n\n","title":"SimilaritySearchServer.dispatch_command"},{"category":"method","location":"server.html#SimilaritySearchServer.dispatch_ctl_command-Tuple{AbstractDict, String}","page":"Server API","text":"dispatch_ctl_command(parsed::AbstractDict, base_url::String) -> Int\n\nRoutes a parsed -ctl command (see build_ctl_settings) to its ctl_* implementation – the -ctl counterpart of dispatch_command (cli_handlers.jl). jobs is a nested subcommand (jobs queue|result|block|resume|kill|gc), so its own %COMMAND% is resolved one level deeper than the others.\n\n\n\n\n\n","title":"SimilaritySearchServer.dispatch_ctl_command"},{"category":"method","location":"server.html#SimilaritySearchServer.execute_add_token-Tuple{Dict}","page":"Server API","text":"execute_add_token(cmd_args::Dict) -> Int\n\nadd-token: writes one access token straight into the token database of a working directory. This is how the first token is created, because a server started with [auth] enabled refuses to run while no token exists, and the administrative endpoint that creates tokens is itself behind authentication.\n\nIt opens the token database for writing, so it cannot run while a server holds that working directory open. Later tokens are created through POST /api/v1/admin/tokens, or with similarity-search-ctl add-token, which is the same endpoint.\n\n\n\n\n\n","title":"SimilaritySearchServer.execute_add_token"},{"category":"method","location":"server.html#SimilaritySearchServer.execute_dump-Tuple{Dict}","page":"Server API","text":"execute_dump(cmd_args::Dict) -> Int\n\ndump (PLAN.md §4.4/§1): exports a dataset as a portable bundle directory – dataset.avro (every metadata record, deleted or not, via Persistence.DumpRecord), project/ (a copy of the project directory, which is the index: load restores it as it is and never rebuilds it from the Avro rows, so the bundle comes back with the same graph topology and posting lists it had when it was written), and manifest.json (indexkind/distance/ metaschema/joingroup/holdsmetadata/key – everything load needs to recreate a descriptor.json at the target, on top of the dataset it opens read-only so it can run concurrently with a live server serving the same dataset (see describe's identical read_only=true reasoning).\n\n\n\n\n\n","title":"SimilaritySearchServer.execute_dump"},{"category":"method","location":"server.html#SimilaritySearchServer.execute_load-Tuple{Dict}","page":"Server API","text":"execute_load(cmd_args::Dict) -> Int\n\nload (PLAN.md §4.4/§1): the inverse of execute_dump – always targets the HTTP-layout directory (<workdir>/datasets/{id}/), so a subsequently-(re)started serve picks it up via Server.reload_datasets! with no extra step, regardless of which layout the original dump came from. Refuses to overwrite an existing target (--dataset must be a fresh id). Copies project/ back as it is (see execute_dump for why this is a copy and not a reconstruction from the rows) and replays every dataset.avro row into fresh Schema.MetadataRecords under its original doc_id – both declared_json and extra_json were captured separately at dump time (not pre-merged), so this reconstructs the exact original MetadataRecord, not a re-derived approximation of one.\n\n\n\n\n\n","title":"SimilaritySearchServer.execute_load"},{"category":"method","location":"server.html#SimilaritySearchServer.execute_rebuild-Tuple{Dict}","page":"Server API","text":"execute_rebuild(cmd_args::Dict) -> Int\n\nrebuild (PLAN.md §1): purges soft-deleted (tombstoned) documents and, for a text index, retrains the Vocabulary from scratch against the dataset's current live corpus – the only way to recover from vocabulary drift (PLAN.md §1's note: TextSearch.jl's Vocabulary never mutates after training, so append-time out-of-vocabulary tokens are silently and permanently dropped otherwise).\n\nnote: Full reindex only -- there is no lighter 'compact posting lists' mode\nAn earlier draft of this plan described a second, lighter rebuild mode for text indices (filter_lists!, compacting posting lists without retraining the vocabulary). Verified against the current TextSearch.jl/SimilaritySearch.jl source: no such function exists anywhere in either package today (grepped both repos; BM25InvertedFile has no compaction method at all). This command therefore only implements the one mode that is real: a full reindex from the live corpus.\n\nSince every dense/text index in this app is a dense array with no notion of a \"gap\", purging tombstones means the survivors get renumbered to fresh sequential docids (1..n') matching their new index positions – their old metadata rows are deleted and rewritten under the new ids, not merely filtered in place. For a SearchGraph, the fresh index is additionally passed through SimilaritySearch.rebuild (a real library function, not hand-rolled): it recomputes graph topology seeing the whole survivor set at once, rather than the incremental one-vertex-at-a-time bias `additem!` reintroduces – PLAN.md §1's \"use specific functions when available\" guidance for a case where one exists.\n\n\n\n\n\n","title":"SimilaritySearchServer.execute_rebuild"},{"category":"method","location":"server.html#SimilaritySearchServer.generate_default_config-Tuple{}","page":"Server API","text":"generate_default_config() -> String\n\nGenerates a default TOML configuration with explanatory comments.\n\nReturns\n\nString: The default configuration content as a string.\n\n\n\n\n\n","title":"SimilaritySearchServer.generate_default_config"},{"category":"method","location":"server.html#SimilaritySearchServer.load_config-Tuple{String}","page":"Server API","text":"load_config(path::String) -> Dict\n\nLoads the TOML configuration from the given path. If the file does not exist, it generates a default one.\n\nArguments\n\npath::String: Path to the TOML configuration file.\n\nReturns\n\nDict: The parsed TOML configuration dictionary.\n\n\n\n\n\n","title":"SimilaritySearchServer.load_config"},{"category":"method","location":"server.html#SimilaritySearchServer.main-Tuple{Vector{String}}","page":"Server API","text":"main(args::Vector{String}) -> Cint\n\nEntry point for similarity-search (PLAN.md §1's data-operations CLI): the eleven build-through-load subcommands plus its own interactive guided form. Only ever reached for this one binary – similarity-search-ctl.jl/similarity-search-server.jl call main_ctl/main_serve directly instead of this function (each wrapper statically knows which binary it is, so there's no env-var sniffing needed to pick an entry point, unlike the SIMSEARCH_CLI_BIN-based dispatch a previous chunk used here). serve used to still be reachable through this same parser/function too, sharing it with the data-ops binary; it now has a genuinely separate ArgParseSettings (build_serve_settings, cli.jl) and its own main_serve below, matching PLAN.md's \"thin, single-purpose serve binary\" framing in full, not just for its own fields (chunk 21).\n\n\n\n\n\n","title":"SimilaritySearchServer.main"},{"category":"method","location":"server.html#SimilaritySearchServer.main_ctl-Tuple{Vector{String}}","page":"Server API","text":"main_ctl(args::Vector{String}) -> Cint\n\nEntry point for similarity-search-ctl (PLAN.md §1's admin/control CLI): parses against build_ctl_settings (a completely separate ArgParseSettings from the data-operations one – see ctl.jl's header note) and dispatches through dispatch_ctl_command, except for interactive (chunk 16), which routes to run_interactive_ctl instead. No --config/workdir loading at all – every -ctl command is a plain HTTP call to --host/--port, nothing else.\n\n\n\n\n\n","title":"SimilaritySearchServer.main_ctl"},{"category":"method","location":"server.html#SimilaritySearchServer.main_serve-Tuple{Vector{String}}","page":"Server API","text":"main_serve(args::Vector{String}) -> Cint\n\nEntry point for similarity-search-server (PLAN.md §1): parses against its own build_serve_settings (chunk 21 – genuinely separate from the data-operations build_settings, not shared) and handles exactly its two commands, serve and interactive – nothing else is reachable through this binary at all.\n\nserve's --host/--port/--workdir (all optional, cli.jl) layer over --config's TOML file over a hardcoded literal, in that priority order – fixes a real, previously unnoticed bug found in chunk 18: the original code read get(config, \"workdir\", \"data\"), a top-level key generate_default_config never actually writes (it's nested under [paths]), so a --config file's documented workdir setting was silently never applied; serve always fell back to the hardcoded \"data\" regardless of what the TOML said.\n\n\n\n\n\n","title":"SimilaritySearchServer.main_serve"},{"category":"function","location":"server.html#SimilaritySearchServer.parse_commandline","page":"Server API","text":"parse_commandline(args::Vector{String}=ARGS) -> Dict\n\nParses args against a freshly built build_settings object. Split out from build_settings so interactive.jl can introspect the same settings structure (same subcommands, same per-subcommand fields/defaults/choices) via its own fresh call to build_settings(), without needing the already-parsed result this function returns.\n\n\n\n\n\n","title":"SimilaritySearchServer.parse_commandline"},{"category":"function","location":"server.html#SimilaritySearchServer.parse_ctl_commandline","page":"Server API","text":"parse_ctl_commandline(args::Vector{String}=ARGS) -> Dict\n\nParses args against a freshly built build_ctl_settings object – mirrors cli.jl's build_settings/parse_commandline split, kept separate here since -ctl's settings are a genuinely different object.\n\n\n\n\n\n","title":"SimilaritySearchServer.parse_ctl_commandline"},{"category":"function","location":"server.html#SimilaritySearchServer.parse_serve_commandline","page":"Server API","text":"parse_serve_commandline(args::Vector{String}=ARGS) -> Dict\n\nsimilarity-search-serve's counterpart to parse_commandline – parses against build_serve_settings instead of the data-operations build_settings.\n\n\n\n\n\n","title":"SimilaritySearchServer.parse_serve_commandline"},{"category":"method","location":"server.html#SimilaritySearchServer.resolve_batch_threads_pct-Tuple{AbstractDict}","page":"Server API","text":"resolve_batch_threads_pct(resources_cfg::AbstractDict) -> Float64\n\nThe share of threads reserved for job execution, from the [resources] section of the configuration file (PLAN.md §2). batch_threads_pct states it directly; query_threads_pct states its complement, and is used when the first is absent. When both are present and they do not add up to 100, batch_threads_pct is the one that applies, and the discrepancy is reported rather than resolved silently.\n\nOut-of-range or non-numeric values fall back to 20, which is the default of the generated configuration file.\n\n\n\n\n\n","title":"SimilaritySearchServer.resolve_batch_threads_pct"},{"category":"method","location":"server.html#SimilaritySearchServer.run_interactive-Tuple{}","page":"Server API","text":"run_interactive() -> Int\n\nEntry point for the interactive subcommand (PLAN.md §1). Gated on a real TTY (the PLAN.md-mandated defense-in-depth against a malformed LocalCLIExecutor-spawned Job subprocess deadlocking on readline/request with no human on the other end, §5.5). Command menu → per-argument prompts (in _command_fields's declared order, --workdir first so dataset-discovery prompts after it can use the workdir the operator actually picked) → confirm-before-run panel → dispatch via the same dispatch_command the non-interactive path uses, so there is exactly one command-name → execute_* mapping regardless of how the arguments were collected.\n\n\n\n\n\n","title":"SimilaritySearchServer.run_interactive"},{"category":"method","location":"server.html#SimilaritySearchServer.run_interactive_ctl-Tuple{String}","page":"Server API","text":"run_interactive_ctl(base_url::String) -> Int\n\nEntry point for similarity-search-ctl interactive (PLAN.md §1) – the -ctl counterpart of run_interactive(), gated on the same real-TTY check. base_url is already resolved by main_ctl from --host/--port (global flags parsed before this ever runs, unlike similarity-search's per-command --workdir), so there is no prompt for it here.\n\nTwo structural differences from the data-operations version, not oversights:\n\n--dataset fields (stats/dump/load) are always free text, never a menu of locally-discovered ids – -ctl is a remote HTTP client with no workdir of its own (_prompt_field(...; allow_dataset_discovery=false)), so there is nothing on this process's filesystem to scan.\njobs is a nested subcommand (jobs queue|result|block|resume|kill|gc) – picking it shows a second menu for its own children before any field prompting starts, since _command_menu_names only enumerates one level at a time.\n\nDispatches via the exact same dispatch_ctl_command(parsed, base_url) the non-interactive path uses (reconstructing the same {\"%COMMAND%\" => ..., name => args} shape parse_ctl_commandline itself would have produced), so there is exactly one command-name → ctl_* mapping regardless of how the arguments were collected.\n\n\n\n\n\n","title":"SimilaritySearchServer.run_interactive_ctl"},{"category":"method","location":"server.html#SimilaritySearchServer.run_interactive_serve-Tuple{String}","page":"Server API","text":"run_interactive_serve(config_path::String) -> Int\n\nEntry point for similarity-search-server interactive (PLAN.md §1) – the guided form for serve's own settings. No command menu (unlike run_interactive/run_interactive_ctl) since serve is a single-purpose binary with exactly one thing to configure, matching PLAN.md's own \"the menu degenerates to that one guided form directly\" wording. Prompts for host/port/workdir, each pre-filled with whatever --config's TOML file already resolves to (or the hardcoded literal, absent that) – the same layering main's non-interactive serve branch applies, so the shown default is honest about what would actually happen on a bare Enter, not a fixed placeholder. Confirm-before-run, then dispatches through run_serve, the exact function main's own serve branch calls, so there is exactly one \"start the server\" code path regardless of how the settings were collected.\n\n\n\n\n\n","title":"SimilaritySearchServer.run_interactive_serve"},{"category":"method","location":"server.html#SimilaritySearchServer.run_serve-Tuple{String, Int64, String}","page":"Server API","text":"run_serve(host::String, port::Int, workdir::String) -> Cint\n\nThe actual \"start serving\" body – extracted out of main's serve branch so both the plain non-interactive CLI path and run_interactive_serve's confirm-then-run step (interactive.jl, PLAN.md §1) call the exact same function, the same \"one command-name -> handler mapping regardless of how the arguments were collected\" principle dispatch_command/dispatch_ctl_command already apply to their own binaries.\n\n\n\n\n\n","title":"SimilaritySearchServer.run_serve"},{"category":"method","location":"server.html#SimilaritySearchServer.wire_kind-Tuple{Any}","page":"Server API","text":"wire_kind(engine) -> String\n\nThe index_kind name this CLI and the HTTP API use for a project, read back from the open project itself rather than from a descriptor file. The inverse of Server.parse_index_kind.\n\n\n\n\n\n","title":"SimilaritySearchServer.wire_kind"},{"category":"constant","location":"server.html#SimilaritySearchServer.CLI_CHOICES","page":"Server API","text":"CLI_CHOICES\n\nEnumerated choice lists for arguments whose valid values are a closed set. ArgParse.jl has no first-class \"choices\" field on ArgParseField – only an opaque range_tester validation closure – so this is the one place those lists are declared, used both to build each argument's range_tester below (so the non-interactive CLI now rejects an invalid value immediately, rather than failing later inside the relevant execute_* function or a library call) and by the interactive layer (interactive.jl) to render a selection menu instead of free text for these fields, per PLAN.md's Interactive Mode \"single source of truth\" principle – applied here to the one piece of metadata ArgParseField genuinely can't carry on its own.\n\n\n\n\n\n","title":"SimilaritySearchServer.CLI_CHOICES"},{"category":"macro","location":"server.html#SimilaritySearchServer.@cli_engine_call-Tuple{Any}","page":"Server API","text":"@cli_engine_call expr\n\nRuns an engine call, and on an SSE.EngineError prints it and evaluates to the exit code cli_exit_code gives it. Use it as\n\nvalue = @cli_engine_call SSE.allknn(handle; k=k)\nvalue isa Cint && return value\n\nwhich keeps the mapping in one place instead of a try/catch at every command.\n\n\n\n\n\n","title":"SimilaritySearchServer.@cli_engine_call"},{"category":"type","location":"server.html#SimilaritySearchServer.Server.AppState","page":"Server API","text":"AppState\n\nHolds the global application state for the server.\n\nFields\n\nworkdir::String: Root working directory for data and jobs.\njob_mgr::Any: Job manager instance.\ncursor_mgr::Any: Result-cursor manager instance (§5.4's pagination protocol).\nexecutor::Any: Job executor backend.\ntoken_mgr::Any: Token manager instance (§4.1 identity model).\ndatasets::Dict{String, ProjectManager}: Cache of open datasets.\nengines::Dict{String, AbstractSearchEngine}: Cache of open search engines.\nlock::ReentrantLock: Thread-safe lock for modifying caches.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.AppState"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.FILTER_CANDIDATES-Tuple{Int64}","page":"Server API","text":"FILTER_CANDIDATES(k) -> Int\n\nHow many candidates a filtered search reads before applying the predicate.\n\nSSE.search defaults to 2k, which suits a predicate most items satisfy; this API keeps the max(5k, k+20) budget it has always used, since a metadata filter here is typically narrow and the caller has no way to pass its own budget yet. A filtered page can still come back short, and that is what insufficient_results reports.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.FILTER_CANDIDATES"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._body_dataset-Tuple{HTTP.Request, Vararg{String}}","page":"Server API","text":"_body_dataset(req, keys...) -> String\n\nThe dataset a request names in its body, for the two routes whose scope is not in the path: job submission (\"dataset\") and the queries that span two datasets (\"dense_index\", \"lexical_index\"). Returns \"*\" when the body names none, so that such a request requires a permission over every dataset rather than accidentally requiring none.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._body_dataset"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._engine_dimension-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine}","page":"Server API","text":"_engine_dimension(engine::AbstractSearchEngine) -> Union{Nothing, Int}\n\nThe dense vector dimension of engine's first indexed item, for op_log's dimension field – nothing for a text index (vocabulary size isn't \"dimension\" in the same sense) or an empty index (nothing indexed yet to measure).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._engine_dimension"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._engine_distance_name-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine}","page":"Server API","text":"_engine_distance_name(engine::AbstractSearchEngine) -> Union{Nothing, String}\n\nThe index's distance function name (e.g. \"SqL2\"), for op_log's distance_name field (PLAN.md §4.3) – the same string(nameof(typeof(SimilaritySearch.distance(engine.backend.index)))) pattern execute_dump/execute_describe already use. nothing for a text index (BM25/ weighted-inverted-file scoring isn't a SimilaritySearch.distance metric in this sense) or an untrained/empty one.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._engine_distance_name"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._filter_predicate-Tuple{AbstractDict, Any}","page":"Server API","text":"_filter_predicate(filter_spec) -> Function\n\nThe wire's filter object as the predicate SSE.search takes.\n\nThe engine's filtering keyword is a plain Julia function over (record, meta), so the JSON spec stays this module's business – Schema.matches_filter already knows how to read it – and the engine never learns a wire format.\n\nschema is the dataset's own declaration, which is what makes a comparison happen in the declared type: a field declared :timestamp compares as an instant rather than as the text that stores it. A dataset that declares nothing passes an empty declaration and compares as it always did.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._filter_predicate"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._format_results-Tuple{Any, Int64}","page":"Server API","text":"_format_results(hits, k) -> (results, insufficient_results)\n\nTurns the engine's hits into the wire's shape, and flags insufficient_results when fewer than k came back (PLAN.md §5.4's explicit signal, rather than silently returning a short page).\n\nSoft-deleted hits are dropped here rather than hidden by the engine: SSE.search reports them with deleted=true and no doc_id, on purpose, and it is this API's choice not to show them.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._format_results"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._guard-Tuple{Any, HTTP.Request, SimilaritySearchServer.Server.AppState, Symbol, AbstractString}","page":"Server API","text":"_guard(handler, req, app, op::Symbol, dataset::AbstractString) -> HTTP.Response\n\nRuns handler() if the request may perform op on dataset, and answers 401 or 403 otherwise. Every /api/v1 route is registered through this function in run_server, which is where the authorization table of this server can be read in one place.\n\ndataset is the dataset the route acts on, or \"*\" for a route that is not about one dataset. With app.auth_enabled false the handler runs without any check, which is the behavior of a server that has never had authentication.\n\n401 when no token is presented, or the token does not exist, or it has expired. The response says which of those it was only as far as \"invalid or expired\": a caller that does not hold a token learns nothing from the difference.\n403 when the token is valid and does not carry the permission. The response names the permission that was required, because its holder is a legitimate caller who needs to know what to ask for.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._guard"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._hydrate_join_id-Tuple{SimilaritySearchServer.Server.AppState, Vector, Any}","page":"Server API","text":"_hydrate_join_id(app, ordered_members, doc_id) -> String\n\nResolves doc_id's original/external id by checking each of ordered_members in turn until one has metadata for it — the same \"hydrate once, from whichever member actually has it\" idea handle_hybrid_search already applies ad hoc between exactly two datasets, generalized to however many members a join group has. Callers sort ordered_members so the group's holds_metadata member (if any) is tried first.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._hydrate_join_id"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._hydrate_raw-Tuple{Any, Any}","page":"Server API","text":"_hydrate_raw(handle, raw) -> Vector{SSE.SearchResult}\n\nThe engine's raw live-search tuple as the hit type its public search returns, so every caller here sees one shape. Only the beam-override path needs this (see _run_search).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._hydrate_raw"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._paginate_results-Tuple{SimilaritySearchServer.Server.AppState, String, AbstractVector, Int64}","page":"Server API","text":"_paginate_results(app, index, results, page_size) -> Dict\n\nMaterializes results behind a fresh Cursors cursor (PLAN.md §5.4) and returns its first page plus the cursor_id for polling the rest via GET /api/v1/cursors/{cursor_id}. Purely opt-in — a caller that never sends page_size keeps getting the full results list inline, unchanged from before this existed.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._paginate_results"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._parse_offset_limit-Tuple{AbstractDict, Int64}","page":"Server API","text":"_parse_offset_limit(params, total) -> (offset, limit)\n\nPlain, stateless offset/limit pagination (PLAN.md §5.4) for listing endpoints cheap enough to recompute per request (GET /api/v1/datasets, GET /api/v1/jobs) — distinct from the opaque Cursors-backed pagination handle_search uses for an actually expensive-to-recompute result set. offset clamps to 0; limit defaults to total (i.e. \"no limit\") when absent or unparseable.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._parse_offset_limit"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._reload_one_dataset!-Tuple{SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"_reload_one_dataset!(app, id) -> Bool\n\nReopens exactly one dataset from disk into app.handles, unconditionally (overwriting any existing entry for id) – the shared piece both reload_datasets! (cold-start, every dataset, skip-if-already-loaded) and handle_reload_dataset (one dataset, on purpose, always overwrite) build on. Returns false without touching app if id has no descriptor.json (not a real dataset directory).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._reload_one_dataset!"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._request_identity-Tuple{SimilaritySearchServer.Server.AppState, HTTP.Request}","page":"Server API","text":"_request_identity(app, req) -> (user, token_fingerprint)\n\nWho a request is logged as. The operation log used to store the Authorization header verbatim, which put live credentials in a column family of the project, in every dump of it, and in front of anyone allowed to read the log.\n\nuser is the name on the token, and is present when the token is valid – the log then says who did something rather than with what. token_fingerprint is token_fingerprint of what was presented, which distinguishes two tokens of the same user and is the only identification available when authentication is disabled and a token is presented anyway.\n\nThe token is looked up again here rather than carried over from _guard: a point read of RocksDB costs microseconds, and a handler that reads its own identity does not depend on having been reached through a particular guard.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._request_identity"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._request_token-Tuple{HTTP.Request}","page":"Server API","text":"_request_token(req::HTTP.Request) -> Union{Nothing, String}\n\nBest-effort extraction of the caller-presented token from an Authorization header (Bearer <token> or the bare token string), for op_log telemetry purposes only – nothing if the header is absent. Not authentication: no request handler in this codebase currently validates a presented token against Tokens.get_token at all (identity enforcement per PLAN.md §4.1 isn't wired up yet, a separate, larger prerequisite) – this just records whatever the caller happened to send, unverified, so a future telemetry query already has the field populated once real auth enforcement lands.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._request_token"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._resolve_beamsearch_override-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine, AbstractDict}","page":"Server API","text":"_resolve_beamsearch_override(engine, overrides) -> (bs_or_nothing, warn_or_errmsg)\n\nValidates a caller-supplied beamsearch_overrides dict against BEAMSEARCH_SAFETY_MULTIPLIER times the index's current baseline (PLAN.md §3). Unspecified fields default to the baseline's own value, so a caller only needs to name the field(s) it actually wants to raise. Returns:\n\n(bs::BeamSearch, exceeds_baseline::Bool) on success — exceeds_baseline is true when the (still-allowed, sub-cap) override asks for more than the baseline itself, the signal handle_search uses to attach a WARN/response-header (PLAN.md §3's \"log a WARN event and return an HTTP warning header\" for elevated-but-allowed parameters).\n(nothing, error_message::String) if the override exceeds the hard safety cap, or if engine's index has no BeamSearch to override at all (text/exact indices).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._resolve_beamsearch_override"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._run_search-Tuple{Any, String, HTTP.Request, Any, Int64}","page":"Server API","text":"_run_search(handle, index, req, query, k; filter_spec, bs_override, text, metrics, identity) -> Vector{SSE.SearchResult}\n\nShared search call site for handle_search (dense, optionally post-filtered) and handle_ftsearch (single-index text): runs the search and logs exactly one op_log \"search\" record around it (PLAN.md §4.3), so cost accounting lives in one place instead of at every search-shaped handler. Not used by handle_hybrid_search/handle_ftsearch_group, which span more than one dataset's op_log – attributing one combined operation across several projects needs its own decision.\n\nEverything goes through the engine's public search/ftsearch except a per-request beam-search override, which the public surface does not expose (it takes minrecall, a calibrated target, rather than raw beam parameters). That one path still calls IndexEngine.search_live; it is the last search-side internal this module reaches for.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._run_search"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._typed_item-Tuple{Symbol, AbstractDict}","page":"Server API","text":"_typed_item(kind::Symbol, item::AbstractDict) -> Union{Nothing,SSE.AbstractItem}\n\nOne wire item as the engine's own item type, or nothing when it carries no payload for this kind of project (a text item with no text, a dense one with no vector).\n\nThe metadata half – doc_id, keywords, refs and the free-form meta – travels on the item itself, which is why this module no longer writes MetadataRecords by hand: ids are the engine's to assign, and having two places assign them is how they drift.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._typed_item"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._update_descriptor!-Tuple{SimilaritySearchServer.Server.AppState, String, Dict}","page":"Server API","text":"_update_descriptor!(app, id, updates::Dict)\n\nReads a dataset's persistent sidecar, merges updates in, and writes it back — the same read-merge-write shape handle_create_dataset uses at creation time, but for a post-creation update (currently only handle_calibrate's beamsearch_baseline).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._update_descriptor!"},{"category":"method","location":"server.html#SimilaritySearchServer.Server._with_query_slot-Tuple{Any, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"_with_query_slot(f, app::AppState)\n\nRuns f holding one of the server's query slots, waiting for one if all are taken.\n\nA request waits rather than being refused: a caller that asked for a search wants the search, and the wait is bounded by the searches ahead of it rather than by anything unbounded. What the waiting says is reported, not hidden – /metrics carries how many searches are running and how many are waiting, which is what tells an operator whether this bound is binding at all, and simsearch_query_wait_seconds_total accumulates the time spent waiting.\n\nThis is the semaphore, not §2's request collector. The collector would move every search onto a pool of worker tasks fed by a channel; this bounds admission and leaves the request on the task that received it. The two answer the same question about overload, and one of them is ten lines.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server._with_query_slot"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.build_heavy_job_command-Tuple{String, AbstractDict, String, String}","page":"Server API","text":"build_heavy_job_command(kind, data, output_path, workdir) -> Union{Vector{String}, Nothing}\n\nTranslates a friendly JSON job-submission body ({\"dataset\": ..., \"k\": ...}) into the exact CLI args Executors.submit will spawn for one of HEAVY_JOB_KINDS. Returns nothing if a required field is missing, which the caller turns into a 400.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.build_heavy_job_command"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.dataset_descriptor-Tuple{SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"dataset_descriptor(app, id) -> Union{Dict, Nothing}\n\nMerges the persistent sidecar (index_kind/distance/created_at/join_group/ holds_metadata/key) with live stats from app.handles when the dataset happens to be currently loaded (doc_count, tombstone_count/_ratio) — reload_datasets! populates app.handles for every on-disk dataset right at server startup, so in the normal case the two sources agree; loaded == false still shows up for the brief window before that startup reload runs, or if a dataset was explicitly unloaded some other way. Returns nothing if id isn't a real dataset directory. get!s in the join-group fields with their pre-join-group defaults so a descriptor written before that feature existed still round-trips.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.dataset_descriptor"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.datasets_root-Tuple{SimilaritySearchServer.Server.AppState}","page":"Server API","text":"The directory every dataset of this server lives under.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.datasets_root"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.default_textmodel-Tuple{Any}","page":"Server API","text":"default_textmodel(engine_type) -> Union{Nothing,AbstractTextModelSpec}\n\nWhat a project of this kind is created with when the request does not say.\n\nA text project must state its text model explicitly – the engine refuses to guess, because the ways to obtain a vocabulary are not interchangeable – so this server picks FitFromCorpus, which is what it has always used: fit from whatever the dataset itself is given. Anything else needs a profile shipped to the server, which the API does not do yet.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.default_textmodel"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.descriptor_path-Tuple{SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"descriptor_path(app, id) -> String\n\nPath of a dataset's small persistent sidecar (index_kind/distance/created_at/ meta_schema) — recorded at creation time so GET /api/v1/datasets/GET /api/v1/datasets/{id} can list every dataset without needing it loaded into app.handles, and so reload_datasets! can reopen each one (with its original meta_schema, hence its original secondary-index column families) on server startup.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.descriptor_path"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.engine_error_response-Tuple{EngineError}","page":"Server API","text":"engine_error_response(e::SSE.EngineError) -> HTTP.Response\n\nThe whole error boundary, in one table: an engine error becomes a status code by its category, never by its message.\n\nSSE.NotFound -> 404. Something named does not exist.\nSSE.ConflictingState -> 409. The project is real and the request is fine, but the project is not in a state that can serve it (a staged backlog, an untrained profile). Retrying after the state changes works, which is what makes this a 409 and not a 400.\nSSE.InvalidRequest -> 400. Retrying unchanged cannot help.\nSSE.StorageFailure -> 500. Not the caller's fault.\n\nThe body carries error (the message, for a human) and kind (the concrete type name, for a client that wants to branch without parsing prose).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.engine_error_response"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_block_job-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_block_job(req, app, job_id) -> HTTP.Response\n\nPOST /api/v1/jobs/{job_id}/block (PLAN.md §5.5, admin-only: similarity-search-ctl jobs block): moves a queued job to blocked/, where the dispatcher never scans for work – distinct from kill (stops something already running) and the best-effort DELETE. 404 if the job doesn't exist; 409 if it's not currently queued (e.g. already running, or already blocked) since the request landed too late for a \"pause before it starts\" operation to make sense.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_block_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_calibrate-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_calibrate(req, app, index) -> HTTP.Response\n\nPOST /api/v1/datasets/{id}/calibrate (PLAN.md §5.6): runs IndexEngine.calibrate!'s real SearchModels-driven hyperparameter sweep against index's already-indexed data (or an explicit queries ground-truth set, if given), installs the best-found BeamSearch as the index's new default, and persists it into the dataset's descriptor. Only applies to a searchgraph (approximate dense) index — exact/exhaustive indices have no BeamSearch to tune, and text indices aren't in scope for this pass.\n\nBody (all optional): {\"minrecall\": 0.9, \"numqueries\": 64, \"ksearch\": 10, \"queries\": [[...], ...]}.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_calibrate"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_delete_dataset-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_delete_dataset(req, app, id)\n\nReally deletes the dataset: closes its RocksDB handle, drops it from the in-memory caches, and removes its on-disk directory.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_delete_dataset"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_delete_item-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_delete_item(req, app, index)\n\nSoft-deletes a document (PLAN.md §1): marks its doc_id in the engine's tombstone set so future searches exclude it, without removing it from the underlying index.\n\nThe engine writes the deletion mark to storage as part of the call, which is what makes it visible to a CLI rebuild. That command is the one that removes deleted items, and it runs as a separate subprocess that reads only what is on disk.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_delete_item"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_delete_job-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_delete_job(req, app, job_id) -> HTTP.Response\n\nDELETE /api/v1/jobs/{job_id} (PLAN.md §5.5): best-effort cancellation of a job that hasn't started running yet – moves it straight to failed/ with error: \"cancelled by operator\" via Jobs.cancel_job!. Distinct from kill (POST .../kill), the only way to stop an already-running job: DELETE against a running or already-finished job is a 409, not a silent no-op and not a fallback to killing it (PLAN.md §1's own note explicitly separates the two). Guards the same dispatcher race handle_kill_job already does above (the job transitions queued/ -> running/ in the same instant this request lands) with a try/catch, reported as the same 409 rather than a 500.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_delete_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_exists-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_exists(req, app, index) -> HTTP.Response\n\nGET /api/v1/datasets/{id}/exists?ids=a,b,c (PLAN.md §5.1): lightweight existence/tombstone check by id, without paying for a full fetch's metadata decode. Accepts either internal doc_ids or original ids, same resolution as handle_fetch.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_exists"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_fetch-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_fetch(req, app, index)\n\nBatch metadata retrieval by id (PLAN.md §5.1). Accepts either the internal integer doc_id or the caller-supplied original id (resolved via a linear scan — see Project.find_by_doc_id; there is no secondary index from external id to doc_id in this pass).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_fetch"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_ftsearch_group-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_ftsearch_group(req, app)\n\nPOST /api/v1/search/group (PLAN.md §5.3/§7): join-group-aware full-text search, distinct from the simpler per-index POST /api/v1/datasets/{id}/ftsearch (still supported unchanged, e.g. by test_02_full_text_search.jl). Takes a join_group and a key: either one specific key string (routes to the one text-kind member tagged with that key) or the reserved wildcard \"*\" (fans out to every text-kind member of the group). Responses for key: \"*\" are grouped by key, not fused into one ranked list — PLAN.md is explicit that per-field lexical fusion is a different, harder scoring problem out of scope here; a caller wanting a single list across fields does that client-side. Metadata is hydrated once at the end via _hydrate_join_id, preferring the group's holds_metadata member when one is tagged.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_ftsearch_group"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_get_dataset-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_get_dataset(req, app, id) -> HTTP.Response\n\nGET /api/v1/datasets/{id} (PLAN.md §5.7): single-dataset detail, including its join_group/holds_metadata/key (null/false/null for a dataset never tagged into a join group — see dataset_descriptor).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_get_dataset"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_get_job_result-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_get_job_result(req, app, job_id)\n\nGET /api/v1/jobs/{job_id}/result (PLAN.md §5.5): streams back the raw file a completed heavy job's CLI subprocess wrote (see handle_submit_job's result_ref bookkeeping). 409 if the job hasn't reached completed yet, 404 if there's no result_ref at all (e.g. a job submitted via the raw \"command\" escape hatch, which doesn't set one).\n\nA dump job's result_ref is a bundle directory (dataset.avro, manifest.json and a copy of the project directory), not a single file – unlike every other HEAVY_JOB_KINDS result, it can't be streamed back as one HTTP body (and the bundle is meant to stay on the shared local filesystem the LocalCLIExecutor/operator already has access to, not leave it over HTTP). For that case this returns a small JSON pointer (bundle_dir + the parsed manifest.json) instead of raw bytes; a load job's result_ref (the new dataset's descriptor.json) is a plain file and falls through to the normal byte-streaming path unchanged.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_get_job_result"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_get_join_group-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_get_join_group(req, app, id) -> HTTP.Response\n\nGET /api/v1/datasets/{id}/join_group (PLAN.md §5.7/§7): the full JoinGroup id belongs to — its sibling members and which one holds_metadata. members is empty (not a 404) if the dataset exists but was never tagged with a join_group, since that's a normal, valid state (most datasets in this pass aren't part of one), not an error.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_get_join_group"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_get_op_log-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_get_op_log(req, app, id) -> HTTP.Response\n\nGET /api/v1/datasets/{id}/log?offset=&limit= (PLAN.md §1's admin log command – backs similarity-search-ctl log, the one command from that section's enumeration left unimplemented until now, see chunk 15's note): a paginated, newest-first dump of id's op_log column family. Real content as of this chunk – Telemetry.log_operation is now wired into search/ftsearch/append/delete (_run_search/handle_append/ handle_delete_item). Cheap-enough-to-recompute full scan + plain offset/limit (_parse_offset_limit, same as handle_list_jobs/handle_list_datasets), not a Cursors-backed cursor – op_log is already append-only historical data, none of Cursors' \"materialize an expensive-to-recompute result set\" concern applies here. 404 if id isn't a currently-loaded dataset (its op_log CF is only reachable through a live RocksDB.DB handle, same constraint every other loaded-dataset-only endpoint already has).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_get_op_log"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_get_vocab-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_get_vocab(req, app, id) -> HTTP.Response\n\nGET /api/v1/datasets/{id}/vocab?scan= (PLAN.md §5.8): what a text dataset's vocabulary covers and what it does not.\n\nReports the size of the vocabulary, the documents it was fitted from, its most frequent tokens, and the running out-of-vocabulary counters: how many queries this dataset has answered, how many tokens they carried, and what fraction of those the vocabulary did not have. That fraction is the signal for a rebuild, which refits the vocabulary over the documents the dataset holds now.\n\nscan=true adds the same fraction measured over the stored documents instead of over the queries. That reading costs time linear in the size of the dataset, so it is bounded: at most sample documents are read, spread evenly over the dataset, and the response says how many it read of how many exist. The default is SSE.DEFAULT_OOV_SCAN_SAMPLE, about a second's work whatever the dataset's size; sample=0 reads every document, which is a request that can take minutes on a large dataset and is better made offline with describe.\n\n404 for a dataset that does not exist or is not loaded, 409 for one that holds no text or whose vocabulary has not been fitted yet – the same condition index! resolves.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_get_vocab"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_healthz-Tuple{HTTP.Request}","page":"Server API","text":"handle_healthz(req) -> HTTP.Response\n\nLiveness (PLAN.md §5.8): the process is up and able to answer HTTP at all. Deliberately checks nothing else — that's what /readyz is for.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_healthz"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_hybrid_search-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_hybrid_search(req, app)\n\nFuses a dense (dense_index) and lexical (lexical_index) search over the same shared doc_id space via RRF (PLAN.md §6), hydrating metadata once at the end from whichever of the two datasets actually has it for a given id.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_hybrid_search"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_jobs_gc-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_jobs_gc(req, app) -> HTTP.Response\n\nPOST /api/v1/admin/jobs/gc (PLAN.md §5.5/§1's similarity-search-ctl jobs gc, and the note that job GC and result-cursor GC share one command since they're the same kind of spool-record lifecycle): sweeps open/ cursors whose TTL lapsed into expired/ (Cursors.gc_expired!), then purges every file now sitting in expired/ (Cursors.purge_expired!) and every completed/failed job record older than retention_seconds (Jobs.purge_finished!, default 24h, overridable in the body – {\"retention_seconds\": 3600} – since a fixed default would make this awkward to exercise in a hardened test without actually waiting a day).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_jobs_gc"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_kill_job-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_kill_job(req, app, job_id) -> HTTP.Response\n\nPOST /api/v1/jobs/{job_id}/kill (PLAN.md §5.5, admin-only): forcibly terminates a running job via the JobExecutor's kill_job!(executor, handle) (§5.5's kill(handle) abstraction) and marks it failed with error: \"killed by operator\". Unlike DELETE (best-effort, only for a not-yet-started job), this is the explicit, always-available override for a job already consuming the reserved compute pool. 409 if the job isn't currently running (nothing to kill) – including the race where the dispatcher's own poll loop finishes the job (naturally, right as this request lands) before finish_job! below gets to run: that raises inside Jobs.update_job_content! (the record is no longer in running/), caught here and reported as the same 409 rather than a 500.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_kill_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_list_datasets-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_list_datasets(req, app) -> HTTP.Response\n\nGET /api/v1/datasets?offset=&limit= (PLAN.md §5.7/§5.4): lists every dataset directory found under <workdir>/datasets/, not just the ones currently cached in app.handles — this listing works purely off each dataset's descriptor.json sidecar and doesn't need reload_datasets! to have run first (see dataset_descriptor's note). Paginated with plain offset/limit (§5.4) — cheap to recompute per request, unlike handle_search's cursor-backed pagination.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_list_datasets"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_list_jobs-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_list_jobs(req, app) -> HTTP.Response\n\nGET /api/v1/jobs?status=&kind=&offset=&limit= (PLAN.md §5.4/§5.5): lists jobs across every spool state, optionally filtered to one status and/or one kind, paginated with plain offset/limit (§5.4) — cheap enough to recompute per request that it doesn't need Cursors. Doesn't support PLAN.md's index_uuid filter: job records in this pass don't track which dataset/index they targeted as a queryable field, and adding that is its own separate bookkeeping change, not part of this endpoint.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_list_jobs"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_list_tokens-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_list_tokens(req, app) -> HTTP.Response\n\nGET /api/v1/admin/tokens (backs similarity-search-ctl log-tokens, PLAN.md §1's admin command surface): an audit listing of every token currently stored, expired or not – token/user/permissions/created_at/expires_at for each, same fields add-token's own response already exposes in plaintext (this is a local admin/control-plane surface, not a public API – see PLAN.md §1's note on how -ctl reaches it).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_list_tokens"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_metrics-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_metrics(req, app) -> HTTP.Response\n\nPrometheus text-format metrics (PLAN.md §5.8), limited to what's actually measurable today: dataset/job counts and per-loaded-dataset doc/tombstone counts. op_log-derived counters (distance_evaluations_total, query_latency_seconds) are still omitted here — Telemetry.log_operation is now wired into search/ftsearch/append/delete (see _run_search/handle_append/handle_delete_item), so cf_op_log has real data to aggregate, but scraping/summarizing it into Prometheus counters (across however many loaded datasets, without re-scanning the whole CF on every /metrics poll) is its own separate design question, not attempted in this pass.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_metrics"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_poll_cursor-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_poll_cursor(req, app, cursor_id) -> HTTP.Response\n\nGET /api/v1/cursors/{cursor_id}?limit=N (PLAN.md §5.4): returns the next page of a result cursor's materialized results and advances it (see handle_search's opt-in page_size param for how a cursor gets created in the first place). 404 if the cursor never existed at all; 409 if it did but isn't open anymore (already exhausted or expired) — the same \"not found\" vs. \"found but not in the right state\" distinction handle_get_job_result already draws for jobs.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_poll_cursor"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_prune_tokens-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_prune_tokens(req, app) -> HTTP.Response\n\nPOST /api/v1/admin/tokens/prune (backs similarity-search-ctl prune-tokens): revokes every token whose expires_at has already passed, via Tokens.prune_expired!.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_prune_tokens"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_readyz-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"handle_readyz(req, app) -> HTTP.Response\n\nReadiness (PLAN.md §5.8): the working directory is accessible and the job/token managers this process depends on for every request were actually constructed. Crash recovery (Jobs.requeue_stale_running!) already runs synchronously in main before run_server is ever called, so there's no separate \"WAL replay pending\" state to check here — by the time this handler is reachable, that pass has already completed.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_readyz"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_reload_dataset-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_reload_dataset(req, app, id) -> HTTP.Response\n\nPOST /api/v1/admin/datasets/{id}/reload (PLAN.md §5.7): (re)opens id from disk into app.handles via _reload_one_dataset!, replacing any existing entry – the deliberate counterpart to reload_datasets!'s cold-start skip-if-already-loaded guard. Closes the current in-memory handle first if id happens to still be loaded (so this also works as a plain \"refresh this dataset's engine from its snapshot\" op, not only after an explicit unload). 404 if id has no descriptor.json on disk at all.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_reload_dataset"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_resume_job-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_resume_job(req, app, job_id) -> HTTP.Response\n\nPOST /api/v1/jobs/{job_id}/resume (PLAN.md §5.5): the inverse of handle_block_job – moves a blocked job back to queued/. 409 if it isn't currently blocked.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_resume_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_submit_job-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_submit_job(req, app, kind)\n\nEnqueues a job (PLAN.md §5.5). Actual dispatch (queued/ -> running/ -> completed//failed/) is owned exclusively by the background loop started alongside the server (Executors.run_dispatcher!), never by this handler.\n\nTwo ways to submit, in priority order: (1) an explicit \"command\" array in the body is always honored as-is (the original, low-level escape hatch — test_e2e.jl's build job relies on this); (2) for kind in HEAVY_JOB_KINDS, a friendly params body (dataset/k/epsilon/queries) is translated into the real CLI invocation via build_heavy_job_command, with a result_ref pointing at the output file the CLI subprocess will write passed into Jobs.create_job!'s extra so it's part of the job's very first write (see that function's docstring for why a separate post-creation update would race the dispatcher). Jobs.finish_job!'s merge!-based content update preserves it through every later state transition, so GET /api/v1/jobs/{id}/result can serve it once status == completed with no further plumbing. Anything else is a 400.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_submit_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.handle_unload_dataset-Tuple{HTTP.Request, SimilaritySearchServer.Server.AppState, String}","page":"Server API","text":"handle_unload_dataset(req, app, id) -> HTTP.Response\n\nPOST /api/v1/admin/datasets/{id}/unload (PLAN.md §5.7's \"hot-reload a single dataset\" open issue): closes this dataset's RocksDB handle and drops it from app.handles, so an external process – concretely, the CLI rebuild (§1) – can safely open it for exclusive writing without hitting RocksDB's per-process write lock. The intended flow for refreshing a live server's view of a dataset after an out-of-band CLI rebuild: unload -> run rebuild -> reload (handle_reload_dataset below). 404 if id isn't currently loaded (nothing to unload).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.handle_unload_dataset"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.inprocess_batch_cap-Tuple{Int64, Real}","page":"Server API","text":"inprocess_batch_cap(total_threads, batch_pct) -> Int\n\nHow far an operation that runs inside this process may spread over threads.\n\nIndexing, calibration and the other batch operations of the engine divide their work with SimilaritySearch.@BATCHES, which dispatches one task per batch and takes the batch count from getminbatch: 8 * Threads.nthreads() by default, capped by the context's maxbatches. In a job that number is already right, because a job is a subprocess started with JULIA_NUM_THREADS set to its share. In this process it is not: an append that triggers index! would spread over every thread of the machine and compete with the searches _with_query_slot is bounding.\n\nCapping the batch count at the reserved share is what the library's own documentation describes as trading parallelism for memory, and here for latency: with fewer batches than threads, the remaining threads stay available to answer queries. The cost is that in-process indexing is slower, which is the trade §2 wanted made explicit.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.inprocess_batch_cap"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.join_group_members-Tuple{SimilaritySearchServer.Server.AppState, AbstractString}","page":"Server API","text":"join_group_members(app, join_group) -> Vector{Dict{String,Any}}\n\nScans every dataset descriptor under <workdir>/datasets/ for ones tagged with the given join_group (PLAN.md §1/§7's JoinGroup) – there's no separate join-group table, the group is purely an emergent grouping of datasets that happen to share the same tag in their own descriptor. Reads the raw persistent descriptor directly (not dataset_descriptor's live-stats merge) since callers here only need the small set of identifying fields (id/index_kind/key/holds_metadata), not per-request doc counts.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.join_group_members"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.parse_index_kind-Tuple{AbstractString}","page":"Server API","text":"parse_index_kind(kind::AbstractString) -> (engine_type, backend_type)\n\nThe wire's name for a kind of project, as the pair the engine actually takes.\n\nA project is two independent choices now – what it holds (DenseEngine, SparseEngine, FullTextEngine) and what indexes it (SearchGraph, BM25InvertedFile, ...) – while this API has always had a single index_kind string. The old names keep working and map to the pair they always meant; sparse_invfile is new, and is the one that also needs dimension. Raises SSE.UnknownBackend, so a handler reports it the same way it reports any other invalid request.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.parse_index_kind"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.parse_meta_schema-Tuple{AbstractDict}","page":"Server API","text":"parse_meta_schema(data::AbstractDict) -> Schema.MetaSchema\n\nThe optional \"meta_schema\" of a POST /api/v1/datasets body, as the engine's own declaration (PLAN.md §4.5). The wire form is an array of {\"name\": ..., \"type\": ...}, with the type one of string, int64, float64, bool or timestamp:\n\n{\"id\": \"papers\", \"index_kind\": \"bm25_invfile\",\n \"meta_schema\": [{\"name\": \"year\", \"type\": \"int64\"}, {\"name\": \"lang\", \"type\": \"string\"}]}\n\nAn absent meta_schema is the empty declaration, which is what every dataset had before this existed: meta is stored as it arrives. Declaring is additive – an undeclared field is still accepted – so this says what the declared fields must hold, not what an item may carry. Raises SSE.InvalidOption for a malformed entry or an unknown type, which the handler answers as a 400 like any other invalid request.\n\nThere is no indexed flag. §4.5 proposed one, to back a pre-filter over a meta_idx_<field> column family; the decision on 2026-09-13 was that meta is stored with a schema and not indexed, and that filtering keeps using the indexes the engine already maintains (keywords, refs, doc_id).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.parse_meta_schema"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.query_slot_count-Tuple{Int64, Real, Int64}","page":"Server API","text":"query_slot_count(total_threads, batch_pct, configured) -> Int\n\nHow many searches this server runs at once (PLAN.md §2).\n\nconfigured is [resources] max_concurrent_queries, and any positive value is used as given. Zero derives it from the thread split: the threads not reserved for job execution, which is what the rest of the machine is for. At least one, so a server with a tiny share still answers.\n\nThe bound exists because unbounded concurrency does not buy throughput and does cost memory. Past the number of threads, more concurrent searches share the same cores and every one of them gets slower; and each concurrent search holds a context from the engine's pool, which grows to the highest concurrency ever reached and does not shrink.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.query_slot_count"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.reload_datasets!-Tuple{SimilaritySearchServer.Server.AppState}","page":"Server API","text":"reload_datasets!(app::AppState) -> Vector{String}\n\nScans <workdir>/datasets/ and reopens every dataset found there into app.handles – the fix for the previously-documented gap where a restarted similarity-search-serve could list/describe existing datasets (their descriptor.json sidecar is enough for that) but not search/append/ftsearch/calibrate them until they were recreated, since nothing ever called IndexEngine.restore_engine from the HTTP startup path. Meant to be called once, right after AppState is constructed and before run_server starts accepting requests.\n\nSkips any id already present in app.handles (a no-op in the normal \"cold start with empty dicts\" case, but this guard also makes the function safe to call against an already-populated AppState, e.g. from a test that only unloaded one dataset). To force-refresh a dataset that's already loaded, see handle_reload_dataset instead (PLAN.md §5.7's \"hot-reload a single dataset\" note) – this function deliberately never overwrites an already-loaded entry on its own.\n\nA dataset created with POST /api/v1/datasets and never appended to reopens as an empty engine of its declared index_kind and distance, which is the state it was left in.\n\nReturns the list of dataset ids actually (re)loaded, for a startup log line.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.reload_datasets!"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.rrf_fuse-Tuple{Vector}","page":"Server API","text":"rrf_fuse(rank_lists; k=60) -> (ids, scores)\n\nReciprocal Rank Fusion (PLAN.md §6): combines several already-ranked (best-first) id lists into one score per id, sum(1 / (rank + k)) across the lists it appears in.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.rrf_fuse"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.run_server-Tuple{String, Int64, SimilaritySearchServer.Server.AppState}","page":"Server API","text":"run_server(host::String, port::Int, app::AppState; async::Bool=false)\n\nRegisters all HTTP routes and starts the Oxygen.jl server.\n\nArguments\n\nhost::String: The host to bind the server to (e.g., \"127.0.0.1\").\nport::Int: The port to listen on.\napp::AppState: The global application state.\nasync::Bool: If true, returns immediately instead of blocking (used by tests).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.run_server"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.serialize_meta_schema-Tuple{Any}","page":"Server API","text":"serialize_meta_schema(schema::Schema.MetaSchema) -> Vector\n\nThe inverse of parse_meta_schema: the declaration in the shape a response reports it, [{\"name\": ..., \"type\": ...}, ...]. Used by the dataset endpoints, which read the declaration from the project itself – the engine stores it there, so a restarted server recovers it by opening the dataset and there is no second copy to keep in step.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.serialize_meta_schema"},{"category":"method","location":"server.html#SimilaritySearchServer.Server.token_fingerprint-Tuple{Nothing}","page":"Server API","text":"token_fingerprint(token) -> Union{Nothing, String}\n\nA token as it may be written down: the first eight bytes of its SHA-256, in hexadecimal.\n\nEnough to tell two tokens apart in a log and to match a record against a token someone holds, and not enough to present as one. nothing for no token.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.token_fingerprint"},{"category":"constant","location":"server.html#SimilaritySearchServer.Server.BEAMSEARCH_SAFETY_MULTIPLIER","page":"Server API","text":"BEAMSEARCH_SAFETY_MULTIPLIER\n\nThe §3 Core execution barrier's safety multiplier: a per-request beamsearch_overrides (§3/§7) may not exceed this factor times the index's own calibrated baseline (or IndexEngine.DEFAULT_BEAMSEARCH if calibrate!/POST .../calibrate was never called) — anchored to a real, per-index baseline rather than the arbitrary global constants (bsize<=20, Δ<1.7) PLAN.md's own §3 warning flags as unjustified.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.BEAMSEARCH_SAFETY_MULTIPLIER"},{"category":"constant","location":"server.html#SimilaritySearchServer.Server.DATASET_ID_PATTERN","page":"Server API","text":"id values are interpolated directly into filesystem paths (dataset directories, descriptor sidecar files) — PLAN.md §4.6 flags exactly this class of risk for the similar key field. Whitelist before it ever touches a path.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.DATASET_ID_PATTERN"},{"category":"constant","location":"server.html#SimilaritySearchServer.Server.HEAVY_JOB_KINDS","page":"Server API","text":"HEAVY_JOB_KINDS\n\nJob kinds this pass actually implements as real CLI subcommands (src/cli_handlers.jl's execute_allknn/execute_fft/execute_neardup/execute_hsp/execute_dump/ execute_load), as opposed to arbitrary kinds only reachable via an explicit \"command\" array (see handle_submit_job). dump/load complete PLAN.md §5.5's job-kind enum – chunk 10 shipped them as CLI-only; this pass adds the POST /api/v1/jobs/dump|load surface on top of the same execute_dump/execute_load CLI functions, unchanged.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.HEAVY_JOB_KINDS"},{"category":"constant","location":"server.html#SimilaritySearchServer.Server.JOB_STATE_BY_NAME","page":"Server API","text":"JOB_STATE_BY_NAME\n\nLowercase-name lookup for Jobs.JobState, so handle_list_jobs's ?status= query param can resolve to the enum without hand-rolling a string-match chain.\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.JOB_STATE_BY_NAME"},{"category":"constant","location":"server.html#SimilaritySearchServer.Server.KEY_PATTERN","page":"Server API","text":"A text/lexical join-group member's key (PLAN.md §1/§5.3) is user-supplied and gets echoed back into API responses and compared against elsewhere – whitelist it the same way as dataset id, and separately reject the literal \"*\", which is reserved for ftsearch's fan-out wildcard and must never be a real member's key (PLAN.md §5.3's validation note).\n\n\n\n\n\n","title":"SimilaritySearchServer.Server.KEY_PATTERN"},{"category":"type","location":"server.html#SimilaritySearchServer.Jobs.JobManager","page":"Server API","text":"JobManager\n\nJob spooling manager. Maintains the root working directory.\n\nFields\n\nworkdir::String: Root path of the spooling directories.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.JobManager"},{"category":"type","location":"server.html#SimilaritySearchServer.Jobs.JobRecord","page":"Server API","text":"JobRecord\n\nIn-memory structure representing a Job's metadata.\n\nFields\n\nid::String: Unique job identifier.\nkind::String: Job kind (e.g., searchbatch, build).\ncommand::Vector{String}: The command arguments for the job.\ncreated_at::String: Timestamp of creation.\nprogress::Float64: Job progress (0.0 to 1.0).\nexecutor_handle::Union{String, Nothing}: OS-level handle assigned by the executor.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.JobRecord"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.cancel_job!-Tuple{SimilaritySearchServer.Jobs.JobManager, String}","page":"Server API","text":"cancel_job!(manager::JobManager, id::String) -> Symbol\n\nCancels a job that hasn't started running yet (PLAN.md §5.5's DELETE /api/v1/jobs/{job_id} – distinct from kill, which only ever applies to an already- running job consuming the reserved compute pool, §5.5's own explicit \"kill vs. DELETE\" note). Moves id out of queued//blocked/ straight into failed/ with error=\"cancelled by operator\" – the same \"terminal state + descriptive error\" shape finish_job! already uses for a killed job.\n\nReturns :cancelled on success, :not_cancellable if id is already running/ completed/failed (the caller should reject with 409 – a running job needs kill instead, not a silent no-op or a fallback to killing it), or :not_found if id doesn't exist at all.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.cancel_job!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.create_job!-Tuple{SimilaritySearchServer.Jobs.JobManager, String, Vector{String}}","page":"Server API","text":"create_job!(manager::JobManager, kind::String, command::Vector{String}; id::String=generate_id(), extra::AbstractDict=Dict()) -> String\n\nCreates a job in Queued state by writing it to a temporary file and atomically renaming it to its destination.\n\nArguments\n\nmanager::JobManager: The job manager instance.\nkind::String: The kind of job being created.\ncommand::Vector{String}: Command arguments for the executor.\nid::String: Job id to use; defaults to a fresh generated one. Callers that need to know the id before creation (e.g. to embed it in an output file path) can pass their own – see Server.handle_submit_job.\nextra::AbstractDict: Extra fields merged into the record at creation time (e.g. \"result_ref\"). Must be set here, in the same write as the rest of the record, rather than via a follow-up update_job_content! call – the background dispatcher (Executors.run_dispatcher!) can move a job from queued/ to running/ the instant it's written, and a separate post-creation update would race it (the job may no longer be in queued/ by the time the update runs, and update_job_content! errors if it can't find the record in the state it expects).\n\nReturns\n\nString: The job ID (the generated one, or id if explicitly given).\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.create_job!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.finish_job!-Tuple{SimilaritySearchServer.Jobs.JobManager, String, Bool}","page":"Server API","text":"finish_job!(manager::JobManager, id::String, success::Bool; result=nothing, error=nothing)\n\nMoves a job out of running/ into its terminal state (completed/ on success, failed/ otherwise), recording the result/error payload.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.finish_job!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.get_job-Tuple{SimilaritySearchServer.Jobs.JobManager, String}","page":"Server API","text":"get_job(manager::JobManager, id::String) -> Tuple{Union{JobState, Nothing}, Union{Dict, Nothing}}\n\nSearches for a job across all states and returns its (state, parsed_data).\n\nArguments\n\nmanager::JobManager: The job manager instance.\nid::String: The job ID to look up.\n\nReturns\n\nTuple: Returns the JobState and a dictionary of parsed TOML data if found; otherwise (nothing, nothing).\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.get_job"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.init_job_manager-Tuple{String}","page":"Server API","text":"init_job_manager(workdir::String) -> JobManager\n\nInitializes the atomic spooling directories for jobs.\n\nArguments\n\nworkdir::String: Base directory path to initialize spooling.\n\nReturns\n\nJobManager: Initialized manager instance.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.init_job_manager"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.list_jobs-Tuple{SimilaritySearchServer.Jobs.JobManager, SimilaritySearchServer.Jobs.JobState}","page":"Server API","text":"list_jobs(manager::JobManager, state::JobState) -> Vector{String}\n\nLists the job ids currently sitting in state's spool directory (used by the dispatch loop to find work in queued/).\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.list_jobs"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.purge_finished!-Tuple{SimilaritySearchServer.Jobs.JobManager}","page":"Server API","text":"purge_finished!(manager::JobManager; retention_seconds::Real=86400) -> Vector{String}\n\nThe actual disk-reclaim half of similarity-search-ctl jobs gc (PLAN.md §5.5's warning that completed/failed grow unboundedly otherwise, since nothing else ever removes a finished job's file). Deletes every completed//failed/ job record whose finished_at is older than retention_seconds ago, along with its result_ref file if it has one and it still exists (a purged job's result is unreachable via GET /api/v1/jobs/{id}/result anyway, since the record itself is gone). Returns the ids removed.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.purge_finished!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.requeue_stale_running!-Tuple{SimilaritySearchServer.Jobs.JobManager}","page":"Server API","text":"requeue_stale_running!(manager::JobManager) -> Vector{String}\n\nCrash-recovery pass: any job left in running/ when the server starts (because a previous serve process died mid-dispatch) is re-queued so the dispatcher picks it back up. Returns the ids that were requeued.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.requeue_stale_running!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.update_job_content!-Tuple{SimilaritySearchServer.Jobs.JobManager, SimilaritySearchServer.Jobs.JobState, String, Dict}","page":"Server API","text":"update_job_content!(manager::JobManager, state::JobState, id::String, updates::Dict)\n\nRewrites a job's record (while it stays in state's directory) with the given field updates merged in, via write-to-temp-then-rename so a reader never observes a torn record. Used by the dispatcher to record the real executor_handle once Executors.submit returns it.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.update_job_content!"},{"category":"method","location":"server.html#SimilaritySearchServer.Jobs.update_job_state!-Tuple{SimilaritySearchServer.Jobs.JobManager, String, SimilaritySearchServer.Jobs.JobState, SimilaritySearchServer.Jobs.JobState}","page":"Server API","text":"update_job_state!(manager::JobManager, id::String, old_state::JobState, new_state::JobState)\n\nTransitions a job between states by atomically moving it between directories.\n\nArguments\n\nmanager::JobManager: The job manager instance.\nid::String: The job ID.\nold_state::JobState: Expected current state.\nnew_state::JobState: State to transition to.\n\n\n\n\n\n","title":"SimilaritySearchServer.Jobs.update_job_state!"},{"category":"type","location":"server.html#SimilaritySearchServer.Executors.AbstractJobExecutor","page":"Server API","text":"AbstractJobExecutor\n\nMain abstraction for any Job execution backend.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.AbstractJobExecutor"},{"category":"type","location":"server.html#SimilaritySearchServer.Executors.LocalCLIExecutor","page":"Server API","text":"LocalCLIExecutor <: AbstractJobExecutor\n\nLocal execution backend. Launches child CLI processes to do heavy lifting without blocking the server or the RocksDB database.\n\nFields\n\nrunning_processes::Dict{String, Base.Process}: A mapping of handles (PIDs) to running processes.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.LocalCLIExecutor"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors._job_command-Tuple{Vector{String}, Int64}","page":"Server API","text":"_job_command(cli_args::Vector{String}, threads::Int) -> Cmd\n\nThe command a job runs, with its thread budget. Base.julia_cmd() carries the current process's -C/-J/-g options but not its --threads, so without this a job subprocess starts with one thread regardless of how the server was started.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors._job_command"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.job_thread_budget-Tuple{Int64, Real}","page":"Server API","text":"job_thread_budget(total_threads::Int, batch_pct::Real; max_jobs::Int=4) -> (max_concurrent, threads_per_job)\n\nHow much of the machine job execution may use, from the share of threads reserved for it ([resources] batch_threads_pct, PLAN.md §2).\n\nA job runs as its own process, so what bounds it is a number of processes and a number of threads each. The reserved share is the product: at most max_concurrent jobs at once, each with threads_per_job, and max_concurrent * threads_per_job never exceeds the share.\n\nmax_jobs is the ceiling on concurrency, so that a large machine runs a few jobs with several threads each rather than many jobs with one thread each; indexing and the operations over a whole dataset are parallel inside one process, and splitting them across more processes does not make them faster.\n\nBoth results are at least 1: a share that rounds to zero still executes jobs, one at a time on one thread, because a server that accepts a job and never runs it is worse than a slow one.\n\njob_thread_budget(64, 20)   # (4, 3)  -- 12 threads of the 12.8 reserved\njob_thread_budget(8, 20)    # (1, 1)\njob_thread_budget(32, 50)   # (4, 4)\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.job_thread_budget"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.kill_job!-Tuple{SimilaritySearchServer.Executors.LocalCLIExecutor, String}","page":"Server API","text":"kill_job!(executor::LocalCLIExecutor, handle::String)\n\nForces the process to stop if it is running (SIGTERM).\n\nArguments\n\nexecutor::LocalCLIExecutor: The execution backend instance.\nhandle::String: The OS-level handle (PID).\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.kill_job!"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.poll-Tuple{SimilaritySearchServer.Executors.LocalCLIExecutor, String}","page":"Server API","text":"poll(executor::LocalCLIExecutor, handle::String) -> String\n\nQueries the OS-level liveness state of the process. Note: Logical progress is read from the .job.toml file, but this verifies if the worker is still alive.\n\nArguments\n\nexecutor::LocalCLIExecutor: The execution backend instance.\nhandle::String: The OS-level handle (PID) returned by submit.\n\nReturns\n\nString: The current status (\"running\", \"completed\", \"failed\", \"unknown\").\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.poll"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.result_path-Tuple{SimilaritySearchServer.Executors.LocalCLIExecutor, String}","page":"Server API","text":"result_path(executor::LocalCLIExecutor, handle::String) -> String\n\nReturns the theoretical path where the worker will leave its final results (cursors or logs).\n\nArguments\n\nexecutor::LocalCLIExecutor: The execution backend instance.\nhandle::String: The OS-level handle (PID).\n\nReturns\n\nString: The expected path for results.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.result_path"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.run_dispatcher!-Tuple{SimilaritySearchServer.Jobs.JobManager, SimilaritySearchServer.Executors.AbstractJobExecutor}","page":"Server API","text":"run_dispatcher!(job_mgr::Jobs.JobManager, executor::AbstractJobExecutor; max_concurrent::Int=4, threads_per_job::Int=1, poll_interval::Real=0.5)\n\nBackground loop (meant to be run inside @async) that owns every queued/ -> running/ -> completed/|failed/ job transition (PLAN.md §5.5). The HTTP layer only ever calls Jobs.create_job! to enqueue; this loop is what actually dispatches and finishes jobs. Crash recovery for jobs orphaned in running/ by a previous process must be done by the caller (via Jobs.requeue_stale_running!) before starting this loop.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.run_dispatcher!"},{"category":"method","location":"server.html#SimilaritySearchServer.Executors.submit-Tuple{SimilaritySearchServer.Executors.LocalCLIExecutor, Vector{String}}","page":"Server API","text":"submit(executor::LocalCLIExecutor, cli_args::Vector{String}; threads::Int=1) -> String\n\nSubmits a job by launching a CLI subprocess.\n\nArguments\n\nexecutor::LocalCLIExecutor: The execution backend instance.\ncli_args::Vector{String}: Command line arguments to pass to the subprocess.\n\nReturns\n\nString: A 'handle' (in this case, the PID string) used to query or kill the process.\n\n\n\n\n\n","title":"SimilaritySearchServer.Executors.submit"},{"category":"type","location":"server.html#SimilaritySearchServer.Tokens.TokenManager","page":"Server API","text":"TokenManager\n\nManages the global token database.\n\nFields\n\ndb::RocksDB.DB: The main RocksDB database handle for tokens.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.TokenManager"},{"category":"type","location":"server.html#SimilaritySearchServer.Tokens.TokenRecord","page":"Server API","text":"TokenRecord\n\nRepresents a token and its Access Control List (ACL).\n\nFields\n\ntoken_str::String: The secret token string.\nuser::String: The user associated with the token.\npermissions::Vector{String}: A list of permitted actions.\ncreated_at::String: Token creation timestamp.\nexpires_at::Union{String, Nothing}: Expiration timestamp or nothing if it doesn't expire.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.TokenRecord"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.any_token_exists-Tuple{SimilaritySearchServer.Tokens.TokenManager}","page":"Server API","text":"any_token_exists(manager::TokenManager) -> Bool\n\nWhether the database holds at least one token. serve uses it to refuse to start with authentication enabled and no token defined, a state in which no request could be answered.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.any_token_exists"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.create_token!-Tuple{SimilaritySearchServer.Tokens.TokenManager, String, Vector{String}}","page":"Server API","text":"create_token!(manager::TokenManager, user::String, permissions::Vector{String}; expires_at::Union{String, Nothing}=nothing) -> String\n\nCreates a new token and stores it in the database.\n\nArguments\n\nmanager::TokenManager: The token manager instance.\nuser::String: User name or identifier.\npermissions::Vector{String}: List of permissions.\nexpires_at::Union{String, Nothing}: Optional expiration timestamp.\n\nReturns\n\nString: The generated token string.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.create_token!"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.get_token-Tuple{SimilaritySearchServer.Tokens.TokenManager, String}","page":"Server API","text":"get_token(manager::TokenManager, token_str::String) -> Union{TokenRecord, Nothing}\n\nRetrieves a token by its string representation.\n\nArguments\n\nmanager::TokenManager: The token manager instance.\ntoken_str::String: The token string to search for.\n\nReturns\n\nTokenRecord: The retrieved token record, or nothing if not found.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.get_token"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.has_permission-Tuple{SimilaritySearchServer.Tokens.TokenRecord, Symbol, AbstractString}","page":"Server API","text":"has_permission(record::TokenRecord, op::Symbol, dataset::AbstractString) -> Bool\n\nWhether record grants op on dataset. A permission matches when its operation is at least op (see PERMISSION_OPS) and its scope is either dataset itself or *.\n\ndataset is \"*\" for an endpoint that is not about one dataset – listing datasets, the job endpoints, the token endpoints – and only a permission scoped to * matches it. A token restricted to one dataset therefore uses that dataset's endpoints and cannot list what else the server holds.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.has_permission"},{"category":"function","location":"server.html#SimilaritySearchServer.Tokens.is_expired","page":"Server API","text":"is_expired(record::TokenRecord, at::DateTime=now(UTC)) -> Bool\n\nWhether the token's expires_at is in the past. A token with no expires_at does not expire. A timestamp that does not parse counts as expired: create_token! rejects such a value, so a stored one is either damaged or was written by an older version, and refusing it is the safe reading.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.is_expired"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.list_tokens-Tuple{SimilaritySearchServer.Tokens.TokenManager}","page":"Server API","text":"list_tokens(manager::TokenManager) -> Vector{TokenRecord}\n\nLists every token currently stored, expired or not – TokenManager's db has no column families (a plain default-CF RocksDB, unlike ProjectManager), so this is a straight RocksDB.DBIterator(manager.db) scan, no cf= needed. Backs both similarity-search-ctl log-tokens (an audit listing) and prune_expired!'s own scan below.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.list_tokens"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.normalize_permissions-Tuple{Any}","page":"Server API","text":"normalize_permissions(permissions) -> Vector{String}\n\nThe same permissions in the canonical operation:dataset form, with unrecognized entries removed. Applied when a token is created, so that what is stored is what will be compared.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.normalize_permissions"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.open_token_manager-Tuple{String}","page":"Server API","text":"open_token_manager(workdir::String) -> TokenManager\n\nOpens or creates the global token RocksDB database in the working directory.\n\nArguments\n\nworkdir::String: The working directory containing the database.\n\nReturns\n\nTokenManager: The initialized token manager.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.open_token_manager"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.parse_permission-Tuple{AbstractString}","page":"Server API","text":"parse_permission(s::AbstractString) -> Union{Nothing, Tuple{Symbol, String}}\n\nOne permission string as the pair it means: \"write:corpus_es\" is (:write, \"corpus_es\"), and the bare form \"read\" is (:read, \"*\"), which is how a permission that names no dataset applies to every dataset. nothing for a string that is not a permission, so that a token carrying one is not thereby granted something.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.parse_permission"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.prune_expired!-Tuple{SimilaritySearchServer.Tokens.TokenManager}","page":"Server API","text":"prune_expired!(manager::TokenManager) -> Int\n\nDeletes every token whose expires_at is a real (non-nothing) timestamp already in the past, returning how many were removed. Backs similarity-search-ctl prune-tokens. expires_at is a free-form, user-supplied string at create_token! time (never validated against one format anywhere in this codebase) – a token whose expires_at doesn't parse as a Dates.DateTime is skipped rather than aborting the whole sweep over one bad value.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.prune_expired!"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.revoke_token!-Tuple{SimilaritySearchServer.Tokens.TokenManager, String}","page":"Server API","text":"revoke_token!(manager::TokenManager, token_str::String)\n\nRevokes (deletes) a token from the database.\n\nArguments\n\nmanager::TokenManager: The token manager instance.\ntoken_str::String: The token string to delete.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.revoke_token!"},{"category":"method","location":"server.html#SimilaritySearchServer.Tokens.validate_token-Tuple{SimilaritySearchServer.Tokens.TokenManager, Union{Nothing, AbstractString}}","page":"Server API","text":"validate_token(manager::TokenManager, token_str) -> Union{Nothing, TokenRecord}\n\nThe record for token_str if the token exists and has not expired, and nothing otherwise. This is the function a request handler calls; get_token is the plain lookup and does not consider expiry.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.validate_token"},{"category":"constant","location":"server.html#SimilaritySearchServer.Tokens.PERMISSION_OPS","page":"Server API","text":"PERMISSION_OPS\n\nThe three operations a permission can grant, ordered from least to most: :read covers the queries (search, ftsearch, fetch, exists) and the GET endpoints, :write covers insertion, deletion, calibration and job submission, and :admin covers the creation and deletion of datasets, the token endpoints, and the control of jobs.\n\nAn operation includes the ones before it for the same dataset: a token with write:corpus may also read corpus, and admin:* grants everything.\n\n\n\n\n\n","title":"SimilaritySearchServer.Tokens.PERMISSION_OPS"},{"category":"type","location":"server.html#SimilaritySearchServer.Telemetry.MetricsRegistry","page":"Server API","text":"MetricsRegistry\n\nThe counters /metrics reports, accumulated as requests are recorded. Every operation that writes a record into the op_log adds the same numbers here, so the endpoint answers from memory and does not read the log: a metrics collector polls every few seconds, and reading a column family that grows with every request would make each poll more expensive than the requests it measures.\n\nThe log remains the durable record, and GET /api/v1/datasets/{id}/log reads it. These counters cover this process only, and start at zero when it starts, which is what a Prometheus counter is expected to do; simsearch_process_start_time_seconds reports when that was, so a collector can tell a restart from a drop in traffic.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.MetricsRegistry"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.identity_fields-Tuple{Tuple}","page":"Server API","text":"identity_fields(identity) -> Dict{String,Any}\n\nThe (user, token_fingerprint) pair as the two fields of a log record: user and token_fingerprint. Written by every operation that records one, so a record identifies who acted without carrying what they acted with.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.identity_fields"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.log_operation-Tuple{SimilaritySearchEngine.Project.ProjectManager, String, Dict{String, Any}}","page":"Server API","text":"log_operation(manager::ProjectManager, op_type::String, details::Dict{String, Any})\n\nLogs an operation into the op_log Column Family for telemetry. Includes timestamp, operation type, and arbitrary details (like distance evaluations).\n\nArguments\n\nmanager::ProjectManager: The dataset manager.\nop_type::String: The type of operation being logged.\ndetails::Dict{String, Any}: Arbitrary details about the operation.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.log_operation"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.log_request!-Tuple{SimilaritySearchEngine.Project.ProjectManager, Any, String, String, Float64, Any}","page":"Server API","text":"log_request!(manager, ctx, op_type, index_uuid, t0, snapshot; token=nothing, distance_name=nothing, dimension=nothing, extra=Dict{String,Any}())\n\nLogs one op_log record for a request that just finished: elapsed wall-clock time since t0 (time()-based, matching PLAN.md §4.3's \"delta computing time\"), and the real distance-evaluation count for this operation via SimilaritySearch.distance_evaluations diffed against snapshot (from snapshot_costs, taken before the operation ran) – 0 if ctx/snapshot is nothing rather than erroring, since not every op touches an index context (e.g. a soft-delete). identity is the pair Server._request_identity produces, (user, token_fingerprint): who made the request, when its token was valid, and a fingerprint of the token presented. distance_name/dimension are nothing when not meaningful for op_type (see the call sites in server.jl). extra merges in any op-type-specific fields (e.g. append's items_inserted).\n\nUntil 2026-09-14 this recorded the Authorization header verbatim. That put live credentials in a column family of the project, in every dump of it, and in front of anyone allowed to read the log.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.log_request!"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.prometheus_lines-Tuple{SimilaritySearchServer.Telemetry.MetricsRegistry}","page":"Server API","text":"prometheus_lines(registry::MetricsRegistry) -> Vector{String}\n\nThe counters of registry in the Prometheus text format: one counter of requests and one histogram of durations per dataset and operation, and one counter of distance computations and one of inserted items per dataset. A registry that has recorded nothing produces the declarations and no samples, which is a valid exposition and lets a collector distinguish \"no traffic\" from \"this server does not report this\".\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.prometheus_lines"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.record_metric!-Tuple{SimilaritySearchServer.Telemetry.MetricsRegistry, AbstractString, AbstractString, Real}","page":"Server API","text":"record_metric!(registry, dataset, operation, elapsed_seconds; distance_evaluations=0, items_inserted=0)\n\nAdds one request to the counters. Called from log_request! and log_operation with the same values they write into the op_log, so the two cannot disagree about what happened. A nothing registry records nothing, which is what the command lines and the tests that call the logging functions directly pass.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.record_metric!"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.record_wait!-Tuple{SimilaritySearchServer.Telemetry.MetricsRegistry, Real}","page":"Server API","text":"record_wait!(registry, seconds)\n\nAdds one wait for a query slot. A wait of zero counts too: it is the common case on a server below its bound, and counting it keeps the average honest.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.record_wait!"},{"category":"method","location":"server.html#SimilaritySearchServer.Telemetry.snapshot_costs-Tuple{Any}","page":"Server API","text":"snapshot_costs(ctx) -> Union{Nothing, Vector{Int}}\n\nCopies ctx.costdists (the per-batch distance-evaluation counters every AbstractContext – GenericContext/SearchGraphContext/InvertedFileContext alike – carries, per SimilaritySearch.jl's own cost-accounting API) before an operation, so the delta against it after the operation gives that operation's own cost rather than the index's lifetime total (ctx.costdists is never reset). nothing for ctx === nothing (an untrained text engine has no context yet) – callers pass that straight through to log_request!, which then just reports 0 distance evaluations instead of erroring.\n\nThis works for insertion (handle_append), which runs on the engine's own backend.ctx under an exclusive write lock. It does not work for a search: a search runs on a context borrowed from the engine's pool for the duration of the call, shared with whatever else is searching concurrently, so _run_search asks the engine for the count instead (SimilaritySearchEngine.SearchStats) and passes it to log_request! directly.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.snapshot_costs"},{"category":"constant","location":"server.html#SimilaritySearchServer.Telemetry.LATENCY_BUCKETS","page":"Server API","text":"LATENCY_BUCKETS\n\nUpper bounds, in seconds, of the histogram of request durations. They cover the range this server answers in: a text or dense query resolved from memory, at one millisecond, up to a request that waits on storage, at seconds.\n\n\n\n\n\n","title":"SimilaritySearchServer.Telemetry.LATENCY_BUCKETS"},{"category":"type","location":"server.html#SimilaritySearchServer.Cursors.CursorManager","page":"Server API","text":"CursorManager\n\nCursor spooling manager (PLAN.md §5.4/§4.6), structurally identical to Jobs.JobManager — an opaque result cursor is a spool record with the same create/poll/expire lifecycle a Job has, just with three states instead of five and JSON records instead of TOML (JSON round-trips an arbitrary already-materialized result list — a page of search/allknn/etc. results — more naturally than TOML does).\n\nFields\n\nworkdir::String: Root path of the spooling directories.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.CursorManager"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.create_cursor!-Tuple{SimilaritySearchServer.Cursors.CursorManager, String, AbstractVector}","page":"Server API","text":"create_cursor!(manager, index_uuid, results::AbstractVector; page_size=20, ttl_seconds=300) -> String\n\nMaterializes an already-computed, ordered results list behind a fresh opaque cursor id, written once in open/ state. PLAN.md §5.4: a cursor exists specifically because paging through an expensive-to-recompute result set (a large search, a filtered listing that had to over-fetch to satisfy k, a job's result) must reuse the same materialized page(s) on every subsequent request rather than re-running the underlying query per page — so the full results list is stored once, up front, and later poll_cursor! calls only ever slice it.\n\nArguments\n\nmanager::CursorManager\nindex_uuid::String: which dataset/index this cursor's results came from (recorded for operator visibility — PLAN.md's own path convention nests cursors under it, though this implementation keeps the flat layout Jobs.JobManager already established for the same pragmatic reason: no separate per-index directory bookkeeping to keep in sync).\nresults::AbstractVector: the full, already-ordered result set to page through. Must be JSON-serializable (plain Dict/Vector/primitive values — the same shapes every other handler in Server already returns).\npage_size::Int: default page size for poll_cursor! calls that don't override it.\nttl_seconds::Int: how long the cursor stays pollable before poll_cursor!/gc_expired! treat it as expired.\n\nReturns\n\nString: the generated cursor id.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.create_cursor!"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.gc_expired!-Tuple{SimilaritySearchServer.Cursors.CursorManager}","page":"Server API","text":"gc_expired!(manager) -> Vector{String}\n\nSweeps open/ for any cursor whose TTL has lapsed without ever being fully drained and moves it to expired/ (an exhausted/ cursor already reached its natural end and isn't touched here — PLAN.md's similarity-search-ctl jobs gc note groups Job and cursor GC under one operator command, see Server.handle_jobs_gc, which calls this and purge_expired! together). Returns the ids that were moved.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.gc_expired!"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.get_cursor-Tuple{SimilaritySearchServer.Cursors.CursorManager, String}","page":"Server API","text":"get_cursor(manager, id) -> (state, record) or (nothing, nothing)\n\nSearches for a cursor across all three states and returns its (state, parsed_record), mirroring Jobs.get_job. Doesn't mutate anything or check expiry — use poll_cursor! to actually advance/expire a cursor; this is the read-only lookup for e.g. reporting status.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.get_cursor"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.init_cursor_manager-Tuple{String}","page":"Server API","text":"init_cursor_manager(workdir::String) -> CursorManager\n\nInitializes the atomic spooling directories for cursors (<workdir>/cursors/{open,exhausted,expired}/).\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.init_cursor_manager"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.poll_cursor!-Tuple{SimilaritySearchServer.Cursors.CursorManager, String}","page":"Server API","text":"poll_cursor!(manager, id; limit=nothing) -> Union{NamedTuple, Nothing}\n\nReturns the next page of an open/ cursor's materialized results and advances its offset, transitioning it to exhausted/ once the underlying result set is fully drained (or to expired/ if its TTL had already lapsed since the last poll). Returns nothing if id isn't currently in open/ — already exhausted/expired, or never existed at all; callers that need to tell those two cases apart should check get_cursor first.\n\nKeyword Arguments\n\nlimit::Union{Int,Nothing}: page size for this call only; defaults to the cursor's own page_size recorded at creation time.\n\nReturns\n\nA (results, exhausted, total, offset) named tuple, or nothing.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.poll_cursor!"},{"category":"method","location":"server.html#SimilaritySearchServer.Cursors.purge_expired!-Tuple{SimilaritySearchServer.Cursors.CursorManager}","page":"Server API","text":"purge_expired!(manager) -> Vector{String}\n\nThe actual disk-reclaim half of cursor GC: deletes every file already sitting in expired/ (every one of them is, by construction, past its ttl_seconds – that's why gc_expired! moved it there in the first place, so there's no separate age check here). Returns the ids removed.\n\n\n\n\n\n","title":"SimilaritySearchServer.Cursors.purge_expired!"},{"category":"section","location":"engine.html#SimilaritySearchEngine-API","page":"Engine API","text":"","title":"SimilaritySearchEngine API"},{"category":"section","location":"engine.html#Embedded-API","page":"Engine API","text":"The friendly, no-HTTP-required surface — project lifecycle, CRUD, calibration, allknn.","title":"Embedded API"},{"category":"section","location":"engine.html#Errors","page":"Engine API","text":"The typed failures every other module raises: four categories (InvalidRequest, NotFound, ConflictingState, StorageFailure) and the concrete types beneath them. A consumer maps categories to its own vocabulary – status codes, exit codes – rather than reading messages.","title":"Errors"},{"category":"section","location":"engine.html#IndexEngine","page":"Engine API","text":"The AbstractSearchEngine type hierarchy, insertion/search, calibration, and the concurrency (ReadWriteLock/ContextPool) and logging (reporters and observers, see SimilaritySearch.jl's log.jl) machinery underneath the embedded API.","title":"IndexEngine"},{"category":"section","location":"engine.html#Persistence","page":"Engine API","text":"The RocksDB column-family-backed stores (EngineStore, AdjacencyStore, InvertedFileObjectStore) each engine's on_change callback writes into.","title":"Persistence"},{"category":"section","location":"engine.html#Project","page":"Engine API","text":"Project-level metadata storage (ProjectManager), independent of any search engine.","title":"Project"},{"category":"section","location":"engine.html#Schema","page":"Engine API","text":"Typed, optionally-indexed metadata schema declarations.","title":"Schema"},{"category":"type","location":"engine.html#SimilaritySearchEngine.CenterSelectionResult","page":"Engine API","text":"CenterSelectionResult\n\nWhat fft and dnet return: the chosen centers, which center each item was assigned to (assign) and its distance to it (assigndist), the two radii describing the selection (covering and separation), and the two cost counters.\n\nA faithful, typed restatement of SimilaritySearch.CenterSelection, with ids as Int32 to match every other id this package hands back.\n\ncenters are internal _ids (1-based position in the index), not hydrated with metadata.\nassign[i] is a position in centers, from 1 to length(centers) – not an _id. The _id of item i's center is centers[assign[i]].\ncovering is the largest assigndist: the radius the centers need to cover everything.\nseparation is the smallest distance between two centers. These two are different numbers and used to be conflated under one name; see SimilaritySearch.CenterSelection.\n\n\n\n\n\n","title":"SimilaritySearchEngine.CenterSelectionResult"},{"category":"type","location":"engine.html#SimilaritySearchEngine.EmbeddedEngine","page":"Engine API","text":"EmbeddedEngine\n\nBundles everything a script needs to keep working against one open project: its directory layout plus the live Project.ProjectManager/IndexEngine.AbstractSearchEngine pair cli_handlers.jl's _load_dataset_and_engine already threads through separately for a single CLI command, plus the Persistence.EngineStore (a RocksDB column family shared with project.db, see Persistence.open_engine_store) that engine mutations persist into field-by-field, and pending_flush – a flag a DenseEngine{<:ExactBackend}'s SimilaritySearch.CallbackLog callback sets (see create_project) that this module checks and clears from outside the engine's own insertion call, once it's known safe to do so (see _maybe_flush_index!; unused by every backend that persists its index incrementally by itself, which is all of them except the exact ones – see _searchgraph_on_change/_invertedfile_on_change) – worth bundling here since an embedded-API caller makes many calls against the same open project instead of running once and exiting.\n\n\n\n\n\n","title":"SimilaritySearchEngine.EmbeddedEngine"},{"category":"type","location":"engine.html#SimilaritySearchEngine.ExistsResult","page":"Engine API","text":"ExistsResult\n\nWhat exists reports per queried id: the id as asked for, whether a record was found, and whether it is soft-deleted.\n\n\n\n\n\n","title":"SimilaritySearchEngine.ExistsResult"},{"category":"type","location":"engine.html#SimilaritySearchEngine.KnnRow","page":"Engine API","text":"KnnRow\n\nOne row of allknn: item _id and its k nearest neighbours, ascending by distance. _id and neighbors are internal ids – 1-based positions in insertion order – not doc_ids.\n\n\n\n\n\n","title":"SimilaritySearchEngine.KnnRow"},{"category":"type","location":"engine.html#SimilaritySearchEngine.NearDupResult","page":"Engine API","text":"NearDupResult\n\nWhat neardup returns: the selected centers (the ϵ-net of surviving non-duplicate objects), which center covers each object (assign) and its distance to it (assigndist), the threshold radius epsilon and the maximum covering radius covering, plus the cost counters.\n\nA faithful, typed restatement of SimilaritySearch.NearDupSelection, with ids as Int32 to match every other id this package hands back.\n\n\n\n\n\n","title":"SimilaritySearchEngine.NearDupResult"},{"category":"type","location":"engine.html#SimilaritySearchEngine.OOVCounters","page":"Engine API","text":"OOVCounters\n\nHow many of the tokens this project has been asked for were not in its vocabulary.\n\nA text project fits its vocabulary once, from the documents staged when index! first ran. Everything appended afterwards is encoded against that vocabulary, and a word it does not contain cannot be searched for: it is dropped, silently, at both ends. These counters make that visible, so that the decision to run a rebuild – which refits the vocabulary over what the project holds now – rests on a measurement.\n\nqueries counts the queries that were resolved, tokens the tokens they contained, and oov the ones the vocabulary did not have as typed. Correction may still find a variant for some of those (ftexplain reports which), so this is an upper bound on what a query lost.\n\nThe counts are kept in memory and written to the project when it is closed, or every OOV_FLUSH_EVERY queries. A process that is killed loses the queries since the last write; this is a signal about vocabulary drift, not accounting. rebuild starts them again, since after it the vocabulary is a different one and the old counts describe something that no longer exists.\n\n\n\n\n\n","title":"SimilaritySearchEngine.OOVCounters"},{"category":"type","location":"engine.html#SimilaritySearchEngine.SearchResult","page":"Engine API","text":"SearchResult\n\nOne hit: the internal _id the engine ranked, the caller's own doc_id for it (nothing when the item was appended without one, and when the hit is soft-deleted), the distance, and whether the item is soft-deleted.\n\nThe field names match Schema.MetadataRecord on purpose. The named tuple this replaced called the caller's external id id and the internal one doc_id – exactly backwards from the record, so result.doc_id and record.doc_id were different things and one of them was always the wrong guess.\n\nA soft-deleted hit is reported, not hidden (see IndexEngine.search_live), and carries no doc_id: its metadata is not accessible through search.\n\n\n\n\n\n","title":"SimilaritySearchEngine.SearchResult"},{"category":"type","location":"engine.html#SimilaritySearchEngine.SearchStats","page":"Engine API","text":"SearchStats\n\nA mutable, caller-supplied box for the cost accounting of one search. Pass it as search(h, q, k; stats=SearchStats()) (or to ftsearch) and read stats.distance_evaluations afterwards: the number of distance computations that call performed, which is what a frontend logs per request and what a recall/cost experiment measures.\n\nIt exists because the count is produced inside IndexEngine.search_live, on a context borrowed from the engine's pool for the duration of the call, and is therefore not recoverable from outside afterwards – the pool hands the same context to the next concurrent search, and ctx.costdists is a running total, never reset. A caller that snapshots a context around a search attributes another thread's work to its own request.\n\nReused across calls if you want a running total of your own; each search overwrites the field rather than accumulating, so add it up yourself if that is what you want.\n\n\n\n\n\n","title":"SimilaritySearchEngine.SearchStats"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.text_profile-Tuple{EmbeddedEngine}","page":"Engine API","text":"text_profile(handle::EmbeddedEngine) -> Union{Nothing, TextSearch.TextProfile}\n\nThe text model this project searches with: the one handed to create_project, or the one its first index! call fitted from the staged corpus. nothing for a dense project, and for a text project that has not been indexed yet.\n\nWorth reaching for after a FitFromCorpus: save_profile(dir, text_profile(h)) writes the fitted vocabulary and weights out as a portable profile, so a sibling project can be created against the same model (textmodel=BaseProfile(load_profile(dir))) instead of fitting its own and ending up with a different vocabulary over the same language.\n\nWritten as an explicit extension of IndexEngine.text_profile rather than as a bare text_profile(handle::EmbeddedEngine) = .... The bare form compiles – using .IndexEngine makes the name available but not yet resolved in this scope, so Julia quietly creates a second, unrelated function shadowing the first – and that is the hazard, not a convenience: the two would then be one name with two disjoint method tables, and which one a call site got would depend on whether it had already touched the imported binding. Extending keeps a single generic function whose methods cover an engine and a handle alike (the same reasoning as the open_project note in SimilaritySearchEngine.jl).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.text_profile"},{"category":"method","location":"engine.html#SimilaritySearchEngine._as_meta_schema-Tuple{MetaSchema}","page":"Engine API","text":"_as_meta_schema(spec) -> Schema.MetaSchema\n\nThe declaration a caller wrote, as the type the project stores. A MetaSchema passes through; nothing is the empty declaration; anything else is read as a list of fields, each one a name => type pair or a (name, type) tuple:\n\ncreate_project(w, ds; meta_schema = [\"year\" => :int64, \"lang\" => :string])\n\n\n\n\n\n","title":"SimilaritySearchEngine._as_meta_schema"},{"category":"method","location":"engine.html#SimilaritySearchEngine._assemble_text_index-Tuple{TextProfile, Type, Any, SimilaritySearchEngine.Persistence.InvertedIndexStore, Function}","page":"Engine API","text":"_assemble_text_index(profile, kind, distance, store, adj_factory) -> AbstractInvertedFile\n\nRebuilds a queryable text index from what was persisted, without reindexing anything.\n\nThe cheap, small half is reconstructed from the profile – vocabulary, scorer or weighting model, and the query pipeline all come from the library's own profile-taking constructor, which is also what derives the spelling-variant map once instead of per query. The large half comes from storage: document vectors read into memory, posting lists left on disk behind adj_factory's adjacency list, and the per-document bookkeeping each backend keeps (doclens for BM25, sizes for the weighted one) recomputed from those vectors rather than stored a second time where the two could drift.\n\nMeasured on 265k Gutenberg paragraphs, 2026-09-11: 1.1s against 20.4s to recompute a BM25 index from the raw objects, and that is with the document vectors loaded eagerly.\n\n\n\n\n\n","title":"SimilaritySearchEngine._assemble_text_index"},{"category":"method","location":"engine.html#SimilaritySearchEngine._batch_query_database-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine, Any}","page":"Engine API","text":"_batch_query_database(engine, queries) -> SimilaritySearch.AbstractDatabase\n\nqueries in the form this project's index searches a whole batch with: a MatrixDatabase of Float32 columns for a dense project, a VectorDatabase of SparseVector{Float32,Int32} for a sparse one.\n\nEach query goes through _search_query first, so a batch rejects exactly what a single query rejects (wrong payload kind, wrong dimension) and says the same thing when it does – the batch entry point is not a second, laxer door into the same index.\n\n\n\n\n\n","title":"SimilaritySearchEngine._batch_query_database"},{"category":"method","location":"engine.html#SimilaritySearchEngine._count_oov!-Tuple{EmbeddedEngine, AbstractString}","page":"Engine API","text":"_count_oov!(handle::EmbeddedEngine, text::AbstractString)\n\nAdds one query to OOVCounters: its tokens, and how many of them the vocabulary does not contain as typed.\n\nThe query is tokenized once more here, with the vocabulary's own TextConfig, rather than read out of the search that just ran: the library resolves and encodes a query inside search, and none of that resolution comes back. Tokenizing a query string costs a few microseconds against a search that walks posting lists, and the alternative is for this module to assemble the query pipeline itself again, which is the arrangement IndexEngine.resolve_query's docstring records as removed.\n\nCounts nothing for a project whose vocabulary has not been fitted, and never raises: a failure to measure must not fail the search that was measured.\n\n\n\n\n\n","title":"SimilaritySearchEngine._count_oov!"},{"category":"method","location":"engine.html#SimilaritySearchEngine._current_size-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}}","page":"Engine API","text":"_current_size(engine::IndexEngine.AbstractSearchEngine) -> Int\n\nThe count that determines the next item's _id, which has to be the staged count wherever staging and indexing are separate: for a DenseEngine{GraphBackend}, the number of staged vectors (length(database(engine.backend.index)), i.e. engine.backend.index.db's own count), and for a FullTextEngine, the number of staged texts (length(engine.staged)). Since append_items! only stages on those two, length(engine.backend.index) (the encoded/indexed count) would lag behind and hand out _ids already taken.\n\nThe fallback method is length(engine.backend.index), which is right for every backend that indexes on insertion – the exact dense ones and a sparse project's InvertedFile – because there the index already reflects the item just added.\n\n\n\n\n\n","title":"SimilaritySearchEngine._current_size"},{"category":"method","location":"engine.html#SimilaritySearchEngine._invertedfile_on_change-Tuple{SimilaritySearchEngine.Persistence.InvertedFileObjectStore}","page":"Engine API","text":"_invertedfile_on_change(obj_store::Persistence.InvertedFileObjectStore) -> Function\n\nThe on_change callback for a FullTextEngine: on every push_item!/append_items! report for range sp:ep, saves only that range's raw indexed objects (bags-of-words or SparseVectors – see IndexEngine.invertedfile_objects) as a new, never-again-rewritten block in obj_store (Persistence.append_objects!) – never the whole (growing) index as one value. Safe to do synchronously, right inside the callback (unlike the whole-index save DenseEngine{<:ExactBackend} uses, see _maybe_flush_index!): an inverted file's LOG only fires after a call's mutation is fully done, so there's no partial-state hazard here the way there is for a SearchGraph (see SimilaritySearch.CallbackLog's docstring). Reconstruction (IndexEngine.build_sparseinvertedfile, used by open_project) rebuilds the whole index by replaying every saved object back through the library's own insertion – see Persistence.InvertedFileObjectStore's docstring for the scaling trade-off that implies.\n\nOnly a sparse project is wired to this now: a text project persists its index (Persistence.InvertedIndexStore) and has no observer of its own.\n\n\n\n\n\n","title":"SimilaritySearchEngine._invertedfile_on_change"},{"category":"method","location":"engine.html#SimilaritySearchEngine._invfile_adj_factory-Tuple{SimilaritySearchEngine.Persistence.InvertedIndexStore}","page":"Engine API","text":"_invfile_adj_factory(store; maxlists, baselists) -> Function\n\nThe vocsize -> AbstractAdjList an inverted file is built around when its posting lists live in store instead of in memory. IndexEngine calls it when it constructs the index and never looks inside the result – see IndexEngine._text_index.\n\n\n\n\n\n","title":"SimilaritySearchEngine._invfile_adj_factory"},{"category":"method","location":"engine.html#SimilaritySearchEngine._invfile_store-Tuple{SimilaritySearchEngine.Project.ProjectManager}","page":"Engine API","text":"_invfile_store(project) -> Persistence.InvertedIndexStore\n\nThe project's persisted inverted index (posting lists and per-document vectors). See Persistence.INVFILE_POSTINGS_CF.\n\n\n\n\n\n","title":"SimilaritySearchEngine._invfile_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine._maybe_flush_index!-Tuple{EmbeddedEngine}","page":"Engine API","text":"_maybe_flush_index!(handle::EmbeddedEngine)\n\nPersists handle.engine.backend.index if (and only if) handle.pending_flush[] is set, then clears the flag. Callers must only call this from a point where the just-finished mutation is fully done – in particular, not from inside an SimilaritySearch.CallbackLog callback itself. Right after an IndexEngine.add_item!/index! call returns to this module is always safe. A no-op for DenseEngine{GraphBackend}/FullTextEngine: each has its own dedicated incremental on_change (see _searchgraph_on_change/ _invertedfile_on_change) that never touches pending_flush; only DenseEngine{<:ExactBackend} (ExhaustiveSearch/ParallelExhaustiveSearch) still uses this whole-index path.\n\n\n\n\n\n","title":"SimilaritySearchEngine._maybe_flush_index!"},{"category":"method","location":"engine.html#SimilaritySearchEngine._resolve_records-Tuple{SimilaritySearchEngine.Project.ProjectManager, Any}","page":"Engine API","text":"_resolve_records(project, raw_id) -> Vector{Schema.MetadataRecord}\n\nThe records raw_id names, whether it is an internal _id or a caller-supplied doc_id.\n\nTries the numeric reading first, because it is a direct key lookup, and it can only ever name one record. A numeric-looking doc_id therefore resolves as an _id if one exists with that value – an ambiguity inherent to accepting both in one argument, and the reason fetch_items and exists share this function rather than each inventing its own precedence.\n\nEverything else is a doc_id lookup through Project.find_all_by_doc_id, which returns however many records carry exactly that doc_id – zero, one, or several. A vector, not a record, because doc_id is a caller-chosen external id that nothing here requires to be unique: appending two items under the same one is allowed, and the scan this replaced answered such a lookup with whichever of them it reached first.\n\n\n\n\n\n","title":"SimilaritySearchEngine._resolve_records"},{"category":"method","location":"engine.html#SimilaritySearchEngine._search_query-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine, Any}","page":"Engine API","text":"_search_query(engine, vector) -> Vector{Float32} | SparseVector{Float32,Int32}\n\nvector in the form this project's backend searches with.\n\nA dense project's index evaluates a distance against a contiguous vector; a sparse project's InvertedFile walks the query's nonzero positions to pick posting lists, and reads its nzind/nzval directly. So the conversion is the project's, not the caller's – and the sparse case cannot fall back on the dense one: convert(Vector{Float32}, sparse_query) type-checks, densifies, and then dies inside select_posting_lists on a Float32 used as an index.\n\nA dense vector handed to a sparse project is rejected rather than sparsified. It would usually be a mistake worth naming (a project's items are SparseItems, so its queries are sparse too), and sparsifying silently would turn a dimension mismatch into posting lists selected from whatever the values happened to be.\n\n\n\n\n\n","title":"SimilaritySearchEngine._search_query"},{"category":"method","location":"engine.html#SimilaritySearchEngine._search_with_filter-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine, SimilaritySearchEngine.Project.ProjectManager, Any, Int64, Any}","page":"Engine API","text":"_search_with_filter(engine, project, query, k, predicate; minrecall, candidates)\n\nTop-k under a metadata predicate: asks the index for candidates hits, keeps the ones the predicate accepts, stops at k.\n\nOne pass, not a widening loop. The candidate budget is what the caller pays for and what the caller therefore names: every candidate costs two RocksDB reads and two decodes (the record and its meta), so a budget that grows itself until k is satisfied would let a selective predicate turn one query into a full scan without anybody asking for it. See search for what to set it to.\n\n\n\n\n\n","title":"SimilaritySearchEngine._search_with_filter"},{"category":"method","location":"engine.html#SimilaritySearchEngine._searchgraph_on_change-Tuple{SimilaritySearchEngine.Persistence.EngineStore, SimilaritySearchEngine.Persistence.AdjacencyStore}","page":"Engine API","text":"_searchgraph_on_change(store::Persistence.EngineStore, adj_store::Persistence.AdjacencyStore) -> Function\n\nThe on_change callback for a DenseEngine{GraphBackend}: fires from inside IndexEngine.index!(engine::IndexEngine.DenseEngine{GraphBackend}) (never from add_item!/ append_items! anymore – see that function's docstring for why staging and indexing are now two separate steps), once per sp:ep range SimilaritySearch.index!'s own internal batching processes. Saves each object i in that range's own direct-links-only neighbor list under its own key in adj_store (Persistence.save_neighbors!, IndexEngine.direct_neighbors) – adjacency lives in its own RocksDB column family, keyed by object id (see Persistence.AdjacencyStore) – never the whole (growing) graph as one value – and persists ep as the new :graph_len engine field (Persistence's small per-field store), so a restart mid-catch-up resumes exactly where this range left off rather than redoing (or worse, silently forgetting) it.\n\nRaw vectors are not handled here – unlike before, they're persisted directly by append_items! at stage time (see its own docstring), since they're durable and meaningful the moment they're staged, well before any graph-linking work happens; only adjacency and :graph_len genuinely need to wait for SimilaritySearch.index! itself to compute them, which is exactly what makes it safe to do this synchronously, right inside the callback (unlike the whole-graph save every other engine kind uses, see _maybe_flush_index!): this is exactly the direct-links-only slice SimilaritySearch.CallbackLog hands over at that point, and reconstruction (IndexEngine.build_searchgraph, used by open_project) reconnects every reverse link itself, once, up to the restored :graph_len, after replaying every staged vector and looking up every graph-indexed object's saved adjacency.\n\n\n\n\n\n","title":"SimilaritySearchEngine._searchgraph_on_change"},{"category":"method","location":"engine.html#SimilaritySearchEngine.allknn-Tuple{EmbeddedEngine}","page":"Engine API","text":"allknn(handle::EmbeddedEngine; k=10) -> Vector{KnnRow}\n\nRuns SimilaritySearch.allknn synchronously against the project's dense index – no Job/spool machinery at all, unlike the HTTP API's POST /api/v1/jobs/allknn (PLAN.md §8.5's \"runs in-process\" framing for the embedded surface). Errors if the project is a text index or empty (mirrors cli_handlers.jl's _require_dense). Returns one KnnRow per item, its _id being the item's 1-based position in insertion order (matching execute_allknn's own output convention), not its doc_id.\n\nA row can be shorter than k: the library pads unfilled neighbour slots with id 0, and this stops at the first one rather than reporting a neighbour that does not exist.\n\n\n\n\n\n","title":"SimilaritySearchEngine.allknn"},{"category":"method","location":"engine.html#SimilaritySearchEngine.append_items!-Tuple{EmbeddedEngine, AbstractItem}","page":"Engine API","text":"append_items!(handle::EmbeddedEngine, item::Schema.AbstractItem) -> Int\n\nOne item, for a caller that has one. Note the performance warning above: this is the single-item call, and for a dense project it costs one fsync for one vector.\n\n\n\n\n\n","title":"SimilaritySearchEngine.append_items!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.bichromatic_kclosestpairs-Tuple{EmbeddedEngine, Any}","page":"Engine API","text":"bichromatic_kclosestpairs(handle::EmbeddedEngine, B; k::Int=1, min_k::Int=max(k, 8)) -> Vector{Tuple{Int32,Int32,Float32}}\n\nRuns SimilaritySearch.bichromatic_kclosestpairs synchronously between the project's own dense index (playing the role of idxA, already indexed) and B – a second, separate collection of vectors (any SimilaritySearch.AbstractDatabase, e.g. a plain Vector{Vector{Float32}} or MatrixDatabase) that is not itself indexed by this project, or by anything else – e.g. the same LSI space's vectors for a different half of a corpus than the one this project indexes. Errors if the project is a text index or empty (same guard as allknn).\n\nThere is deliberately no way to pass two EmbeddedEngine projects here: this embedded API manages one project (one index) at a time, and B never needs its own project/persistence of its own to take part in this – it's queried directly, in memory, exactly as SimilaritySearch.bichromatic_kclosestpairs itself expects.\n\nReturns up to k (i, j, dist) tuples, ascending by distance – i is this project's own internal _id (in idxA), j is B's own position (1-based, in B's own order, not related to any project's _id space) – neither is hydrated with metadata.\n\n\n\n\n\n","title":"SimilaritySearchEngine.bichromatic_kclosestpairs"},{"category":"method","location":"engine.html#SimilaritySearchEngine.calibrate!-Tuple{EmbeddedEngine}","page":"Engine API","text":"calibrate!(handle::EmbeddedEngine; levels=IndexEngine.DEFAULT_MINRECALL_LEVELS, numqueries=64, ksearch=10, queries=nothing) -> IndexEngine.OptBeamSearch\n\nRuns IndexEngine.calibrate!'s real hyperparameter sweep for each recall level in levels and immediately persists just the resulting opt_beamsearch table (consulted by search's own minrecall keyword) so it survives a later close_project! + open_project round-trip – there is no descriptor.json in this embedded API for Server.handle_calibrate's separate persistence path to write to, so this EngineStore field is this API's sole persistence mechanism for it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.calibrate!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.close_project!-Tuple{EmbeddedEngine}","page":"Engine API","text":"close_project!(handle::EmbeddedEngine; compact::Bool=true)\n\nFlushes the engine's index one final time and closes the project's RocksDB connection (and, for a DenseEngine{GraphBackend} that ever had a vector appended, its MMapMatrixDatabase file too – see _searchgraph_on_change). For a graph-backed dense project and for the inverted-file backends (sparse and text) there is nothing left to flush – every block was already saved the instant it was reported – so this only does the final :index save for DenseEngine{<:ExactBackend}. Every other field (deleted_ids, opt_beamsearch, ...) is, for every kind, already persisted immediately by whichever call changed it (delete_item!, calibrate!, ...).\n\nThen, unless compact=false, it compacts the project's storage through compact_project! – a no-op for a read-only handle or a session that only read. That is what keeps the next open fast (2.97s versus 0.28s on a 50k-vector project, measured 2026-09-10); pass compact=false when closing is on a latency path and the next open is not.\n\n\n\n\n\n","title":"SimilaritySearchEngine.close_project!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.closestpairs-Tuple{EmbeddedEngine}","page":"Engine API","text":"closestpairs(handle::EmbeddedEngine; k::Int=1, min_k::Int=max(k, 8)) -> Vector{Tuple{Int32,Int32,Float32}}\n\nRuns SimilaritySearch.closestpairs synchronously against the project's dense index – the k globally closest pairs among every item currently indexed (approximate if the index itself is, e.g. an untuned SearchGraph), same in-process framing as allknn. Errors if the project is a text index or empty (same guard as allknn).\n\nReturns up to k (i, j, dist) tuples, ascending by distance – i/j are internal _ids, not hydrated with metadata.\n\n\n\n\n\n","title":"SimilaritySearchEngine.closestpairs"},{"category":"method","location":"engine.html#SimilaritySearchEngine.compact_project!-Tuple{EmbeddedEngine}","page":"Engine API","text":"compact_project!(handle::EmbeddedEngine; force::Bool=false) -> Bool\n\nCompacts the project's RocksDB storage now, and reports whether it actually ran.\n\nSkipped, and false returned, when the handle is read-only (it cannot write) or when this session has not written anything – opening a project to run queries and closing it again leaves nothing to compact, and compaction is not free. force=true runs it regardless, for a caller that knows another process did the writing.\n\nWorth understanding rather than treating as housekeeping: what a bulk write session leaves behind is a write-ahead log the next open has to replay, and that shows up as open latency, not write latency. Measured 2026-09-10 on a 50k-vector dense project: 2.97s to reopen without this, 0.28s with it, for 0.03s of compaction. It runs automatically from close_project! (pass compact=false there to skip it), so an explicit call is for compacting mid-session – after a big ingest that the process will keep serving from, say.\n\n\n\n\n\n","title":"SimilaritySearchEngine.compact_project!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.create_project-Tuple{String, String}","page":"Engine API","text":"create_project(workdir, dataset; engine=DenseEngine, backend=nothing, distance=nothing,\n               minrecall=0.9, dimension=nothing, textmodel=nothing, schema_version=1) -> EmbeddedEngine\n\nCreates a brand-new project directly on disk at <workdir>/<dataset>, with no HTTP server or CLI subprocess involved – the same on-disk layout similarity-search build already produces, so a project created this way can later be inspected/rebuilt/dumped by the CLI, or reopened by open_project.\n\nTwo keywords say what the project is, and they are separate on purpose:\n\nengine is what it holds – DenseEngine (dense vectors, the default), SparseEngine (sparse vectors you encoded) or FullTextEngine (text this package encodes). It also fixes the item type append_items! will accept: DenseItem, SparseItem, TextItem.\nbackend is the index that holds it, defaulting to that engine's own (IndexEngine.default_backend): a SearchGraph, an InvertedFile, a BM25InvertedFile. IndexEngine.BACKENDS is the table of legal pairings, and an illegal one is refused here with a message naming the alternatives rather than failing later on a MethodError.\n\nSo create_project(workdir, \"ds\") is a dense project on a self-tuning SearchGraph, and every other shape is one or two keywords away:\n\ncreate_project(w, ds; engine=DenseEngine,    backend=ExhaustiveSearch)\ncreate_project(w, ds; engine=SparseEngine,   dimension=50_000)\ncreate_project(w, ds; engine=FullTextEngine, backend=TextInvertedFile,\n                      textmodel=DefaultProfile(:es))\n\ndimension is required for a sparse project and refused for any other. An InvertedFile is a fixed array of posting lists, so its size is structural rather than descriptive: it has to be known before the first item, and every SparseItem appended has to agree with it.\n\ndistance, left at its default nothing, defers to whichever default IndexEngine.default_distance names for the chosen backend (SqL2() for the dense ones, NormCosine() for an InvertedFile) instead of this function imposing one blanket default across every index kind. minrecall is the target recall a DenseEngine{GraphBackend} autotunes BeamSearch toward as it grows (see IndexEngine.DenseEngine{GraphBackend}); it must be given here, at creation, since it's carried on the engine and restored verbatim by open_project rather than re-derived later – calibrate! remains available afterwards as a separate, explicit re-optimization pass. Ignored for index types with no BeamSearch to autotune. textmodel says where a text project's vocabulary comes from, and a text project cannot be created without it (see IndexEngine.AbstractTextModelSpec for why it has no default). Three forms, in the order worth preferring them:\n\ntextmodel=DefaultProfile(:es) — recommended. The published profile for a language, refitted to this project's own corpus at the first index! call: a vocabulary fitted over a whole Wikipedia edition, with its stopword set, lemma map and expansion network, adapted without fitting an embedding. Resolved from the profile library textsearch install maintains; IndexEngine.default_profile_path says so, and how to install it, when the one you asked for is not there.\ntextmodel=BaseProfile(load_profile(\"path/to/profile.zip\")) — a profile you already have, used as it is. The project is trained before its first item is staged.\ntextmodel=FitFromCorpus(TextConfig(language=:es); min_ndocs=3) — deliberately no base profile: fit one at the first index! call from at most max_documents of this project's own corpus, delegated whole to TextSearch.fit_profile. Bounded in cost and thin in vocabulary, so terms appended later that the sample never held are dropped from then on.\n\nPassing a textmodel to a dense or sparse project is an error rather than an ignored keyword: there is no reading under which it does anything, and swallowing it silently is how a project ends up not being the kind its author thought it was. That check is the engine's rather than the backend's, because InvertedFile is a legal backend for both a sparse and a text project, so the backend alone cannot answer it. The spec is persisted with the project and restored verbatim by open_project.\n\nschema_version is stamped onto every Schema.MetadataRecord append_items! writes for this project's lifetime (not persisted/restored itself – like minrecall before it was carried on the engine, a caller reopening this same project later must pass the same schema_version again to keep tagging new records consistently with old ones); it's never validated against meta's actual shape, purely a version tag for the caller's own interpretation of it.\n\nThe engine's index itself is persisted incrementally as it grows, not rewritten wholesale on every append_items! call: for a SearchGraph, every index! call saves just the direct-links-only block it just built (see _searchgraph_on_change); for the inverted-file backends – a text project's and a sparse project's alike – every insertion likewise saves just the newly-encoded objects it just indexed (see _invertedfile_on_change). Only an exact dense backend (ExhaustiveSearch/ParallelExhaustiveSearch) instead flags a pending whole-index save that happens right after each add_item! call returns (see _maybe_flush_index!); close_project! forces one final flush of that so nothing recent is lost – every other backend never has anything left to flush there, since each block is already durable the instant it is saved.\n\n\n\n\n\n","title":"SimilaritySearchEngine.create_project"},{"category":"method","location":"engine.html#SimilaritySearchEngine.declare_meta_schema!-Tuple{EmbeddedEngine, Any}","page":"Engine API","text":"declare_meta_schema!(handle::EmbeddedEngine, fields) -> Schema.MetaSchema\n\nDeclares what meta holds from now on, on a project that already exists, and returns the declaration. fields takes the same forms as create_project's meta_schema keyword.\n\nRecords already written are not read and not checked. The declaration governs what is appended after it, which is what makes declaring cheap on a project of any size. A field declared now may therefore hold values of another type in older records; a rebuild is what rewrites them.\n\nThe version of the declaration increases with each call, so MetadataRecord.schema_version of a record says which declaration was in force when it was written.\n\n\n\n\n\n","title":"SimilaritySearchEngine.declare_meta_schema!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.delete_item!-Tuple{EmbeddedEngine, Integer}","page":"Engine API","text":"delete_item!(handle::EmbeddedEngine, _id::Integer)\n\nSoft-deletes _id (future searches exclude it, the underlying index is untouched) and immediately persists just the deleted_ids field – mirrors Server.handle_delete_item.\n\n\n\n\n\n","title":"SimilaritySearchEngine.delete_item!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.dnet-Tuple{EmbeddedEngine, Integer}","page":"Engine API","text":"dnet(handle::EmbeddedEngine, k::Integer; verbose::Bool=false) -> CenterSelectionResult\n\nRuns SimilaritySearch.dnet (density-based ball partitioning) synchronously against the project's dense index – selects representative centers by carving the database into balls of approximately length(db) ÷ k elements. Errors if the project is not a dense index or is empty.\n\nReturns a CenterSelectionResult.\n\n\n\n\n\n","title":"SimilaritySearchEngine.dnet"},{"category":"method","location":"engine.html#SimilaritySearchEngine.exists-Tuple{EmbeddedEngine, Any}","page":"Engine API","text":"exists(handle::EmbeddedEngine, ids) -> Vector{ExistsResult}\n\nFor each id in ids (internal _id or a doc_id, see _resolve_records), reports whether a record exists and, if so, whether it's been soft-deleted. One result per queried id, in order, including the ones that were not found – unlike fetch_items, which drops those and returns one entry per matching record.\n\nSince a doc_id may be carried by several items, the two flags summarize them: exists is true when at least one record matched, and deleted is true only when every match is soft-deleted, i.e. when asking for that id leaves nothing live to retrieve.\n\n\n\n\n\n","title":"SimilaritySearchEngine.exists"},{"category":"method","location":"engine.html#SimilaritySearchEngine.fetch_items-Tuple{EmbeddedEngine, Any}","page":"Engine API","text":"fetch_items(handle::EmbeddedEngine, ids) -> Vector{Schema.StoredItem}\n\nBatch retrieval by id – each element of ids may be the internal _id or a doc_id (see _resolve_records). An id that resolves to nothing is skipped and a doc_id shared by several items brings back all of them, so the result can be shorter or longer than ids: it is a flat vector of whatever matched, in the order ids gave, ties broken by ascending _id.\n\nEach item comes back whole: the record's fixed fields, its meta as a plain Dict{String,Any}, and payload – the very text or vector that was indexed, read from the engine via IndexEngine.stored_payload.\n\nBoth halves of that are changes from what this used to return. It used to hand back a Dict{String,Any} per item with the record's fields merged into the decoded meta, which meant a meta key called \"doc_id\" silently overwrote the record's own, and it could not return the payload at all: \"text\" was a reserved key stripped on the way in, so a text project's fetch answered with everything about a paragraph except the paragraph.\n\n\n\n\n\n","title":"SimilaritySearchEngine.fetch_items"},{"category":"method","location":"engine.html#SimilaritySearchEngine.fft-Tuple{EmbeddedEngine, Integer}","page":"Engine API","text":"fft(handle::EmbeddedEngine, k::Integer; start::Int=0, verbose::Bool=false) -> FFTResult\n\nRuns SimilaritySearch.fft's Farthest-First-Traversal synchronously against the project's dense index – picks k well-separated items (e.g. as diverse candidate cluster centers), the same \"runs in-process, no job queue\" framing as allknn. Errors if the project is a text index or empty (same guard as allknn). Unlike allknn/closestpairs/bichromatic_kclosestpairs, this is a brute-force sweep over the raw vectors (SimilaritySearch.distance/database of the engine's index) rather than a graph search, so it works the same regardless of how well-tuned (or untuned) the project's BeamSearch is.\n\nReturns an FFTResult (an alias of CenterSelectionResult) – every field SimilaritySearch.fft itself reports (see its docstring and SimilaritySearch.CenterSelection), with ids as Int32 to match every other id this package hands back. centers are internal _ids (1-based position in the index), not hydrated with metadata – look them up yourself (e.g. via fetch_items) if you need it. assign[i] is a position in centers, so item i's center is centers[assign[i]].\n\n\n\n\n\n","title":"SimilaritySearchEngine.fft"},{"category":"method","location":"engine.html#SimilaritySearchEngine.ftexplain-Tuple{EmbeddedEngine, AbstractString}","page":"Engine API","text":"ftexplain(handle::EmbeddedEngine, text; policy=QueryPolicy()) -> Vector{String}\n\nOne human-readable line per query token that ftsearch would search as something other than what was typed – \"musica appears in only 9 documents, searched as música instead\" – and an empty vector when the query is searched verbatim.\n\nA search that silently substitutes what was asked for owes the person a way to see it: this is the \"showing results for ...\" half, and QueryPolicy(correction=:off) is the \"search instead for ...\" half. Resolves the query the same way ftsearch does under the same policy, so the two never disagree; it does not run the search itself.\n\nErrors for a project whose text engine has not been trained yet, and for a dense project (which has no query text to resolve).\n\n\n\n\n\n","title":"SimilaritySearchEngine.ftexplain"},{"category":"function","location":"engine.html#SimilaritySearchEngine.ftsearch","page":"Engine API","text":"ftsearch(handle::EmbeddedEngine, text, k::Int=10; policy=QueryPolicy()) -> Vector{SearchResult}\n\nText search against a bm25/weighted-inverted-file project (mirrors Server.handle_ftsearch). Same soft-delete marker behavior as search; minrecall is accepted only for parity with search and is a no-op here since no text engine has a BeamSearch to calibrate.\n\npolicy::TextSearch.QueryPolicy says how to treat the query: whether to correct its spelling against the project's vocabulary (correction=:auto by default, :off to search exactly what was typed, :always to bridge every token) and whether to widen it with the profile's query-expansion network (expansion, expansion_k). Both are guesses about intent rather than properties of the index, which is why they travel with the query instead of being fixed at project creation. The default is inert for a project fitted under the default TextConfig(), whose policy already folds case and diacritics; it starts mattering for one built on a profile that preserves them. Use ftexplain to see what a query was actually searched as.\n\nstats behaves exactly as in search.\n\n\n\n\n\n","title":"SimilaritySearchEngine.ftsearch"},{"category":"method","location":"engine.html#SimilaritySearchEngine.index!-Tuple{EmbeddedEngine}","page":"Engine API","text":"index!(handle::EmbeddedEngine)\n\nCatches up encoding/indexing over whatever's been staged via append_items! since the last call (or since creation) – one uniform entry point for every project kind: idempotent, safe to call repeatedly, only ever processes the backlog (see IndexEngine.index!(engine::IndexEngine.DenseEngine{IndexEngine.GraphBackend})'s docstring for the exact contract, shared verbatim by the text engines). Nothing staged via append_items! becomes visible to search/ftsearch/allknn/fft/ closestpairs/bichromatic_kclosestpairs until this runs at least once.\n\nFor a text project created with a FitFromCorpus, the first call additionally runs that fit (producing a TextSearch.TextProfile: vocabulary, weights and lineage) over every item staged so far and builds the real index against it – there is no separate training entry point; whatever you've append_items!-ed before the first index! call is the training corpus, and tokens absent from it are out-of-vocabulary forever after. That one-time transition (:profile) is persisted immediately after it happens; every later call only ever touches the encoded posting-list blocks already covered by the engine's own incremental on_change, so this does not re-save :deleted_ids or anything else on every catch-up call.\n\nA project created with a BaseProfile (see create_project) was already trained before its first item was staged, so there is no such transition and every call here is a pure catch-up.\n\nA no-op for the backends that have no staging split – a sparse project's InvertedFile and a dense project's ExhaustiveSearch/ParallelExhaustiveSearch both index on insertion, so there is never a backlog here to catch up. It is deliberately a no-op and not an error: a caller writing create_project / append_items! / index! / search gets four lines that mean the same thing for every kind of project, and does not have to know which backends need the third one. The cost of that is a call that does nothing, which is the cheaper mistake – the alternative made switching a project from a graph to a brute-force scan a change to every script that fed it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.index!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.meta_schema-Tuple{EmbeddedEngine}","page":"Engine API","text":"meta_schema(handle::EmbeddedEngine) -> Schema.MetaSchema\n\nWhat this project declares about the meta of its items (PLAN.md §4.5). Empty for a project that declares nothing, which is the default and the behavior every project had before this existed: meta is stored as it arrives.\n\nA declared field is checked and converted when an item is appended, so its value is the type it claims. Declaring is additive: an undeclared field is still accepted and stored as it arrives. See declare_meta_schema! to declare on a project that already exists, and Schema.MetaSchema for what a declaration is.\n\n\n\n\n\n","title":"SimilaritySearchEngine.meta_schema"},{"category":"method","location":"engine.html#SimilaritySearchEngine.neardup-Tuple{EmbeddedEngine, Real}","page":"Engine API","text":"neardup(handle::EmbeddedEngine, epsilon::Real; verbose::Bool=false, recall::Real=1.0) -> NearDupResult\n\nRuns SimilaritySearch.neardup synchronously against the project's dense index – finds the ϵ-net of non-duplicate objects whose mutual distances are at least epsilon. Objects closer than epsilon to an existing center are considered near-duplicates and assigned to it. Errors if the project is not a dense index or is empty.\n\nReturns a NearDupResult.\n\n\n\n\n\n","title":"SimilaritySearchEngine.neardup"},{"category":"method","location":"engine.html#SimilaritySearchEngine.open_project-Tuple{String, String}","page":"Engine API","text":"open_project(workdir, dataset; read_only=false) -> EmbeddedEngine\n\nReopens a project previously created by create_project, restoring its search engine – including the minrecall target and any calibrated opt_beamsearch it was created/calibrated with – field-by-field from its Persistence.EngineStore (a DenseEngine{GraphBackend}'s index specifically via IndexEngine.build_searchgraph replaying its saved insertion blocks, see _searchgraph_on_change). Falls back to a fresh, empty SearchGraph/SqL2/minrecall=0.9 engine if this project has never been saved yet, mirroring Server._reload_one_dataset!'s own \"create if nothing persisted yet\" fallback.\n\nPass read_only=true to inspect a project a live similarity-search-serve process (or another script) still has open for writing (mirrors Project.open_project's own read_only kwarg, used the same way by the CLI's describe command) – a plain (non-read_only) open against a directory something else already has open for writing raises RocksDB's own real lock error, not a friendly one this function invents.\n\n\n\n\n\n","title":"SimilaritySearchEngine.open_project"},{"category":"function","location":"engine.html#SimilaritySearchEngine.search","page":"Engine API","text":"search(handle::EmbeddedEngine, queries::AbstractVector{<:AbstractVector{<:Real}}, k::Int=10) -> Vector{Vector{SearchResult}}\n\nHydrated batch search: one inner vector of SearchResult per query, in the order queries gave, each ordered by ascending distance.\n\nThe same results search returns for one query, computed for all of them in one parallel pass (searchbatch) and hydrated the same way – one doc_id lookup and one soft-delete check per hit. It carries searchbatch's constraints with it (write lock, no staged backlog, dense or sparse only), because it is that call plus hydration.\n\nNo filter keyword here: filtering is a per-query candidate budget (see search's candidates), and mixing that with a batch whose whole point is one uniform k for every query would make the cost of a call impossible to read off it. Ask for a larger k and apply the predicate yourself, or loop over the single-query search when the predicate is what matters.\n\n\n\n\n\n","title":"SimilaritySearchEngine.search"},{"category":"function","location":"engine.html#SimilaritySearchEngine.search-2","page":"Engine API","text":"search(handle::EmbeddedEngine, vector, k::Int=10; filter=nothing) -> Vector{SearchResult}\n\nDense vector search, hydrated with each hit's original id (mirrors Server.handle_search minus the HTTP/telemetry/pagination machinery). filter, if given, is a (record::Schema.MetadataRecord, meta) -> Bool predicate function called on each candidate's record and (lazily decoded, see Schema.decode_meta) meta – e.g. (record, meta) -> meta !== nothing && meta[\"year\"] >= 2020. A caller here already has real Julia functions to work with, not a JSON wire format to encode a filter into, so filter is just that function, not a name-keyed spec for some interpreter to replay. meta is fetched (one extra Project.get_meta read per candidate) even if record alone would've been enough for a given predicate – the common case (filtering on a meta field) needs it, and there's no way to know a predicate won't from here.\n\ncandidates is how many hits the index is asked for before filtering, and it matters whenever filter is given: the search returns fewer than k results when fewer than k of those candidates satisfy the predicate, quietly, because the alternative – growing the budget until k is met – turns a selective predicate into a full scan of the collection at two RocksDB reads per item. The default of 2k suits a predicate that most items pass (a language, a type, a recent date). Match it to how selective yours is: a predicate that roughly one item in n satisfies needs about n * k candidates to fill a page of k, so search(h, q, 10; filter=f, candidates=8192) is the shape of a query filtering on something rare, and the cost of that is ~8k candidate reads. Without filter, candidates is unused.\n\nWithout filter, this is a plain top-k search: soft-deleted candidates are not hidden or backfilled – they're returned with deleted=true and no hydrated metadata (see IndexEngine.search_live), so doc_id is nothing for them. Getting k live results back is a paging concern for a layer above this one (e.g. a server walking successive windows via a cursor), not something this function does itself.\n\nminrecall, for a DenseEngine{GraphBackend}, searches at (approximately) that target recall using a calibrated BeamSearch from engine.backend.opt_beamsearch instead of its current default (see IndexEngine.search_live) – if that table is still empty, this triggers a one-off calibrate! over IndexEngine.DEFAULT_MINRECALL_LEVELS and persists the resulting opt_beamsearch so that calibration isn't silently repeated on every future search. Ignored for any other engine kind.\n\nReturns a Vector{SearchResult} – _id is the internal id, doc_id the caller's own, and note that those two field names sit the opposite way round from the named tuple this replaced.\n\nstats, if given a SearchStats, receives this call's distance-evaluation count – the only way to get it, since the context that counted them goes back to the engine's pool when the call returns.\n\n\n\n\n\n","title":"SimilaritySearchEngine.search"},{"category":"function","location":"engine.html#SimilaritySearchEngine.searchbatch","page":"Engine API","text":"searchbatch(handle::EmbeddedEngine, queries, k::Int=10) -> (ids, dists)\n\nk nearest neighbours for every query at once: two (k, length(queries)) matrices, of UInt32 internal ids and Float32 distances, exactly as the library returns them. A column with fewer than k hits is padded with 0 ids and typemax(Float32) distances.\n\nThis is the raw, unhydrated form – no doc_id, no soft-delete flags, no RocksDB reads at all – and it exists because that is what the operations built on a batch of queries (evaluation loops, offline scoring, feeding another index) actually want. search(handle, queries, k) is the hydrated form.\n\nA whole-dataset operation, in the same family as allknn/fft, not a concurrent search: it takes the project's write lock (full exclusivity, including from other batch calls and from index!) and refuses to run while items are staged but not yet indexed. See IndexEngine.searchbatch_live for why that is not a choice.\n\nWorth it whenever there are many queries: measured 2026-09-10 over 50k dense vectors on 8 threads, 39k queries/s here against 3.6k/s calling search one query at a time.\n\nA text project has no batch entry point here – its queries are strings the library resolves one at a time – so call ftsearch in a loop.\n\n\n\n\n\n","title":"SimilaritySearchEngine.searchbatch"},{"category":"method","location":"engine.html#SimilaritySearchEngine.vocabulary_report-Tuple{EmbeddedEngine}","page":"Engine API","text":"vocabulary_report(handle::EmbeddedEngine; scan::Bool=false) -> Union{Nothing, Dict{String,Any}}\n\nWhat this project's vocabulary covers, and what it does not (PLAN.md §5).\n\nReports the size of the vocabulary, how many documents it was fitted from, its ten most frequent tokens, and the running counters of OOVCounters: how many queries were resolved against it, how many tokens they carried, and what fraction of those the vocabulary did not have.\n\nThat fraction is the drift signal. A vocabulary is fitted once, from the documents staged when index! first ran, and a project whose subject matter moves ends up unable to search for words it now holds. rebuild refits the vocabulary over the documents that are there now, and these counters are what say whether that is worth doing.\n\nscan=true adds live_oov_rate, the same fraction measured over the stored documents instead of over the queries: a document is read and tokenized again, and its tokens are looked up. The two answer different questions. The query rate says the words being asked for are missing, which a rebuild fixes only if those words are in the documents; the document rate says the words already stored are missing from the vocabulary, which is exactly what a rebuild fixes.\n\nsample bounds that pass. The cost of reading every document is linear in the size of the project – 11.6 s over 12.3 million tokens, measured 2026-09-14 – and unbounded, so at most sample documents are read, spread evenly over the project by taking every nth one. Both ends of the project are therefore represented, which matters because drift accumulates at the end: the documents appended after the vocabulary was fitted are the ones it does not cover. The report says how many documents were read (live_documents_scanned) out of how many exist (live_documents_total) and whether it read all of them (live_sampled). sample <= 0 reads every document, which is what an offline command such as describe asks for.\n\nRuns under the project's read lock, like a search: the documents it reads are the ones an append is writing.\n\nReturns nothing for a project that does not hold text, and for one whose vocabulary has not been fitted yet.\n\n\n\n\n\n","title":"SimilaritySearchEngine.vocabulary_report"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.DEFAULT_OOV_SCAN_SAMPLE","page":"Engine API","text":"DEFAULT_OOV_SCAN_SAMPLE\n\nHow many documents vocabulary_report reads by default when asked to measure the out-of-vocabulary rate over the project's own documents.\n\nReading every document costs time linear in the project: 11.6 s over 12.3 million tokens, measured 2026-09-14. That is an answer to a question an operator asks while waiting for it, so it is bounded instead: a rate measured over twenty thousand documents, spread over the whole project, decides whether to rebuild with room to spare, and costs about a second whatever the project's size. sample=0 reads everything, for a caller that is offline and wants the exact figure.\n\n\n\n\n\n","title":"SimilaritySearchEngine.DEFAULT_OOV_SCAN_SAMPLE"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.OOV_FLUSH_EVERY","page":"Engine API","text":"How many queries pass before the counters are written to the project.\n\n\n\n\n\n","title":"SimilaritySearchEngine.OOV_FLUSH_EVERY"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.ConflictingState","page":"Engine API","text":"ConflictingState <: EngineError\n\nThe project is real and the request is well formed, but the project is not in a state where it can be served – items staged but not indexed, a text project not trained yet, an empty collection. The same call succeeds after the state changes, which is what separates this from InvalidRequest. HTTP: 409.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.ConflictingState"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.CorruptedStorage","page":"Engine API","text":"Stored bytes or recorded state that this version cannot interpret.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.CorruptedStorage"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.EmptyProject","page":"Engine API","text":"An operation over the whole collection, on a collection with nothing in it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.EmptyProject"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.EngineError","page":"Engine API","text":"EngineError <: Exception\n\nEverything this package raises on purpose.\n\nFour abstract categories sit under it – InvalidRequest, NotFound, ConflictingState, StorageFailure – and the concrete types under those carry the detail. The category is the contract. A consumer maps categories to its own vocabulary (the HTTP server to status codes, a CLI to exit codes) and reads the message only to show a human; a consumer that wants to distinguish two situations inside one category matches the concrete type instead.\n\nThis exists because the alternative is classifying by message text, which every consumer is forced into when a library raises bare error(\"...\"), and which breaks silently the first time somebody improves the wording. The engine itself knows nothing about status codes or exit codes and should not learn: mapping lives at each boundary, not here.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.EngineError"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.InvalidOption","page":"Engine API","text":"A keyword that does not apply here, is missing, or holds a value this call cannot use.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.InvalidOption"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.InvalidRequest","page":"Engine API","text":"InvalidRequest <: EngineError\n\nThe call cannot be honoured as written: wrong kind of item for the project, a keyword that does not apply, a dimension that does not match. Retrying it unchanged will fail again. Boundaries that speak HTTP report this as 400.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.InvalidRequest"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.NoIndex","page":"Engine API","text":"A text project with a profile but no index on disk – index! builds one from staged text.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.NoIndex"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.NotFound","page":"Engine API","text":"NotFound <: EngineError\n\nSomething the call named does not exist. HTTP: 404.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.NotFound"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.NotTrained","page":"Engine API","text":"A text project whose profile has not been fitted yet.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.NotTrained"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.NothingStaged","page":"Engine API","text":"Nothing has ever been staged, so there is nothing to index.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.NothingStaged"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.PayloadMismatch","page":"Engine API","text":"An item or query of the wrong kind for this project (a DenseItem for a text project, ...).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.PayloadMismatch"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.PendingBacklog","page":"Engine API","text":"Items are staged but not indexed, and this operation reads structures that only the next index! call grows. Carries both counts, so a caller can say how far behind it is.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.PendingBacklog"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.ProfileNotInstalled","page":"Engine API","text":"A linguistic profile the project asks for, absent from the local profile library.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.ProfileNotInstalled"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.StorageFailure","page":"Engine API","text":"StorageFailure <: EngineError\n\nWhat was read back from storage cannot be interpreted: an unknown tag, a project recorded in a shape this version does not know. Not the caller's fault and not fixable by retrying. HTTP: 500.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.StorageFailure"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.UnknownBackend","page":"Engine API","text":"An engine/backend pairing this package does not have, or a backend name it does not know.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.UnknownBackend"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.UnsupportedOperation","page":"Engine API","text":"An operation this kind of project or index does not implement.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.UnsupportedOperation"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Errors.WrongDimension","page":"Engine API","text":"A vector whose length is not the project's own.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Errors.WrongDimension"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.AbstractSearchEngine","page":"Engine API","text":"AbstractSearchEngine\n\nCommon supertype for every concrete engine kind (DenseEngine, DenseEngine, SparseEngine, FullTextEngine). Each kind carries only the state it actually needs – e.g. minrecall only exists on DenseEngine{GraphBackend}, profile only on the text engines – and the concrete Julia type itself is what tells add_item!/search_live/etc. and restore_engine which behavior to run, instead of a separate enum tag every method would have to branch on.\n\nEvery concrete engine shares three fields: deleted_ids::Set{UInt32} (logically deleted document ids), lock::ReadWriteLock (guards concurrent access – see its docstring for why this needs to be a reader/writer lock, not a plain mutex), and search_ctx_pool (each concurrent search_live call's own private context – see ContextPool for why that's separate from ctx, which insertion keeps to itself).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.AbstractSearchEngine"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.AbstractTextModelSpec","page":"Engine API","text":"AbstractTextModelSpec\n\nThe text-model decision a text project is created with, as a value. Three answers to \"where does this project's vocabulary come from\", in the order worth preferring them:\n\nDefaultProfile(:es) – the published profile for a language, refitted to this project's own corpus at the first index! call: weights calibrated over millions of paragraphs, plus a stopword set, lemma map and expansion network inherited rather than derived. Read its docstring for what the refit narrows and what refit=false keeps – the two answer different needs.\nBaseProfile(profile) – a profile the caller already has, used as it is.\nFitFromCorpus(textconfig) – no base profile: fit one here, from this project's own staged corpus.\n\nThere is no default and no nothing, because the third is a trap worth being made to choose. Fitting from the project's own corpus is delegated to TextSearch.fit_profile, which runs the full pipeline – LSI, expansion network, lemma clustering – so it is bounded by a sample cap rather than by the corpus, and its vocabulary is correspondingly thin. Every term a later batch introduces that the sample never held is out-of-vocabulary and silently dropped, forever. Nothing about the project's behaviour afterwards distinguishes \"I chose this\" from \"I never said\", which is exactly why it cannot be arrived at by omission.\n\nPrefer a profile you did not fit here. That is the whole shape of this hierarchy: the two good answers both start from a model fitted over more text than this project has – which is where calibrated weights, a stopword set, a lemma map and an expansion network can come from at all – and the third exists for the case where no such model is to be had.\n\nEach is a type rather than a flag, and carries its own related options, so an option that only means something on one path cannot be handed to the other – the same reason the engine hierarchy discriminates on concrete types instead of an enum tag.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.AbstractTextModelSpec"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.BaseProfile","page":"Engine API","text":"BaseProfile(profile::TextProfile)\n\nIndex against profile – a TextSearch.TextProfile fitted elsewhere, typically load_profile(\"wiki20231101-es.zip\") over one of TextSearch.jl's corpus profiles.\n\nThe project is trained from the moment it is created: nothing is inferred from the data appended later, the vocabulary covers the language rather than whichever batch arrived first, and the project gains whatever stopword set, lemma map and query-expansion network the profile carries.\n\nWhich of those artifacts are applied is a property of the profile, not of this wrapper, and it stays that way: change it before handing it over, with TextSearch's own with_applied(p; lemmas=false). Re-exposing the same switches here would be a second place to say the same thing, and the profile is what gets persisted.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.BaseProfile"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.ContextPool","page":"Engine API","text":"ContextPool{T}\n\nA pool of T search contexts, each safe to hand to exactly one concurrent search_live call at a time – avoiding both of the two bad alternatives: sharing one T across concurrent searches (unsafe – its scratch buffers get corrupted by concurrent mutation, confirmed directly by a segfault) and rebuilding a fresh T from scratch on every call (wasteful – T's own scratch buffers, beam state, visited-vertices, per-batch cost counters, are sized by maxbatches/similar and non-trivial to allocate, so a high query rate would reallocate them constantly).\n\ntemplate is never mutated – it's read-only, existing only to be deepcopy'd into a new private worker when checkout! finds available empty. Every returned worker goes back into available via checkin! for the next caller to reuse instead of allocating again, so the pool grows lazily to (and then stays at) however many concurrent searches this engine has actually seen at once – never pre-allocated speculatively, never shrunk either, on the assumption that peak concurrency is roughly stable over an engine's lifetime.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.ContextPool"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.DefaultProfile","page":"Engine API","text":"DefaultProfile(language::Symbol; nickname=..., refit=true, max_documents=1000)\n\nIndex against the published profile for language (:en, :es, :pt), adapted to this project's own corpus at the first index! call.\n\nThis is the choice to reach for. TextSearch.refit_profile blends the base's token counts with this project's own, recomputes the weights from the blend, and inherits the stopword set, lemma map and expansion network instead of re-deriving them. No embedding is fitted, which is what makes a refit cheap next to a fit and is the point of bootstrapping.\n\nWhat a refit actually gives you, and what it does not\n\nMeasured: the published English paragraph profile holds 335,336 tokens; refitting it against an 800-document sample gives 12,238, of which 2,649 are tokens the sample never contained and the base kept. Fitting on that sample alone would give 9,589.\n\nSo a refit is not language-wide vocabulary coverage. It narrows to this corpus's own vocabulary, widened about a quarter by the base. What it inherits is the part an indexing corpus cannot produce for itself: idf and BM25 weights calibrated over millions of paragraphs rather than hundreds, plus the artifacts – for that English base, 96 stopwords, 16,661 lemmas and 10,655 expansion entries.\n\nrefit=false is therefore not just \"skip a step\": it indexes against the base untouched, all 335,336 tokens of it, so a term this project has never seen is still in the vocabulary and still searchable when a later batch brings it. The weights are Wikipedia's rather than yours. Choose by which you need – calibration for this corpus, or coverage beyond it. With refit=false this is exactly BaseProfile(load_profile(path)) with the path resolved for you.\n\nmax_documents caps the refit sample as it does on FitFromCorpus, and here the cap is cheap: the sample's only job is to say how this corpus differs from the base.\n\nWhere the profile comes from\n\n~/.textsearch/profiles/<nickname>.zip (or under $TEXTSEARCH_HOME) – the library of installed profiles textsearch install maintains, reused rather than reinvented. Nothing is bundled with this package and nothing is downloaded: the profiles are 70-160 MB each. If the one you asked for is not installed, default_profile_path says so and prints the command that installs it.\n\nnickname defaults to DEFAULT_PROFILE_NICKNAMES[language] and can be overridden to point at any installed profile – a refit of your own, a different Wikipedia snapshot, a domain-specific base.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.DefaultProfile"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.DenseEngine","page":"Engine API","text":"DenseEngine{B}\n\nA project of dense vectors: it takes DenseItems, and B says which dense index holds them – GraphBackend for an approximate, self-tuning SearchGraph, ExactBackend for a brute-force scan.\n\nThis replaced a SearchGraphEngine and a GenericEngine{IndexType}, which split dense projects by index kind at the level where a caller chooses. The kind of data is what a caller knows; which index holds it is a backend decision, and one that can now change without the project becoming a different type of thing.\n\nOnly GraphBackend has a BeamSearch to calibrate, so calibrate!, current_beamsearch and minrecall-driven search require that backend rather than being defined-but-inert on the other.\n\nFields\n\nbackend::B: the index, its context, and the backend's own state.\nsearch_ctx_pool::ContextPool: one private context per concurrent search_live call (see ContextPool). The backend's own ctx is reserved for insertion.\ndeleted_ids::Set{UInt32}: logically deleted document ids – UInt32 across every engine kind, matching the id type search results come back as.\nlock::ReadWriteLock: see its docstring for why a reader/writer lock and not a mutex.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.DenseEngine"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.FitFromCorpus","page":"Engine API","text":"FitFromCorpus(textconfig::TextConfig=TextConfig();\n              min_ndocs=1, max_documents=1000, stopwords=nothing,\n              encoder=NamedTuple(), expansion=NamedTuple(), lemmas=NamedTuple())\n\nFit this project's text model from its own staged corpus, under textconfig – the corpus-independent policy (normalization, tokenization, language), with no base profile. This is the explicit form of \"I have no pre-fitted model\", and the least good of the three choices: see AbstractTextModelSpec for what it costs and DefaultProfile for what to reach for instead.\n\nThe fit is deferred to the first index! call, and delegated whole to TextSearch.fit_profile – this package does not fit profiles, it asks the library to. That means every fit runs the full pipeline: an LSI over the sample, a query-expansion network from it, and a lemma map clustered from the same embeddings. There is no cheap variant, and the numbers are not small: measured on 3,000 paragraphs, that pipeline takes 48.8s where a bare vocabulary-and-weights pass takes 0.2s.\n\nmax_documents is why this is affordable at all\n\nThe fit reads at most max_documents of the staged corpus (1,000 by default), sampled by an even stride across it rather than taken from the front – the front is whatever batch arrived first, one book or one import, and a stride spans everything staged. Without the cap the cost grows with the corpus: the same pipeline over 300,000 paragraphs took 863s.\n\nThe cap is a real trade, not a free win, and it cuts the wrong way from the one thing this path is worst at. A vocabulary fitted from 1,000 sampled documents is smaller than one fitted from the whole corpus, so more of what gets appended later is out-of-vocabulary and silently dropped. max_documents=0 lifts the cap for a caller who would rather pay; the honest fix is not to fit here at all.\n\nOptions\n\nmin_ndocs, stopwords, encoder, expansion and lemmas are passed through to TextSearch.fit_profile; see it for the full set each accepts.\n\nmin_ndocs: drop a token seen in fewer documents than this before the encoder runs. Not cosmetic – the expansion network is an all-pairs kNN over the vocabulary, so pruning cuts the most expensive stage quadratically.\nstopwords: a document-frequency threshold in (0, 1], or nothing (the default) not to detect any. Given, it becomes the library's stopwords=(; doc_freq_threshold=...), which costs a second pass over the sample because the counts have to be recomputed under the pipeline that drops the flagged tokens.\nencoder, expansion, lemmas: the library's own option groups, as NamedTuples – encoder=(; outdim=128), lemmas=(; apply=true), and so on.\n\nTwo options this spec used to carry are gone, because the library's fit does not express them and delegation is the policy: the VectorModel weighting scheme (it always fits IdfWeighting/TfWeighting) and min_occs (it prunes by document count only).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.FitFromCorpus"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.FullTextEngine","page":"Engine API","text":"FullTextEngine\n\nA project of text: it takes TextItems, holds the text model that turns them into something an inverted file can index, and searches through whichever inverted file its backend names.\n\nThis replaced BM25Engine and InvertedFileEngine, which were near-duplicates – the same staged text, the same profile, the same deferred fit – differing in which library index they built and how a query was encoded against it. Both of those are the backend's business now (see GraphBackend for the family), so the scheme a caller wants (BM25, tf-idf, a set similarity) is a field rather than a type.\n\nUntrained (backend.index === nothing, profile === nothing) until either a BaseProfile/DefaultProfile supplies a profile at creation or the first index! call produces one. add_item!/append_items! only ever stage raw text; index! is the explicit step that trains, when it has to, and encodes/indexes the backlog.\n\nFields\n\nbackend::TextBackend: the inverted file (or nothing), its context, which kind it is, and the distance for the kinds that take one.\nprofile::Union{Nothing, TextProfile}: the whole text model – vocabulary and weights plus the corpus-produced artifacts and the lineage. One field, and the TextConfig the tokenizer runs is derived from it, so an index cannot end up tokenizing documents through a different lemma map than the one it saves. backend.index === nothing if and only if this is nothing.\nfitspec::Union{Nothing, DeferredFit}: what the deferred training does, consulted only by the index! call that has to produce a profile and never again. nothing for a project created from a BaseProfile, which has nothing to defer.\nstaged::Vector{String}: every raw text ever staged, in insertion order – the text-engine counterpart of a dense backend's .db. index! catches up the range (index === nothing ? 0 : length(index))+1:length(staged).\nsearch_ctx_pool, deleted_ids, lock: as on DenseEngine.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.FullTextEngine"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.GraphBackend","page":"Engine API","text":"GraphBackend\nExactBackend{IndexType}\nSparseBackend\nTextBackend\n\nThe index a project searches through, with the context it searches with and whatever else that particular index kind needs to be driven.\n\nFour of them because there are four ways to hold an index, not because there are four kinds of project: DenseEngine is parameterized by which dense backend it has, and FullTextEngine holds a TextBackend whose kind says which library inverted file is underneath. The engine is the data it takes; the backend is the index and the distance. Keeping them apart is what lets a project of sparse vectors exist at all – before this, an inverted file and its distance were reachable only through a text engine, and so only by a caller who had text.\n\nGraphBackend carries minrecall/opt_beamsearch, and it is the only one that does. A beam is a SearchGraph's to tune; putting that state on the engine would give every exact and every inverted-file project two fields it can only ever leave empty.\nExactBackend{IndexType} covers ExhaustiveSearch and ParallelExhaustiveSearch, which differ in nothing this module has to know about.\nSparseBackend records the dimension it was created with, because an InvertedFile is a fixed array of posting lists: the dimension is structural, not descriptive, and every item appended has to agree with it.\nTextBackend's index is nothing until the engine has a profile to build it from, and its kind is BM25InvertedFile or TextInvertedFile – which, with distance, is the whole of what used to be the difference between two separate engine types.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.GraphBackend"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.OptBeamSearch","page":"Engine API","text":"OptBeamSearch\n\nminrecall::Float32 => BeamSearch map of hyperparameters calibrated for each expected recall quality (see calibrate! and DenseEngine).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.OptBeamSearch"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.ReadWriteLock","page":"Engine API","text":"ReadWriteLock\n\nAny number of readers may hold this concurrently, but a writer needs it exclusively (no readers, no other writer) – unlike a plain ReentrantLock, which only ever allows one holder at a time regardless of what it's used for.\n\nThis distinction matters here specifically because every underlying index kind (SearchGraph, ExhaustiveSearch/ParallelExhaustiveSearch, BM25InvertedFile, InvertedFile) safely supports running multiple searches concurrently against the same index – that's exactly what their own searchbatch/allknn/@BATCHES-driven parallel search paths already rely on internally – but none of them tolerates a search running concurrently with an insertion or deletion into the same index (a search reading index.adj/index.db while add_item! resizes/appends to those same arrays is exactly the kind of concurrent read-during-mutation none of them are built for). So search_live/current_beamsearch take a read lock (concurrent with each other, exclusive of any write), while add_item!/index!/ mark_deleted!/calibrate! take a write lock (exclusive of everything).\n\nImplemented with a Threads.Condition guarding a plain reader count: read_lock briefly locks it only to bump/drop that count (so the actual read work runs without holding the condition's own lock, letting readers overlap), while write_lock holds the condition's lock for its whole body – once acquired, no new reader can even start, and the writer waits for any in-flight readers to finish first.\n\nwarning: Never take a write lock from inside a read-locked section\nA read-to-write \"upgrade\" on the same logical operation deadlocks: the write lock would wait for the active reader count to drop to zero, including the one this same call is itself holding. search_live's minrecall-driven auto-calibrate! (a write) is therefore resolved before its own read lock is acquired, never nested inside it – see that method's implementation for the pattern to follow for anything similar in the future.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.ReadWriteLock"},{"category":"type","location":"engine.html#SimilaritySearchEngine.IndexEngine.SparseEngine","page":"Engine API","text":"SparseEngine\n\nA project of sparse vectors: it takes SparseItems and indexes them in a SimilaritySearch.InvertedFile under any distance that reads a sparse vector – cosine over the weights, or one of Dist.Sets.* over the nonzero positions.\n\nNew with this restructuring, and the point of it. An inverted file and its distance used to be reachable only through a text engine, which meant a caller holding sparse vectors had to pretend to have text: fit a profile it did not want, stage strings it did not have. This is the layer that was implied and missing – vocabulary-free, no profile, no staged text.\n\nWhich is also why BM25 is not one of its distances. BM25InvertedFile holds a Vocabulary and scores from getndocs/avgdoclen; BM25 is not a metric over sparse vectors but a scoring rule over a corpus, so it belongs to FullTextEngine, which has the corpus statistics. Sparse in, sparse out, and nothing here knows what a token is.\n\nFields\n\nbackend::SparseBackend: the inverted file, its context, the distance and the dimension.\nsearch_ctx_pool, deleted_ids, lock: as on DenseEngine.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.SparseEngine"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._batch_cap-Tuple{Nothing}","page":"Engine API","text":"The batch policy of a context: the schedule always, and maxbatches unless it keeps the library's default.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._batch_cap"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._index_text_block!-Tuple{FullTextEngine, Any, Int64}","page":"Engine API","text":"_index_text_block!(engine::FullTextEngine, texts, base::Int)\n\nIndexes texts as one block, as documents base+1 …, whichever text backend this is.\n\nBuilds the block as its own small index first, then splices the result into the live one: document vectors and lengths are appended, and the block's posting lists – their ids shifted from block-local to global – are handed to persist_block!, which either merges them into an in-memory adjacency list or writes them to storage, depending on what kind the live index was built with.\n\nWhy a scratch index rather than push_item! per text, which is what this used to do: the library's batch path encodes a whole block in parallel (@BATCHES inside append_items!), while push_item! is sequential by contract. The indirection through a second index buys that parallelism without teaching this module how a document is encoded – it still doesn't know, it just asks for a block and takes the result apart.\n\nBoth text backends take the same shape here because their posting lists hold the same thing – document ids – and they differ only in what each document is stored as (term frequencies against BM25, model weights against the weighted one) and in the per-document number each keeps beside it (doclens, sizes).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._index_text_block!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._nearest_beamsearch-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}, Real}","page":"Engine API","text":"_nearest_beamsearch(engine::DenseEngine{GraphBackend}, minrecall::Real) -> BeamSearch\n\nThe BeamSearch calibrated for minrecall (see calibrate!), auto-calibrating DEFAULT_MINRECALL_LEVELS first if engine.backend.opt_beamsearch is still empty. An exact match is used if present; otherwise the closest calibrated level at or below minrecall is used, falling back to the lowest calibrated level available if minrecall sits below all of them.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._nearest_beamsearch"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._require_no_backlog-Tuple{DenseEngine, AbstractString}","page":"Engine API","text":"allknn_live(engine::DenseEngine, k::Int)\nclosestpairs_live(engine::DenseEngine, k::Int, min_k::Int)\nbichromatic_kclosestpairs_live(engine::DenseEngine, B, k::Int, min_k::Int)\nfft_live(engine::DenseEngine, k::Integer, start::Int, verbose::Bool)\ndnet_live(engine::DenseEngine, k::Integer, verbose::Bool)\nneardup_live(engine::DenseEngine, epsilon::Float32, verbose::Bool, recall::Float32)\n\nWrite-locked wrappers around SimilaritySearch.allknn/fft/dnet/neardup/closestpairs/ bichromatic_kclosestpairs – found live, 2026-09-09, that embedded.jl's whole-dataset operations called these directly with no lock at all, an obvious first hazard: a scan racing an add_item!/index! resizing the same index.adj/index.db it reads.\n\nNot a read_lock, unlike search_live – these six need FULL exclusivity, including from each other, not just from writes. Tried read_lock first (matching search_live's \"any number of concurrent reads\" model) and it is NOT safe here: confirmed live with a concurrent-access test spawning several of these at once, which failed immediately with threads static cannot be used concurrently or nested. All six parallelize internally via @BATCHES, and the global scheduler defaults to :static (SimilaritySearch.get_batch_scheduler()) – Julia's @threads :static refuses to run two such regions at the same time on the same thread pool, full stop, regardless of what data each one touches. A single search_live query never hits this (one query's beam search is plain sequential, no @threads involved), which is exactly why that docstring's \"concurrent reads are fine\" claim holds for searches but does not extend to these whole-dataset scans. So these take write_lock instead: only one of the six (and no ordinary search) runs at a time, each with the full machine free to parallelize internally – appropriate for what these already are throughout this package's docs, occasional heavyweight admin-style calls, not a hot per-request path. Full exclusivity also means it's safe to reuse engine.backend.ctx directly here (same reasoning search_live's own docstring gives for why insertion may reuse it under write_lock) – no ContextPool needed, since nothing else can be touching it at the same time.\n\nA second, unrelated bug found by the same concurrency test, fixed by _require_no_backlog: even under full write_lock exclusivity, closestpairs still crashed – BoundsError deep in SimilaritySearch.Bichromatic.bichromatic_search!, reading past the end of SearchGraph's own adjacency list. Root cause has nothing to do with locking: closestpairs(idx, ctx) is bichromatic_kclosestpairs(idx, ctx, database(idx)) (self-comparison), so its loop runs over 1:length(database(idx)) – the staged count, which grows the instant append_items! runs – while the adjacency list it reads is only as large as length(idx) (g.len[], the connected count, which only grows when an index! call catches the backlog up). This package's own \"Decoupled Staging and Indexing\" design (see the README) makes a nonzero gap between those two counts a normal, supported state, not an edge case – calling closestpairs while any items are staged-but-not-yet-indexed was already unsafe before this change, sequentially, with no concurrency involved at all; the new concurrency test is simply the first thing that ever happened to call it in that state. Fixed by refusing to run rather than crashing confusingly, applied to all six for the same reason the non-empty guard already covers all six uniformly, even though only closestpairs/a self-comparing bichromatic_kclosestpairs are actually known to read the adjacency list this way today.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._require_no_backlog"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._sample-Tuple{Any, Integer}","page":"Engine API","text":"_sample(corpus, max_documents) -> corpus, or an evenly-strided view of it\n\nAt most max_documents of corpus, taken by an even stride rather than from the front.\n\nThe front of a staged corpus is whatever batch arrived first – one book, one import, one day's ingest – so a prefix is a biased sample of exactly the axis a text model should not be biased on. A stride spans everything staged and needs no RNG, so two runs over the same corpus fit the same model. max_documents <= 0 means no cap.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._sample"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._search_context-Tuple{Type{SimilaritySearch.SearchGraphContext}}","page":"Engine API","text":"The context a search borrows from the pool: one batch slot, and the caller's logging.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._search_context"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine._text_index-Tuple{TextProfile, Type, Any, Union{Nothing, Function}}","page":"Engine API","text":"_text_index(profile::TextProfile, kind::Type, distance) -> AbstractInvertedFile\n\nThe library index a trained text engine searches through, built from profile itself.\n\nBuilt from the profile rather than from profile.model/.voc because the profile-taking constructors assemble the index's QueryPipeline too – deriving the variant map once and attaching the profile's expansion network if the profile applies it. That is state this module used to keep a second copy of, in a variants field it derived and cached itself. One map, in the index, is the whole reason that field is gone.\n\ndistance is ignored for BM25, which scores through its own bm25score and has no metric to choose (see default_distance).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine._text_index"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.add_item!-Tuple{DenseEngine, Any}","page":"Engine API","text":"add_item!(engine::AbstractSearchEngine, item)\n\nAdds a single item to the index. Thread-safe wrapper. For a DenseEngine{GraphBackend}, this only stages item (see insert_dense!) – it does not become searchable until an explicit index! call. FullTextEngine have the exact same split: item is raw text, staged into engine.staged – always allowed, whether or not the engine has been trained yet – and does not get encoded/indexed until an explicit index! call. DenseEngine{<:ExactBackend} (ExhaustiveSearch/ParallelExhaustiveSearch) is the one engine kind that still indexes synchronously, with no staging step at all.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.add_item!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.allknn_live-Tuple{DenseEngine, Int64}","page":"Engine API","text":"allknn_live(engine::DenseEngine, k::Int)\n\nAll-pairs k nearest neighbours over everything the engine currently indexes, under the project's write lock. A whole-dataset operation rather than a search: it is exclusive, it refuses to run while items are staged but not indexed, and its internal parallelism is why it cannot be nested under a concurrent region.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.allknn_live"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.apply_searchgraph_vectors!-Tuple{SearchGraph, Any}","page":"Engine API","text":"apply_searchgraph_vectors!(index::SearchGraph, block)\n\nReplays one vector block (as returned by Persistence.load_dense_vector_blocks, shaped (sp, ep, vectors)) into index: appends its vectors to index.db. Does not touch index.adj/index.len[] – build_searchgraph handles adjacency (via a separate per-object lookup) and finalizing length itself, once, after every vectors block has been applied.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.apply_searchgraph_vectors!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.backend_tag-Tuple{SimilaritySearchEngine.IndexEngine.GraphBackend}","page":"Engine API","text":"backend_tag(backend) -> Symbol\n\nWhich backend a saved project had, as a symbol.\n\nA symbol and not a type, and that is the whole reason kind is one too. The previous shape persisted Julia types – BM25Engine, ExhaustiveSearch – into the project's own store, which tied the on-disk format to the names in this file: this restructuring renamed every one of those types, and any project written under the old names became unreadable by construction. Symbols cut that tie. A rename is now a rename, and restore_engine maps a symbol it does not recognise to an error naming the ones it does, rather than to a MethodError about a type the reader has never heard of.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.backend_tag"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.build_searchgraph-Tuple{Any, Any, Function, Integer}","page":"Engine API","text":"build_searchgraph(distance, vector_blocks, load_neighbors::Function, graph_len::Integer) -> SearchGraph\n\nRebuilds a SearchGraph against distance: replays each of vector_blocks (as returned by Persistence.load_dense_vector_blocks from the project's MMapMatrixDatabase file) in order via apply_searchgraph_vectors! to reconstruct index.db in full – every vector ever staged, whether or not it was ever actually graph-indexed (see index!'s stage-then-index split: append_items!/add_item! only ever stage into .db, never graph-connect by themselves anymore).\n\ngraph_len (persisted separately, see Persistence's :graph_len engine field) is the count that actually matters for the graph structure: it can be <= length(index.db) if the process closed (or crashed) after staging some vectors but before an explicit index! call caught them up. Only object ids 1:graph_len get their saved direct neighbor list restored (load_neighbors(i), typically i -> Persistence.load_neighbors(adjacency_store, i)) and reverse-connected – restoring index.len[] to graph_len, not to the full staged count, so a later explicit index! call picks up exactly where indexing left off, over the same .db (already fully restored here) it would have seen pre-restart.\n\nThe load_neighbors(i) loop runs in parallel via SimilaritySearch.@BATCHES (2026-09-09). Measured live at a realistic scale (200K items, warm/no-compilation reopen): this loop – one RocksDB point-read per object – cost 1.2-1.6s sequentially, comparable to or larger than connect_reverse_links! right after it (0.9-1.1s, itself already @BATCHES-parallel in SimilaritySearch 1.4.1) – together the dominant fraction of a warm reopen's total ~2.3-2.5s. Safe to parallelize: AbstractAdjList.add! documents itself as thread-safe (guarded by its own glock), and a RocksDB point-read (Base.get, in RocksDB.jl) shares no mutable state across calls – each call converts its own key to bytes and reads into its own freshly-allocated output buffer, so concurrent reads against the same DB handle don't contend on anything Julia-level; RocksDB's own C++ engine is built for concurrent reads against one handle in the first place. index.adj is resized to its final graph_len once, up front, specifically so no add! call needs to trigger resize! itself under load – add!'s own lock only ever has to guard a plain append! this way, not a growth check too.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.build_searchgraph"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.build_sparseinvertedfile-Tuple{Any, Integer, Any}","page":"Engine API","text":"build_sparseinvertedfile(distance, dimension, object_blocks) -> InvertedFile\n\nRebuilds a SparseEngine's inverted file by replaying every saved sparse vector through the library's own batch append_items! – a full rebuild-by-reinsertion on every open, which is what a text project stopped doing when it began persisting its index (Persistence.InvertedIndexStore). A sparse project still does it: its items are vectors the caller encoded, so the objects it saves are what it would have to write anyway, and the same treatment is a follow-up rather than a difference of principle.\n\nBatch append_items!, not one push_item! per object: push_item! documents itself as not thread-safe – one call per object, in a plain loop, is fully sequential – while append_items!(idx, ctx, ::AbstractDatabase, n) parallelizes internally via @BATCHES.\n\ndimension comes from the saved state rather than from the vectors: an empty project has none to read it off, and one whose blocks happen to hold no nonzero in the last position would otherwise come back a different shape than it was created with.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.build_sparseinvertedfile"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.calibrate!-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}}","page":"Engine API","text":"calibrate!(engine::DenseEngine{GraphBackend}; levels=DEFAULT_MINRECALL_LEVELS, numqueries=64, ksearch=10, queries=nothing) -> OptBeamSearch\n\nRuns SimilaritySearch.jl's real optimize_index! hyperparameter sweep (PLAN.md §5.6 — a SearchModels-driven stochastic search over BeamSearchSpace, not a hand-rolled one) against engine.backend.index once per recall level in levels, storing each resulting BeamSearch into engine.backend.opt_beamsearch[level] (and returning that table) so search_live can later serve a per-request minrecall from calibrated hyperparameters instead of a single one-size-fits-all default. Only defined for a DenseEngine{GraphBackend}; errors for any other engine kind, which has no BeamSearch to calibrate. Re-running this periodically as the indexed data changes keeps opt_beamsearch accurate – the right hyperparameters for a given recall level drift as the index grows.\n\nKeyword Arguments\n\nlevels: a single target recall or a collection of them (0-1) to calibrate for — see MinRecall. Defaults to DEFAULT_MINRECALL_LEVELS.\nnumqueries: size of the query sample drawn from the already-indexed database when queries isn't given.\nksearch: neighbors retrieved per query while evaluating candidate configurations.\nqueries::Union{Nothing, AbstractVector{<:AbstractVector{Float32}}}: an explicit ground-truth query set (PLAN.md §5.6's \"supplied ... set\") instead of an internally sampled one.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.calibrate!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.checkin!-Union{Tuple{T}, Tuple{SimilaritySearchEngine.IndexEngine.ContextPool{T}, T}} where T","page":"Engine API","text":"checkin!(pool::ContextPool{T}, ctx::T)\n\nReturns a context obtained from checkout! to the pool once the caller is done with it, for the next checkout! to reuse instead of allocating a new one.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.checkin!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.checkout!-Union{Tuple{SimilaritySearchEngine.IndexEngine.ContextPool{T}}, Tuple{T}} where T","page":"Engine API","text":"checkout!(pool::ContextPool{T}) -> T\n\nHands back a T context exclusively owned by the caller until returned via checkin! – an existing idle one if pool.available has one, otherwise a fresh deepcopy of pool.template.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.checkout!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.create_engine-Tuple{Type{SearchGraph}}","page":"Engine API","text":"create_engine(::Type{SearchGraph}; distance, minrecall, textmodel, on_change, log_io) -> DenseEngine{GraphBackend}\ncreate_engine(::Type{ExhaustiveSearch}; distance, minrecall, textmodel, on_change, log_io) -> DenseEngine{ExactBackend{ExhaustiveSearch}}\ncreate_engine(::Type{ParallelExhaustiveSearch}; distance, minrecall, textmodel, on_change, log_io) -> DenseEngine{ExactBackend{ParallelExhaustiveSearch}}\n\nEvery keyword is required, and none of them has a default here – see default_distance and DEVELOPMENTSTRATEGY.md. `createproject` is the public boundary that chooses; this function is told.\n\nCreates a new, empty dense search engine of the given index type, built immediately against distance. minrecall only means anything for a SearchGraph; it's accepted (and ignored) on the other two, so a caller can pass the same keyword set uniformly regardless of index type. A textmodel, by contrast, is rejected rather than ignored on all three: a text model handed to an index of vectors is a caller mistake with no reading under which it does anything, and swallowing it silently is how a project ends up not being the kind its author thought it was. on_change::Union{Nothing,Function}, if given, is installed as a SimilaritySearch.CallbackLog observer: an (index, sp, ep) -> nothing callback fired on every :add! event a push_item!/append_items! call reports – e.g. to persist the range sp:ep that was just inserted. log_io::Union{Nothing,IO}, if given, adds a second InformativeLog reporter writing the same throttled status line to this IO – an open file handle or stdout/stderr both work; purely informative, and being a reporter rather than an observer it changes nothing about what gets persisted.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.create_engine"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.create_sparse_engine-Tuple{}","page":"Engine API","text":"create_engine(::Type{InvertedFile}; distance, dimension, on_change, log_io) -> SparseEngine\n\nCreates a project of sparse vectors – a SimilaritySearch.InvertedFile under distance, with no vocabulary, no profile and no staged text. See SparseEngine.\n\ndimension is required and structural: an inverted file is a fixed array of posting lists, so it has to be sized before the first item, and every SparseItem appended must agree with it. It is not inferred from the first item on purpose – a project whose dimension depends on which item happened to arrive first is a project whose second batch can fail for reasons the caller never stated.\n\nThis shares an index type with the text engine's weighted backend, which is why it is reached through create_sparse_engine rather than by dispatching on InvertedFile: the same library index serves two different kinds of project, and the kind is the caller's to name.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.create_sparse_engine"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.current_beamsearch-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}}","page":"Engine API","text":"current_beamsearch(engine::AbstractSearchEngine) -> Union{Nothing, BeamSearch}\n\nThe BeamSearch configuration currently installed on a DenseEngine{GraphBackend}'s index (nothing for anything else — exact indices have no beam to configure, text indices aren't dense at all). This is SimilaritySearch.jl's own BeamSearch() default until calibrate! has been called at least once (or until the growth autotuner has had a chance to run), since construction no longer installs a calibrated BeamSearch synchronously.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.current_beamsearch"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.default_backend-Tuple{Type}","page":"Engine API","text":"default_backend(engine::Type) -> Type\nvalidate_backend(engine::Type, backend::Type) -> Type\n\nThe backend an engine kind gets when the caller does not name one, and the check that a named one belongs to it.\n\nSearchGraph for a dense project (approximate and self-tuning, the useful default at any size), InvertedFile for a sparse one (its only backend), BM25InvertedFile for text – what \"full-text search\" means before anybody asks for something else.\n\nvalidate_backend earns its place because the pairing is checkable and the failure is otherwise obscure: engine=DenseEngine, backend=BM25InvertedFile would reach create_engine and die there on a MethodError about keyword arguments, naming nothing a caller could act on.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.default_backend"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.default_distance-Tuple{Type{SearchGraph}}","page":"Engine API","text":"default_distance(backend::Type) -> PreMetric\n\nThe distance a backend is built with when the caller does not name one.\n\nIt lives here, as a function of the index kind, because the alternative is what this package had: a default on create_engine and a distance=nothing sentinel on create_project, with the caller branching on which of the two to let win. Two defaults for one decision, and a ?: at the call site to arbitrate them. Now create_project resolves the sentinel through this function once and always passes a real distance down, which is also why nothing below the public surface carries a default any more (see DEVELOPMENT_STRATEGY.md).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.default_distance"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.default_profile_path-Tuple{DefaultProfile}","page":"Engine API","text":"default_profile_path(spec::DefaultProfile) -> String\n\nThe installed profile file spec names, under textsearch_home.\n\nRaises if it is not installed, and the message carries the command that installs or downloads it: \"no such file\" naming a path under ~/.textsearch is not actionable to someone who has never run the CLI, and this is the most likely first thing a caller of DefaultProfile hits.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.default_profile_path"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.direct_neighbors-Tuple{SearchGraph, Integer}","page":"Engine API","text":"direct_neighbors(index::SearchGraph, i::Integer) -> Vector{UInt32}\n\nObject i's direct (not yet reverse-connected) neighbor list, read at exactly the moment OBSERVE reports the range containing i – i.e. before connect_reverse_links! has added anything else into it.\n\nwarning: what `index` looks like inside a `CallbackLog` callback, for a `SearchGraph`\nA SearchGraph's :add! event fires before connect_reverse_links! runs for sp:ep (searchgraph/insertions.jl) – so index.adj for that exact range holds only the direct links just computed, none of the reverse links other nodes will later add into it. This is by design, not a bug to route around: direct_neighbors captures exactly that direct-links-only slice, and build_searchgraph reconnects every reverse link once, only after every staged vector and graph-indexed object's adjacency entry has been replayed – reconnecting on an already-complete graph isn't safe (it isn't idempotent; it would duplicate reverse edges), which is exactly why this format never saves reverse links in the first place. InvertedFile/BM25InvertedFile have no such hazard – their own events fire only after all of a call's mutation is done, so invertedfile_objects can read sp:ep's objects straight out of index.db with nothing left pending.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.direct_neighbors"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.engine_kind-Tuple{Type}","page":"Engine API","text":"engine_kind(engine::Type) -> Symbol\n\nThe payload_kind an engine type stands for, before any instance exists.\n\npayload_kind answers it for a live engine, which is what every guard wants; this answers it for the type a caller named, which is what create_project needs before it has built anything. Two functions rather than one because at that point there is no instance to ask.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.engine_kind"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.extra_state_fields-Tuple{Symbol}","page":"Engine API","text":"extra_state_fields(kind::Type) -> Tuple{Vararg{Symbol}}\n\nThe field names snapshot_state includes for a saved project beyond :kind, :backend, :deleted_ids and :index – the static list a loader needs before an engine instance exists to call snapshot_state on.\n\nOnly the exact dense backends are covered, and they need nothing: their whole index is a single :index field. Every other backend needs its own dedicated path instead – a graph's distance/minrecall/beam plus its replayed insertion blocks, a sparse project's distance/dimension, a text project's profile and objects – because none of them has a plain :index field to read back at all (see snapshot_state's docstring).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.extra_state_fields"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.index!-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}}","page":"Engine API","text":"index!(engine::DenseEngine{GraphBackend})\n\nCatches up the graph structure over whatever's been staged into engine.backend.index.db since the last call (or since creation) – extends SimilaritySearch.index! with the same meaning the text engines' index! methods give it: \"do the expensive part explicitly, now.\" Unlike those, this is idempotent and can be called any number of times: SimilaritySearch.index!(idx, ctx) itself already starts from length(idx) + 1 (the current graph-indexed count) up through length(database(idx)) (everything staged so far, see insert_dense!), so calling this repeatedly as more vectors accumulate only ever processes the backlog, never redoing already-indexed work, and calling it with nothing new staged is a cheap no-op.\n\nadd_item!/append_items! on a DenseEngine{GraphBackend} only ever stage raw vectors into engine.backend.index.db – they do not make new items searchable by themselves anymore (unlike DenseEngine{<:ExactBackend}/FullTextEngine, which still index synchronously on every add_item!). Call this explicitly – interactively, on a schedule, whatever fits the caller – to actually build graph connections for the backlog; search_live only ever sees items this has processed. This is also what unlocks batch-shaped insertion: staging is cheap and safe to do many times in a row without paying graph-construction cost until the caller actually wants it.\n\nErrors if engine.backend.index.db is completely empty (nothing has ever been staged) – mirrors SimilaritySearch.index!'s own @assert n > 0.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.index!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.index!-Tuple{FullTextEngine}","page":"Engine API","text":"index!(engine::FullTextEngine)\nindex!(engine::FullTextEngine)\n\nCatches up encoding/indexing over whatever's been staged into engine.staged since the last call (or since creation) – the exact same contract as index!: idempotent, safe to call any number of times, only ever processes the backlog ((engine.backend.index === nothing ? 0 : length(engine.backend.index))+1:length(engine.staged)), and a no-op when nothing new is staged.\n\nIf this engine was created from a FitFromCorpus, the first call additionally runs that fit (see fit_profile) over every text staged so far (engine.staged, not just what's staged in this particular call) and builds the real index against the resulting profile – TextSearch.jl needs a Vocabulary before either index type can be constructed at all, and retraining isn't supported, so this only ever happens once per engine, whichever index! call first finds engine.profile === nothing. Tokens never seen at that fit are out-of-vocabulary on every later batch and silently dropped when encoded (a TextSearch.jl limitation, not a bug in this package) – if engine.staged at first-index! time isn't representative of the text you'll keep appending, later recall will suffer.\n\nAn engine created from a BaseProfile skips all of that: it was trained before the first item was ever staged, so every call here is a pure catch-up and the vocabulary never depends on what happened to be appended first. That is the shape to prefer for anything but a self-contained corpus – see create_engine, which makes the choice explicit.\n\nadd_item!/append_items! on a FullTextEngine only ever stage raw text into engine.staged – exactly like DenseEngine{GraphBackend}, they do not make new items searchable by themselves; search_live only ever sees items this has processed. DenseEngine{<:ExactBackend} (ExhaustiveSearch/ParallelExhaustiveSearch) is the one engine kind with no such split at all – it always evaluates directly against db, so it has no index! method of its own.\n\nErrors if engine.staged is completely empty (nothing has ever been staged) – mirrors index!(engine::DenseEngine{GraphBackend})'s empty-.db error.\n\nRebuilding a project written before the index was persisted\n\nA text project persists its index (Persistence.InvertedIndexStore); one written before that has only the raw objects it saved, which nothing reads anymore. Such a project opens with its profile and its staged text intact and no index at all, and one call rebuilds it:\n\nh = open_project(workdir, dataset)\nindex!(h)     # re-indexes every staged text, and persists the result\n\nThat works because the staged text was always persisted in full, independently of the index, so the rebuild has the same input the original indexing had. It is a deliberate one-line procedure rather than an automatic migration: this package has no released version and no project large enough to make an invisible, once-per-project rebuild worth the code that would have to keep the old format readable.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.index!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.invertedfile_objects-Tuple{SimilaritySearch.InvertedFiles.AbstractInvertedFile, Integer, Integer}","page":"Engine API","text":"invertedfile_objects(index::AbstractInvertedFile, sp::Integer, ep::Integer) -> Vector\n\nRange sp:ep's raw indexed objects (index.db – bags-of-words for a BM25InvertedFile, SparseVectors for an InvertedFile), read at the moment LOG reports this range. Unlike direct_neighbors's SearchGraph counterpart, there's no before/after-connect_reverse_links! timing to worry about here – push_item!/ append_items! for an inverted file fully finalize an object's contribution to the posting lists before LOG fires at all (see SimilaritySearch.CallbackLog's docstring), so this can be read at any point after the report, not just at exactly that moment.\n\ndatabase(index, i) for a sparse-vector-backed index hands back a Special.Sparse. SparseVecView – a view into index.db's own packed storage, fine for reading/scoring but not directly re-insertable (confirmed empirically, two different ways it breaks):\n\nMaterializing it into a plain SparseVector and pushing that into a fresh InvertedFile works (that's the same shape add_item!'s own vectorize already produces for a live insert).\nPushing that same SparseVector into a fresh BM25InvertedFile does not: push_item!(idx::BM25InvertedFile, ctx, doc::T) where T<:Union{AbstractString, AbstractVector,TokenizedText} (TextSearch.BM25's tokenizing overload) is more specific than the generic push_item!(idx, ctx, obj) for any SparseVector (it is an AbstractVector), so it gets (mis)treated as literal pre-tokenized input instead of an already-computed bag. BM25InvertedFile needs a genuine BOW (Dict{UInt32,Int32}) instead, which the generic method's pairiterator(::Dict) = d accepts directly. Hence _materialize dispatches on the target index type, not just the view.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.invertedfile_objects"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.is_text_index_type-Tuple{Type{BM25InvertedFile}}","page":"Engine API","text":"is_text_index_type(::Type) -> Bool\n\nWhether a backend type is one of the text ones – the ones that take a AbstractTextModelSpec and reject nothing else. InvertedFile and TextInvertedFile both name the weighted engine (see FullTextEngine).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.is_text_index_type"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.mark_deleted!-Tuple{SimilaritySearchEngine.IndexEngine.AbstractSearchEngine, Integer}","page":"Engine API","text":"mark_deleted!(engine::AbstractSearchEngine, doc_id::Integer)\n\nMarks a document as logically deleted. doc_id is converted to UInt32, matching deleted_ids's element type across every engine kind (see AbstractSearchEngine).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.mark_deleted!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.maybe_trim_cache!-Tuple{Any}","page":"Engine API","text":"maybe_trim_cache!(adj) -> Any\n\nGives an adjacency list that caches something the chance to shed it, once a query has fully resolved. The generic method does nothing; Persistence.LazyPostings adds one that evicts its least frequently used posting lists. Called from search_live after the search returns – never during one.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.maybe_trim_cache!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.payload_kind-Tuple{DenseEngine}","page":"Engine API","text":"payload_kind(engine::AbstractSearchEngine) -> Symbol\n\nWhat this project indexes: :dense, :sparse or :text.\n\nReplaces an istextindex predicate that a dozen guards used as a stand-in for \"not dense\". With two engine kinds that reading happened to hold; with three it is simply false – a SparseEngine is neither text nor dense – so a guard meaning \"this needs raw vectors\" now says payload_kind(engine) === :dense rather than !is_text_index(engine).\n\nIt also names the item type a project accepts: :dense takes DenseItems, :sparse takes SparseItems, :text takes TextItems.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.payload_kind"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.persist_block!-Tuple{SimilaritySearch.AbstractAdjList, Int64, Any, Dict{UInt32, Vector{UInt32}}}","page":"Engine API","text":"persist_block!(adj, base::Int, docvecs, postings::Dict{UInt32,Vector{UInt32}})\n\nRecords one block's new postings against adj, whatever kind of adjacency list that is.\n\nThe generic method here is the in-memory one: it simply appends each token's new document ids to the list it already holds. A persistent adjacency list (Persistence.LazyPostings) adds a method that writes the block to storage instead – which is why this is a function call and not an if: IndexEngine has no dependency on Persistence, and does not acquire one by being able to hand a block to whatever adjacency list its index was built with.\n\nbase is the id of the last document indexed before this block, and docvecs are the block's own term vectors, both ignored by the in-memory method (the caller has already spliced them into the live index) and both needed by a persistent one.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.persist_block!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.read_lock-Tuple{Function, SimilaritySearchEngine.IndexEngine.ReadWriteLock}","page":"Engine API","text":"read_lock(f::Function, rw::ReadWriteLock)\n\nRuns f() with rw held as a reader – concurrently with any other number of readers, but never while a writer holds it (see ReadWriteLock).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.read_lock"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.resolve_query-Tuple{FullTextEngine, AbstractString, QueryPolicy}","page":"Engine API","text":"resolve_query(engine, text::AbstractString, policy::QueryPolicy) -> TextSearch.ResolvedQuery\n\nRuns text through the library's query pipeline – the one living on this engine's index – under policy, and hands back both what to search for and what correction did to each spelling that was typed.\n\nExists for ftexplain, and for nothing else: search_live does not call it, because TextSearch's own search takes the same policy keyword and resolves the query itself. Correcting a query is a substitution the person who typed it is owed a report of, which is what TextSearch.explain(rq.resolution) renders and what QueryPolicy(correction=:off) undoes.\n\nThis module used to assemble the pipeline here: tokenize, resolve, rebuild the term list, restrict the network to expansion_sources, weight the neighbours – and cache its own variant map to afford it. All of that is query_tokens now, and the map lives once, inside the index, where the profile-taking constructor put it.\n\nErrors if engine has no profile yet – nothing indexed, so no vocabulary to resolve against.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.resolve_query"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.restore_engine-Tuple{Any}","page":"Engine API","text":"restore_engine(state; on_change, log_io) -> AbstractSearchEngine\n\nRebuilds a concrete engine from state (as produced by snapshot_state, or assembled field-by-field via extra_state_fields from an EngineStore), dispatching on state.kind to reconstruct exactly the engine type that was saved – including, for a DenseEngine{GraphBackend}, the minrecall target it was growing toward, so a reopened project keeps autotuning toward the same target instead of reverting to some other default. on_change/log_io are as in create_engine.\n\nOnly an exact dense project's state carries a plain index; every other kind carries what its own build_* function needs instead:\n\nDenseEngine{GraphBackend}: distance, vector_blocks (Persistence.load_dense_vector_blocks(dense_vectors_store)), load_neighbors (typically i -> Persistence.load_neighbors(adjacency_store, i)), graph_len (Persistence.load_field(store, :graph_len, 0) – may be < length of the restored .db if some staged vectors were never caught up by an explicit index! call before the project last closed) – see build_searchgraph.\nFullTextEngine: profile, fitspec, prebuilt_index (the index assembled from what was persisted, or nothing when the project has none yet), adj_factory (what a new index's posting lists are built around, since a project trained by its first index! has no index at restore time), distance, and staged (every raw text ever staged, flattened from Persistence.load_staged_text_blocks, which can be longer than what the index holds if a backlog was still pending an index! call when the project last closed). Nothing is rebuilt here: a text index is assembled or absent.\nSparseEngine: distance, dimension, and object_blocks – the same encoded posting-list blocks a text project replays, minus the profile there is no vocabulary for.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.restore_engine"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.search_live-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}, Any, Int64}","page":"Engine API","text":"search_live(engine::AbstractSearchEngine, query, k::Int; bs_override, minrecall, policy) -> (id=..., dist=..., deleted=...)\n\nPerforms a plain top-k search and reports each candidate's soft-delete status instead of hiding deleted candidates and silently backfilling behind them: deleted[i] tells the caller whether id[i] is a soft-deleted document, rather than that document being dropped and a live one from further down the ranking taking its place under the same k slots. Compensating for deletions by overfetching here doesn't scale (the overfetch would have to grow with the total number of deletions) and it hides from the caller which ids were even considered; a caller that wants k live results after filtering out the deleted ones is expected to page for more (e.g. via a cursor over successive top-k' windows) at a layer above this one, which is also where such a policy belongs.\n\nKeyword Arguments\n\nbs_override::Union{Nothing, BeamSearch}: an ad hoc BeamSearch configuration to search with for this call only (PLAN.md §3/§7's per-request beamsearch_overrides), instead of engine.backend.index.algo[]'s calibrated default. Only meaningful for a DenseEngine{GraphBackend} — accepted and ignored on every other engine kind, which has no BeamSearch to override in the first place. Takes priority over minrecall below.\nminrecall::Union{Nothing, Real}: search at (approximately) this target recall instead of engine.backend.index.algo[]'s current default, by looking up the matching calibrated BeamSearch in engine.backend.opt_beamsearch (see calibrate!/_nearest_beamsearch — auto-calibrating DEFAULT_MINRECALL_LEVELS first if that table is still empty). Only meaningful for a DenseEngine{GraphBackend}; accepted and ignored on every other engine kind, which has no calibrated BeamSearch table to consult.\npolicy::QueryPolicy: how to treat a text query – whether to correct its spelling against the vocabulary and whether to widen it with the profile's expansion network (see resolve_query). Only meaningful for FullTextEngine; accepted and ignored on every dense engine kind, whose queries are vectors with nothing to resolve. The default QueryPolicy() is inert for a profile fitted under the default TextConfig(): that policy already folds case and diacritics, so there are no orthographic variants left to bridge, and a locally-fitted profile carries no expansion network to widen with.\n\nwarning: Concurrency: a *read* lock, not exclusive -- multiple searches run concurrently\nNone of the underlying index kinds (SearchGraph, ExhaustiveSearch/ ParallelExhaustiveSearch, BM25InvertedFile, InvertedFile) support searching concurrently with an insertion into the same index – a search reading index.adj/ index.db while add_item! is resizing/appending to those same arrays is exactly the kind of concurrent read-during-mutation none of them are built to tolerate. But all of them do support running multiple searches concurrently against the same index – that's exactly what their own searchbatch/allknn/@BATCHES-driven parallel search paths already rely on internally. So search_live takes engine.lock as a read_lock: concurrent with any other number of searches, exclusive of add_item!/index!/mark_deleted!/calibrate! (each a write_lock) and of each other. See ReadWriteLock for the full contract, including why a DenseEngine{GraphBackend}'s own minrecall-driven auto-calibrate! below has to be resolved before the read lock is acquired, not nested inside it.That guarantees the index data (index.adj/index.db) tolerates concurrent reads, but not by itself enough: engine.backend.ctx is a mutable, reused scratch context (beam buffers, visited-vertices state, per-batch cost counters, ...), and two concurrent searches sharing that same object corrupt each other's scratch space – confirmed directly, a segfault inside SearchGraph's own beamsearch_inner_beam from two threads racing on shared visited-vertices state via a shared ctx. So every search_live method borrows a private context from ContextPool via checkout!/checkin! instead of reusing engine.backend.ctx (which stays reserved for insertion, itself already exclusive via write_lock, so sharing it there is fine) or allocating a brand-new context on every single call.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.search_live"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.searchbatch_live-Tuple{Union{SparseEngine, DenseEngine}, SimilaritySearch.AbstractDatabase, Int64}","page":"Engine API","text":"searchbatch_live(engine::Union{DenseEngine,SparseEngine}, Q::AbstractDatabase, k::Int) -> (ids, dists)\n\nk nearest neighbours for every query in Q at once, as (k, length(Q)) matrices of UInt32 ids and Float32 distances (0/typemax(Float32) padding a column with fewer than k hits).\n\nA whole-dataset operation, not a search. It belongs with allknn_live and the other five rather than with search_live, and for the same reason: it parallelizes internally through SimilaritySearch.searchbatch's own @BATCHES, so under the default :static scheduler it cannot run beside another such region – hence write_lock (full exclusivity, including from other batch calls) instead of search_live's read_lock, and hence _require_no_backlog for the same staged-versus-connected gap. The difference in cost is worth the constraint: one batch of queries runs an order of magnitude faster than the same queries one search_live call at a time (measured 2026-09-10 on 50k dense vectors, 8 threads: 3.6k queries/s sequential, 39k/s batched).\n\nDeleted ids are not filtered here – this is the raw index answer, and telling which hits are soft-deleted costs a lookup per hit that a caller wanting raw matrices did not ask for. search(handle, queries, k) hydrates them; this does not.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.searchbatch_live"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.snapshot_state-Tuple{DenseEngine{SimilaritySearchEngine.IndexEngine.GraphBackend}}","page":"Engine API","text":"snapshot_state(engine::AbstractSearchEngine) -> NamedTuple\n\nThe full set of engine's state that must survive a save (see Persistence.EngineStore) – ctx/lock are transient and rebuilt fresh by restore_engine. The returned kind field says what the project holds (:dense, :sparse or :text) and backend which index held it (see backend_tag) – two symbols rather than one Julia type, which is what makes a saved project survive a rename of the types in this file. Together they are the discriminator restore_engine dispatches on, via Val{kind}. Used for a one-shot full save (e.g. right after create_engine, when there's nothing indexed yet); routine mutations instead persist just the one or two fields they touched (see extra_state_fields for the field lists a caller needs to read back before an engine instance exists to call this on).\n\nOnly DenseEngine{<:ExactBackend} carries a plain :index – for a brute-force index the whole thing is a single value, so saving it is saving the field. No other backend has such a field to save at all, and each carries instead what its own build_* function needs to replay its saved insertion blocks:\n\na graph-backed dense project: distance, plus minrecall/opt_beamsearch (see SimilaritySearch.CallbackLog/direct_neighbors/build_searchgraph);\na sparse project: distance and dimension – structural, since an InvertedFile is a fixed array of posting lists and cannot be rebuilt without its size;\na text project: profile, fitspec and distance (see invertedfile_objects).\n\nA text project saves one profile where two engines used to save a voc/model pair: a TextSearch.TextProfile holds both, along with the artifacts and lineage neither of them carried, so there is one value to write and no way for the two halves to be saved out of step with each other.\n\nNo backend includes staged/.db's raw items either, for the same reason: those live in their own dedicated, incrementally-persisted store (a SearchGraph's MMapMatrixDatabase file, a text engine's Persistence.StagedTextStore), not a plain EngineStore field – rewriting the whole (potentially large, ever-growing) staged sequence into EngineStore on every mutation is exactly the \"single ever-growing blob\" this design avoids. A caller restoring any of them assembles staged/vector_blocks into the state NamedTuple by hand from that dedicated store (see restore_engine's docstring) rather than getting it from snapshot_state.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.snapshot_state"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.stored_payload-Tuple{FullTextEngine, Integer}","page":"Engine API","text":"stored_payload(engine::AbstractSearchEngine, id::Integer) -> Union{String, Vector{Float32}, Nothing}\n\nThe very text or vector indexed under internal id, or nothing if id is out of range.\n\nThis is the read-back side of add_item!, and it exists because a search result is not much use without it: a hit carries an id and a score, and everything else a caller knows about the item comes from its metadata record – which deliberately does not hold the payload, since the payload already lives here. Before this existed the only way to a text project's paragraph was engine.staged[id], reaching past the public surface into a field.\n\nReads from wherever the engine already keeps the item, with no second copy anywhere: a text engine's staged (every paragraph ever staged, in insertion order, which is id order), a SearchGraph's or a DenseEngine{<:ExactBackend}'s own database. A dense payload is materialized into a fresh Vector{Float32} rather than handed out as a view, because the view aliases the index's live storage and the caller has no way to know that.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.stored_payload"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.text_profile-Tuple{FullTextEngine}","page":"Engine API","text":"text_profile(engine::AbstractSearchEngine) -> Union{Nothing, TextProfile}\ntext_vocabulary(engine::AbstractSearchEngine) -> Union{Nothing, TextSearch.Vocabulary}\n\nThis engine's text model, or nothing for a dense engine kind (and for a text engine that has not been trained yet – see FullTextEngine). text_vocabulary is the shortcut for the vocabulary inside it, which is what bagofwords/token2id/resolve_query all work against.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.text_profile"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.textsearch_home-Tuple{}","page":"Engine API","text":"textsearch_home() -> String\n\nWhere textsearch install keeps its profile library: $TEXTSEARCH_HOME, or ~/.textsearch.\n\nThe convention is replicated here rather than called, because it lives in the textsearch CLI app – which is an application, not a package this one can depend on. One line, and the env var is the part that matters: a caller who moved their profile library expects both halves to agree about where it went.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.textsearch_home"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.train_profile-Tuple{FitFromCorpus, Any}","page":"Engine API","text":"train_profile(spec::DeferredFit, corpus; verbose=false) -> TextProfile\n\nThe profile a deferred spec produces, given the corpus staged by the time index! first runs.\n\nOne entry point over the two deferred specs, because index! should not care which it is holding – it asks for a profile and gets one. What happens underneath is entirely different in cost and in quality:\n\nFitFromCorpus fits one from the sample, through the library.\nDefaultProfile loads the installed base for its language and, unless refit=false, adapts it with TextSearch.refit_profile: the base's counters blended with this corpus's, the weights recomputed, the stopword set, lemma map and expansion network inherited. No embedding is fitted, which is what makes this the cheap path and the one with the better vocabulary – the opposite of the trade FitFromCorpus has to make.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.train_profile"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.validate_textmodel-Tuple{Type, Any}","page":"Engine API","text":"validate_textmodel(backend::Type, textmodel) -> Union{Nothing, AbstractTextModelSpec}\n\nChecks textmodel against a backend – required for a text one, refused for a dense one – and hands back the spec (or nothing). Raises the same errors create_engine would.\n\nA backend and not an engine, which is a real limit worth naming: InvertedFile is legal under both SparseEngine and FullTextEngine, so this cannot tell a sparse project from a text one. create_project therefore checks the sparse case itself, against the engine the caller named, and only delegates here for a text project.\n\nSplit out so a caller can run the check before committing to anything: create_project opens the project's RocksDB directory before it ever reaches create_engine, so letting the error surface there would leave a created directory and a held write lock behind on a call that failed. create_engine still checks for itself, for callers that reach it directly.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.validate_textmodel"},{"category":"method","location":"engine.html#SimilaritySearchEngine.IndexEngine.write_lock-Tuple{Function, SimilaritySearchEngine.IndexEngine.ReadWriteLock}","page":"Engine API","text":"write_lock(f::Function, rw::ReadWriteLock)\n\nRuns f() with rw held exclusively – no concurrent readers, no concurrent writer (see ReadWriteLock). Never call this from code already running inside a read_lock on the same rw – see ReadWriteLock's deadlock warning.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.write_lock"},{"category":"method","location":"engine.html#TextSearch.fit_profile-Tuple{FitFromCorpus, Any}","page":"Engine API","text":"TextSearch.fit_profile(spec::FitFromCorpus, corpus; verbose=false) -> TextProfile\n\nFits a profile from corpus as FitFromCorpus spec asks – by handing the whole job to the library.\n\nThis method is an adapter and nothing else: it samples corpus down to spec.max_documents (see _sample) and calls TextSearch.fit_profile(spec.textconfig, sample; ...). This package does not fit profiles. It used to – a vocabulary, a weighting scheme and an optional stopword pass, about forty lines – and every one of those lines was a second implementation of something the library does, with the drift that implies. Delegating is the policy, and the forty lines are gone.\n\nWhat the caller gets for it is the full pipeline rather than the cheap subset: an LSI over the sample, a query-expansion network derived from it, and a lemma map clustered from the same embeddings. That is strictly more than the old body produced and it costs accordingly, which is what spec.max_documents exists to bound and what DefaultProfile exists to avoid.\n\nverbose is passed through, defaulting to false here rather than the library's true: this runs inside an index! call, which is not a place a caller asked for a progress report.\n\n\n\n\n\n","title":"TextSearch.fit_profile"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.IndexEngine.BACKENDS","page":"Engine API","text":"BACKENDS\n\nWhich backends each engine kind accepts, and in which order – the first is that engine's default.\n\nA table rather than a set of methods because it is read three ways: to default a backend, to check one, and to say in an error message what the alternatives were. Three methods would drift from one another; one table cannot.\n\nInvertedFile appears under both SparseEngine and FullTextEngine, and that is not a mistake: the same library index serves a project of sparse vectors and a project of text whose vectors come from a profile. Which of the two you get is engine's to say – the entire reason engine and backend are separate arguments.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.BACKENDS"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.IndexEngine.BATCH_SCHEDULER","page":"Engine API","text":"BATCH_SCHEDULER\n\nWhich Threads.@threads schedule every context built here hands to @BATCHES.\n\n:dynamic is the only schedule a process holding more than one project can use. Julia refuses to enter a @threads :static region while another one is already running anywhere in the process – the flag it tests is global, so the refusal does not depend on the two regions sharing any data:\n\nindex!(a) => `@threads :static` cannot be used concurrently or nested\nindex!(b) => ok\n\nThat is two different projects, each with its own ReadWriteLock. write_lock excludes callers of the same engine and nothing else, so nothing was ever excluding these two, and both index! and calibrate! run in-process inside a server's request handler.\n\nSimilaritySearch 1.4.2 made :dynamic its own global default for that reason (sadit/SimilaritySearch.jl#63), so this constant no longer corrects a dangerous default. It is still passed on every context: a schedule this package's correctness depends on belongs where it takes effect, not in whatever the base library happens to default to on the day someone reads this.\n\nNot :default, which names whatever Threads.@threads picks unannotated – :dynamic today, and the one name Julia reserves the right to redefine. What is wanted here is the schedule, not Julia's future choice of it.\n\nNot :greedy, which is the schedule for very uneven per-batch cost. @BATCHES has already averaged the cost of minbatch elements into each batch by the time the schedule is chosen, so little imbalance is left to recover and the shared channel is paid for nothing: measured on 30k 128-dimensional vectors over 16 threads, three runs each, allknn took 0.55 s under :static, 0.56 s under :dynamic and 0.68 s under :greedy, and index! 5.5 s, 5.24 s and 5.6 s.\n\nA search never reaches any of this. SEARCH_CONTEXT_MAXBATCHES makes minbatch equal the whole range, and @BATCHES runs a range that fits in one batch serially in the caller's own task, never entering a threaded region at all. The schedule is set on those contexts too so that raising that cap later cannot reintroduce the defect.\n\nThis does not relax allknn_live's write_lock. The scheduler was one of two reasons for full exclusivity there; the other, that the six share engine.backend.ctx directly, is untouched by any of this.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.BATCH_SCHEDULER"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.IndexEngine.DEFAULT_MINRECALL_LEVELS","page":"Engine API","text":"DEFAULT_MINRECALL_LEVELS\n\nThe recall levels calibrate! populates a DenseEngine{GraphBackend}'s OptBeamSearch with when no explicit levels are given.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.DEFAULT_MINRECALL_LEVELS"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.IndexEngine.DEFAULT_PROFILE_NICKNAMES","page":"Engine API","text":"DEFAULT_PROFILE_NICKNAMES\n\nThe installed-profile nickname DefaultProfile looks for, per language. Official published profiles are available for :en, :es, :eu, :fr, :it, :pt, and :ru.\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.DEFAULT_PROFILE_NICKNAMES"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.IndexEngine.SEARCH_CONTEXT_MAXBATCHES","page":"Engine API","text":"SEARCH_CONTEXT_MAXBATCHES\n\nHow many batch slots a context borrowed for one search carries: one.\n\nA context is two things at once, the scratch space of an operation and the cap on how far that operation spreads over threads (SimilaritySearch.getminbatch reads ctx.maxbatches). The library's default, 8 * Threads.nthreads(), is right for an operation that processes many elements with @BATCHES – insertion, allknn, searchbatch. A single query is one element and uses one slot.\n\nThe difference is not small for a SearchGraph, whose context preallocates one visited-set buffer per slot: measured on 64 threads, 128.2 MB with the default against 0.25 MB with one slot, and the same query answers with the same identifiers either way. Multiplied by the number of concurrent searches a server admits, that is the difference between gigabytes of scratch that never shrink and a few megabytes (see ContextPool).\n\n\n\n\n\n","title":"SimilaritySearchEngine.IndexEngine.SEARCH_CONTEXT_MAXBATCHES"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.AdjacencyStore","page":"Engine API","text":"AdjacencyStore\n\nA object_id::Integer => direct_neighbors::Vector{UInt32} store, one entry per indexed object, backed by its own RocksDB column family (ADJACENCY_CF). Only ever used for a SearchGraph's direct links (see IndexEngine.direct_neighbors/ build_searchgraph) – InvertedFile/BM25InvertedFile have no equivalent per-object adjacency concept; their own incremental persistence is InvertedFileObjectStore instead.\n\nShares its underlying RocksDB.DB connection, same as EngineStore; no separate close of its own to call.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.AdjacencyStore"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.BagDocVec","page":"Engine API","text":"BagDocVec\n\nThe third shape, and the reason the stored values carry a tag: a weighted project under a set distance (Dist.Sets.*) indexes bags of words rather than weighted vectors, because those distances read set membership and not weights at all. TextSearch.jl picks the encoding from the distance, so the store has to accept whichever one the project ended up with.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.BagDocVec"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.DocVec","page":"Engine API","text":"DocVec\nWeightedDocVec\n\nThe two shapes a document is stored as, one per text backend: BM25InvertedFile keeps integer term frequencies (bm25score recomputes the query-document intersection from them), while the weighted backend keeps the model's Float32 weights. Both are named here because this module writes and reconstructs them, and the first lives several modules deep inside SimilaritySearch.jl.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.DocVec"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.DumpRecord","page":"Engine API","text":"DumpRecord\n\nOne project row in a dump bundle's Avro export (PLAN.md §4.4) – the cross-language interop format load (or any non-Julia Avro reader) consumes. Mirrors Schema.MetadataRecord field-for-field (all concrete types, so they go straight into Avro columns – confirmed empirically – rather than through a JSON-encoded declared_json blob the way the pre-Schema redesign version of this struct needed) plus meta_json for the one part that can't be a native Avro column (meta's values are heterogeneous/Any-typed, which Avro.jl's schema inference rejects outright – see Schema.MetadataRecord's own docstring) and tombstoned.\n\nDeliberately does not carry vector/text: unlike the pre-Schema redesign version of this struct (where MetadataRecord.extra held the complete original raw item, vector/ text included, making declared_json/extra_json alone a lossless copy), meta here never contains vector/text at all – both live only in the engine's own dedicated, 1..n-indexed column family (see EngineStore/InvertedFileObjectStore). A caller wiring up a real execute_dump/execute_load on top of this must separately export/import that column family (already exactly what EngineStore's own block-based fields are for) for a dump bundle to be losslessly restorable – this struct alone is the metadata half only.\n\nFields\n\n_id::Int32: matches Schema.MetadataRecord's _id – the index position add_item!/push_item! assigned it.\nschema_version::Int: copied from the record, see Schema.MetadataRecord.\ndoc_id::Union{String,Nothing}: copied from the record.\nkeywords::Vector{String}: copied from the record.\nrefs::Vector{String}: copied from the record.\nmeta_json::Union{String,Nothing}: the record's meta, already JSON-encoded (see Schema.encode_meta/Project.get_raw_meta) – nothing if this _id had no meta stored at all (not the same as an empty JSON object).\ntombstoned::Bool: whether this _id was in the engine's deleted_ids at dump time. load restores tombstones from the copied JLD2 snapshot's own deleted_ids (already authoritative, see Server.handle_delete_item), not by recomputing them from this flag – it's carried here purely for a non-Julia consumer that only has the Avro file.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.DumpRecord"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.DumpRecord-Tuple{MetadataRecord, Union{Nothing, String}, Bool}","page":"Engine API","text":"DumpRecord(record::Schema.MetadataRecord, meta_json::Union{String,Nothing}, tombstoned::Bool) -> DumpRecord\n\nConvenience constructor for a real execute_dump caller: pulls every Schema.MetadataRecord field across as-is, pairing it with its already-encoded meta_json (e.g. Project.get_raw_meta(project, record._id)) and tombstoned flag.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.DumpRecord"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.EngineStore","page":"Engine API","text":"EngineStore\n\nA field_name::Symbol => value store for one IndexEngine.AbstractSearchEngine's state, backed by one RocksDB column family (ENGINE_CF) with one key per struct field – replacing a single JLD2 blob holding the whole engine (index included) so that a mutation touching only one field (e.g. deleted_ids on a soft-delete) only ever rewrites that one key, not the whole index.\n\nShares its underlying RocksDB.DB connection (typically Project.ProjectManager.db) – closing that DB (e.g. via Project.close_project) is what closes this store too; it has no separate close of its own to call.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.EngineStore"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.InvertedFileObjectStore","page":"Engine API","text":"InvertedFileObjectStore\n\nAn append-only sequence of raw indexed objects (see IndexEngine.invertedfile_objects) for one inverted-file-backed project – a FullTextEngine or a SparseEngine – backed by its own RocksDB column family (INVFILE_DB_CF) – kept separate from EngineStore for the same reason AdjacencyStore is: this can grow to one entry per indexed object, and shouldn't share a keyspace with the handful of other, small engine fields.\n\nUnlike a SearchGraph, an inverted file's posting lists have no direct/reverse-link split to worry about – push_item! fully finalizes each object's contribution before LOG even fires (see SimilaritySearch.CallbackLog's docstring), so what's saved here is simply every object ever indexed, in insertion order; reloading rebuilds the whole index by replaying them through the library's own append_items! again (see IndexEngine.build_sparseinvertedfile) rather than trying to persist posting lists directly.\n\nOnly a sparse project still works this way. A text project persists its index instead – see InvertedIndexStore – which is what removed the rebuild from its every open.\n\nwarning: Scaling: a rebuild-by-reinsertion design, not an on-disk index format\nReloading this way means every raw object saved here, and the whole rebuilt index, has to fit in memory at once, and reloading costs the full original indexing time over again (proportional to the object count) rather than being O(1) or streamed. That's a fine trade for collections up to a few million documents – it is not a design for billion-document corpora, which need genuinely disk-backed posting lists (paged/streamed from disk on demand, never fully materialized in memory) – a different architecture this project does not attempt.\n\nKeyed directly by each block's own starting id (sp, see Schema.be_key) rather than a generic block-count-and-prefix scheme: its own dedicated column family has no need for that extra indirection, the id is the key, and iterating the column family already yields blocks back in ascending sp order for free (see Schema.be_key's docstring). IndexEngine.invertedfile_objects(index, sp, ep) returns a plain Vector with no sp of its own, so append_objects! takes sp as a separate argument instead.\n\nShares its underlying RocksDB.DB connection, same as EngineStore; no separate close of its own to call.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.InvertedFileObjectStore"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.InvertedIndexStore","page":"Engine API","text":"InvertedIndexStore\n\nThe two column families of INVFILE_POSTINGS_CF/INVFILE_DOCVECS_CF as one handle, sharing the project's own RocksDB.DB connection like every other store here.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.InvertedIndexStore"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.LazyPostings","page":"Engine API","text":"LazyPostings(store::InvertedIndexStore, vocsize::Int, maxlists::Int, baselists::Int)\n\nA BM25InvertedFile's posting lists, read from RocksDB on demand instead of held in memory, with a bounded cache in front.\n\nThis is the extension point SimilaritySearch.jl documents: an AbstractAdjList{UInt32} needs only neighbors/neighbors_length/eachindex, so the search code neither knows nor cares that a list came from disk. Being lazy here is close to free – a query reads one list per term, not one per candidate – which is why this half is lazy and the document vectors are not (see load_invfile_docvecs). Measured on 265k Gutenberg paragraphs, 2026-09-11: 66.8 ms per query fully resident against 68.0 ms with these lists on disk, while the resident lists cost 55.8 MB of the index's 237.5 MB.\n\nThe cache, and why trimming is not part of a read\n\ncache holds decoded lists; uses counts how often each was asked for. When trim_postings_cache! finds more than maxlists entries it evicts the least frequently used down to baselists, so the cost is paid in bulk rather than on the unlucky read that crossed the threshold, and a hot list is never evicted by a burst of one-off lookups within a single query.\n\nEviction happens only between queries, never inside one. trim_postings_cache! is called by the engine once a search has fully resolved; a read never evicts. The reason is correctness under concurrency as much as latency: several searches run against this index at once (they take a read lock, not an exclusive one), and a list evicted mid-query would be re-read and re-decoded by whichever of them was still walking it.\n\nCounting lists rather than bytes is deliberate too: what the cache is protecting against is re-reading and re-decoding, and that cost tracks the number of lookups, not their size.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.LazyPostings"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.StagedTextStore","page":"Engine API","text":"StagedTextStore\n\nAn append-only sequence of raw, not-yet-encoded text blocks for one FullTextEngine, backed by its own RocksDB column family (STAGED_TEXT_CF) – the text-engine counterpart of a DenseEngine{GraphBackend}'s dense_vectors.mmapdb: every item add_item!/append_items! has ever staged (see IndexEngine.FullTextEngine's staged field), whether or not IndexEngine.index!(engine::IndexEngine.FullTextEngine) has caught it up into the real BM25InvertedFile/InvertedFile yet.\n\nKeyed by each block's own starting position (sp, see Schema.be_key) exactly like InvertedFileObjectStore – same reasoning: no prefix/counter needed, and iteration already comes back in ascending order.\n\nShares its underlying RocksDB.DB connection, same as EngineStore; no separate close of its own to call.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.StagedTextStore"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Persistence.WeightedDocVec","page":"Engine API","text":"See DocVec.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.WeightedDocVec"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence._encode_docvec-Tuple{SimilaritySearch.Special.Sparse.SparseVecView{Vector{Int32}, Vector{UInt32}}}","page":"Engine API","text":"_encode_docvec(v) -> Vector{UInt8}\n_decode_docvec(bytes) -> DocVec\n\nA document's term vector as (n::Int32, nnz::Int32, nzind::Vector{Int32}, nzval::Vector{UInt32}), written by hand rather than through a generic serializer.\n\nThis is the high-volume, hot path of the whole store – one value per document, read once per scored candidate – which is exactly the case where a generic object-graph format is not worth its overhead (the same reasoning that keeps the vocabulary in a hand-written format rather than JLD2).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence._encode_docvec"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence._optional_string-Tuple{Any}","page":"Engine API","text":"read_dump_records(filepath::String) -> Vector{DumpRecord}\n\nReads back an Avro file written by write_dump_records, reconstructing plain DumpRecords (rather than handing back Avro.jl's own row-view type) so callers don't need to depend on Avro.jl's internals beyond this module.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence._optional_string"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.append_objects!-Tuple{SimilaritySearchEngine.Persistence.InvertedFileObjectStore, Integer, Vector}","page":"Engine API","text":"append_objects!(store::InvertedFileObjectStore, sp::Integer, objects::Vector)\n\nSaves objects (the raw indexed objects for one push_item!/append_items! report over range sp:ep – see IndexEngine.invertedfile_objects) under sp as key (see Schema.be_key) – never rewriting any earlier block. See load_object_blocks to read the whole sequence back in order.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.append_objects!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.append_staged_texts!-Tuple{SimilaritySearchEngine.Persistence.StagedTextStore, Integer, Vector{String}}","page":"Engine API","text":"append_staged_texts!(store::StagedTextStore, sp::Integer, texts::Vector{String})\n\nSaves texts (one append_items! batch's worth of freshly staged raw text) under sp as key (see Schema.be_key) – never rewriting any earlier block. See load_staged_text_blocks to read the whole sequence back in order.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.append_staged_texts!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.dense_vectors_path-Tuple{String}","page":"Engine API","text":"dense_vectors_path(dir::String) -> String\n\nThe on-disk path of a project's dense-vector file (see open_dense_vectors).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.dense_vectors_path"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.docvec_nnz-Tuple{AbstractDict}","page":"Engine API","text":"docvec_nnz(v) -> Int\n\nHow many terms a stored document vector holds, whichever of the three shapes it is – what the weighted backend keeps in its sizes array, recomputed at assembly instead of stored twice.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.docvec_nnz"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.has_field-Tuple{SimilaritySearchEngine.Persistence.EngineStore, Symbol}","page":"Engine API","text":"has_field(store::EngineStore, field::Symbol) -> Bool\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.has_field"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.has_inverted_index-Tuple{SimilaritySearchEngine.Persistence.InvertedIndexStore}","page":"Engine API","text":"has_inverted_index(store::InvertedIndexStore) -> Bool\n\nWhether anything has ever been written to this store – the question open_project asks to tell a project whose index is persisted from one written before that existed (which has to be migrated, once) or one that is simply empty.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.has_inverted_index"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.invalidate_postings!-Tuple{SimilaritySearchEngine.Persistence.LazyPostings, Any}","page":"Engine API","text":"invalidate_postings!(a::LazyPostings, tokens)\n\nDrops the cached lists of tokens, which a just-written block has lengthened on disk.\n\nDropping rather than patching: a token whose list just grew is not necessarily one anybody queries, and the next read pays a single point lookup either way.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.invalidate_postings!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_dense_vector_blocks-Tuple{Any}","page":"Engine API","text":"load_dense_vector_blocks(dense_db) -> Vector\n\nReads back every vector in dense_db (as returned by open_dense_vectors, or nothing) as a single (sp=1, ep=n, vectors) block – the same shape IndexEngine.build_searchgraph already expects from a vector_blocks sequence (see Persistence's older RocksDB-block scheme this replaces), except there's exactly one block here: an mmap'd file has no batch boundaries of its own worth preserving, unlike the old per-append-call RocksDB blocks. Returns an empty Vector (no blocks at all) for dense_db === nothing (nothing ever appended).\n\nCopies every vector out of the memory-mapped file (copy(dense_db[i]), not the raw view getindex normally returns) since these vectors are handed to a fresh, independent, RAM- backed VectorDatabase that must outlive this read and never alias the mapped memory.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_dense_vector_blocks"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_field-Tuple{SimilaritySearchEngine.Persistence.EngineStore, Symbol, Any}","page":"Engine API","text":"load_field(store::EngineStore, field::Symbol, default)\n\nReads back the value last saved under field's key, or default if it was never saved.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_field"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_invfile_docvecs-Tuple{SimilaritySearchEngine.Persistence.InvertedIndexStore}","page":"Engine API","text":"load_invfile_docvecs(store::InvertedIndexStore) -> Vector{DocVec}\n\nEvery persisted document vector, in document-id order.\n\nKept resident, unlike the posting lists, and that asymmetry is the measured heart of this design rather than an accident. TextSearch.jl scores a candidate by reading its term vector (bm25score(..., idx.db[docID]) in onmatch!), and the merge makes every document holding any query token a candidate – so a query containing a frequent token turns a lazy db into one point read per document in the collection. Measured on 265k Gutenberg paragraphs (2026-09-11): 66.8 ms per query with the vectors resident, 1771.9 ms with them read on demand, for the same query and the same index. The posting lists, read once per query term, cost 1.8% (see LazyPostings).\n\nLoading them back costs 0.76s for that corpus, against 20.4s to recompute the index from the raw objects.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_invfile_docvecs"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_neighbors-Tuple{SimilaritySearchEngine.Persistence.AdjacencyStore, Integer}","page":"Engine API","text":"load_neighbors(store::AdjacencyStore, object_id::Integer) -> Vector{UInt32}\n\nReads back object_id's direct neighbor list, or an empty vector if it was never saved.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_neighbors"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_object_blocks-Tuple{SimilaritySearchEngine.Persistence.InvertedFileObjectStore}","page":"Engine API","text":"load_object_blocks(store::InvertedFileObjectStore) -> Vector{<:Vector}\n\nEvery block append_objects! has written, in the order they were appended – which is ascending sp, decoded from each key rather than taken from the order the iteration happens to produce.\n\nThe two coincide today, because Schema.be_key is big-endian and RocksDB compares keys bytewise (see its docstring). Sorting anyway is three lines and removes the dependency: a block order that silently depended on the key encoding would corrupt a project's document numbering, not fail it, and would do so only past the first 256 blocks – exactly the kind of bug a small test corpus never reaches.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_object_blocks"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.load_staged_text_blocks-Tuple{SimilaritySearchEngine.Persistence.StagedTextStore}","page":"Engine API","text":"load_staged_text_blocks(store::StagedTextStore) -> Vector{Vector{String}}\n\nEvery block append_staged_texts! has written, in the order they were appended – a plain iteration over store.dict already comes back in ascending sp order (see Schema.be_key's docstring), so this needs no separate counter key or re-sorting. A caller wants vcat(load_staged_text_blocks(store)...) for the flat, restore-ready Vector{String} IndexEngine.restore_engine expects as state.staged.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.load_staged_text_blocks"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_adjacency_store-Tuple{RocksDB.DB}","page":"Engine API","text":"open_adjacency_store(db::RocksDB.DB) -> AdjacencyStore\n\nWraps db's ADJACENCY_CF column family (which must already be open on db – see Project.open_project's extra_cf_names) as an AdjacencyStore.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_adjacency_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_dense_vectors-Tuple{String}","page":"Engine API","text":"open_dense_vectors(dir::String; read_only::Bool) -> Union{SimilaritySearch.MMapMatrixDatabase, Nothing}\n\nReopens this project's dense-vector file if it exists, or nothing if it doesn't – which is the normal state for a project that has never had a dense item appended yet (the file is created lazily, on the very first append, since its dimension isn't known before then; see embedded.jl's _searchgraph_on_change), not an error condition.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_dense_vectors"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_engine_store-Tuple{RocksDB.DB}","page":"Engine API","text":"open_engine_store(db::RocksDB.DB) -> EngineStore\n\nWraps db's ENGINE_CF column family (which must already be open on db – see Project.open_project's extra_cf_names) as an EngineStore.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_engine_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_inverted_index_store-Tuple{RocksDB.DB}","page":"Engine API","text":"open_inverted_index_store(db::RocksDB.DB) -> InvertedIndexStore\n\nWraps db's INVFILE_POSTINGS_CF and INVFILE_DOCVECS_CF column families (both of which must already be open on db) as a InvertedIndexStore.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_inverted_index_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_invertedfile_object_store-Tuple{RocksDB.DB}","page":"Engine API","text":"open_invertedfile_object_store(db::RocksDB.DB) -> InvertedFileObjectStore\n\nWraps db's INVFILE_DB_CF column family (which must already be open on db – see Project.open_project's extra_cf_names) as an InvertedFileObjectStore.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_invertedfile_object_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.open_staged_text_store-Tuple{RocksDB.DB}","page":"Engine API","text":"open_staged_text_store(db::RocksDB.DB) -> StagedTextStore\n\nWraps db's STAGED_TEXT_CF column family (which must already be open on db – see Project.open_project's extra_cf_names) as a StagedTextStore.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.open_staged_text_store"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.save_field!-Tuple{SimilaritySearchEngine.Persistence.EngineStore, Symbol, Any}","page":"Engine API","text":"save_field!(store::EngineStore, field::Symbol, value)\n\nWrites just value under field's key, leaving every other field's key untouched.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.save_field!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.save_fields!-Tuple{SimilaritySearchEngine.Persistence.EngineStore, NamedTuple}","page":"Engine API","text":"save_fields!(store::EngineStore, fields::NamedTuple)\n\nWrites every field => value pair in fields – used for a full initial save (e.g. right after create_engine) or a one-off multi-field transition (e.g. a text engine's first training batch setting voc/model/index together), as opposed to save_field!'s single-key writes for routine mutations.\n\nWrites one key at a time via save_field! rather than RocksDB.batch – that Tier-3 helper's RocksDBBatchProxy currently writes every key to the \"default\" column family regardless of which column family the wrapped RocksDBDict is scoped to (verified directly against a throwaway db: RocksDB.batch(d) do b; b[\"x\"] = 1; end on a dict scoped to a non-\"default\" column family lands nothing there), which would silently misplace every field this function saves. Not atomic across fields as a result; each field in this NamedTuple is small and this is only ever called for rare, one-shot transitions, so a partial write on a crash mid-loop is an acceptable risk here.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.save_fields!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.save_neighbors!-Tuple{SimilaritySearchEngine.Persistence.AdjacencyStore, Integer, Vector{UInt32}}","page":"Engine API","text":"save_neighbors!(store::AdjacencyStore, object_id::Integer, neighbors::Vector{UInt32})\n\nWrites object_id's direct neighbor list, leaving every other object's entry untouched.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.save_neighbors!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.trim_postings_cache!-Tuple{SimilaritySearchEngine.Persistence.LazyPostings}","page":"Engine API","text":"trim_postings_cache!(a::LazyPostings) -> Int\n\nEvicts the least frequently used cached lists down to a.baselists, but only if there are more than a.maxlists of them; returns how many it dropped.\n\nCall it after a query (or a batch of them) has fully resolved – never from inside one. See LazyPostings.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.trim_postings_cache!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.write_dump_records-Tuple{String, Vector{SimilaritySearchEngine.Persistence.DumpRecord}}","page":"Engine API","text":"write_dump_records(filepath::String, records::Vector{DumpRecord}) -> String\n\nWrites records as an Avro object container file. Errors if records is empty – Avro.writetable has no schema to infer from a table with no rows, so an empty project's dump must be handled by the caller before reaching here (see execute_dump).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.write_dump_records"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Persistence.write_invfile_block!-Tuple{SimilaritySearchEngine.Persistence.InvertedIndexStore, Integer, Any, Dict{UInt32, Vector{UInt32}}}","page":"Engine API","text":"write_invfile_block!(store::InvertedIndexStore, base::Integer, docvecs, postings::Dict{UInt32,Vector{UInt32}})\n\nPersists one freshly indexed block: the docvecs of documents base+1 … base+length(docvecs), and the new document ids each token in postings gained (already offset to global ids and ascending).\n\nThe posting half is a read-modify-write per touched token – get the token's current list, append, put – which is what makes an incremental index! possible at all: a block adds documents to the lists of the tokens it happens to contain, and nothing else in the index changes. The whole block goes in one WriteBatch, so a crash mid-block leaves the store at the previous block's boundary rather than half-updated.\n\nAppending is correct only because document ids grow monotonically: every id in this block is larger than every id already in any list, so appending preserves the ascending order the merge in TextSearch.jl's search relies on.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.write_invfile_block!"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.ADJACENCY_CF","page":"Engine API","text":"ADJACENCY_CF\n\nName of the RocksDB column family a SearchGraph's per-object direct-neighbor lists are stored in (see AdjacencyStore) – separate from ENGINE_CF so this (potentially one-entry-per-indexed-object) data doesn't share a keyspace with the handful of other engine fields (deleted_ids, minrecall, ...). Pass this in Project.open_project's extra_cf_names alongside ENGINE_CF, for the same reason (every column family that exists on disk must be listed on every open, not just the first).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.ADJACENCY_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.DENSE_VECTORS_FILENAME","page":"Engine API","text":"DENSE_VECTORS_FILENAME\n\nFilename (relative to a project's directory, alongside its RocksDB files) of the SimilaritySearch.MMapMatrixDatabase file backing a DenseEngine{GraphBackend}'s dense vectors.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.DENSE_VECTORS_FILENAME"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.ENGINE_CF","page":"Engine API","text":"ENGINE_CF\n\nName of the RocksDB column family an EngineStore is backed by. Pass this in Project.open_project's extra_cf_names so the column family is listed on every open, not just the first (see that keyword's docstring).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.ENGINE_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.INVFILE_DB_CF","page":"Engine API","text":"INVFILE_DB_CF\n\nName of the RocksDB column family an InvertedFileObjectStore is backed by. Pass this in Project.open_project's extra_cf_names alongside ENGINE_CF/ ADJACENCY_CF, for the same reason (every column family that exists on disk must be listed on every open, not just the first).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.INVFILE_DB_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.INVFILE_DOCVECS_CF","page":"Engine API","text":"invfilepostings's companion, keyed by document id. See [`INVFILEPOSTINGS_CF`](@ref).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.INVFILE_DOCVECS_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.INVFILE_POSTINGS_CF","page":"Engine API","text":"INVFILE_POSTINGS_CF\nINVFILE_DOCVECS_CF\n\nNames of the two RocksDB column families a InvertedIndexStore is backed by. Pass them in Project.open_project's extra_cf_names alongside the other stores' own, for the same reason (every column family that exists on disk must be listed on every open, not just the first).\n\nThey hold the index, not the objects that were indexed: INVFILE_POSTINGS_CF maps a token id to the ascending ids of the documents carrying it, and INVFILE_DOCVECS_CF maps a document id to the vector it was indexed as – term frequencies under BM25, model weights under the weighted backend, each value tagged with which (see _encode_docvec). InvertedFileObjectStore, which a BM25 project used to write instead, stores the raw bags-of-words and leaves the whole index to be recomputed on every open – 20.4s for 265k paragraphs of Project Gutenberg, measured 2026-09-11, growing linearly. Reading these two back costs 1.1s for the same corpus, and the posting half of it need not be read at all (see LazyPostings).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.INVFILE_POSTINGS_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Persistence.STAGED_TEXT_CF","page":"Engine API","text":"STAGED_TEXT_CF\n\nName of the RocksDB column family a StagedTextStore is backed by. Pass this in Project.open_project's extra_cf_names alongside ENGINE_CF/ADJACENCY_CF/ INVFILE_DB_CF, for the same reason (every column family that exists on disk must be listed on every open, not just the first).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Persistence.STAGED_TEXT_CF"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Project.ProjectManager","page":"Engine API","text":"ProjectManager\n\nManages a project backed by RocksDB.\n\nFields\n\ndataset::String: Unique identifier of the dataset stored in this project.\ndb::RocksDB.DB: The main RocksDB database handle.\ncf_records::RocksDB.ColumnFamily: Column family for the fixed Schema.MetadataRecord half of each item (see put_metadata!).\ncf_meta::RocksDB.ColumnFamily: Column family for the free-form meta half – kept apart from cf_records so a caller that only wants meta (or only the record) never pays to read/decode the other.\ncf_op_log::RocksDB.ColumnFamily: Column family for operation logs.\ncf_idx_keywords::RocksDB.ColumnFamily: Secondary index from a keyword value to the _ids of records carrying it (see put_metadata!).\ncf_idx_refs::RocksDB.ColumnFamily: Secondary index from a referenced doc_id to the _ids of records whose refs list it.\n\nThere is no per-project typed-field schema carried here anymore – every project has exactly these column families, always; only keywords/refs are indexed, and that's fixed, not configurable per project.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.ProjectManager"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project._id_key-Tuple{Int32}","page":"Engine API","text":"_id_key(id::Int32)\n\nThe 4 bytes an _id is stored and compared as. Every integer key in this package is big-endian, for the one reason Schema.be_key documents: RocksDB sorts keys bytewise, so this is what makes a range of ids iterate in numeric order. These keys used to be native little-endian, which only ever ordered correctly below 256.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project._id_key"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.backfill_docid_index!-Tuple{SimilaritySearchEngine.Project.ProjectManager}","page":"Engine API","text":"backfill_docid_index!(manager::ProjectManager) -> Int\n\nPopulates DOCID_CF from cf_records for a project written before that index existed, and returns how many entries it wrote (0 when there was nothing to do).\n\nRuns at most once per project: the marker key (DOCID_INDEX_MARKER) is written at the end, and its presence is what later opens check – not whether the column family holds anything, which would rescan forever a project whose records all carry an empty doc_id. The scan itself costs exactly one pass over cf_records, which is what a single doc_id lookup used to cost before this index existed (see find_by_doc_id), so the one-off migration is cheaper than the first lookup it replaces.\n\nNever called on a read_only handle: it writes, and a read-only handle cannot. Such a handle simply has no index to consult until a writer opens the project once.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.backfill_docid_index!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.close_project-Tuple{SimilaritySearchEngine.Project.ProjectManager}","page":"Engine API","text":"close_project(manager::ProjectManager)\n\nCloses the project and its underlying RocksDB connection.\n\nArguments\n\nmanager::ProjectManager: The project manager to close.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.close_project"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.compact_all!-Tuple{SimilaritySearchEngine.Project.ProjectManager}","page":"Engine API","text":"compact_all!(manager::ProjectManager)\n\nForces a full manual compaction of every column family in the project.\n\nEvery one of them, not just the default: RocksDB.compact!(db) without a cf compacts the default column family alone, and a project keeps its records, metadata, secondary indexes and the engine's own state in separate ones.\n\nThis is what turns a bulk write session's leftovers into flat SST files. Without it the next open pays for replaying the write-ahead log, which is not a small effect: measured 2026-09-10 on a 50k-vector dense project, reopening took 2.97s before compacting and 0.28s after, for a compaction that itself cost 0.03s.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.compact_all!"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.docid_key-Tuple{AbstractString, Int32}","page":"Engine API","text":"docid_key(doc_id::AbstractString, id::Int32) -> Vector{UInt8}\n\nThe DOCID_CF key for (doc_id, _id): doc_id's bytes, then _id_key's\n\nComposite rather than plain doc_id because nothing in this package makes doc_id\n\nunique – two items may legitimately carry the same one – and a plain key would silently keep only the last of them.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.docid_key"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.find_all_by_doc_id-Tuple{SimilaritySearchEngine.Project.ProjectManager, AbstractString}","page":"Engine API","text":"find_all_by_doc_id(manager::ProjectManager, doc_id::AbstractString) -> Vector{MetadataRecord}\n\nEvery record carrying exactly this doc_id, in ascending _id order.\n\nA vector rather than a single record because nothing in this package makes doc_id unique: it is a caller-chosen external id, and appending two items that share one is allowed. The old doc_id path returned whichever of them a full scan happened to reach first, which is a coin flip dressed as an answer.\n\nCost is the number of records it returns, not the number the project holds – the point of DOCID_CF. It seeks to doc_id's bytes and walks while they still prefix the key, keeping only keys of exactly length(doc_id) + 4 bytes: the longer ones belong to a different, longer doc_id that happens to start the same way (\"doc_2\" walking over \"doc_20\"'s entries), and they are skipped rather than decoded. A doc_id that prefixes many others therefore costs a short walk over their keys – never a record read, never a scan of the collection.\n\nFalls back to find_by_doc_id's full scan when the index has not been built yet, which happens only on a read_only handle opened against a project written before this index existed: backfill_docid_index! fills it in on the first writable open.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.find_all_by_doc_id"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.find_by_doc_id-Tuple{SimilaritySearchEngine.Project.ProjectManager, String}","page":"Engine API","text":"find_by_doc_id(manager::ProjectManager, doc_id::String) -> Union{MetadataRecord, Nothing}\n\nLinear scan over cf_records looking for a record whose doc_id matches. There is no secondary index from doc_id to _id in this pass, so this is the fallback path for fetch requests that use the caller-supplied external id rather than the internal _id – acceptable for the project sizes this prototype targets, not meant as a hot-path lookup. Only decodes the (small, fixed-shape) record for each row – never touches cf_meta – so this is cheaper than the old extra-scanning version, but still O(n).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.find_by_doc_id"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.generate_id-Tuple{}","page":"Engine API","text":"generate_id() -> String\n\nGenerates a collision-safe 64-bit identifier (16 hex chars).\n\nReturns\n\nString: A randomly generated hex string.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.generate_id"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.get_meta-Tuple{SimilaritySearchEngine.Project.ProjectManager, Integer}","page":"Engine API","text":"get_meta(manager::ProjectManager, id::Integer; lazy::Bool=true) -> Any\n\nRetrieves and decodes the meta half for id via Schema.decode_meta (see its docstring for the lazy tradeoff). nothing if id has no stored meta at all (either it never had one, or id doesn't exist).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.get_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.get_metadata-Tuple{SimilaritySearchEngine.Project.ProjectManager, Integer}","page":"Engine API","text":"get_metadata(manager::ProjectManager, id::Integer) -> Union{MetadataRecord, Nothing}\n\nRetrieves a Schema.MetadataRecord from the project by its internal _id. Does not touch cf_meta – use get_meta/get_raw_meta for that half.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.get_metadata"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.get_raw_meta-Tuple{SimilaritySearchEngine.Project.ProjectManager, Integer}","page":"Engine API","text":"get_raw_meta(manager::ProjectManager, id::Integer) -> Union{String, Nothing}\n\nThe stored meta bytes for id, completely unparsed (see Schema.raw_meta) – for a caller that only forwards meta unmodified (e.g. straight into an HTTP response).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.get_raw_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.open_project-Tuple{String, String}","page":"Engine API","text":"open_project(path::String, dataset::String; read_only::Bool=false, extra_cf_names::Vector{String}=String[]) -> ProjectManager\n\nOpens or creates a RocksDB-backed project at the given path.\n\nArguments\n\npath::String: The directory path where the project will be stored.\ndataset::String: The unique identifier of the dataset held by this project.\n\nKeyword Arguments\n\nread_only::Bool: Opens without acquiring RocksDB's exclusive per-process write lock (RocksDB.jl's opendb(...; read_only=true)) – lets a read-only caller (e.g. the CLI describe command) inspect a project a similarity-search-serve process still has open for writing, instead of failing with a lock-contention error. Never pass this when the caller intends to write (e.g. rebuild): a read-only handle can't put!/delete!.\nextra_cf_names::Vector{String}: additional column family names to open alongside this module's own (\"records\"/\"meta\"/\"op_log\"/\"idx_keywords\"/\"idx_refs\"), for a caller (e.g. IndexEngine's persistence layer) that wants to share this same RocksDB connection/directory for its own column family rather than opening a second DB. Project doesn't interpret these names at all – it just makes sure RocksDB knows about them, since every column family that exists on disk must be listed at opendb time on every open, not only the first (see RocksDB.create_column_family's docstring): a caller that creates one ad hoc after the fact and never adds it here would find the next open_project call failing.\n\nReturns\n\nProjectManager: The initialized project manager object.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.open_project"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Project.put_metadata!-Tuple{SimilaritySearchEngine.Project.ProjectManager, MetadataRecord, Any}","page":"Engine API","text":"put_metadata!(manager::ProjectManager, record::MetadataRecord, meta)\n\nInserts or updates record (and its accompanying free-form meta, if any – see Schema.metadata_record) in the project, keyed by record._id in both cf_records and cf_meta. Also (re)writes record's keywords/refs entries into their respective secondary index column families – this is not incremental: any previous index entries for this _id are left in place (an update that removes a keyword/ref leaks a stale index row pointing at an _id whose current record no longer carries it) – acceptable for the append-mostly, rarely-updated workloads this targets, not a general upsert.\n\nArguments\n\nmanager::ProjectManager: The target project manager.\nrecord::MetadataRecord: The fixed-shape record to insert or update.\nmeta: The free-form data to store alongside it (a Dict, Vector, nothing, or anything else Schema.encode_meta accepts) – omitted entirely from cf_meta when nothing (not written as a JSON null).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.put_metadata!"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Project.DOCID_CF","page":"Engine API","text":"DOCID_CF\n\nColumn family holding the secondary index from doc_id to _id. Its keys are doc_id's bytes followed by the 4 bytes of _id_key, the same composite shape cf_idx_keywords/cf_idx_refs already use, so a seek to a doc_id lands on the run of entries that carry it – see find_all_by_doc_id. Composite because doc_id is not unique: a plain doc_id key would keep only the last item appended under it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.DOCID_CF"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Project.DOCID_INDEX_MARKER","page":"Engine API","text":"DOCID_INDEX_MARKER\n\nSentinel key written into DOCID_CF once its backfill has run, so a project whose doc_ids are all empty is not rescanned on every open. It starts with a NUL byte, which no doc_id produced by a caller realistically does, and find_all_by_doc_id ignores it anyway (its key is not length(doc_id) + 4 bytes long).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Project.DOCID_INDEX_MARKER"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.AbstractItem","page":"Engine API","text":"AbstractItem\n\nSomething to append to a project: DenseItem (a dense vector), SparseItem (a sparse one) or TextItem (a string), each carrying its own external id, tags, references and free-form metadata.\n\nThis package takes typed values in and hands typed values back. It does not accept a JSON-shaped Dict as an item and pick it apart, which is what it used to do – the caller splits its own data, because the caller is the one who knows what its fields mean. The single exception is meta, which is free-form by definition and stays a Dict{String,Any}. On the way out the same rule holds: StoredItem, not a merged dictionary. The one raw-JSON path that remains is raw_meta, reserved for an HTTP layer forwarding stored bytes it never inspects.\n\nWhich concrete type an item is is the check. A project indexing dense vectors takes DenseItems, one indexing sparse vectors takes SparseItems and one indexing text takes TextItems; handing over the wrong one raises, where the dictionary form silently skipped any item whose expected key was missing and returned a count that quietly disagreed with what the caller passed.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.AbstractItem"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.DenseItem","page":"Engine API","text":"DenseItem(vector; doc_id=nothing, keywords=String[], refs=String[], meta=Dict{String,Any}())\n\nOne dense vector to index, with its metadata.\n\nvector is converted to Vector{Float32} on construction – the element type the engine indexes in – so a caller holding Float64s or a SubArray does not have to convert first, and the conversion happens once, here, rather than per insertion.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.DenseItem"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.MetaField","page":"Engine API","text":"MetaField(name, type)\n\nOne declared field of a project's meta: the key it occupies, and which of META_FIELD_TYPES its value must have.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.MetaField"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.MetaSchema","page":"Engine API","text":"MetaSchema(fields; version=1)\n\nWhat a project declares about its meta: a list of MetaFields, and a version that changes when that list changes.\n\nThe declaration is additive. A field that is not declared is stored as it arrives, which is what every project did before any of this existed; declaring a field only says what its value must be when it is present. A record is not required to carry every declared field, and a project whose schema is declared after it already holds records does not have those records checked – the declaration governs what is written from then on.\n\nWhat declaring buys is that the value is the type it claims: an :int64 field cannot hold \"2024\" for some records and 2024 for others, so a filter over it compares numbers rather than whatever JSON happened to store. The values still live in the meta column family as JSON; nothing is stored in the record itself, whose shape does not vary by project (see MetadataRecord).\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.MetaSchema"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.MetadataRecord","page":"Engine API","text":"MetadataRecord\n\nFixed-shape record every indexed object carries, independent of whatever meta a caller attaches to it (see metadata_record/Project's own dedicated meta column family) – there is no per-project typed-field schema to declare or validate against anymore: every record has exactly these fields, always. All-concrete field types, so Vector{MetadataRecord} writes/reads directly through Avro.writetable/ readtable (confirmed empirically – unlike a meta/Dict{String,Any} field, which Avro.jl schema inference rejects outright since Any isn't a concrete value type; that's why meta is a separate JSON blob, never a field on this struct).\n\nFields\n\n_id::Int32: internal sequential id, 1..n – the same key the engine's own raw vector/text column family is indexed by (see Persistence), so a record and its vector/text share one id space, never duplicated across the two. Leading underscore flags it as the internal handle (vs. doc_id below), but it's kept as a real, always-present field – callers need it back to line up with result matrices/ids from search, not just to recover data by key.\nschema_version::Int: version tag for whatever shape the accompanying meta happens to follow – carried here purely for the caller's own interpretation of meta, not validated or enforced by this module.\ndoc_id::Union{String,Nothing}: the caller-supplied external id, if the appended item had one (its raw \"doc_id\" key) – nothing if it didn't.\nkeywords::Vector{String}: free tags, indexed for lookup (see Project).\nrefs::Vector{String}: doc_ids of related records – not validated against anything at write time (a ref may point at a doc_id that doesn't exist yet, or ever, e.g. a forward reference inserted before its target), so this is a plain unchecked list, not a foreign key.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.MetadataRecord"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.SparseItem","page":"Engine API","text":"SparseItem(vector; doc_id=nothing, keywords=String[], refs=String[], meta=Dict{String,Any}())\n\nOne sparse vector to index, with its metadata.\n\nA SparseVector{Float32,Int32}, and only that: the element and index types an inverted file indexes in, converted once here rather than per insertion. A bag of counts is not accepted in its place, deliberately – a Dict{UInt32,Int32} and a sparse vector mean different things to a scorer (presence-and-count against weight), and taking both would make the item's type stop answering which one this is.\n\nThe vector's length is its dimension, and every item in a project must agree with the dimension that project was created with: an inverted file is a fixed array of posting lists, so the dimension is structural rather than descriptive.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.SparseItem"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.StoredItem","page":"Engine API","text":"StoredItem\n\nOne indexed item read back out: its MetadataRecord fields, its free-form meta, and its payload – the very text, dense vector or sparse vector that was indexed.\n\npayload is what a search result needs and what the dictionary-returning fetch_items could not give: text was a reserved key, stripped before anything reached the metadata store, so a hit came back with everything about the paragraph and not the paragraph. It is nothing only when the payload cannot be read back – a DenseEngine{<:ExactBackend} whose id is out of range, or an engine kind with no per-item storage to consult.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.StoredItem"},{"category":"type","location":"engine.html#SimilaritySearchEngine.Schema.TextItem","page":"Engine API","text":"TextItem(text; doc_id=nothing, keywords=String[], refs=String[], meta=Dict{String,Any}())\n\nOne text document to index, with its metadata. In a paragraph-level project this is one paragraph, not one book: the item is whatever unit a search should hand back.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.TextItem"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.be_key-Tuple{Integer}","page":"Engine API","text":"be_key(id::Integer) -> Vector{UInt8}\ndecode_be_key(key) -> UInt32\n\nAn integer id as a RocksDB key, big-endian, and back.\n\nOne rule for every integer key this package writes, in any column family: big-endian, four bytes. Not because of the machine – every platform this runs on is little-endian, and hton is a byte swap that costs nothing next to the point read that follows it (measured: 0.41s against 0.54s per 10 million keys, both dominated by allocating the 4-byte vector) – but because RocksDB compares keys bytewise. Native little-endian bytes sort as [65536, 256, 1, 257, 2, 1000, 255, 65535]; big-endian ones sort numerically.\n\nThe package no longer depends on that ordering to be correct – the loaders that read a whole column family back sort by the id they decode from each key (Persistence.load_invfile_docvecs, Persistence.load_object_blocks) – but iterating a range of ids in order is a property worth keeping, and having one rule means nobody has to ask which encoding a given column family uses.\n\ncollect materializes a concrete Vector{UInt8} rather than a lazy reinterpret view over a temporary array: WriteBatch defers the actual write until write!, and a lazy view is not reliably kept alive across that gap – confirmed with a 200-key round-trip repro where a handful of keys came back missing from get on every run.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.be_key"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.coerce_meta-Tuple{MetaSchema, Any}","page":"Engine API","text":"coerce_meta(schema::MetaSchema, meta) -> meta\n\nmeta with every declared field checked, and converted to the declared type where the conversion preserves the value. Returns meta itself when the schema declares nothing, so a project without a schema pays nothing.\n\nAccepted, per declared type:\n\ndeclared accepted stored as\n:string a string the string\n:int64 an integer, or a float with no fractional part Int64\n:float64 any real number Float64\n:bool true or false Bool\n:timestamp a DateTime, or a string it parses from the ISO 8601 string\n\nA declared field that is absent, or nothing, is left absent: a record need not carry every declared field. Anything else raises PayloadMismatch, which is an InvalidRequest, so the frontier reporting it answers 400 rather than storing a value of the wrong type.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.coerce_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.declared_type-Tuple{MetaSchema, AbstractString}","page":"Engine API","text":"declared_type(schema, name) -> Union{Nothing, Symbol}\n\nThe declared type of the field name, or nothing when the project did not declare it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.declared_type"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.decode_be_key-Tuple{AbstractVector{UInt8}}","page":"Engine API","text":"See be_key.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.decode_be_key"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.decode_meta-Tuple{Vector{UInt8}}","page":"Engine API","text":"decode_meta(bytes::Union{Vector{UInt8},Nothing}; lazy::Bool) -> Any\n\nInverse of encode_meta. nothing/empty input decodes to nothing.\n\nWith lazy=true (the default), returns JSON3's own lazy Object/Array view straight over bytes – cheap to produce (confirmed empirically: allocates roughly as much as a full Dict parse just to index top-level keys, but nothing nested is materialized until actually accessed) and useful for a caller that only reads a field or two out of meta. This is not a free round-trip on write, though: writing a parsed JSON3.Object back out with JSON3.write still walks and reserializes every value – empirically no cheaper than writing a plain Dict (slightly more allocation, in fact). A caller that only needs to pass meta through unmodified (e.g. straight into an HTTP response body) should skip decoding entirely and reuse the raw bytes from storage directly – that's the only actually-zero-cost path.\n\nPass lazy=false for a fully materialized Dict{String,Any}/Vector{Any} instead, when the caller needs to mutate or merge meta rather than just read from it.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.decode_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.encode_meta-Tuple{Any}","page":"Engine API","text":"encode_meta(meta) -> String\n\nJSON3-encodes meta (a Dict, a Vector, or any other JSON3-writable value), for storage in Project's dedicated meta column family. Returns the String JSON3.write itself produces rather than converting it to a Vector{UInt8} – that conversion isn't a free reinterpretation, it's a real copy (confirmed empirically: pointer differs before/after, with allocation to match), so it'd just be paying twice to get back to bytes JSON3.write already builds internally before wrapping them as a String. No need to pay it at all here: RocksDB.jl's own put! already accepts a plain String and converts it to bytes via codeunits (zero-copy) right before handing it to the C API.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.encode_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.get_field-Tuple{MetadataRecord, Any, String}","page":"Engine API","text":"get_field(record::MetadataRecord, meta, name::String) -> Any\n\nRetrieves a field by name either from record's fixed fields (_id, doc_id, keywords, refs, schema_version) or from free-form meta dictionary / object.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.get_field"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.matches_filter-Tuple{MetadataRecord, Any, AbstractDict, Any}","page":"Engine API","text":"matches_filter(record::MetadataRecord, meta, filter::AbstractDict, schema) -> Bool\n\nPost-filter predicate for /search-family endpoints (PLAN.md §5.4). filter maps field names to either a bare value (equality) or a spec dict supporting gte/lte/gt/lt (range), in/nin (set membership), and eq/neq. A field missing from the record/meta fails the filter.\n\nschema is the project's MetaSchema, which is what makes a comparison happen in the declared type rather than in whatever JSON stored: with a field declared :timestamp, both the stored value and the operand of the filter are read as instants, so two spellings of one instant compare equal. It is a required argument, not an option with a default: a project always has a schema, empty when it declares nothing, and nothing is accepted as that empty declaration. An undeclared field compares exactly as it did before.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.matches_filter"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.metadata_record-Tuple{AbstractItem, Int32, Int64}","page":"Engine API","text":"metadata_record(item::AbstractItem, _id::Int32, schema_version::Int) -> MetadataRecord\n\nitem's fixed-shape half, stamped with the id the project assigned it.\n\nThe payload is deliberately not in here and not duplicated anywhere near it: a vector or a text already lives in the engine's own dedicated, 1..n-indexed storage (see Persistence), and this record shares that id space with it.\n\nThis replaces the split_item(_id, version, ::AbstractDict) that used to pull doc_id, keywords and ref out of a raw dictionary by key and sweep the rest into meta. There is nothing left to split: an AbstractItem arrives already separated, by a caller who knows which of its fields are tags and which are payload. What was a parsing step with a reserved-key list is now a projection.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.metadata_record"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.payload-Tuple{DenseItem}","page":"Engine API","text":"payload(item::AbstractItem)\n\nThe part of item that gets indexed – the vector of a DenseItem, the text of a TextItem – as opposed to the metadata that travels beside it.\n\nExists so code that treats both kinds uniformly (staging, persistence) does not branch on the concrete type just to reach the one field whose name differs.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.payload"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.raw_meta-Tuple{Vector{UInt8}}","page":"Engine API","text":"raw_meta(bytes::Union{Vector{UInt8},Nothing}) -> Union{String,Nothing}\n\nThe actually-zero-cost path for a caller that only forwards meta unmodified (a fetch/HTTP handler splicing it into a hand-built response, or writing it straight out as a response body) and never inspects its fields – unlike either decode_meta mode, this never touches JSON3 at all: no lazy Object, no Dict, nothing parsed or walked, just the stored bytes handed back as a String.\n\nString(::Vector{UInt8}) takes ownership of bytes and empties it as a side effect (a real move, not a copy – that's the whole point here), so pass a bytes this call is free to consume; if the caller still needs bytes afterwards (e.g. to also call decode_meta on the same underlying data), copy it first.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.raw_meta"},{"category":"method","location":"engine.html#SimilaritySearchEngine.Schema.typed_value-Tuple{Any, AbstractString, Any}","page":"Engine API","text":"typed_value(schema, name, value) -> Any\n\nvalue read as the type name was declared with, for comparing it. A :timestamp becomes a DateTime, so two instants are compared as instants and not as the strings that store them; every other declared type is already stored as itself. An undeclared field is returned unchanged, which is how a project without a schema compares.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.typed_value"},{"category":"constant","location":"engine.html#SimilaritySearchEngine.Schema.META_FIELD_TYPES","page":"Engine API","text":"META_FIELD_TYPES\n\nThe types a field of meta can be declared as. :timestamp is an ISO 8601 instant, stored as the string Dates.DateTime prints and compared as an instant rather than as text.\n\n:geopoint of PLAN.md §4.5 is not here. That type existed to be indexed, and a value-to-ids index is the wrong shape for \"within a radius\" or \"inside a box\"; a project that stores coordinates declares two :float64 fields or leaves them undeclared.\n\n\n\n\n\n","title":"SimilaritySearchEngine.Schema.META_FIELD_TYPES"},{"category":"section","location":"index.html#SimilaritySearchEngine.jl","page":"Home","text":"SimilaritySearchEngine.jl is an embedded, high-performance similarity search library for Julia. It provides persistent, transactional management for collections of dense vectors, sparse representations, and full-text documents. The storage layer is backed by SimilaritySearch.jl, TextSearch.jl, and RocksDB.jl.\n\nThe library executes directly within the host Julia process without requiring external HTTP services, background daemons, or separate IPC brokers.","title":"SimilaritySearchEngine.jl"},{"category":"section","location":"index.html#Core-Capabilities","page":"Home","text":"Project Lifecycle Management: Structured management of project storage via create_project, open_project, and close_project!.\nDecoupled Architecture: Clear distinction between the stored payload type (DenseEngine, SparseEngine, FullTextEngine) and the underlying indexing backend (SearchGraph, ExhaustiveSearch, InvertedFile, BM25InvertedFile).\nDense Vector Search: Approximate nearest neighbor search via SearchGraph (supporting automatic hyperparameter recall tuning) or exact linear scanning via ExhaustiveSearch and ParallelExhaustiveSearch.\nSparse Vector Search: Native inverted index retrieval (InvertedFile) over user-provided sparse representations, evaluated under cosine similarity or set distance metrics (Dist.Sets.*).\nFull-Text Retrieval: Integrated text preprocessing, BM25 scoring, and TF-IDF inverted files configured via linguistic profiles (TextProfile).\nPersisted Text Index: Text projects on either backend persist the index itself – posting lists read lazily from storage behind a least-frequently-used cache, document vectors resident – so reopening a project assembles it instead of recomputing it (1.4 s against 20.4 s over 265,000 paragraphs).\nLifecycle Decoupling (Staging vs. Indexing): Incremental staging of documents via append_items! with deferred, batch-oriented index construction via index!.\nLogical Deletions: Non-destructive soft deletes (delete_item!) that preserve index integrity while marking candidates in query responses.\nBatch Query Execution: searchbatch(handle, queries, k) resolves an entire set of queries in one parallel pass and returns the raw (ids, dists) matrices; search(handle, queries, k) returns the same hits hydrated with their doc_id and soft-delete state.\nGlobal Metric Operations: In-process execution of all-pairs nearest neighbors (allknn), diverse sampling (fft), closest pair discovery (closestpairs), and cross-dataset closest pairs (bichromatic_kclosestpairs).\nQuery Cost Accounting: search(handle, query, k; stats=SearchStats()) reports the distance computations performed by that one call – the only vantage point from which they can be attributed, since a query runs on a context borrowed from the engine's pool and returned when it ends.\nIndexed External Identifiers: doc_id resolution is backed by its own column family, and a doc_id shared by several items resolves to all of them.\nStorage Compaction: compact_project!, and an automatic compaction when a writing session closes, keep reopen latency flat after bulk ingestion.\nTyped Errors: EngineError and its four categories (InvalidRequest, NotFound, ConflictingState, StorageFailure) describe why a call failed, in types rather than prose.\nGranular Column-Family Persistence: Fine-grained persistence where mutations update only the affected RocksDB column families or memory-mapped files.\n\n","title":"Core Capabilities"},{"category":"section","location":"index.html#Installation","page":"Home","text":"The package is not in the General registry yet, so install it from its repository. Its whole dependency chain — SimilaritySearch, TextSearch, RocksDB and the RocksDB_jll binaries — is registered, so this resolves and precompiles without local checkouts or a build step:\n\nusing Pkg\nPkg.add(url=\"https://github.com/sadit/SimilaritySearchEngine.jl\")\n\nTo develop the package itself, clone the repository and develop the clone. No Manifest.toml is committed, so Pkg.instantiate() resolves the base libraries from the registry; use Pkg.develop(path=...) on each of them to work against your own checkouts instead.\n\nusing Pkg\nPkg.develop(path=\"path/to/SimilaritySearchEngine.jl\")\n\n","title":"Installation"},{"category":"section","location":"index.html#Documentation-Structure","page":"Home","text":"This reference is one half of the project site; the other half is the manual, which holds the prose for both packages of this repository.\n\nReference (here)\n\nEngine API: every exported type, interface and function of SimilaritySearchEngine, plus its Errors, Schema, Project, IndexEngine and Persistence submodules.\nServer API: SimilaritySearchServer — the HTTP handlers, the job spool, the two command lines, tokens, telemetry and cursors.\n\nManual\n\nManual Home: what each package is for, and which one you need.\nArchitecture: Comprehensive specification of the submodules, type hierarchies, concurrency locks, and on-disk storage layout.\nGetting Started Tutorial: End-to-end tutorial covering dense search, sparse retrieval, BM25 text search, filtering, calibration, and whole-dataset operations.\nMultilingual Paragraph Search: Large-scale case study evaluating full-text retrieval across multilingual Project Gutenberg corpora and Wikipedia.\nServer Manual and Server Tutorial: the service layer — endpoints, jobs, error mapping, and a walkthrough from an empty working directory to a query over HTTP.","title":"Documentation Structure"}]
}
