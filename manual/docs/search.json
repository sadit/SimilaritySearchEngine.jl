[
  {
    "objectID": "architecture.html",
    "href": "architecture.html",
    "title": "Architecture",
    "section": "",
    "text": "This document describes the architectural design and internal subsystems of SimilaritySearchEngine.jl. It covers the module organization, the engine type hierarchy, the decoupled staging-versus-indexing lifecycle, the multithreaded concurrency model, and the hybrid persistence layout combining RocksDB column families with memory-mapped array storage."
  },
  {
    "objectID": "architecture.html#module-map",
    "href": "architecture.html#module-map",
    "title": "Architecture",
    "section": "Module map",
    "text": "Module map\nThe package is five source files, four of them real submodules (Schema, Project, IndexEngine, Persistence) plus embedded.jl, which defines EmbeddedEngine and the top-level functions (create_project, append_items!, search, …) directly in the SimilaritySearchEngine module itself — the friendly surface a script actually calls.\n\n\n\n\n\nflowchart LR\n    Schema[\"Schema&lt;br/&gt;&lt;small&gt;MetadataRecord (_id, doc_id,&lt;br/&gt;keywords, refs), meta split&lt;/small&gt;\"]\n    Project[\"Project&lt;br/&gt;&lt;small&gt;ProjectManager, metadata CRUD&lt;/small&gt;\"]\n    IndexEngine[\"IndexEngine&lt;br/&gt;&lt;small&gt;AbstractSearchEngine hierarchy&lt;/small&gt;\"]\n    Persistence[\"Persistence&lt;br/&gt;&lt;small&gt;EngineStore, AdjacencyStore, ...&lt;/small&gt;\"]\n    Embedded[\"embedded.jl&lt;br/&gt;&lt;small&gt;EmbeddedEngine + top-level API&lt;/small&gt;\"]\n    Caller([\"Julia script, or&lt;br/&gt;SimilaritySearchServer\"])\n\n    Schema --&gt; Project\n    Schema --&gt; Embedded\n    Project --&gt; Embedded\n    IndexEngine --&gt; Embedded\n    Persistence --&gt; Embedded\n    Embedded --&gt; Caller\n\n\n\n\n\n\nIndexEngine and Persistence are deliberately independent of Project/Schema and of each other: IndexEngine knows nothing about RocksDB (its persistence hook is a plain (index, sp, ep) -&gt; nothing callback), and Persistence knows nothing about metadata schemas or search engines (it stores plain Julia values under string keys). embedded.jl is the one place that wires all four together — every other module could, in principle, be reused on its own.\nSchema/Project have no per-project typed-field schema to declare anymore: every project has exactly the same fixed-shape Schema.MetadataRecord (_id, doc_id, keywords, refs) plus a free-form meta JSON blob for everything else appended with an item — _id is the internal, sequential, always-present handle (the same id space the engine’s own vector/text store uses); doc_id is the caller’s own external id, if any; keywords/refs are the only two fields with a secondary index (Project’s fixed idx_keywords/idx_refs column families), everything else lives in meta, looked up straight from Project, never indexed."
  },
  {
    "objectID": "architecture.html#engine-type-hierarchy",
    "href": "architecture.html#engine-type-hierarchy",
    "title": "Architecture",
    "section": "Engine Type Hierarchy",
    "text": "Engine Type Hierarchy\nSimilaritySearchEngine.jl parameterizes search collections through two distinct abstractions: 1. Engine (AbstractSearchEngine): Defines the data domain of the stored payloads (DenseEngine, SparseEngine, FullTextEngine). 2. Backend (AbstractSearchIndex): Defines the underlying search data structure (SearchGraph, ExhaustiveSearch, ParallelExhaustiveSearch, InvertedFile, BM25InvertedFile).\ncreate_project(w, ds)                                               # Dense vectors, SearchGraph (defaults)\ncreate_project(w, ds; engine=DenseEngine, backend=ExhaustiveSearch) # Dense vectors, exact linear scan\ncreate_project(w, ds; engine=SparseEngine, dimension=50_000)        # Sparse vectors, InvertedFile\ncreate_project(w, ds; engine=FullTextEngine, backend=BM25InvertedFile,\n                      textmodel=DefaultProfile(:es))                # Full text, BM25 scoring\n\n\n\n\n\nclassDiagram\n    class AbstractSearchEngine {\n        &lt;&lt;abstract&gt;&gt;\n    }\n    class DenseEngine~B~ {\n        backend: B\n        search_ctx_pool: ContextPool\n        deleted_ids: Set~UInt32~\n        lock: ReadWriteLock\n    }\n    class SparseEngine {\n        backend: SparseBackend\n        search_ctx_pool: ContextPool\n        deleted_ids: Set~UInt32~\n        lock: ReadWriteLock\n    }\n    class FullTextEngine {\n        backend: TextBackend\n        profile: Union~Nothing, TextProfile~\n        fitspec: Union~Nothing, DeferredFit~\n        staged: Vector~String~\n        search_ctx_pool: ContextPool\n        deleted_ids: Set~UInt32~\n        lock: ReadWriteLock\n    }\n    class GraphBackend {\n        index: SearchGraph\n        ctx: SearchGraphContext\n        minrecall: Union~Nothing, Float32~\n        opt_beamsearch: OptBeamSearch\n    }\n    class ExactBackend~IndexType~ {\n        index: IndexType\n        ctx: GenericContext\n    }\n    class SparseBackend {\n        index: InvertedFile\n        ctx: InvertedFileContext\n        distance: PreMetric\n        dimension: Int\n    }\n    class TextBackend {\n        index: Union~Nothing, AbstractInvertedFile~\n        ctx: InvertedFileContext\n        kind: Type\n        distance: Union~Nothing, PreMetric~\n    }\n    AbstractSearchEngine &lt;|-- DenseEngine\n    AbstractSearchEngine &lt;|-- SparseEngine\n    AbstractSearchEngine &lt;|-- FullTextEngine\n    DenseEngine *-- GraphBackend\n    DenseEngine *-- ExactBackend\n    SparseEngine *-- SparseBackend\n    FullTextEngine *-- TextBackend\n\n\n\n\n\n\n\nArchitectural Properties of the Hierarchy\n\nFirst-Class Sparse Engine (SparseEngine): Provides direct inverted-index search over externally encoded sparse vectors. Callers supply pre-computed SparseVector objects without requiring linguistic tokenizers or text profiles.\nPayload Domain Introspection (payload_kind): Returns :dense, :sparse, or :text, allowing client layers to inspect payload requirements.\nBackend Compatibility Registry (BACKENDS): Enforces valid combinations of payload engines and search backends:\n\nDenseEngine accepts SearchGraph, ExhaustiveSearch, and ParallelExhaustiveSearch.\nSparseEngine accepts InvertedFile.\nFullTextEngine accepts BM25InvertedFile and TextInvertedFile.\n\nDomain Separation of BM25: BM25InvertedFile is restricted to FullTextEngine because BM25 scoring relies on corpus-level document frequencies and average document lengths rather than purely geometric vector properties.\nStructural Dimension Constraints: SparseBackend requires an explicit dimension parameter at initialization because an InvertedFile allocates a fixed array of posting lists.\nEncapsulated Linguistic Modeling: FullTextEngine encapsulates its entire linguistic configuration within a TextProfile (profile), including vocabulary frequencies, term weighting schemes, stopword lists, lemma clusters, and query-expansion graphs."
  },
  {
    "objectID": "architecture.html#staging-vs.-indexing-a-graph-and-a-text-project-have-the-split-the-rest-index-on-insertion",
    "href": "architecture.html#staging-vs.-indexing-a-graph-and-a-text-project-have-the-split-the-rest-index-on-insertion",
    "title": "Architecture",
    "section": "Staging vs. indexing: a graph and a text project have the split, the rest index on insertion",
    "text": "Staging vs. indexing: a graph and a text project have the split, the rest index on insertion\nappend_items!/add_item! only ever stages an item for a graph-backed dense project and for a text one; search_live/ftsearch/allknn/fft/closestpairs/bichromatic_kclosestpairs only ever see items an explicit index! call has actually processed, not merely staged.\nThe other two backends have no split to speak of. An exact index (ExhaustiveSearch/ ParallelExhaustiveSearch) always evaluates directly against db, and an InvertedFile builds its posting lists on insertion, so for both of them append_items! makes an item searchable immediately. index! is still defined for them, as a no-op: the same four lines — create_project, append_items!, index!, search — should mean the same thing for every kind of project, and a caller should not have to know which backends need the third one. The cost is a call that does nothing, which is the cheaper mistake; the alternative made switching a project from a graph to a brute-force scan a change to every script that fed it.\nSimilaritySearch.jl’s own SearchGraph separates staging raw vectors into its database from the (expensive) graph-connection work, and this package extends that same split to the text backends, which have no such native support of their own to lean on:\n\ngraph-backed dense: insert_dense!(index::SearchGraph, ctx, item) = push_item!(index.db, item) only ever appends item to index.db — cheap, and durable the instant it happens (see Persistence below), but index.adj (the graph itself) is untouched. IndexEngine.index!(engine::DenseEngine{GraphBackend}) is the explicit, idempotent step that actually builds graph connections for whatever has been staged since the last call (or since creation) — it delegates to SimilaritySearch.index!(engine.index, engine.ctx), which itself only ever processes the backlog (length(engine.index)+1:length(database(engine.index))), so calling it repeatedly, or with nothing new staged, is a cheap no-op.\ntext: add_item! just pushes raw text onto engine.staged — the text-engine counterpart of index.db, since TextSearch.jl’s inverted-file types have no raw-item staging area of their own to reuse. IndexEngine.index!(engine) (no corpus argument) catches up the exact same backlog range ((engine.index === nothing ? 0 : length(engine.index))+1:length(engine.staged)), encoding each staged text (bagofwords for BM25; for the weighted engine, handing the text straight to TextInvertedFile, whose own push_item! vectorizes through the profile’s model) and push_item!ing it into the real index — with one extra twist on its first call for a project created without a profile: since neither index kind can even be constructed without a Vocabulary, that call runs the engine’s fitspec over what is staged so far and builds the real index against the resulting TextProfile, before running the same backlog loop. Every later call is a plain catch-up, identical in shape to the graph’s.\nA project created from a BaseProfile skips that entirely, and the difference is not only when the work happens: a vocabulary fitted from a sample of the first batch is frozen there, so every term appended later that it never saw is out-of-vocabulary and silently dropped. Starting from a profile fitted over the language — DefaultProfile(:es), or a BaseProfile of one you hold — is what makes an incrementally-grown text project behave.\n\nThis decouples when an item becomes durable (immediately, on append_items!) from when it becomes searchable (only after an explicit index!), so a caller controls when the expensive graph-connection/encoding cost is paid — once per big batch, on a schedule, or right before the next round of queries — instead of paying it synchronously on every single insert the way an exact or an inverted-file backend does."
  },
  {
    "objectID": "architecture.html#concurrency-model",
    "href": "architecture.html#concurrency-model",
    "title": "Architecture",
    "section": "Concurrency Model",
    "text": "Concurrency Model\nIndex data structures in SimilaritySearch.jl and TextSearch.jl support concurrent read operations, but mutate unsafely if writes occur concurrently with reads or other writes.\nSimilaritySearchEngine.jl coordinates multithreaded access via two synchronization mechanisms: ReadWriteLock and ContextPool.\n\nReader-Writer Synchronization: ReadWriteLock\nEach engine maintains a ReadWriteLock to coordinate thread access: - Read Lock (Shared): Acquired by query operations (search, ftsearch, allknn, closestpairs). Allows multiple worker threads to execute searches in parallel. - Write Lock (Exclusive): Acquired by mutating operations (append_items!, index!, delete_item!, calibrate!). Blocks all active readers and concurrent writers.\n\n\n\n\n\nsequenceDiagram\n    participant S1 as search (thread 1)\n    participant S2 as search (thread 2)\n    participant W as append_items! (thread 3)\n    participant L as engine.lock\n\n    S1-&gt;&gt;L: read_lock (readers: 1)\n    S2-&gt;&gt;L: read_lock (readers: 2)\n    Note over S1,S2: Concurrent search execution\n    W-&gt;&gt;L: write_lock (blocks until readers == 0)\n    S1-&gt;&gt;L: release (readers: 1)\n    S2-&gt;&gt;L: release (readers: 0)\n    L-&gt;&gt;W: write_lock granted (exclusive)\n    Note over W: Execute mutation and update persistence\n    W-&gt;&gt;L: release\n\n\n\n\n\n\n\n\nContext Isolation: ContextPool\nSearch contexts (SearchGraphContext, GenericContext, InvertedFileContext) contain internal scratch buffers (beam priority queues, visited-vertex bitvectors, and accumulator caches). If concurrent search threads share a single context instance, memory corruption occurs.\nContextPool manages a dynamic pool of isolated context instances:\n\n\n\n\n\nflowchart LR\n    A[\"Search Query Entry\"] --&gt; B{\"pool.available&lt;br/&gt;non-empty?\"}\n    B -- Yes --&gt; C[\"pop! available context\"]\n    B -- No --&gt; D[\"deepcopy(pool.template)\"]\n    C --&gt; E[\"Execute search using checked-out context\"]\n    D --&gt; E\n    E --&gt; F[\"Return context to pool.available\"]\n\n\n\n\n\n\nThe pool expands dynamically to match the peak concurrent query volume and retains allocated buffers to eliminate runtime allocation overhead.\n\n\nCost Accounting Across the Pool Boundary\nPooling has a consequence for measurement. SimilaritySearch.jl accounts for the work an operation performs in the counters carried by the context itself (ctx.costdists), which accumulate and are never reset; the cost of a single operation is therefore a difference between two readings taken around it. That technique is sound for insertion, which runs on the engine’s own ctx under an exclusive write lock, and unsound for search: the context a query ran on was borrowed from the pool for the duration of the call and returned at the end, so a reading taken afterwards from outside describes whatever else has since been searching concurrently, not the query in question.\nThe count is consequently produced where it is unambiguous – inside IndexEngine.search_live, between checkout and checkin – and surfaced to the caller through a supplied SearchStats instance:\nstats = SearchStats()\nresults = search(h, query, 10; stats)\nstats.distance_evaluations\nsearch, ftsearch and the filtered search path all accept it, the filtered path reporting the cost of the candidate search it actually ran rather than of the k results it returned. The field is overwritten by each call rather than accumulated. This is the mechanism behind the distance_evaluations field of the server’s op_log, which has no other way to attribute work to the request that caused it."
  },
  {
    "objectID": "architecture.html#persistence-one-storage-unit-per-concern",
    "href": "architecture.html#persistence-one-storage-unit-per-concern",
    "title": "Architecture",
    "section": "Persistence: one storage unit per concern",
    "text": "Persistence: one storage unit per concern\nEarlier iterations of this design re-serialized the entire engine state — including the whole index — on every mutation. The current design instead gives each kind of state its own RocksDB column family (or, for a SearchGraph’s raw vectors, its own file outside RocksDB entirely — see below), so a mutation only ever rewrites the storage it actually touched.\n\n\n\n\n\nflowchart TB\n    subgraph db[\"one RocksDB directory per project\"]\n        records[(\"records&lt;br/&gt;&lt;small&gt;_id → MetadataRecord (fixed shape)&lt;/small&gt;\")]\n        meta[(\"meta&lt;br/&gt;&lt;small&gt;_id → free-form meta (JSON)&lt;/small&gt;\")]\n        oplog[(\"op_log\")]\n        idxkw[(\"idx_keywords&lt;br/&gt;&lt;small&gt;keyword → _ids&lt;/small&gt;\")]\n        idxrefs[(\"idx_refs&lt;br/&gt;&lt;small&gt;referenced doc_id → _ids&lt;/small&gt;\")]\n        engine[(\"engine &lt;small&gt;(ENGINE_CF)&lt;/small&gt;&lt;br/&gt;&lt;small&gt;one key per engine struct field&lt;/small&gt;\")]\n        adj[(\"searchgraph_adj &lt;small&gt;(ADJACENCY_CF)&lt;/small&gt;&lt;br/&gt;&lt;small&gt;object_id → direct neighbors&lt;/small&gt;\")]\n        stagedtext[(\"staged_text &lt;small&gt;(STAGED_TEXT_CF)&lt;/small&gt;&lt;br/&gt;&lt;small&gt;append-only raw, not-yet-encoded text blocks&lt;/small&gt;\")]\n        sparsedb[(\"sparse_db &lt;small&gt;(INVFILE_DB_CF)&lt;/small&gt;&lt;br/&gt;&lt;small&gt;append-only encoded object blocks&lt;/small&gt;\")]\n    end\n    mmapfile[[\"dense_vectors.mmapdb&lt;br/&gt;&lt;small&gt;-- not RocksDB --&lt;br/&gt;one row per staged vector&lt;/small&gt;\"]]\n\n    ProjectManager -- \"put_metadata! / get_metadata\" --&gt; records\n    ProjectManager -- \"get_meta / get_raw_meta\" --&gt; meta\n    ProjectManager -- \"secondary lookups\" --&gt; idxkw\n    ProjectManager -- \"secondary lookups\" --&gt; idxrefs\n    EngineStore -- \"save_field! / load_field\" --&gt; engine\n    AdjacencyStore -- \"save_neighbors! / load_neighbors\" --&gt; adj\n    StagedTextStore -- \"append_staged_texts! / load_staged_text_blocks\" --&gt; stagedtext\n    InvertedFileObjectStore -- \"append_objects! / load_object_blocks\" --&gt; sparsedb\n    append_items_dense[\"append_items!&lt;br/&gt;(SearchGraph)\"] --&gt; mmapfile\n    append_items_text[\"append_items!&lt;br/&gt;(BM25InvertedFile/InvertedFile)\"] --&gt; stagedtext\n\n\n\n\n\n\nEvery RocksDB column family above lives in the same directory/connection as the project’s metadata store (Project.ProjectManager.db) — Project.open_project’s extra_cf_names keyword is how IndexEngine’s persistence layer gets its own column families listed alongside Project’s, since every column family that exists on disk must be named at opendb time on every open, not only the first. dense_vectors.mmapdb is the one exception: a plain file, alongside (not inside) the RocksDB directory – raw text has no fixed-size-record shape to exploit that way, so staged_text stays a RocksDB column family, structured exactly like sparse_db (append-only, blocks keyed by starting position) but holding raw, un-encoded text instead of already-encoded objects.\n\nEngineStore: per-field keys, not one blob\nEngineStore is a field_name::Symbol =&gt; value map, one RocksDB key per struct field. save_field!/load_field touch exactly one key; save_fields! is only used for rare, one-shot multi-field transitions (right after create_engine, or the moment a text engine’s vocabulary is first trained). A soft delete only ever rewrites :deleted_ids; a calibrate! call only ever rewrites :opt_beamsearch.\n\n\nDense vectors: a memory-mapped file, not a RocksDB column family\nA graph-backed project’s raw vectors are fixed-dimension Float32 data — exactly what SimilaritySearch.MMapMatrixDatabase is for, and its own append-only counter/growth/fsync discipline already gives durable, in-order, crash-safe storage with no need for RocksDB’s batch-keying scheme, at a much cheaper per-vector read than a RocksDB point lookup (confirmed empirically against VectorDatabase/BlockMatrixDatabase in SimilaritySearch.jl’s own test suite). append_items! writes staged vectors directly into this file, at stage time — not through CallbackLog at all, unlike everything else in this section — since a vector is durable-worthy the moment it’s staged, long before any graph-linking work happens. The file (dense_vectors.mmapdb) is created lazily, on the first dense append, since its dimension isn’t known before then.\nBatch size matters a lot here: MMapMatrixDatabase fsyncs once per append_items! call, never per individual vector, so the number of calls a caller makes (not the total vector count) drives wall-clock time — confirmed empirically, 200,000 128-dim vectors as one 200,000-item call took ~0.10s; the same total split into calls of 10 items took ~2.1s; one call per single item measured roughly 1000x slower than the all-at-once case. Prefer fewer, larger append_items! calls over many small ones.\n\n\nIncremental adjacency/posting-list persistence: different shapes, by necessity\nThe graph/posting-list structure itself is the other expensive, ever-growing part, so it is never saved as a single value either. Its incremental format is driven by SimilaritySearch.CallbackLog, an observer that fires a flush(index, sp, ep) callback on every :add! event a mutating call reports — SimilaritySearch.jl standardizes every index kind’s structural events onto this one name, regardless of which entry point triggered it, and the observation channel carries nothing else, so CallbackLog never has to branch on the event itself. What does differ by index kind is the shape of what gets saved, and when this callback fires relative to append_items!.\n\n\n\n\n\nsequenceDiagram\n    participant Caller as index!(handle)\n    participant Idx as SearchGraph\n    participant CB as CallbackLog\n    participant ES as EngineStore\n    participant AS as AdjacencyStore\n\n    Note over Caller,Idx: append_items! already staged vectors into&lt;br/&gt;dense_vectors.mmapdb -- no CallbackLog involved\n    Caller-&gt;&gt;Idx: SimilaritySearch.index!(engine.index, ctx)\n    Idx-&gt;&gt;Idx: build graph connections for the staged backlog (range sp:ep)\n    Idx-&gt;&gt;CB: LOG(:add!, index, ctx, sp, ep)\n    Note right of Idx: fires BEFORE connect_reverse_links!&lt;br/&gt;index.adj[sp:ep] holds only direct links\n    CB-&gt;&gt;AS: save_neighbors!(i, direct_neighbors(i)) for i in sp:ep\n    CB-&gt;&gt;ES: save_field!(:graph_len, ep)\n    Idx-&gt;&gt;Idx: connect_reverse_links! (in memory only)\n\n\n\n\n\n\nFor a SearchGraph, LOG fires before connect_reverse_links! runs for that range — so what’s captured at that instant is exactly the direct-links-only slice. This is by design, not a bug to route around: connect_reverse_links! isn’t idempotent (re-running it on an already-complete graph would duplicate reverse edges), so the on-disk format never saves reverse links at all. :graph_len (how far graph-indexing actually got, which can lag behind how many vectors are staged in dense_vectors.mmapdb if the process closed between an append_items! and its follow-up index!) lets reloading resume exactly where indexing left off, over the same, already-fully-restored .db.\nThe inverted-file backends — BM25InvertedFile and TextInvertedFile for a text project, InvertedFile for a sparse one — have no direct/reverse-link hazard: their own LOG only fires after a push_item! call’s mutation is fully done, so InvertedFileObjectStore simply appends the raw encoded objects just inserted (invertedfile_objects) as a new block. For a text project that LOG fires from inside an index! call’s backlog loop (see “Staging vs. indexing” above) rather than from append_items! directly — append_items! only ever writes to StagedTextStore, the raw-text counterpart of dense_vectors.mmapdb, and sparse_db’s already-encoded blocks only grow once an index! call actually catches the backlog up. For a sparse project there is no such indirection and no StagedTextStore at all: the caller handed over the encoded vector itself, so the insertion is the encoding and the block is written on append_items!. Reloading index itself still rebuilds the whole thing by replaying every saved encoded object back through the library’s own push_item! — a rebuild-by-reinsertion design, not a real on-disk posting-list format — while staged (every raw text ever staged, whether or not it’s been encoded yet) is restored separately, straight from StagedTextStore, and handed to restore_engine alongside object_blocks. This is a deliberate scaling limit, not an oversight: it’s fine up to a few million documents, since the whole corpus and the whole rebuilt index have to fit in memory and reloading costs the full original indexing time again. It is not a design for billion-document corpora, which need genuinely disk-backed, paged-on-demand posting lists.\nAn exact backend (ExhaustiveSearch/ParallelExhaustiveSearch) has no incremental format of its own — its on_change just flags pending_flush[], and embedded.jl saves the whole index as a single :index value right after the triggering add_item! call returns. This is acceptable for these exact, in-memory-only index kinds, which have no adjacency or posting-list structure to split apart in the first place, and index synchronously on every add_item!.\n\n\nReopening a project\n\n\n\n\n\nsequenceDiagram\n    participant Caller as open_project\n    participant MM as dense_vectors.mmapdb\n    participant ES as EngineStore\n    participant AS as AdjacencyStore\n    participant IE as IndexEngine\n\n    Caller-&gt;&gt;ES: load_field(:kind)\n    Caller-&gt;&gt;MM: open_dense_vectors(dir) → vector_blocks (one block, every staged vector)\n    Caller-&gt;&gt;AS: load_neighbors(i) for i in 1:graph_len\n    Caller-&gt;&gt;ES: load_field(:graph_len, 0)\n    Caller-&gt;&gt;IE: build_searchgraph(distance, vector_blocks, load_neighbors, graph_len)\n    IE-&gt;&gt;IE: replay every staged vector into index.db (in full)\n    IE-&gt;&gt;IE: add! each of the first graph_len objects' saved direct neighbors\n    IE-&gt;&gt;IE: connect_reverse_links! once, over 1:graph_len\n    IE--&gt;&gt;Caller: reconstructed SearchGraph -- index.len[] = graph_len\n    Caller-&gt;&gt;IE: restore_engine(state, on_change) → DenseEngine{GraphBackend}\n\n\n\n\n\n\ngraph_len can be &lt;= length(index.db): every staged vector is always fully restored into .db (that’s durable from the moment it was staged), but only the first graph_len of them were ever graph-indexed before the project last closed — if it closed between an append_items! and its follow-up index! call, the gap is restored as staged-but-not-yet- searchable, waiting on a future index!(handle) call to catch it up, exactly as it was before closing.\nrestore_engine dispatches on Val(state.kind) — :dense, :sparse or :text — and each branch then reads state.backend to pick the index within that kind. Both are symbols, not Julia types, and that is what makes a saved project survive this file being rewritten: the previous shape persisted the type BM25Engine into the project’s own store, so renaming the types (which this restructuring did to every one of them) made every existing project unreadable by construction. A symbol cuts that tie, and an unrecognised one maps to an error naming the ones the reader does know instead of a MethodError about a type it has never heard of.\nIndexEngine.extra_state_fields only covers the exact backends, and covers them with () — their whole index is a single :index field, so there is nothing extra to list. No other backend has such a field at all: each needs its own combination of EngineStore reads (:minrecall/:opt_beamsearch for a graph; :distance/:dimension for a sparse project; :profile/:fitspec/:distance for a text one) plus its own dedicated block-store reads (vector_blocks/load_neighbors/graph_len for a graph; object_blocks/staged for the inverted-file kinds), assembled by hand in open_project rather than driven by a generic per-kind field list."
  },
  {
    "objectID": "architecture.html#logging-informative-output-and-persistence-are-independent",
    "href": "architecture.html#logging-informative-output-and-persistence-are-independent",
    "title": "Architecture",
    "section": "Logging: informative output and persistence are independent",
    "text": "Logging: informative output and persistence are independent\nEvery engine’s context carries two logging slots, and that split is what makes the independence structural instead of a convention to remember. This engine used to hand-roll it — one slot holding three backends of two different kinds, with a comment explaining which of them could affect persistence — until SimilaritySearch.jl grew the distinction itself.\n\nctx.reporters — where progress goes to be read. Always at least one InformativeLog printing a throttled status line to stderr; a log_io adds a second one writing the same line to any caller-given IO (an open file handle, or stdout/stderr). Purely informative, and it cannot be anything else: a reporter is only ever handed a message.\nctx.observers — what reacts to a structural change. A CallbackLog here forwards every :add! report, untouched by any throttling, to whatever on_change closure embedded.jl installed. This is the actual persistence trigger.\n\n\n\n\n\n\nflowchart LR\n    Mutation[\"push_item! / append_items!\"] --&gt; INFORM[\"INFORM&lt;br/&gt;(ctx.reporters)\"]\n    Mutation --&gt; OBSERVE[\"OBSERVE :add!&lt;br/&gt;(ctx.observers)\"]\n    INFORM --&gt; Stderr[\"InformativeLog&lt;br/&gt;→ stderr, throttled\"]\n    INFORM --&gt; FileIO[\"InformativeLog(log_io)&lt;br/&gt;→ any IO, throttled&lt;br/&gt;(optional)\"]\n    OBSERVE --&gt; CallbackLog[\"CallbackLog&lt;br/&gt;→ on_change(index, sp, ep)&lt;br/&gt;(persistence trigger)\"]\n\n\n\n\n\n\nThe two channels cannot interfere. Silencing the console is reporters=[], and persistence continues untouched; conversely no observer can be lost by turning output down, because turning output down does not reach that slot."
  },
  {
    "objectID": "architecture.html#soft-deletes",
    "href": "architecture.html#soft-deletes",
    "title": "Architecture",
    "section": "Soft deletes",
    "text": "Soft deletes\nmark_deleted! adds a document’s id to deleted_ids and nothing else — the underlying index is never touched, so no rebuild or backfill is needed. search_live reports a deleted flag per candidate instead of silently dropping deleted candidates and backfilling with the next-best live one: compensating for deletions by overfetching would have to grow with the total number of deletions, and it would hide from the caller which ids were even considered. A caller that wants k live results after filtering is expected to page for more (e.g. via a cursor over successive top-k' windows) at a layer above this one — SimilaritySearchServer, not this package."
  },
  {
    "objectID": "architecture.html#whole-dataset-operations",
    "href": "architecture.html#whole-dataset-operations",
    "title": "Architecture",
    "section": "Whole-dataset operations",
    "text": "Whole-dataset operations\nallknn, fft, closestpairs, and bichromatic_kclosestpairs all run synchronously, in-process, directly against a dense project’s index — no job queue, unlike the HTTP API’s POST /api/v1/jobs/allknn. Each does real work proportional to the whole dataset (or two datasets, for the bichromatic case): allknn is every item’s own top-k neighbors at once; fft (Farthest-First-Traversal) picks k well-separated items for diverse sampling; closestpairs finds the k globally closest pairs within one project (near-duplicate/echo detection); bichromatic_kclosestpairs finds the k closest pairs between a project’s own index and a second, unindexed collection of vectors. All four require a graph-backed dense project that’s had index! run at least once — none apply to a text project."
  },
  {
    "objectID": "architecture.html#where-this-leaves-similaritysearchserver",
    "href": "architecture.html#where-this-leaves-similaritysearchserver",
    "title": "Architecture",
    "section": "Where this leaves SimilaritySearchServer",
    "text": "Where this leaves SimilaritySearchServer\nThis package covers project lifecycle, core CRUD, calibration, and the whole-dataset operations above. Pagination policy over soft-deleted results, HTTP/CLI surfaces, job queues, and telemetry all live one layer up, in the sibling SimilaritySearchServer package, which depends on this one and is kept in sync with it as this package’s design settles."
  },
  {
    "objectID": "tutorials/getting-started.html",
    "href": "tutorials/getting-started.html",
    "title": "Getting Started",
    "section": "",
    "text": "This tutorial provides an end-to-end walkthrough of SimilaritySearchEngine.jl. Using paragraphs from Mary Shelley’s Frankenstein (included in the package test fixtures), we examine: - Dense vector similarity search using Latent Semantic Indexing (LSI) embeddings and SearchGraph. - Sparse vector indexing with InvertedFile. - Full-text retrieval with BM25 and TF-IDF models. - Logical soft deletions and metadata filtering. - Target recall calibration with calibrate!. - Closing and reopening persistent projects. - Whole-dataset metric operations (allknn, fft, closestpairs, and bichromatic_kclosestpairs)."
  },
  {
    "objectID": "tutorials/getting-started.html#installation",
    "href": "tutorials/getting-started.html#installation",
    "title": "Getting Started",
    "section": "Installation",
    "text": "Installation\nSimilaritySearchEngine.jl is not in the General registry yet, so it is installed from its repository. Everything it depends on is registered, so the command below is the whole installation — no local checkouts of SimilaritySearch.jl/TextSearch.jl/RocksDB.jl, and no build step for the RocksDB binaries:\nusing Pkg\nPkg.add(url=\"https://github.com/sadit/SimilaritySearchEngine.jl\")\nIf you intend to modify the package while following this tutorial, clone it and develop the clone instead:\nusing Pkg\nPkg.develop(path=\"path/to/SimilaritySearchEngine.jl\")\nThis tutorial reads its corpus from the package’s own test/data/, so a cloned copy is the convenient way to follow it verbatim; with the Pkg.add form, point the paths below at any JSONL file of your own with the same fields."
  },
  {
    "objectID": "tutorials/getting-started.html#the-frankenstein-dataset",
    "href": "tutorials/getting-started.html#the-frankenstein-dataset",
    "title": "Getting started",
    "section": "The Frankenstein dataset",
    "text": "The Frankenstein dataset\nThis package’s own test suite ships test/data/frankenstein.jsonl — one JSON object per paragraph of the novel, already split, cleaned, and annotated by test/generate_datasets.jl. Loading it gives you a Vector of plain Dicts, each already shaped to match Schema.MetadataRecord’s record/data/metadata contract:\nusing JSON\n\nitems = [JSON.parse(l) for l in readlines(\"test/data/frankenstein.jsonl\")]\nlength(items)  # 724 paragraphs\nfirst(keys(items[1]))\nEach item has:\n\n\"text\": the paragraph itself (raw data — never duplicated into metadata).\n\"doc_id\": an external id like \"frankenstein_1\" (the record’s fixed external-id field).\n\"keywords\": character names detected in that paragraph, e.g. [\"Elizabeth\", \"Victor\"] (the record’s fixed, indexed tag field).\n\"ref\": the previous paragraph’s doc_id (the record’s fixed, unvalidated reference field — a forward/backward link, here used to keep narrative order available even though nothing enforces refs point at something that exists).\n\"word_count\"/\"verb_count\": free-form metadata (meta) — anything not a reserved key (vector/text/doc_id/keywords/ref) falls through here as-is, flat, with no wrapper key of its own.\n\nThere’s no \"vector\" key yet — that’s the dense section’s job, below."
  },
  {
    "objectID": "tutorials/getting-started.html#dense-vectors-with-lsi-searchgraph",
    "href": "tutorials/getting-started.html#dense-vectors-with-lsi-searchgraph",
    "title": "Getting started",
    "section": "Dense vectors with LSI (SearchGraph)",
    "text": "Dense vectors with LSI (SearchGraph)\ncreate_project makes a brand-new project directly on disk, at &lt;workdir&gt;/&lt;dataset&gt;. Two keywords say what it is: engine is what the project holds — DenseEngine, SparseEngine or FullTextEngine — and backend is the index that holds it. Both default, so create_project(workdir, \"frankenstein_dense\") means exactly the line below: a dense project on a SearchGraph (an approximate, self-tuning index) autotuning toward minrecall=0.9.\nusing SimilaritySearchEngine\n\nworkdir = mktempdir()\nh = create_project(workdir, \"frankenstein_dense\"; engine=DenseEngine, backend=SearchGraph, minrecall=0.9)\n\nGetting real dense vectors quickly, with LSI\nA SearchGraph project needs Vector{Float32}s to index — if you don’t already have an embedding model at hand, TextSearch.jl’s Latent Semantic Indexing (LSI) gives you real, meaningful dense vectors straight from a text corpus, in-process, with no external model or API call: fit once on your corpus, then project any text (an indexed paragraph, or a brand-new query) into that same dense space.\nusing TextSearch: LatentSemanticIndexing, outdim\nusing TextSearch.LSI: vectorize\n\ncorpus = [it[\"text\"] for it in items]\nlsi = LatentSemanticIndexing(corpus; maxoutdim=128, verbose=false)\noutdim(lsi)  # 128 -- the full corpus easily supports it\n(maxoutdim is a cap, not a guarantee: outdim(lsi) is min(maxoutdim, vocabulary size, corpus size) — a tiny placeholder corpus of just a handful of documents would yield far fewer dimensions than requested; 724 real paragraphs comfortably reach 128.)\nItems are appended as a Vector of dicts, each carrying a \"vector\" key plus the record fields loaded above. We’ll only insert the first half of the book here, and hold the second half back in reserve, unindexed, for the bichromatic example further down:\nhalf = length(items) ÷ 2\n\nbatch = [\n    DenseItem(vectorize(lsi, it[\"text\"]);\n              doc_id = it[\"doc_id\"], keywords = it[\"keywords\"], refs = it[\"ref\"],\n              meta = Dict{String,Any}(\"word_count\" =&gt; it[\"word_count\"],\n                                      \"verb_count\" =&gt; it[\"verb_count\"]))\n    for it in items[1:half]\n]\nappend_items!(h, batch)  # inserted == 362\n(append_items! accepts a batch — passing many items in one call amortizes better than one append_items! call per item, though both work; see Architecture for why batch size matters even more for a dense project specifically.)\n\n\nIndexing the staged vectors\nFor a SearchGraph project, append_items! only stages raw vectors — cheaply, and durably, but without doing any of the (expensive) graph-connection work. Nothing is searchable yet:\nsearch(h, vectorize(lsi, \"the monster and its creator\"), 5)  # Vector{...}[] -- empty, nothing indexed yet\nindex! explicitly builds the graph over whatever’s been staged since the last call (or since creation) — call it once you’re ready for staged items to become searchable, whether that’s right after every append_items! call, on your own periodic schedule, or just once before a batch of queries:\nindex!(h)\nIt’s cheap to call again later with nothing new staged (a no-op), and only ever processes the backlog — call it as often as fits your workflow. Searching now finds the 362 staged paragraphs:\nquery = vectorize(lsi, \"the monster and its creator\")\nresults = search(h, query, 5)\nfor r in results\n    println(r.doc_id, \" =&gt; distance \", r.distance)\nend\nPass a filter predicate — a plain (record, meta) -&gt; Bool function over each candidate’s fixed Schema.MetadataRecord (doc_id, keywords, refs, …) and its free-form meta (whatever else you appended it with) — to restrict results without building a separate query language:\nmentions_someone = search(h, query, 5; filter = (record, meta) -&gt; !isempty(record.keywords))\n\n\nSoft deletes\ndelete_item! marks a document as logically deleted; the index itself is untouched. It takes the internal _id (the position add_item! assigned it, e.g. as returned in a search result’s doc_id field), not the \"doc_id\" you supplied when appending:\ndelete_item!(h, 1)  # internal _id 1, not any \"doc_id\"/metadata value\n\nresults = search(h, query, 5)\nfirst(results).deleted  # true if internal _id 1 made the top-5 candidates\nA soft-deleted candidate is reported, not hidden and backfilled — if you need k live results after filtering out deletions, overfetch and page yourself, or let SimilaritySearchServer’s cursor layer do it for you.\n\n\nCalibrating for a target recall\ncalibrate! runs a real hyperparameter search and persists the result, so a later search(...; minrecall=0.95) call can serve that target recall from a tuned BeamSearch instead of the constructor’s default:\ncalibrate!(h; levels=[0.8, 0.9, 0.95, 0.97])\nprecise = search(h, query, 5; minrecall=0.95)\nIt needs a reasonably sized project to have anything meaningful to search against and tune over — this 724-paragraph corpus is plenty; a toy corpus of just a handful of items would print a wall of “ignoring configuration”/“too few queries” warnings instead (harmless, just noisy), since there wouldn’t be enough distinct items for its own internal sample queries.\n\n\nClosing and reopening\nclose_project!(h)\n\nh2 = open_project(workdir, \"frankenstein_dense\")\nsame_query_results = search(h2, query, 5)\nclose_project!(h2)\nEvery insertion was already persisted incrementally as it happened (see Architecture), so close_project! only needs to flush anything still pending for index kinds that don’t persist incrementally on their own. Staging and indexing are tracked separately, too: reopening restores exactly how far index! actually got, not just how much was staged – had we appended more items after the last index! call above and closed without indexing them, they’d still be there (staged, with their metadata already fetchable) after reopening, just not yet searchable, waiting on a future index! call to catch them up.\nsame_query_results will be close to, but isn’t guaranteed to be byte-identical to, the results from before closing: SearchGraph is an approximate index, and rebuilding it from persisted vectors (open_project replays every insertion in the same order) doesn’t guarantee an identical internal graph topology to the one built live, so its beam search can explore a slightly different candidate set — expect the same ballpark of hits (often exactly the same), not a bitwise-reproducible top-k, unless you calibrate and search with an explicit minrecall on both sides."
  },
  {
    "objectID": "tutorials/getting-started.html#sparse-vectors-you-encoded-yourself",
    "href": "tutorials/getting-started.html#sparse-vectors-you-encoded-yourself",
    "title": "Getting started",
    "section": "Sparse vectors you encoded yourself",
    "text": "Sparse vectors you encoded yourself\nA dense project takes vectors you produced; a sparse project does too, and the difference is only in their shape. Reach for it when your encoder is sparse — a learned sparse retriever, a feature table, a bag of ids, an interaction matrix’s row. Nothing in this path tokenizes anything: the engine has no vocabulary, no profile and no TextConfig, and it never sees a string.\nThe encoding here is deliberately crude — hash each word into a fixed dimension and normalize — because its only job is to stand in for whatever your own encoder does:\nusing SimilaritySearch: Dist\nusing SparseArrays: sparsevec\n\nconst DIM = 1 &lt;&lt; 16\n\nfunction bag(text)\n    counts = Dict{Int32,Float32}()\n    for w in split(lowercase(text), r\"[^\\p{L}\\p{N}]+\"; keepempty=false)\n        k = Int32(mod(hash(w), DIM) + 1)\n        counts[k] = get(counts, k, 0f0) + 1f0\n    end\n    ind = sort!(collect(keys(counts)))\n    val = Float32[counts[i] for i in ind]\n    sparsevec(ind, val ./ sqrt(sum(abs2, val)), DIM)   # NormCosine reads its inputs normalized\nend\n\nhs = create_project(workdir, \"frankenstein_sparse\";\n                    engine=SparseEngine, dimension=DIM, distance=Dist.NormCosine())\n\nappend_items!(hs, [SparseItem(bag(it[\"text\"]); doc_id=it[\"doc_id\"], keywords=it[\"keywords\"])\n                   for it in items])\nindex!(hs)                       # a no-op here, and kept in the example on purpose -- see below\n\nhits = search(hs, bag(\"the monster and its creator\"), 5)\nfor r in hits\n    println(r.doc_id, \" =&gt; distance \", round(r.distance; digits=4))\nend\nfrankenstein_258 =&gt; distance 0.4673\nfrankenstein_487 =&gt; distance 0.4848\nfrankenstein_616 =&gt; distance 0.4848\nfrankenstein_403 =&gt; distance 0.4893\nfrankenstein_296 =&gt; distance 0.5149\nThree things about that snippet are worth naming.\ndimension is required, and only here. An InvertedFile is a fixed array of posting lists, so its size is structural rather than descriptive: it has to be known before the first item, and every SparseItem appended has to agree with it. Passing dimension to a dense or a text project is an error rather than an ignored keyword.\nindex! does nothing, and the line stays anyway. An inverted file builds its posting lists on insertion, so the items were searchable the moment append_items! returned. The call is a no-op kept so that create_project / append_items! / index! / search means the same thing for every kind of project — switching a project from a graph to an inverted file should not be a change to every script that feeds it.\nThe query is sparse too, and a dense one is refused:\nsearch(hs, rand(Float32, DIM), 5)\n# ERROR: a sparse project searches with a SparseVector{Float32,Int32}, not a Vector{Float32};\n#        build one with `sparsevec(indices, values, dimension)` over the same dimension the\n#        project was created with (65536)\nThat could have quietly sparsified instead. It does not, because a project whose items are SparseItems has sparse queries by construction, so a dense one is a mistake worth naming — and sparsifying silently would turn a dimension mismatch into posting lists selected from whatever the values happened to be.\nEverything else behaves as the dense section showed: fetch_items hands the payload back as a SparseVector{Float32,Int32} (not densified on the way out), filter predicates, soft deletes and close_project!/open_project are all identical.\nDist.NormCosine() is one choice of distance. Any distance that reads a sparse vector works — Dist.Sets.Jaccard() and its neighbours in Dist.Sets.* score over the nonzero positions instead of the weights, which is what you want when your vector is really a set. BM25 is not on that list: it scores from corpus statistics rather than from a vector, so it belongs to a text project, which is the next section."
  },
  {
    "objectID": "tutorials/getting-started.html#full-text-search-with-bm25",
    "href": "tutorials/getting-started.html#full-text-search-with-bm25",
    "title": "Getting started",
    "section": "Full-text search with BM25",
    "text": "Full-text search with BM25\nText projects work the same way as dense ones, just with a \"text\" key instead of \"vector\", and ftsearch instead of search — and, since the redesign that decoupled staging from indexing, the exact same append_items!(stage)/index!(build) shape as a dense project, not a separate training step. BM25InvertedFile scores via BM25 over raw bags-of-words:\nusing TextSearch: BM25InvertedFile\n\n# `textmodel` is required for a text project -- see the next section for what the\n# alternative is and why neither is a default\nhb = create_project(workdir, \"frankenstein_bm25\"; engine=FullTextEngine, backend=BM25InvertedFile,\n                    textmodel=FitFromCorpus())\n\nappend_items!(hb, [\n    TextItem(it[\"text\"]; doc_id = it[\"doc_id\"], keywords = it[\"keywords\"])\n    for it in items[1:200]\n])  # only stages raw text -- nothing searchable yet\n\nindex!(hb)  # first call: fits the TextProfile from everything staged so far, then indexes it\n\nhits = ftsearch(hb, \"monster creature fear\", 5)\nfor r in hits\n    println(r.doc_id, \" =&gt; distance \", r.distance)\nend\n\nclose_project!(hb)\nindex!’s first call on a text project doubles as training: it fits a TextProfile (vocabulary, tf-idf weights, lineage) and builds the real BM25InvertedFile from every item staged so far, then encodes/indexes that same backlog — there’s no separate training entry point or “must be trained first” error. Every later index! call just catches up whatever’s been staged since, exactly like the dense case: idempotent, safe to call repeatedly, a cheap no-op when nothing new is staged.\nTokens never seen at that first index! call are out-of-vocabulary on every later append and silently dropped, so what you’ve staged before it has to be representative of the text you’ll keep appending. text_profile(hb) hands you the fitted model, and save_profile writes it out for reuse — but the real fix for a project that keeps growing is the next section."
  },
  {
    "objectID": "tutorials/getting-started.html#choosing-the-text-model",
    "href": "tutorials/getting-started.html#choosing-the-text-model",
    "title": "Getting started",
    "section": "Choosing the text model",
    "text": "Choosing the text model\ntextmodel above was not decoration. Creating a text project requires it, and there is no default:\ncreate_project(workdir, \"oops\"; engine=FullTextEngine, backend=BM25InvertedFile)\n# ERROR: a text project (BM25InvertedFile) needs an explicit `textmodel`, because the ways to\n#        get a vocabulary are not interchangeable and the cheap one cannot be undone later: ...\nThree forms answer where the vocabulary comes from. They are not variations on one setting, and the order below is the order worth preferring them in.\n\nDefaultProfile — start from a language, adapt to your corpus\nhes = create_project(workdir, \"noticias\"; engine=FullTextEngine, backend=BM25InvertedFile,\n                     textmodel=DefaultProfile(:es))\nThis is the one to reach for. At the first index! call it loads the published Spanish paragraph profile and refits it against whatever you have staged: the base’s token counts are blended with your corpus’s, the weights recomputed, and the stopword set, lemma map and expansion network inherited. No embedding is fitted, which is what makes a refit cheap next to a fit.\nBe clear about what the refit does and does not give you, because the obvious guess is wrong. Measured on the published English profile: 335,336 tokens in the base, 12,238 after refitting against an 800-document sample, of which 2,649 are tokens the sample never held. Fitting on that sample alone would have given 9,589. So the refit narrows to your corpus’s vocabulary, widened about a quarter by the base — it is not language-wide coverage. What you inherit is the part your own corpus cannot produce: weights calibrated over millions of paragraphs, and (for that base) 96 stopwords, 16,661 lemmas and 10,655 expansion entries.\nIf coverage is what you need — terms a later batch will introduce that today’s corpus has never seen — pass refit=false and index against the base untouched, all 335,336 tokens, with Wikipedia’s weights rather than yours. Calibration or coverage: the two forms answer different questions.\nThe profile is resolved from the library textsearch install maintains, under ~/.textsearch/profiles/. Nothing is bundled or downloaded — the profiles are 70–160 MB — and if the one you asked for is not installed the error says so and prints the command that installs it. refit=false indexes against the base untouched, and nickname= points at any installed profile instead of the per-language default.\n\n\nFitFromCorpus — deliberately no base profile\nFit the model from this project’s own staged corpus, at the first index! call. This is the explicit way to say “I have nothing pre-fitted”, and it is the weakest of the three:\nusing TextSearch: NormalizationConfig\n\nhf = create_project(workdir, \"noticias_propias\"; engine=FullTextEngine, backend=BM25InvertedFile,\n    textmodel=FitFromCorpus(TextConfig(normalization=NormalizationConfig(lc=false, del_diac=false),\n                                       language=:es);\n                            min_ndocs=3, stopwords=0.9))\nIts argument is the corpus-independent policy — normalization, tokenization, and the language the corpus is in. The default TextConfig() lowercases and strips diacritics, which is usually right for English and throws away a distinction that matters in Spanish.\nThe fit itself is handed whole to TextSearch.fit_profile; this package does not fit profiles. That has a consequence worth stating plainly: there is no cheap variant of that pipeline. Every fit runs an LSI over the sample, derives an expansion network from it and clusters a lemma map from the same embeddings. Measured on 3,000 paragraphs it takes about 49 seconds, against 0.2 for a bare vocabulary-and-weights pass — which is why max_documents exists:\n\n\n\n\n\n\n\noption\nwhat it does\n\n\n\n\nmax_documents\nread at most this many documents (1,000 by default), sampled by an even stride across the corpus rather than taken from the front. 0 lifts the cap\n\n\nmin_ndocs\ndrop a token seen in fewer documents than this, before the encoder runs. Not cosmetic: the expansion network is an all-pairs kNN over the vocabulary\n\n\nstopwords\na document-frequency threshold in (0, 1]; tokens above it are dropped and the vocabulary recounted without them. A second pass over the sample\n\n\nencoder, expansion, lemmas\nthe library’s own option groups, as NamedTuples — encoder=(; outdim=128), lemmas=(; apply=true)\n\n\n\nThe cap bounds the cost and cuts against the one thing this path is already worst at: a vocabulary from 1,000 sampled documents is thin, so more of what you append later is out-of-vocabulary and silently dropped. That is the trade, and it is why the section above exists.\n\n\nBaseProfile — a model fitted elsewhere\nhp = create_project(workdir, \"noticias_wiki\"; engine=FullTextEngine, backend=BM25InvertedFile,\n                    textmodel=BaseProfile(load_profile(\"wiki20231101-es.zip\")))\nThis trains the project before its first item is ever staged, and is what DefaultProfile does underneath once it has resolved and refitted — use this form when you already hold the file, or when you want the base used exactly as it is. To load a profile but decline one of its artifacts, change the profile rather than the project: BaseProfile(with_applied(p; lemmas=false)).\nEither way the spec is persisted with the project and restored verbatim by open_project. And a project that fitted its own model can export it for the next one to index against:\nsave_profile(\"mi-perfil\", text_profile(hes))\ncreate_project(workdir, \"hermano\"; engine=FullTextEngine, backend=BM25InvertedFile,\n               textmodel=BaseProfile(load_profile(\"mi-perfil\")))\nPassing a textmodel to a dense project is an error rather than an ignored keyword — there is no reading under which it does anything."
  },
  {
    "objectID": "tutorials/getting-started.html#correcting-and-widening-a-query",
    "href": "tutorials/getting-started.html#correcting-and-widening-a-query",
    "title": "Getting started",
    "section": "Correcting and widening a query",
    "text": "Correcting and widening a query\nA query travels with a QueryPolicy, because whether to correct a misspelling or widen a search is a guess about what the person meant — a property of the query, not of the index:\nftsearch(hes, \"musica clasica\", 5)                                   # :auto (the default)\nftsearch(hes, \"musica clasica\", 5; policy=QueryPolicy(correction=:off))   # exactly as typed\nUnder :auto, a typed token is bridged to another spelling of the same word only where the evidence says the typed one is wrong — it is absent from the vocabulary, or far rarer than another spelling of it. On a corpus indexed with del_diac=false, musica reaches música and clasica reaches clásica; a correctly typed query is left alone. Under the default TextConfig() this is inert, since folding case and diacritics already collapsed those spellings into one.\nA search that silently substitutes what was asked for owes the caller a way to see it, which is ftexplain:\njulia&gt; ftexplain(hes, \"musica clasica\")\n2-element Vector{String}:\n \"musica is in 1 document against música's 60, so it reads as a misspelling; searched as música (variant) instead\"\n \"clasica is in 1 document against clásica's 60, so it reads as a misspelling; searched as clásica (variant) instead\"\nQueryPolicy also carries expansion/expansion_k, which widen the query with the profile’s query-expansion network — inert unless the project was created from a profile that carries one."
  },
  {
    "objectID": "tutorials/getting-started.html#full-text-search-with-tf-idf",
    "href": "tutorials/getting-started.html#full-text-search-with-tf-idf",
    "title": "Getting started",
    "section": "Full-text search with TF-IDF",
    "text": "Full-text search with TF-IDF\nTextInvertedFile is the other text index kind: a classic TF-IDF-weighted (IdfWeighting + TfWeighting) inverted index, cosine distance by default — a good baseline to compare against BM25’s ranking on the same corpus. (InvertedFile selects the same engine and is accepted interchangeably; TextInvertedFile is the name of what actually gets built.)\nusing TextSearch: TextInvertedFile\n\nht = create_project(workdir, \"frankenstein_tfidf\"; engine=FullTextEngine, backend=TextInvertedFile,\n                    textmodel=FitFromCorpus())\n\nappend_items!(ht, [\n    TextItem(it[\"text\"]; doc_id = it[\"doc_id\"], keywords = it[\"keywords\"])\n    for it in items[1:200]\n])\n\nindex!(ht)  # same stage-then-index shape as BM25 above\n\nhits = ftsearch(ht, \"monster creature fear\", 5)\nfor r in hits\n    println(r.doc_id, \" =&gt; distance \", r.distance)\nend\n\nclose_project!(ht)\nSame insertion/search shape as BM25 — only backend changes — so switching between the two to compare rankings on your own corpus is a one-line change."
  },
  {
    "objectID": "tutorials/getting-started.html#expensive-whole-dataset-operations",
    "href": "tutorials/getting-started.html#expensive-whole-dataset-operations",
    "title": "Getting started",
    "section": "Expensive, whole-dataset operations",
    "text": "Expensive, whole-dataset operations\nallknn, fft, closestpairs, and bichromatic_kclosestpairs all run synchronously, in-process — no job queue, unlike the HTTP API’s POST /api/v1/jobs/allknn — which is exactly why they’re grouped here as their own section: each one does real work proportional to the whole dataset (or two datasets, for the bichromatic case), so run them against a project you actually want an answer for, not casually inside a hot loop. All four need a dense (SearchGraph) project; reopen the one from the LSI section above if you closed it:\nh2 = open_project(workdir, \"frankenstein_dense\")\n\nAll-pairs nearest neighbors\nallknn runs SimilaritySearch.jl’s batch all-nearest-neighbors — every item’s own top-k neighbors, all at once:\nneighbors = allknn(h2; k=5)\nfirst(neighbors)  # (id=1, neighbors=[...], dists=[...])\nid/neighbors are internal _ids (1-based insertion position), not doc_ids — see Metadata lookups to resolve them back to paragraph text.\n\n\nFarthest-first traversal (diverse sampling)\nfft picks k items that are as far from each other as possible — useful for a diverse, representative sample of the corpus (e.g. candidate cluster centers), rather than the “closest to a query” shape every other search here has:\nR = fft(h2, 8; verbose=false)\nR.centers      # 8 well-separated internal _ids\nR.separation   # the smallest distance between two of those 8\nR.covering     # the largest distance from any item to its nearest one\nR.assign[i] says which of the 8 item i belongs to — a position in R.centers, from 1 to 8, so R.centers[R.assign[i]] is that center’s own _id and R.assigndist[i] is how far item i sits from it. That makes fft double as a quick partition of the corpus around k spread-out seeds.\n\n\nk closest pairs (near-duplicates and echoes within one corpus)\nclosestpairs finds the k globally closest pairs of items within a single project — useful for spotting near-duplicate or strongly echoing passages:\npairs = closestpairs(h2; k=5)\nfor (i, j, d) in pairs\n    ri = get_metadata(h2.project, i)\n    rj = get_metadata(h2.project, j)\n    println(ri.doc_id, \" &lt;-&gt; \", rj.doc_id, \"  dist=\", d)\n    println(\"  A: \", first(items[i][\"text\"], 90))\n    println(\"  B: \", first(items[j][\"text\"], 90))\nend\nRunning this against the first half of the novel (h2 as built above) turns up passages like Elizabeth’s account of Justine’s trial echoed a few paragraphs later almost verbatim, at a distance well below typical unrelated pairs — exactly the kind of near-duplicate/ strong echo this function is for.\n\n\nBichromatic k closest pairs (across two collections)\nbichromatic_kclosestpairs finds the k closest pairs between a project’s own index and a second, separate collection of vectors that isn’t indexed by anything — here, the first half of the novel (already indexed as h2, see above) against the second half’s vectors (computed with the same LSI model, but never inserted anywhere):\nusing SimilaritySearch: MatrixDatabase\n\nsecond_half_vectors = [vectorize(lsi, it[\"text\"]) for it in items[(half + 1):end]]\nB = MatrixDatabase(hcat(second_half_vectors...))\n\npairs = bichromatic_kclosestpairs(h2, B; k=5)\nfor (i, j, d) in pairs\n    ri = get_metadata(h2.project, i)             # i is h2's own internal _id\n    other = items[half + j]                       # j is B's own 1-based position\n    println(ri.doc_id, \" &lt;-&gt; \", other[\"doc_id\"], \"  dist=\", d)\n    println(\"  A: \", first(items[i][\"text\"], 100))\n    println(\"  B: \", first(other[\"text\"], 100))\nend\nOn the real novel, this surfaces thematically matched passages across the two halves — e.g. Walton’s opening framing letters about a “strange accident” pairing with an equally breathless first-person account much later in the book, well below the distance of an unrelated cross-half pair."
  },
  {
    "objectID": "tutorials/getting-started.html#metadata-lookups",
    "href": "tutorials/getting-started.html#metadata-lookups",
    "title": "Getting started",
    "section": "Metadata lookups",
    "text": "Metadata lookups\nfetch_items and exists accept either the internal _id (an integer, or a string of one, e.g. \"1\") or the doc_id you supplied when appending. fetch_items returns a StoredItem per resolved id — the record’s fixed fields, its free-form meta, and the payload that was actually indexed:\nit = only(fetch_items(h2, [\"frankenstein_2\"]))\nit._id            # 2\nit.doc_id         # \"frankenstein_2\"\nit.keywords       # [\"Chapter\", ...]\nit.meta[\"word_count\"]\nit.payload        # the vector this project indexed (a paragraph, in a text project)\n\nexists(h2, [1, \"some-doc-id\"])   # Vector{ExistsResult}: (id, exists, deleted)\nAn id that resolves to nothing is dropped by fetch_items (so the result can be shorter than what you asked for) and reported by exists (so it is always the same length).\nget_metadata(h2.project, _id) (used in the closest-pairs examples above) is the lower-level single-record accessor fetch_items itself is built on — it returns just the Schema.MetadataRecord (no meta merged in), which is all those examples needed."
  },
  {
    "objectID": "tutorials/getting-started.html#next-steps",
    "href": "tutorials/getting-started.html#next-steps",
    "title": "Getting started",
    "section": "Next steps",
    "text": "Next steps\nSee Architecture for how all of this is persisted incrementally under the hood, and this package’s docs/ (built with Documenter.jl) for the full function/type reference."
  },
  {
    "objectID": "tutorials/index.html",
    "href": "tutorials/index.html",
    "title": "Tutorials Overview",
    "section": "",
    "text": "This section provides hands-on tutorials for working with SimilaritySearchEngine.jl:\n\nGetting Started: A comprehensive walkthrough using paragraphs from Mary Shelley’s Frankenstein. Demonstrates:\n\nDense vector similarity search using LSI-derived embeddings with SearchGraph.\nSparse vector indexing with InvertedFile.\nFull-text retrieval with BM25 and TF-IDF models.\nLogical soft deletions and metadata filtering.\nTarget recall calibration via calibrate!.\nTransactional persistence (closing and reopening projects).\nWhole-dataset metric operations (allknn, fft, closestpairs, bichromatic_kclosestpairs).\n\nMultilingual Paragraph Search: A large-scale tutorial indexing four corpora across three languages (English, Spanish, Portuguese Project Gutenberg collections, and English Wikipedia). Demonstrates:\n\nFitting and configuring linguistic profiles (TextProfile).\nOrthographic query variant resolution and semantic query expansion.\nEmpirical benchmarking of indexing throughput, memory overhead, and search latency."
  },
  {
    "objectID": "tutorials/paragraph-search.html",
    "href": "tutorials/paragraph-search.html",
    "title": "Paragraph search over books and Wikipedia",
    "section": "",
    "text": "This is the tutorial to read after Getting started. That one used one novel and took every default; this one uses three real corpora, in two languages, each indexed against a text model fitted for it — which is where the decisions that matter start mattering.\nParagraphs, not documents. A search here answers with the paragraph that contains the answer, not with the book it appears in. That choice runs all the way down: it is the unit the corpora are built in, the unit the profiles are fitted on, and the unit the projects index. Retrieving whole books would be close to useless — Moby-Dick is “about” almost anything you can ask — and it is also a worse model, because a document frequency computed over books says nothing (nearly every word is in nearly every book) while one computed over paragraphs is informative."
  },
  {
    "objectID": "tutorials/paragraph-search.html#before-you-start",
    "href": "tutorials/paragraph-search.html#before-you-start",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Before you start",
    "text": "Before you start\nEverything below reads what tutorials/prepare.jl builds. That script downloads the books, extracts the Wikipedia articles, fits a profile per corpus and indexes each into a project. It is measured in tens of minutes and hundreds of megabytes, which is exactly why it is a script and not a section of this page:\n# from the repository root\njulia --project=tutorials tutorials/prepare.jl            # the documented scale, ~1 hour\njulia --project=tutorials tutorials/prepare.jl --smoke    # a few books and articles, ~5 minutes\nEach step (corpora, profiles, projects) is separately runnable and caches to disk, so a run that fails halfway resumes instead of restarting.\n\n\n\n\n\n\nNoteRendering this page yourself\n\n\n\nQuarto’s Julia engine needs a writable runtime directory. On a machine where /run/user/$UID is not writable, quarto render fails with PermissionDenied ... mkdir '/run/user/…/julia' before running a single cell. Point it somewhere else:\nXDG_RUNTIME_DIR=$(mktemp -d) quarto render manual"
  },
  {
    "objectID": "tutorials/paragraph-search.html#the-corpora",
    "href": "tutorials/paragraph-search.html#the-corpora",
    "title": "Paragraph search over books and Wikipedia",
    "section": "The corpora",
    "text": "The corpora\n\n\ntrue\n\n\nprepare.jl writes one JSONL file per corpus, one paragraph per line, and records what it measured. Everything this page reports about the build is read back from that record rather than typed into the prose — a number in a document goes stale silently; a number read from the file the build wrote is either current or missing.\n\n\n\n\n\n\ncorpus\nlanguage\ndocuments\nparagraphs\nper document\nJSONL\n\n\ngutenberg-en\nen\n100\n265,424\n2654.2\n120.1 MB\n\n\ngutenberg-es\nes\n100\n216,247\n2162.5\n89.2 MB\n\n\nwikipedia-en\nen\n1,000\n41,279\n41.3\n30.2 MB\n\n\n\n\n\n\n\nTwo things in that table are worth pausing on.\nWikipedia yields far more paragraphs per article than you would expect. The dump is ordered longest-article-first, so the first shard — which is what a thousand articles come from — is the densest part of the corpus. TextSearch’s own measurements put the corpus-wide figure near 7 paragraphs per article; take them from the front and you get an order of magnitude more. Sampling the convenient slice and extrapolating is how corpus-size estimates go wrong.\nBooks vary enormously, and the average hides it. A hundred Gutenberg volumes is not a hundred comparable things. The English hundred runs from 87 paragraphs to 36,096 — a Complete Works of William Shakespeare that is 13.6% of the corpus on its own — against a median book of 1,859. The Spanish hundred’s largest contributor is not a novel at all but a Diccionario Ingles-Español-Tagalog at 17,682 paragraphs, 8.2% of that corpus. The per-document average in the table above is therefore a number no actual book resembles, and what it costs you shows up in Searching below.\nEach record carries the paragraph plus enough metadata to say where it came from:\n\n\nkeys: doc_id, text, source, lang, book_id, title, author, paragraph\n\ndoc_id: gutenberg-es-61851-p1\ntitle:  El crimen y el castigo — Dostoyevsky, Fyodor\ntext:   Se ha respetado la ortografía y la acentuación del original.…\n\n\ndoc_id is the caller’s own identifier; the engine hands it back on every hit. Everything except text, vector, keywords, ref and doc_id goes into the record’s metadata, which is what a filtered search reads."
  },
  {
    "objectID": "tutorials/paragraph-search.html#profiles",
    "href": "tutorials/paragraph-search.html#profiles",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Profiles",
    "text": "Profiles\nA text project needs to know how to turn text into tokens and how to weight them. That is a TextProfile: a vocabulary and weights, plus whatever a corpus produced — a stopword set, a lemma map, a query-expansion network — plus the lineage saying how it was made.\nprepare.jl fits one per corpus with TextSearch’s own textsearch fit, and not with this package’s fit_profile. The difference is what you get: fit_profile produces a vocabulary and weights, which is all an indexing corpus can yield on its own, while textsearch fit additionally runs an LSI over the corpus and derives a query-expansion network and a lemma map from it. Those artifacts are the reason the two query-side sections below demonstrate something instead of describing it.\nThe configuration used for every corpus is worth reading, because one line in it drives most of this page:\n[normalization]\ndel_diac = false      # keep diacritics\nlc = false            # keep case\ndel_punc = true\ngroup_num = true\ngroup_url = true\nKeeping case and diacritics is a deliberate cost. Folding them is cheaper and gives a smaller vocabulary, and it is what the default TextConfig() does — but it also collapses leon and León into one token before the index ever sees them, which leaves the query side with nothing to work with. Everything in Correcting a query below exists only because these are false.\n\n\n\n\n\n\nprofile\nvocabulary\nstopwords\nlemmas\nexpansion\nfit\n\n\ngutenberg-en\n136,716\n9 (applied)\n28,465\n136,716\n265s\n\n\ngutenberg-es\n337,336\n0\n83,792\n337,336\n476s\n\n\nwikipedia-en\n139,776\n18 (applied)\n23,186\n139,776\n129s\n\n\n\n\n\n\n\nNote the (applied) markers, or rather where they are missing. A freshly fitted profile carries its lemma map and its expansion network without applying them. That is not an oversight, it is the distinction the type exists to make: computing an artifact and deciding to use it are different acts, and the second belongs to whoever indexes with the profile. Stopwords are applied because the vocabulary was counted with them already removed — un-applying those would mean refitting.\nPrinting a profile shows its lineage, which answers “where did this come from?” without guesswork:\n\n\nTextProfile: base\n  fit(doc_freq_threshold=0.5, encoder=lsi, outdim=128, scaling=none, source_path=, trainsize=41279)\n  stopwords: 18 (applied)\n  lemmas: 23186 (carried)\n  query_expansion: 139776 (carried), with distances\n  VectorModel:\n    global_weighting: TextSearch.IdfWeighting()\n    local_weighting: TextSearch.TfWeighting()\n    maxoccs: 33158\n    Vocabulary:\n      vocsize: 139776\n      trainsize: 41279\n      numtokens: 2898621\n      avgdoclen: 70.2202330482812\n      TextConfig:\n        NormalizationConfig:\n            del_diac: false\n            del_dup: false\n            del_punc: true\n            group_num: true\n            group_url: true\n            group_usr: false\n            group_emo: false\n            lc: false\n            re_user: r\"@[^;:,.@#&\\\\\\-\\\"'/:\\*\\(\\)\\[\\]\\¿\\?\\¡\\!\\{\\}~\\&lt;\\&gt;\\|\\s]+\"\n            re_url: r\"(http|ftp|https)://\\S+\"\n            re_num: r\"[-+]?(\\d+\\.?\\d*)|(\\.\\d+)\"\n            emojis: 3923 emoji chars\n        TokenizationConfig:\n            nlist: Int8[1]\n            mark_token_type: true\n            generators: TextSearch.Tokenizer.AbstractTokenGenerator[]\n        pipeline: TokenPipeline(lemmas=nothing, stopwords=18 tokens)\n        language: unknown\n\n\n\nInstalling, listing and describing profiles\nThis tutorial loads profiles from plain files on disk, with load_profile(path), because that needs nothing installed and keeps every path in this page explicit. For day-to-day use TextSearch’s CLI manages a profile library under ~/.textsearch/profiles/ instead:\ntextsearch install tutorials/profiles/gutenberg-es.zip gutenberg-es   # copy in under a nickname\ntextsearch install tutorials/profiles/gutenberg-es.zip                # nickname defaults to the filename\ntextsearch list                                                       # what is installed\ntextsearch info gutenberg-es                                          # vocabulary, artifacts, lineage\ntextsearch uninstall gutenberg-es                                     # reports what it *would* delete\ntextsearch uninstall gutenberg-es --force                             # actually deletes it\nThe nickname is a positional argument, not a flag, and defaults to the zip’s filename. Note what uninstall does without --force: it prints the path, the size and the command that would delete it, and removes nothing. Here the registration is the file — a profile can represent hours of compute and the installed copy may be the only one left — so the destructive reading of the word has to be asked for.\nOnce installed, a nickname works anywhere a path does in the CLI’s own commands. The file itself sits at ~/.textsearch/profiles/&lt;nickname&gt;.zip (or under $TEXTSEARCH_HOME), and load_profile takes that path like any other. Nothing below depends on having installed anything."
  },
  {
    "objectID": "tutorials/paragraph-search.html#searching",
    "href": "tutorials/paragraph-search.html#searching",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Searching",
    "text": "Searching\nA project is created against a profile, and from then on it is trained: the vocabulary covers the language rather than whichever batch happened to arrive first.\n\n\n[gutenberg-en]  the whale rose out of the sea\n  1. Moby Dick; Or, The Whale                    -74.805\n     Secondly: It is well known in the Sperm Whale Fishery, however ignorant the world ashore may be of it, that there have been several memorable historic…\n  2. Moby Dick; Or, The Whale                    -70.770\n     Throughout the Pacific, and also in Nantucket, and New Bedford, and Sag Harbor, you will come across lively sketches of whales and whaling-scenes, gra…\n  3. Moby Dick; Or, The Whale                    -67.435\n     In life, the visible surface of the Sperm Whale is not the least among the many marvels he presents. Almost invariably it is all over obliquely crosse…\n\n[gutenberg-es]  Fortunata y Jacinta en Madrid\n  1. La Odisea                                   -90.754\n     TEBAS (Θήβη y Θῆβαι):\n  2. La Ilíada                                   -90.754\n     TEBAS (Θήβη y Θῆβαι):\n  3. Fortunata y Jacinta: dos historias de casa  -63.504\n     Doña Lupe la invitó, dos días después de la tarde del choque con Jacinta, a volver a visitar a Mauricia. ¡Qué diría doña Guillermina si no volvían! Ne…\n\n[wikipedia-en]  anarchism as a political philosophy\n  1. Anarcho-capitalism                          -141.363\n     Anarcho-capitalism Anarchist activists and scholars do not consider anarcho-capitalism as a part of the anarchist movement, arguing that anarchism has…\n  2. Anarcho-capitalism                          -134.281\n     Anarcho-capitalism Anarchism In both its social and individualist forms, anarchism is usually considered an anti-capitalist and radical left-wing or f…\n  3. Anarcho-capitalism                          -134.086\n     Anarcho-capitalism Rothbard argued that anarcho-capitalism is the only true form of anarchism—the only form of anarchism that could possibly exist in …\n\n\n\nThe distances are BM25 scores, negated so that smaller is better — the same convention every index kind in SimilaritySearch.jl uses, so a caller never has to ask whether high or low wins.\nNothing about the calls above is language-specific. The language lives in the profile the project was created with; ftsearch is the same function in all three.\nIf a dictionary entry outranked the novel you were expecting, that is the corpus and the scoring function telling you something true. Gutenberg’s popularity ranking is not a balanced sample: the Spanish top hundred’s single biggest contributor is a Diccionario Ingles-Español-Tagalog, 17,682 paragraphs — 8.2% of the corpus, and 1.7× the largest volume of prose in it. BM25 normalises by document length, and a two-line dictionary entry sharing one query term with you scores far above a hundred-word paragraph of prose sharing three. So a corpus with one dictionary in it has, in effect, seventeen thousand tiny documents competing for every query that touches their headwords. Neither the index nor the profile is misbehaving — a corpus assembled by popularity is simply not a corpus assembled for retrieval, and this is what that looks like before anyone filters it."
  },
  {
    "objectID": "tutorials/paragraph-search.html#correcting-a-query",
    "href": "tutorials/paragraph-search.html#correcting-a-query",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Correcting a query",
    "text": "Correcting a query\nBecause the profiles keep case and diacritics, a Spanish query typed without accents is not the same query the corpus contains. QueryPolicy’s default handles that, and can explain itself:\n\n\nquery: la musica del corazon\n  ~ musica is in 9 documents against música's 724, so it reads as a misspelling; searched as música (variant), Música (variant) instead\n\nquery: el pais y la republica\n  ~ republica is in 3 documents against República's 307, so it reads as a misspelling; searched as República (variant), república (variant) instead\n\n\n\nEach line is a token the search replaced, with the evidence for replacing it. A correction happens only where the evidence says the typed spelling is wrong — it is absent from the vocabulary, or far rarer than another spelling of the same word. A correctly typed query produces no lines at all:\n\n\nftexplain on a correctly typed query: String[]\n\n\nA search that silently substitutes what was asked for owes the person a way back, which is correction=:off:\n\n\n[gutenberg-es]  la musica del corazon\n  1. Diccionario Bagobo-Español                  -121.829\n     Pipit ca casócot.--Menudear, repitir la petición.\n  2. Diccionario Bagobo-Español                  -94.831\n     Ca-polao.--Letargo, pertubación del jucio.\n\n[gutenberg-es]  la musica del corazon\n  1. The Spanish American Reader                 -134.627\n     [10] =Telegrama=, _telegram_. RELATED WORDS: =Dirección=, _address_; =firma=, _signature_; =telegrama al interior=, =telegrama al exterior= (or =cable…\n  2. Diccionario Bagobo-Español                  -121.829\n     Pipit ca casócot.--Menudear, repitir la petición.\n\n\n\nThis is the “showing results for … / search instead for …” pair every commercial search engine offers, and both halves are one keyword apart."
  },
  {
    "objectID": "tutorials/paragraph-search.html#expanding-a-query",
    "href": "tutorials/paragraph-search.html#expanding-a-query",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Expanding a query",
    "text": "Expanding a query\nThe expansion network relates each vocabulary token to its nearest neighbours in the LSI space the fit computed. prepare.jl turns it on when it creates each project — fit leaves it carried, and applying it is the indexer’s decision:\nbase = load_profile(\"tutorials/profiles/wikipedia-en.zip\")\nprof = TextSearch.with_applied(base; query_expansion=true)\ncreate_project(dir, \"wikipedia-en\"; engine=FullTextEngine, backend=BM25InvertedFile, textmodel=BaseProfile(prof))\nWhat the network holds for a few terms:\n\n\nanarchism    -&gt; anarchist, capitalism, individualist, Individualist, anarcho, anarchists\nphilosophy   -&gt; metaphysics, philosophical, epistemology, Immanuel, Sramana, Sramanas\nrevolution   -&gt; revolutions, Enlightenment, revolutionary, monde, mentalités, autonomy\n\n\nAnd what turning it off does to a search:\n\n\n[wikipedia-en]  anarchism\n  1. Anarcho-capitalism                          -124.245\n     Anarcho-capitalism Anarchist activists and scholars do not consider anarcho-capitalism as a part of the anarchist movement, arguing that anarchism has…\n  2. Anarcho-capitalism                          -121.518\n     Anarcho-capitalism Classification Anarcho-capitalism developed from radical American anti-state libertarianism and individualist anarchism. A strong c…\n\n[wikipedia-en]  anarchism\n  1. Anarchism                                   -18.357\n     Anarchism References Explanatory notes Citations General and cited sources Primary sources Secondary sources Tertiary sources Further reading Criticis…\n  2. Anarcho-capitalism                          -18.200\n     Anarcho-capitalism See also Agorism Anarcho-capitalism and minarchism Consequentialist libertarianism Counter-economics Creative disruption Crypto-ana…\n\n\n\nExpansion widens reach at the cost of precision, which is why it is a property of the query rather than of the index: the same project answers both ways, one keyword apart. expansion_k takes only the nearest few neighbours per token instead of the whole list, which is the middle setting between the two."
  },
  {
    "objectID": "tutorials/paragraph-search.html#building-a-project-from-scratch",
    "href": "tutorials/paragraph-search.html#building-a-project-from-scratch",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Building a project from scratch",
    "text": "Building a project from scratch\nEverything above opened projects prepare.jl had already built. Here is the whole path, live, on a small slice — this is what prepare.jl does, minus the scale:\n\nslice = map(Iterators.take(eachline(corpus_file(\"gutenberg-en\")), 2000)) do line\n    o = JSON3.read(line)\n    meta = Dict{String,Any}(String(k) =&gt; v for (k, v) in pairs(o) if k !== :text && k !== :doc_id)\n    SSE.TextItem(String(o.text); doc_id=String(o.doc_id), meta)   # typed in, typed out\nend\n\nworkdir = mktempdir()\nh = SSE.create_project(workdir, \"slice\";\n        engine    = SSE.FullTextEngine,\n        backend   = TextSearch.BM25InvertedFile,\n        textmodel = SSE.BaseProfile(profiles[\"gutenberg-en\"]))\n\nSSE.append_items!(h, slice)          # stages: durable, but nothing is searchable yet\nisempty(SSE.ftsearch(h, \"the whale\", 3))\n\ntrue\n\n\n\nquiet() do                           # see the note below on the engine's progress log\n    SSE.index!(h)                    # encodes and indexes the backlog\nend\n[r.doc_id for r in SSE.ftsearch(h, \"the whale\", 3)]\n\n3-element Vector{String}:\n \"gutenberg-en-2701-p576\"\n \"gutenberg-en-2701-p1200\"\n \"gutenberg-en-2701-p775\"\n\n\nquiet there is this page’s own helper, redirect_stderr(devnull) do … end. Every index kind installs an InformativeLog that reports insertion progress on stderr, which is the right default for a build that runs for an hour and the wrong one for a document. Nothing about it is configurable through create_project; redirecting the stream is how you turn it off.\nThe split between the two cells is the engine’s staging model: append_items! makes an item durable immediately and searchable never; index! is the explicit, idempotent step that makes it searchable. It is why the first cell finds nothing and the second finds everything, and it is what lets a caller decide when to pay for indexing.\nReusing a profile a project already fitted is the same two lines in the other direction:\n\nTextSearch.save_profile(joinpath(workdir, \"exported\"), SSE.text_profile(h))\nreloaded = TextSearch.load_profile(joinpath(workdir, \"exported\"))\n\n\n\nexported vocabulary: 136,716 tokens\n\n\n\nThe two alternatives, and why this page takes neither\nEvery project on this page was created from a BaseProfile, because prepare.jl had just fitted the profile and held it in hand. The other two forms matter more for a project you are starting from nothing.\nDefaultProfile is the one to reach for, and it is what this page would use if it were not building its own profiles as it goes:\ncreate_project(workdir, \"noticias\"; engine=FullTextEngine, backend=BM25InvertedFile,\n               textmodel=DefaultProfile(:es))\nAt the first index! call it loads the published Spanish paragraph profile — the same wiki20231101-es-paragraphs this tutorial’s own fits are modelled on — and refits it against whatever you staged: counts blended, weights recomputed, stopwords and lemmas and the expansion network inherited. It resolves from the profile library textsearch install maintains, and says how to install the profile if it is not there.\nThe refit narrows the vocabulary to your corpus rather than keeping the language’s: measured on the English base, 335,336 tokens become 12,238 against an 800-document sample. What it buys is calibration and artifacts, not coverage; refit=false keeps the base whole if coverage is what you are after. DefaultProfile’s docstring has the numbers.\nFitFromCorpus says, deliberately, that there is no pre-fitted model and one should be fitted from the project’s own corpus:\ncreate_project(workdir, \"self-fitted\"; engine=FullTextEngine, backend=BM25InvertedFile,\n               textmodel=FitFromCorpus(TextConfig(language=:en); min_ndocs=3, stopwords=0.9))\nThere is no default among the three, and the reason is a trap. That fit is delegated whole to TextSearch.fit_profile, which has no cheap variant — it always runs the LSI, the expansion network and the lemma clustering — so it is bounded by max_documents (1,000 by default) instead of by the corpus. Which means its vocabulary is fitted from a sample: every term appended later that the sample never held is out-of-vocabulary and silently dropped, forever. Nothing about the project’s behaviour afterwards distinguishes “I chose this” from “I never said”, which is exactly why it cannot be arrived at by omission — create_project refuses a text project with no textmodel.\nThe profiles table above is what that trap costs in the other direction: the fits on this page took 129 to 476 seconds each, on corpora of 41,000 to 265,000 paragraphs. That is the price of a real profile, paid once, by a script — and the reason a project should be handed one rather than asked to produce it during its first index!."
  },
  {
    "objectID": "tutorials/paragraph-search.html#what-building-this-cost",
    "href": "tutorials/paragraph-search.html#what-building-this-cost",
    "title": "Paragraph search over books and Wikipedia",
    "section": "What building this cost",
    "text": "What building this cost\nThe numbers below were measured by the build, not estimated:\n\n\n\n\n\n\nproject\nparagraphs\nvocabulary\ncreate\nappend\nindex\nms/paragraph\non disk\n\n\ngutenberg-en\n265,424\n136,716\n3s\n5s\n96s\n0.36\n278 MB\n\n\ngutenberg-es\n216,247\n337,336\n2s\n4s\n44s\n0.20\n201 MB\n\n\nwikipedia-en\n41,279\n139,776\n1s\n1s\n7s\n0.17\n66 MB\n\n\n\n\n\n\n\nThree things in that table are worth naming.\nIncremental persistence is cheap, and the first project pays for the whole run. Every push_item! reports an :add! event and the project’s callback writes that paragraph’s encoded object to RocksDB as its own record, which sounds expensive and is not: measured warm on 3,000 paragraphs, the raw TextSearch push loop takes 0.08 ms per paragraph and the engine’s index! takes 0.129 — 1.6× for durability at every step.\nThe index! column is worth reading with that in mind, because the first row is not comparable with the others. Julia compiles the indexing path on its first execution, and the first project built absorbs all of it: an earlier measurement here reported thirty times the library’s cost per paragraph, and it was measuring compilation. The lesson generalises past this table — the first timing in a fresh Julia session is a compilation measurement wearing a throughput measurement’s clothes.\nA search hit carries an id and a score, and nothing else. ftsearch answers with [SearchResult]s — _id, doc_id, distance, deleted — and getting from there to the paragraph means one fetch_items call per hit, as show_hits above does. That is one metadata read and one payload read per result, and a caller displaying a page of ten hits pays for ten of each.\nWriting this page is what found that: the first version of it could not print a paragraph at all. fetch_items used to return a Dict{String,Any} of the record merged with its metadata, and the paragraph was in neither — text was a reserved key, stripped on the way in — so the page reached through handle.engine.staged[…] into a private field. Both halves are fixed now: fetch_items returns a typed StoredItem whose payload is the indexed text or vector, read through the public IndexEngine.stored_payload.\nCreating a project against a large profile writes the whole profile into it. The create column is not project setup; it is the profile being serialised into the project’s own store so that reopening needs nothing but the project directory. That is the right default — a project that depends on a file somewhere else is a project that breaks when the file moves — but it does mean a shared profile is stored once per project that uses it."
  },
  {
    "objectID": "tutorials/paragraph-search.html#where-to-go-next",
    "href": "tutorials/paragraph-search.html#where-to-go-next",
    "title": "Paragraph search over books and Wikipedia",
    "section": "Where to go next",
    "text": "Where to go next\n\nGetting started — the smaller tour: dense vector search, soft deletes, filtering, recall calibration and the whole-dataset operations (allknn, fft, closestpairs).\nArchitecture — why the staging split exists, how a project is laid out on disk, and what each engine kind persists.\ntutorials/prepare.jl — the build itself, which is also the worked example of driving the engine over a corpus that does not fit in a page."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "SimilaritySearchEngine.jl",
    "section": "",
    "text": "SimilaritySearchEngine.jl is an embedded, in-process library for similarity search and information retrieval in Julia. It provides transactional, persistent storage for collections of dense vectors, sparse representations, and text documents, backed by SimilaritySearch.jl, TextSearch.jl, and RocksDB.jl.\nBy executing directly within the host process, SimilaritySearchEngine.jl eliminates the operational overhead of HTTP servers, background daemons, and inter-process communication layers. Access control is managed directly through standard operating system file permissions on the working directory."
  },
  {
    "objectID": "index.html#what-it-gives-you",
    "href": "index.html#what-it-gives-you",
    "title": "SimilaritySearchEngine.jl",
    "section": "What it gives you",
    "text": "What it gives you\n\nProject lifecycle: create_project, open_project, close_project! manage a project’s on-disk RocksDB directory and the live search engine backing it.\nA project says what it holds, and separately what holds it: engine= is DenseEngine, SparseEngine or FullTextEngine — dense vectors, sparse vectors, text — and backend= is the index underneath it, defaulting to that engine’s own (SearchGraph, InvertedFile, BM25InvertedFile). payload_kind(engine) answers the first question for a project you opened; BACKENDS lists the legal pairings.\nDense vector search over a SearchGraph (an approximate, self-tuning graph index), or an exact ExhaustiveSearch/ParallelExhaustiveSearch, all sharing the same append_items!/search surface — for a SearchGraph, append_items! only stages raw vectors, and the (expensive) graph-connection step happens in a separate, explicit index! call, so a caller controls when that cost is paid.\nSparse vector search over an InvertedFile, for vectors you encoded — a learned sparse retriever, a feature table, a set of ids. It takes SparseItems of a dimension fixed at creation, under any distance that reads a sparse vector (cosine over the weights, or one of Dist.Sets.* over the nonzero positions), and there is no vocabulary, no profile and no tokenizer anywhere in it. BM25 is deliberately not among its distances: it scores from corpus statistics rather than from a vector, so it belongs to a text project.\nFull-text search, where this package does the encoding, over a BM25InvertedFile or a weighted TextInvertedFile, sharing the same append_items!(stage)/index!(build)/ftsearch surface as the dense case — the first index! call on a text project also fits its TextProfile from whatever’s been staged so far, so there’s no separate training step to call first.\nA text model stated explicitly: creating a text project requires a textmodel, and there is no default. Prefer DefaultProfile(:es) — the published profile for a language, refitted to your own corpus at the first index! call, so your weights are calibrated over millions of paragraphs and the stopwords, lemmas and query-expansion network are inherited rather than derived (refit=false keeps the base’s language-wide vocabulary instead). BaseProfile(p) uses a profile you already hold. FitFromCorpus(TextConfig(language=:es)) is the explicit “no base profile” escape, delegated to TextSearch.fit_profile and capped at max_documents to bound its cost — which makes its vocabulary thin, and that is not a decision to arrive at by omission. Queries then travel with a QueryPolicy: whether to correct their spelling against the vocabulary (ftexplain reports what was corrected, correction=:off undoes it) and whether to widen them with the profile’s expansion network.\nTyped in, typed out: items are DenseItem/SparseItem/TextItem values and results are SearchResult/StoredItem/ExistsResult. No JSON-shaped dictionary is taken apart by key name on the way in or assembled on the way out; meta stays a free-form Dict{String,Any}, and get_raw_meta is the one raw path, reserved for the HTTP server.\nSoft deletes: delete_item! marks a document as logically deleted without touching the underlying index; search reports a deleted marker per candidate instead of silently hiding or backfilling it.\nRecall calibration: calibrate! runs SimilaritySearch.jl’s real hyperparameter search so later queries can ask for minrecall=0.95 (or any other target) and get back a BeamSearch tuned for it.\nWhole-dataset operations: allknn, fft (diverse sampling), closestpairs (near-duplicate detection), and bichromatic_kclosestpairs (closest pairs across two collections) all run synchronously, in-process, against a dense project’s index.\nIncremental, per-field persistence: every mutation writes only the storage it actually touched — a SearchGraph’s raw vectors go straight into a memory-mapped file, a soft delete never rewrites the index, and routine inserts never rewrite the whole engine state. See Architecture for the on-disk layout."
  },
  {
    "objectID": "index.html#where-this-fits",
    "href": "index.html#where-this-fits",
    "title": "SimilaritySearchEngine.jl",
    "section": "Where this fits",
    "text": "Where this fits\nThis package is deliberately scoped to project lifecycle, core CRUD, calibration, and the whole-dataset operations (allknn, fft, closestpairs, bichromatic_kclosestpairs).\nIt has two kinds of user, and the embedded API is the surface for both. A Julia program does using SimilaritySearchEngine and calls these functions directly — that is a first-class use, not a testing convenience, which is why the API takes and returns typed Julia values rather than JSON-shaped dictionaries. The sibling SimilaritySearchServer is the other: it wraps this same engine in a CLI and an HTTP server, and it is a consumer of this package rather than the reason it exists. Development happens here first, at the engine level, and is ported there afterwards — so a capability can exist here before it has a server counterpart (sparse projects do today), and a project touched through either stays readable by the other, same on-disk layout and same insertion/search/persistence semantics."
  },
  {
    "objectID": "index.html#where-to-go-next",
    "href": "index.html#where-to-go-next",
    "title": "SimilaritySearchEngine.jl",
    "section": "Where to go next",
    "text": "Where to go next\n\nArchitecture — the module map, the engine type hierarchy, the staging-vs-indexing split, the concurrency model, and the RocksDB-plus-mmap-file persistence layout, each with a diagram.\nTutorial — a hands-on walkthrough: create a dense project and a text project, insert, search, soft-delete, calibrate, and reopen.\nAPI reference — generated from the source docstrings with Documenter.jl, in docs/ (the standard location for a Julia package’s reference docs), not on this site."
  },
  {
    "objectID": "index.html#installation",
    "href": "index.html#installation",
    "title": "SimilaritySearchEngine.jl",
    "section": "Installation",
    "text": "Installation\nSimilaritySearchEngine.jl is not in the General registry yet, so it is installed from its repository. Its dependencies — SimilaritySearch.jl, TextSearch.jl, RocksDB.jl and the RocksDB_jll binaries — are all registered, so nothing else has to be cloned or built:\nusing Pkg\nPkg.add(url=\"https://github.com/sadit/SimilaritySearchEngine.jl\")\nJulia 1.12 or later is required. To modify the package, clone the repository and Pkg.develop(path=...) the clone; no Manifest.toml is committed, so Pkg.instantiate() resolves the base libraries from the registry unless you develop them by path as well."
  },
  {
    "objectID": "index.html#core-capabilities",
    "href": "index.html#core-capabilities",
    "title": "SimilaritySearchEngine.jl",
    "section": "Core Capabilities",
    "text": "Core Capabilities\n\nProject Lifecycle Management: Functions create_project, open_project, and close_project! manage the on-disk RocksDB directory and the associated live search engine.\nEngine and Backend Separation:\n\nPayload Engines: DenseEngine, SparseEngine, and FullTextEngine specify the stored data domain.\nIndex Backends: SearchGraph, ExhaustiveSearch, InvertedFile, and BM25InvertedFile define the underlying indexing data structures.\nThe function payload_kind(engine) identifies the data domain, and the BACKENDS registry enforces valid engine-backend pairings.\n\nDense Vector Search: Approximate nearest-neighbor search via SearchGraph with dynamic beam-search tuning, or exact brute-force search via ExhaustiveSearch and ParallelExhaustiveSearch. For SearchGraph, insertions (append_items!) and graph construction (index!) are decoupled to give callers explicit control over indexing latency.\nSparse Vector Search: Native inverted index search (InvertedFile) over user-encoded sparse representations of fixed dimension. Distance functions include cosine distance over weights and discrete set metrics (Dist.Sets.*) over non-zero coordinate supports.\nFull-Text Retrieval: Lexical search backed by BM25InvertedFile or TF-IDF-weighted TextInvertedFile. The initial index! call fits or initializes a TextProfile from staged texts, establishing the vocabulary and linguistic weighting model.\nExplicit Text Modeling: Text projects require an explicit AbstractTextModelSpec:\n\nDefaultProfile(:lang): Resolves a pre-trained language profile and refits vocabulary frequencies against the staged corpus.\nBaseProfile(p): Utilizes an existing, pre-computed TextProfile.\nFitFromCorpus(cfg): Fits a new profile directly from the staged corpus, bounded by max_documents.\nQuery execution accepts a QueryPolicy to control automatic orthographic correction and semantic query expansion.\n\nStrongly Typed Interface: Inputs are encapsulated in typed containers (DenseItem, SparseItem, TextItem), and queries return structured results (SearchResult, StoredItem, ExistsResult).\nLogical Deletions: The delete_item! function marks documents as logically deleted without mutating the underlying search index. Queries return candidates alongside an explicit deleted flag.\nTarget Recall Calibration: The calibrate! function executes hyperparameter optimization to calibrate beam-search configurations for target recall constraints (minrecall).\nWhole-Dataset Metric Operations: Synchronous, in-process execution of all-pairs nearest neighbors (allknn), diverse center selection (fft), closest pair discovery (closestpairs), and cross-dataset closest pairs (bichromatic_kclosestpairs).\nGranular Storage Persistence: High-throughput persistence utilizing a hybrid layout of RocksDB column families and dedicated memory-mapped vector files (dense_vectors.mmapdb).\nTyped Errors: Deliberate failures are raised as EngineError subtypes grouped into four categories (InvalidRequest, NotFound, ConflictingState, StorageFailure), so a caller can act on a failure without interpreting its message."
  },
  {
    "objectID": "index.html#ecosystem-role",
    "href": "index.html#ecosystem-role",
    "title": "SimilaritySearchEngine.jl",
    "section": "Ecosystem Role",
    "text": "Ecosystem Role\nSimilaritySearchEngine.jl provides the core embedded engine functionality. Higher-level services—HTTP REST APIs, asynchronous job workers, and command-line interfaces—are provided by SimilaritySearchServer, which lives in the same repository under server/ as a separate package with its own dependency set. It consumes this engine’s public API and nothing beneath it; the engine, in turn, knows nothing about HTTP status codes or exit codes."
  },
  {
    "objectID": "index.html#manual-navigation",
    "href": "index.html#manual-navigation",
    "title": "SimilaritySearchEngine.jl",
    "section": "Manual Navigation",
    "text": "Manual Navigation\n\nArchitecture: Detailed specifications of the module architecture, engine type hierarchy, staging-indexing split, concurrency model, and on-disk layout.\nGetting Started Tutorial: Step-by-step tutorial covering dense vectors, sparse indexing, BM25 text search, filtering, calibration, and whole-dataset operations.\nMultilingual Paragraph Search: Comprehensive evaluation over multilingual Project Gutenberg corpora and Wikipedia.\nAPI Reference: Comprehensive API documentation generated with Documenter.jl."
  },
  {
    "objectID": "tutorials/getting-started.html#the-reference-dataset-frankenstein",
    "href": "tutorials/getting-started.html#the-reference-dataset-frankenstein",
    "title": "Getting Started",
    "section": "The Reference Dataset: Frankenstein",
    "text": "The Reference Dataset: Frankenstein\nThe test directory contains test/data/frankenstein.jsonl, which provides the complete novel partitioned into 724 paragraphs with metadata annotations:\nusing JSON\n\nitems = [JSON.parse(l) for l in readlines(\"test/data/frankenstein.jsonl\")]\nlength(items)  # 724 paragraphs\nfirst(keys(items[1]))\nEach JSON object defines the following fields: - \"text\": The raw paragraph string. - \"doc_id\": An external string identifier (e.g., \"frankenstein_1\"). - \"keywords\": Named entities and character names identified in the paragraph (e.g., [\"Elizabeth\", \"Victor\"]). - \"ref\": The doc_id of the preceding paragraph, representing narrative sequence. - \"word_count\", \"verb_count\": Arbitrary quantitative metadata stored in the free-form meta JSON dictionary."
  },
  {
    "objectID": "tutorials/getting-started.html#dense-vector-search-with-lsi-searchgraph",
    "href": "tutorials/getting-started.html#dense-vector-search-with-lsi-searchgraph",
    "title": "Getting Started",
    "section": "Dense Vector Search with LSI (SearchGraph)",
    "text": "Dense Vector Search with LSI (SearchGraph)\n\n1. Creating a Dense Project\nTo initialize a project on disk, call create_project:\nusing SimilaritySearchEngine\n\nworkdir = mktempdir()\nh = create_project(workdir, \"frankenstein_dense\"; engine=DenseEngine, backend=SearchGraph, minrecall=0.9)\nThe engine keyword specifies the payload domain (DenseEngine), while backend defines the indexing algorithm (SearchGraph).\n\n\n2. Generating Semantic Embeddings via LSI\nTo generate continuous vector representations directly from text without external dependencies, we use Latent Semantic Indexing (LSI) from TextSearch.jl:\nusing TextSearch: LatentSemanticIndexing, outdim\nusing TextSearch.LSI: vectorize\n\ncorpus = [it[\"text\"] for it in items]\nlsi = LatentSemanticIndexing(corpus; maxoutdim=128, verbose=false)\noutdim(lsi)  # 128 dimensions\n\n\n3. Staging Vector Items\nWe partition the dataset, appending the first half (362 paragraphs) to the project and reserving the second half for subsequent bichromatic evaluations:\nhalf = length(items) ÷ 2\n\nbatch = [\n    DenseItem(vectorize(lsi, it[\"text\"]);\n              doc_id = it[\"doc_id\"], keywords = it[\"keywords\"], refs = it[\"ref\"],\n              meta = Dict{String,Any}(\"word_count\" =&gt; it[\"word_count\"],\n                                      \"verb_count\" =&gt; it[\"verb_count\"]))\n    for it in items[1:half]\n]\nappend_items!(h, batch)  # Stages 362 vectors into dense_vectors.mmapdb\n\n\n4. Constructing the Proximity Graph\nFor a SearchGraph backend, append_items! writes vectors to disk immediately but does not construct graph edges. To build graph connectivity across the staged backlog, invoke index!:\nindex!(h)  # Builds graph connections over the 362 staged items\n\n\n5. Executing Queries and Metadata Filtering\nOnce indexed, queries are evaluated using search:\nquery = vectorize(lsi, \"the monster and its creator\")\nresults = search(h, query, 5)\n\nfor r in results\n    println(\"Document ID: \", r.doc_id, \" | Distance: \", r.distance)\nend\nTo restrict query evaluation using metadata predicates, pass a filter function (record, meta) -&gt; Bool:\n# Filter results to include only paragraphs with non-empty keywords\nfiltered_results = search(h, query, 5; filter = (record, meta) -&gt; !isempty(record.keywords))\nFiltering is applied to a fixed number of candidates retrieved from the index, candidates, which defaults to 2k. A filtered search returns fewer than k results whenever fewer than k of those candidates satisfy the predicate, and it does so without raising an error: the alternative, enlarging the budget until k results are found, would let a selective predicate silently escalate into a scan of the entire collection, at two RocksDB reads per candidate.\nSize the budget to the selectivity of the predicate. A predicate satisfied by roughly one item in \\(n\\) requires approximately \\(n \\cdot k\\) candidates to fill a page of \\(k\\) results:\n# One document in ~26 carries this year: 20 candidates are not enough for 10 results\nrare = search(h, query, 10; filter = (record, meta) -&gt; meta[\"year\"] == 2005)                   # returns 0\nenough = search(h, query, 10; filter = (record, meta) -&gt; meta[\"year\"] == 2005, candidates=8192) # returns 10\n\n\n6. Batch Queries\nA set of queries is answered in a single parallel pass rather than one call at a time. Passing a vector of query vectors to search returns one result list per query, in the same order:\nqueries = [vectorize(lsi, t) for t in [\"the monster and its creator\",\n                                       \"the arctic expedition\",\n                                       \"letters to Margaret\"]]\nbatched = search(h, queries, 5)     # Vector{Vector{SearchResult}}\nbatched[2][1].doc_id\nsearchbatch returns the same neighbourhoods in raw form – two (k, length(queries)) matrices of internal ids and distances, with no metadata reads at all – which is the appropriate input for evaluation loops, offline scoring, or feeding another index:\nids, dists = searchbatch(h, queries, 5)\nsize(ids)     # (5, 3)\nMeasured over 50,000 dense vectors on 8 threads, searchbatch sustains 39,000 queries/s against 3,800 queries/s query-by-query; the hydrated form reaches 12,600 queries/s, the difference being the metadata read and decode per hit.\nBoth are whole-dataset operations rather than concurrent searches: they acquire the project’s write lock (exclusive, including with respect to other batch calls) and refuse to run while items remain staged but not indexed. This follows from their internal parallelism, which cannot be nested under the default thread scheduler. Text projects have no batch entry point, since their queries are strings the library resolves individually; call ftsearch in a loop.\n\n\n7. Logical Soft Deletions\nThe delete_item! function marks a document as deleted using its internal integer _id. The underlying proximity graph is not restructured:\ndelete_item!(h, 1)  # Mark internal ID 1 as deleted\n\nresults = search(h, query, 5)\nfirst(results).deleted  # Returns true if document 1 is among the top candidates\nQuery results return matching candidates with an explicit deleted::Bool indicator, allowing client applications to handle tombstoned records without index re-indexing.\n\n\n8. Calibrating Search Hyperparameters: calibrate!\nThe calibrate! function executes an internal hyperparameter optimization pass over the graph, calibrating BeamSearch parameters across multiple recall thresholds:\ncalibrate!(h; levels=[0.8, 0.9, 0.95, 0.97])\n\n# Execute search enforcing minimum 95% target recall\nprecise_results = search(h, query, 5; minrecall=0.95)\nWhat a recall target buys is paid for in distance computations, and the price is measurable. Pass a SearchStats instance and the engine records what that specific call cost:\nstats = SearchStats()\nsearch(h, query, 5; minrecall=0.8, stats)\ncheap = stats.distance_evaluations\n\nsearch(h, query, 5; minrecall=0.95, stats)\nprecise = stats.distance_evaluations     # larger: a wider beam visits more of the graph\nThe gap is the entire point of calibration. Over 20,000 random 32-dimensional vectors, a 5-nearest-neighbour query costs 242 distance computations at minrecall=0.8 and 927 at minrecall=0.95 – which, on that collection, is also what the uncalibrated default beam costs, out of 20,000 an exhaustive scan would perform.\nThe field is overwritten by each call rather than accumulated, so read it before issuing the next query. ftsearch and the filtered form of search accept stats as well, the latter reporting the cost of the candidate search it ran underneath rather than of the k results that survived the predicate. An exhaustive backend reports the size of the collection, by definition.\nThe count cannot be obtained any other way: a query runs on a context borrowed from the engine’s pool and returned when the call ends, so the counters reachable from outside afterwards belong to whatever has searched since. This is also how the server fills the distance_evaluations field of its operation log.\n\n\n9. Project Closure and Reopening\nTo close all active RocksDB handles and memory-mapped files:\nclose_project!(h)\nClosing a session that performed writes also compacts the project’s storage. This is not housekeeping: what a bulk ingestion leaves behind is a write-ahead log that the next open must replay, so the cost appears as open latency. On a 50,000-vector project, reopening costs 2.97 s without compaction and 0.48 s with it, for a compaction of 0.16 s. Pass close_project!(h; compact=false) when closing sits on a latency path, or call compact_project! mid-session after a large ingestion the same process will continue to serve.\nReopening the project restores both the stored vectors and graph connectivity:\nh2 = open_project(workdir, \"frankenstein_dense\")\nreopened_results = search(h2, query, 5)\nclose_project!(h2)"
  },
  {
    "objectID": "tutorials/getting-started.html#sparse-vector-search-sparseengine",
    "href": "tutorials/getting-started.html#sparse-vector-search-sparseengine",
    "title": "Getting Started",
    "section": "Sparse Vector Search (SparseEngine)",
    "text": "Sparse Vector Search (SparseEngine)\nSparseEngine indexes user-provided sparse representations (SparseItem) of fixed dimensionality using an InvertedFile backend.\n\nIndexing Pre-Encoded Sparse Vectors\nusing SimilaritySearch: Dist\nusing SparseArrays: sparsevec\n\nconst DIM = 1 &lt;&lt; 16  # 65,536 dimensions\n\n# Example sparse feature encoder (hashed token frequencies)\nfunction bag_of_words_hash(text::AbstractString)\n    counts = Dict{Int32, Float32}()\n    for w in split(lowercase(text), r\"[^\\p{L}\\p{N}]+\"; keepempty=false)\n        k = Int32(mod(hash(w), DIM) + 1)\n        counts[k] = get(counts, k, 0.0f0) + 1.0f0\n    end\n    ind = sort!(collect(keys(counts)))\n    val = Float32[counts[i] for i in ind]\n    sparsevec(ind, val ./ sqrt(sum(abs2, val)), DIM)  # Unit normalization for NormCosine\nend\n\n# Create sparse project\nhs = create_project(workdir, \"frankenstein_sparse\";\n                    engine=SparseEngine, dimension=DIM, distance=Dist.NormCosine())\n\n# Append sparse items\nappend_items!(hs, [SparseItem(bag_of_words_hash(it[\"text\"]); doc_id=it[\"doc_id\"], keywords=it[\"keywords\"])\n                   for it in items])\nindex!(hs)  # Idempotent no-op for InvertedFile; items are indexed during append\n\nhits = search(hs, bag_of_words_hash(\"the monster and its creator\"), 5)\nfor r in hits\n    println(r.doc_id, \" =&gt; Distance: \", round(r.distance; digits=4))\nend\n\n\nKey Properties of SparseEngine\n\nFixed Dimension Requirement: The dimension parameter must be specified at project creation and must match the coordinate dimension of all appended SparseVector objects.\nStrict Query Typing: Queries must be provided as SparseVector{Float32, Int32} matching the configured dimension.\nSupported Distance Metrics: Supports Dist.NormCosine over vector weights or discrete set distances (Dist.Sets.Jaccard, Dist.Sets.Dice) over non-zero coordinate supports."
  },
  {
    "objectID": "tutorials/getting-started.html#full-text-retrieval-with-bm25-fulltextengine",
    "href": "tutorials/getting-started.html#full-text-retrieval-with-bm25-fulltextengine",
    "title": "Getting Started",
    "section": "Full-Text Retrieval with BM25 (FullTextEngine)",
    "text": "Full-Text Retrieval with BM25 (FullTextEngine)\nFor text retrieval without manual vector encoding, FullTextEngine integrates text normalization, vocabulary management, and inverted file scoring.\nusing TextSearch: BM25InvertedFile\n\nhb = create_project(workdir, \"frankenstein_bm25\"; \n                    engine=FullTextEngine, backend=BM25InvertedFile,\n                    textmodel=FitFromCorpus())\n\n# Stage raw text paragraphs\nappend_items!(hb, [\n    TextItem(it[\"text\"]; doc_id = it[\"doc_id\"], keywords = it[\"keywords\"])\n    for it in items[1:200]\n])\n\n# First invocation fits the TextProfile and builds the BM25 inverted index\nindex!(hb)\n\n# Search using natural language queries\nhits = ftsearch(hb, \"monster creature fear\", 5)\nfor r in hits\n    println(r.doc_id, \" =&gt; Score: \", r.distance)\nend\n\nclose_project!(hb)\nA text project persists the inverted index itself, not the objects that were indexed, so reopening it assembles the index rather than recomputing it – 1.4 s against 20.4 s over 265,000 Project Gutenberg paragraphs. This holds for both text backends, BM25 and the weighted TextInvertedFile:\nhb2 = open_project(workdir, \"frankenstein_bm25\")\nftsearch(hb2, \"monster creature fear\", 5)     # same answers, no reindexing\nclose_project!(hb2)\nPosting lists are read from storage as queries ask for them, behind a cache of the most frequently used ones; their size is set at open time with postings_cache_max and postings_cache_base (the maximum number of cached lists, and the number it is trimmed back to when that maximum is exceeded). Per-document term vectors are read into memory instead, because BM25 scores every candidate from its own vector and a query containing a frequent term makes most of the collection a candidate. The architecture guide carries the measurements behind both halves of that choice.\nA project written by an earlier version, which stored the indexed objects instead of the index, opens with no index at all and says so when searched; index!(h) rebuilds it from the staged text, which was always persisted separately. That is the whole migration procedure, and it is explicit on purpose.\nAppending after the first index! writes only what the new block changes, so a project can keep growing without ever being rebuilt. The vocabulary, however, is fixed at the first index! under FitFromCorpus: words absent from the text staged by then stay out of vocabulary no matter how many blocks follow. Stage the corpus you intend to fit before calling index! the first time, or start from a BaseProfile."
  },
  {
    "objectID": "tutorials/getting-started.html#configuring-linguistic-models-abstracttextmodelspec",
    "href": "tutorials/getting-started.html#configuring-linguistic-models-abstracttextmodelspec",
    "title": "Getting Started",
    "section": "Configuring Linguistic Models (AbstractTextModelSpec)",
    "text": "Configuring Linguistic Models (AbstractTextModelSpec)\nText projects require an explicit linguistic model specification passed via the textmodel keyword:\n\n1. DefaultProfile (Recommended)\nLoads a published language profile (e.g., Spanish, English) and refits its vocabulary statistics against the staged corpus during the first index! execution:\nhes = create_project(workdir, \"news_es\"; engine=FullTextEngine, backend=BM25InvertedFile,\n                     textmodel=DefaultProfile(:es))\nThis inherits high-quality stopword lists, lemma clusters, and query-expansion networks from large-scale reference corpora while calibrating term frequencies to the target dataset.\n\n\n2. FitFromCorpus\nFits a TextProfile directly from the staged corpus without an external reference profile:\nusing TextSearch: NormalizationConfig\n\nhf = create_project(workdir, \"custom_corpus\"; engine=FullTextEngine, backend=BM25InvertedFile,\n    textmodel=FitFromCorpus(TextConfig(normalization=NormalizationConfig(lc=false, del_diac=false),\n                                       language=:es);\n                            min_ndocs=3, stopwords=0.9))\nThe fitting process is bounded by max_documents (default 1,000) to control compute time.\n\n\n3. BaseProfile\nInstantiates a text project from an existing, pre-computed TextProfile loaded from disk:\nhp = create_project(workdir, \"wiki_base\"; engine=FullTextEngine, backend=BM25InvertedFile,\n                    textmodel=BaseProfile(load_profile(\"wiki-es.zip\")))"
  },
  {
    "objectID": "tutorials/getting-started.html#query-resolution-and-expansion-querypolicy",
    "href": "tutorials/getting-started.html#query-resolution-and-expansion-querypolicy",
    "title": "Getting Started",
    "section": "Query Resolution and Expansion: QueryPolicy",
    "text": "Query Resolution and Expansion: QueryPolicy\nFull-text queries execute under a QueryPolicy that controls spelling variant correction and semantic expansion:\n# Default policy: automatic orthographic correction\nftsearch(hes, \"musica clasica\", 5)\n\n# Disable correction: search literal tokens as entered\nftsearch(hes, \"musica clasica\", 5; policy=QueryPolicy(correction=:off))\n\nExplaining Corrections: ftexplain\nThe ftexplain function details the specific token substitutions performed by the query pipeline:\nftexplain(hes, \"musica clasica\")"
  },
  {
    "objectID": "tutorials/getting-started.html#full-text-retrieval-with-tf-idf-textinvertedfile",
    "href": "tutorials/getting-started.html#full-text-retrieval-with-tf-idf-textinvertedfile",
    "title": "Getting Started",
    "section": "Full-Text Retrieval with TF-IDF (TextInvertedFile)",
    "text": "Full-Text Retrieval with TF-IDF (TextInvertedFile)\nTo index text using classic TF-IDF weights and cosine distance:\nusing TextSearch: TextInvertedFile\n\nht = create_project(workdir, \"frankenstein_tfidf\"; engine=FullTextEngine, backend=TextInvertedFile,\n                    textmodel=FitFromCorpus())\n\nappend_items!(ht, [\n    TextItem(it[\"text\"]; doc_id = it[\"doc_id\"], keywords = it[\"keywords\"])\n    for it in items[1:200]\n])\nindex!(ht)\n\nhits = ftsearch(ht, \"monster creature fear\", 5)\nclose_project!(ht)"
  },
  {
    "objectID": "tutorials/getting-started.html#whole-dataset-metric-operations",
    "href": "tutorials/getting-started.html#whole-dataset-metric-operations",
    "title": "Getting Started",
    "section": "Whole-Dataset Metric Operations",
    "text": "Whole-Dataset Metric Operations\nFor dense SearchGraph projects, SimilaritySearchEngine.jl provides whole-dataset algorithms that execute synchronously in-process:\nh2 = open_project(workdir, \"frankenstein_dense\")\n\n1. All-Pairs \\(k\\)-Nearest Neighbors: allknn\nComputes the \\(k\\) nearest neighbors for every indexed element:\nneighbors = allknn(h2; k=5)\nfirst(neighbors)  # Returns (id=1, neighbors=[...], dists=[...])\n\n\n2. Diverse Center Selection: fft\nExecutes Farthest First Traversal to select \\(k\\) well-separated representative centers:\nR = fft(h2, 8; verbose=false)\nR.centers      # Identifiers of the 8 chosen centers\nR.separation   # Minimum distance between any two centers\nR.covering     # Maximum distance from any object to its assigned center\n\n\n3. Monochromatic Closest Pairs: closestpairs\nFinds the \\(k\\) closest pairs of objects within the project:\npairs = closestpairs(h2; k=5)\nfor (i, j, d) in pairs\n    ri = get_metadata(h2.project, i)\n    rj = get_metadata(h2.project, j)\n    println(\"Pair (\", ri.doc_id, \", \", rj.doc_id, \") | Distance: \", d)\nend\n\n\n4. Bichromatic Closest Pairs: bichromatic_kclosestpairs\nFinds the \\(k\\) closest pairs between the indexed project \\(A\\) and an external matrix collection \\(B\\):\nusing SimilaritySearch: MatrixDatabase\n\nsecond_half_vectors = [vectorize(lsi, it[\"text\"]) for it in items[(half + 1):end]]\nB = MatrixDatabase(hcat(second_half_vectors...))\n\npairs = bichromatic_kclosestpairs(h2, B; k=5)\nfor (i, j, d) in pairs\n    ri = get_metadata(h2.project, i)\n    other = items[half + j]\n    println(\"Cross-match (\", ri.doc_id, \", \", other[\"doc_id\"], \") | Distance: \", d)\nend"
  },
  {
    "objectID": "tutorials/getting-started.html#metadata-retrieval-fetch_items-and-exists",
    "href": "tutorials/getting-started.html#metadata-retrieval-fetch_items-and-exists",
    "title": "Getting Started",
    "section": "Metadata Retrieval: fetch_items and exists",
    "text": "Metadata Retrieval: fetch_items and exists\nTo retrieve stored records by internal _id or external doc_id:\n# Retrieve full stored record including payload\nitems_fetched = fetch_items(h2, [\"frankenstein_2\"])\nit = only(items_fetched)\nit.doc_id         # \"frankenstein_2\"\nit.keywords       # Indexed keywords array\nit.meta           # Free-form metadata dictionary\nit.payload        # Stored vector or text\n\n# Check record existence and deletion status\nexists(h2, [1, \"frankenstein_2\", \"non_existent_id\"])\nAn external identifier is resolved through a dedicated column family rather than by scanning the collection: 0.05 ms against 59 ms for the equivalent scan, measured on a 50,000-item project. Nothing requires doc_id to be unique, so fetch_items returns every item carrying the requested identifier, in ascending _id order, and the result may therefore be longer than the list of ids it was given. Accordingly, exists reports an identifier as deleted only when every item carrying it has been soft-deleted.\nA numeric argument is read as an internal _id first, and only falls back to doc_id resolution when no record holds that _id."
  },
  {
    "objectID": "tutorials/getting-started.html#summary",
    "href": "tutorials/getting-started.html#summary",
    "title": "Getting Started",
    "section": "Summary",
    "text": "Summary\nThis tutorial demonstrated the core lifecycle of SimilaritySearchEngine.jl: 1. Project creation with explicit payload engines and indexing backends. 2. Ingestion staging via append_items! with deferred graph or vocabulary indexing via index!. 3. Nearest-neighbor querying with metadata filtering, an explicit candidate budget, and soft deletion handling. 4. Batch query execution, hydrated (search) or raw (searchbatch). 5. Hyperparameter calibration (calibrate!) for guaranteed target recall. 6. In-process execution of whole-dataset metric operations. 7. Indexed retrieval by external identifier, and storage compaction on close. 8. Failure handling through typed errors and their four categories."
  },
  {
    "objectID": "architecture.html#subsystem-architecture-and-module-map",
    "href": "architecture.html#subsystem-architecture-and-module-map",
    "title": "Architecture",
    "section": "Subsystem Architecture and Module Map",
    "text": "Subsystem Architecture and Module Map\nThe package is structured into four core submodules and an embedded orchestration layer:\n\nSchema: Defines the metadata record contract (Schema.MetadataRecord) and serialization interfaces.\nProject: Implements project metadata storage (ProjectManager), document ID indexing, and secondary key lookups.\nIndexEngine: Implements the AbstractSearchEngine hierarchy, encapsulating indexing algorithms from SimilaritySearch.jl and TextSearch.jl.\nPersistence: Provides storage adapters for RocksDB column families and memory-mapped files.\nembedded.jl: Implements EmbeddedEngine and exports the high-level transactional API (create_project, append_items!, search, etc.).\n\n\n\n\n\n\nflowchart LR\n    Schema[\"Schema&lt;br/&gt;&lt;small&gt;MetadataRecord (_id, doc_id,&lt;br/&gt;keywords, refs), meta JSON&lt;/small&gt;\"]\n    Project[\"Project&lt;br/&gt;&lt;small&gt;ProjectManager, metadata CRUD&lt;/small&gt;\"]\n    IndexEngine[\"IndexEngine&lt;br/&gt;&lt;small&gt;AbstractSearchEngine hierarchy&lt;/small&gt;\"]\n    Persistence[\"Persistence&lt;br/&gt;&lt;small&gt;EngineStore, AdjacencyStore, ...&lt;/small&gt;\"]\n    Embedded[\"embedded.jl&lt;br/&gt;&lt;small&gt;EmbeddedEngine + top-level API&lt;/small&gt;\"]\n    Caller([\"Host Julia script, or&lt;br/&gt;SimilaritySearchServer\"])\n\n    Schema --&gt; Project\n    Schema --&gt; Embedded\n    Project --&gt; Embedded\n    IndexEngine --&gt; Embedded\n    Persistence --&gt; Embedded\n    Embedded --&gt; Caller\n\n\n\n\n\n\n\nDecoupling of Subsystems\nIndexEngine and Persistence operate independently of Project and Schema: - IndexEngine is agnostic to RocksDB, interacting with persistence exclusively through structural callback hooks (index, sp, ep) -&gt; nothing. - Persistence stores serialized Julia objects under key-value interfaces without semantic knowledge of index internals. - embedded.jl orchestrates these components into a unified transactional engine.\n\n\nUniform Metadata Contract\nProjects use a standardized metadata record structure, Schema.MetadataRecord: - _id: Internal, sequential, zero-based or 1-based integer identifier aligned with vector storage indices. - doc_id: Caller-provided external string identifier. - keywords: Indexed string tags mapped via the secondary index idx_keywords. - refs: Indexed document reference links mapped via idx_refs. - meta: Arbitrary unstructured metadata stored as a serialized JSON object."
  },
  {
    "objectID": "architecture.html#decoupled-lifecycle-staging-vs.-indexing",
    "href": "architecture.html#decoupled-lifecycle-staging-vs.-indexing",
    "title": "Architecture",
    "section": "Decoupled Lifecycle: Staging vs. Indexing",
    "text": "Decoupled Lifecycle: Staging vs. Indexing\nSimilaritySearchEngine.jl decouples the ingestion of raw items from the computationally intensive construction of search index structures:\n[ Ingestion ] ──────&gt; append_items! ──────&gt; [ Staged Backlog ] ──────&gt; index! ──────&gt; [ Searchable Index ]\n                     (Durable in Store)                                              (Searchable in Engine)\n\nOperational Dynamics\n\nSearchGraph (Dense Engine):\n\nappend_items! appends raw Vector{Float32} arrays directly to the memory-mapped vector file dense_vectors.mmapdb. This operation is durable and fast (\\(O(1)\\) per vector), but does not construct graph edges.\nindex! evaluates the backlog range length(index)+1:length(database(index)) and builds proximity graph connections via SimilaritySearch.index!.\n\nFullTextEngine (Text Engine):\n\nappend_items! appends raw text strings to StagedTextStore within RocksDB.\nindex! processes the staged text backlog:\n\nOn the first invocation of an uninitialized project, index! executes the linguistic model specification (fitspec) to build the initial TextProfile and instantiate the inverted index.\nOn subsequent invocations, index! encodes staged strings and inserts posting list entries.\n\n\nExhaustiveSearch and InvertedFile (Sparse Engine):\n\nBecause exact linear scans evaluate directly over the database and sparse inverted files update posting lists upon insertion, append_items! makes items searchable immediately. index! operates as an idempotent no-op."
  },
  {
    "objectID": "architecture.html#persistence-architecture",
    "href": "architecture.html#persistence-architecture",
    "title": "Architecture",
    "section": "Persistence Architecture",
    "text": "Persistence Architecture\nSimilaritySearchEngine.jl employs a fine-grained, column-family-based storage architecture where mutations modify only the affected persistence segments.\n\n\n\n\n\nflowchart TB\n    subgraph db[\"RocksDB Project Directory\"]\n        records[(\"records&lt;br/&gt;&lt;small&gt;_id → MetadataRecord&lt;/small&gt;\")]\n        meta[(\"meta&lt;br/&gt;&lt;small&gt;_id → JSON meta blob&lt;/small&gt;\")]\n        oplog[(\"op_log\")]\n        idxkw[(\"idx_keywords&lt;br/&gt;&lt;small&gt;keyword → _ids&lt;/small&gt;\")]\n        idxrefs[(\"idx_refs&lt;br/&gt;&lt;small&gt;referenced doc_id → _ids&lt;/small&gt;\")]\n        idxdoc[(\"idx_docid&lt;br/&gt;&lt;small&gt;doc_id → _ids&lt;/small&gt;\")]\n        engine[(\"engine&lt;br/&gt;&lt;small&gt;Field-level engine state&lt;/small&gt;\")]\n        adj[(\"searchgraph_adj&lt;br/&gt;&lt;small&gt;object_id → direct neighbors&lt;/small&gt;\")]\n        stagedtext[(\"staged_text&lt;br/&gt;&lt;small&gt;Raw text blocks&lt;/small&gt;\")]\n        sparsedb[(\"sparse_db&lt;br/&gt;&lt;small&gt;Encoded object blocks&lt;/small&gt;\")]\n        postings[(\"invfile_postings&lt;br/&gt;&lt;small&gt;token → document ids&lt;/small&gt;\")]\n        docvecs[(\"invfile_docvecs&lt;br/&gt;&lt;small&gt;document → indexed vector&lt;/small&gt;\")]\n    end\n    mmapfile[[\"dense_vectors.mmapdb&lt;br/&gt;&lt;small&gt;Memory-Mapped Vector File&lt;/small&gt;\"]]\n\n    ProjectManager -- \"Metadata CRUD\" --&gt; records\n    ProjectManager -- \"Unstructured Meta\" --&gt; meta\n    ProjectManager -- \"Secondary Indices\" --&gt; idxkw\n    ProjectManager -- \"Secondary Indices\" --&gt; idxrefs\n    ProjectManager -- \"Secondary Indices\" --&gt; idxdoc\n    EngineStore -- \"save_field! / load_field\" --&gt; engine\n    AdjacencyStore -- \"save_neighbors! / load_neighbors\" --&gt; adj\n    StagedTextStore -- \"append_staged_texts!\" --&gt; stagedtext\n    InvertedFileObjectStore -- \"append_objects!\" --&gt; sparsedb\n    InvertedIndexStore -- \"write_invfile_block!\" --&gt; postings\n    InvertedIndexStore -- \"write_invfile_block! / load_invfile_docvecs\" --&gt; docvecs\n    append_dense[\"append_items! (Dense)\"] --&gt; mmapfile\n    append_text[\"append_items! (Text)\"] --&gt; stagedtext\n\n\n\n\n\n\n\nThe Persisted Text Index\nA text project stores its index: posting lists in invfile_postings, per-document vectors in invfile_docvecs. This covers both text backends – their posting lists hold the same thing, document ids, and they differ only in what each document is stored as. Three shapes occur, and each stored value carries a one-byte tag saying which: integer term frequencies (BM25), Float32 model weights (the weighted backend), and bags of words (the weighted backend under a set distance, which reads membership rather than weights). A sparse project still stores the objects it indexed, in sparse_db, and reconstructs its index on every open; its items are vectors the caller encoded, so what it saves is already what it would have to write, and giving it the same treatment is a follow-up rather than a difference of principle.\nThe difference is not small at scale: over 265,000 Project Gutenberg paragraphs, reconstruction costs 20.4 s and assembly from storage 1.4 s on BM25 and 1.7 s on the weighted backend (measured 2026-09-11 and 2026-09-12). Indexing the same corpus costs 25.7 s and 26.4 s respectively, against 147.3 s before, because a block is now encoded by the library’s parallel batch path rather than one document at a time.\nWriting is incremental and blockwise. Each index! call encodes its backlog as one block, splices the block’s document vectors and lengths into the live index, and hands its posting lists – ids shifted from block-local to global – to IndexEngine.persist_block!. The storage-backed implementation of that function performs a read-modify-write per token the block touched: appending is sound precisely because document ids grow monotonically, so a block’s ids are larger than every id already stored and the ascending order the query-time merge depends on is preserved. IndexEngine never names RocksDB; it dispatches on the kind of adjacency list its index was built with.\nThe unit of that write is the block, and its size has a cost: a token’s list is rewritten once per block that touches it, so the same 50,000 documents cost 5.8 s indexed in one block, 6.8 s in ten, and 10.1 s in fifty (measured 2026-09-12). Indexing in large blocks is therefore worth preferring, which is what append_items! followed by a single index! already does.\nPosting lists are lazy; document vectors are not, and the asymmetry is measured rather than assumed. A query reads one posting list per query term, so serving them from storage costs 1.8% in latency (66.8 ms against 68.0 ms) while removing 55.8 MB of 237.5 MB from memory. Scoring, by contrast, reads a candidate’s term vector (bm25score(..., idx.db[docID])), and the merge makes every document containing any query token a candidate – so a query with a frequent term turns lazy document vectors into one point read per document in the collection: 1,771.9 ms against 66.8 ms for the same query. Document vectors are therefore read into memory at open (0.76 s for that corpus).\nThe lazy posting lists sit behind a least-frequently-used cache bounded by a maximum number of cached lists, trimmed back to a base size when it is exceeded. Eviction happens only after a query has fully resolved, never inside one: concurrent searches hold read locks, not exclusive ones, and a list dropped mid-query would be re-read by whichever of them was still walking it.\nThere is no migration path from the previous format, deliberately. A project written before the index was persisted opens with its profile and its staged text intact and no index at all; searching it says so, and one index! call rebuilds it from that staged text, which was always persisted independently of the index. Keeping the old format readable would mean keeping the whole rebuild-by-reinsertion path alive to serve projects that, with no released version, do not yet exist – so the procedure is explicit and costs a line:\nh = open_project(workdir, dataset)\nindex!(h)\n\n\nExternal Identifier Resolution\ndoc_id is the caller’s own identifier, and it is resolved through the idx_docid column family rather than by scanning records. Keys are composite (doc_id bytes followed by the four bytes of _id), which is what allows several items to share one doc_id: fetch_items returns every record carrying the requested identifier, in ascending _id order, and exists reports it as deleted only when all of them are. Measured on a 50,000-item project, resolving an external identifier costs 0.12 ms through this index against 59 ms for the full scan it replaces.\nA project written before this index existed is migrated on its first writable open: Project.backfill_docid_index! performs a single pass over records and writes a marker key so no later open repeats it. A read-only handle opened against an unmigrated project falls back to the scan.\n\n\nCompaction\nBulk writes leave a write-ahead log that the next open must replay, so compaction is an open-latency concern rather than a write-latency one. close_project! therefore compacts every column family of the project before closing, unless the handle is read-only, the session performed no writes, or the caller passes compact=false; compact_project! exposes the same operation mid-session. Measured on a 50,000-vector dense project, reopening costs 2.97 s without compaction and 0.28 s with it, for a compaction that itself takes 0.03 s.\n\n\nStorage Subsystems\n\nEngineStore (Engine Field State):\n\nStores engine-level configuration as individual key-value entries (:minrecall, :opt_beamsearch, :deleted_ids, :profile).\nMutations (e.g., updating deleted IDs or storing calibration results) rewrite only their specific keys.\n\nDense Vector Storage (dense_vectors.mmapdb):\n\nDense vectors are stored in a dedicated memory-mapped binary file managed by MMapMatrixDatabase.\nBypasses key-value serialization overhead, providing high-speed sequential appends and zero-copy random access.\n\nAdjacencyStore (Graph Topology):\n\nGraph adjacencies are persisted incrementally through CallbackLog observer hooks during index!.\nStores only forward (direct) edges; reverse edges are reconstructed dynamically in memory during project reopening to ensure idempotency.\n\nStagedTextStore and InvertedFileObjectStore:\n\nStagedTextStore holds un-encoded raw text lines.\nInvertedFileObjectStore stores contiguous blocks of encoded posting-list objects.\n\n\n\n\nProject Reopening Lifecycle\n\n\n\n\n\nsequenceDiagram\n    participant Caller as open_project\n    participant MM as dense_vectors.mmapdb\n    participant ES as EngineStore\n    participant AS as AdjacencyStore\n    participant IE as IndexEngine\n\n    Caller-&gt;&gt;ES: load_field(:kind)\n    Caller-&gt;&gt;MM: Open memory-mapped vector file\n    Caller-&gt;&gt;ES: load_field(:graph_len)\n    Caller-&gt;&gt;AS: load_neighbors(i) for i in 1:graph_len\n    Caller-&gt;&gt;IE: build_searchgraph(distance, vectors, neighbors, graph_len)\n    IE-&gt;&gt;IE: Restore forward graph adjacencies\n    IE-&gt;&gt;IE: connect_reverse_links! over 1:graph_len\n    IE--&gt;&gt;Caller: Reconstructed DenseEngine{GraphBackend}\n\n\n\n\n\n\nWhen a project is reopened, stored forward edges are reloaded and bidirectional reverse links are regenerated, restoring the search index to its exact state prior to closure."
  },
  {
    "objectID": "architecture.html#dual-logging-architecture-reporters-vs.-observers",
    "href": "architecture.html#dual-logging-architecture-reporters-vs.-observers",
    "title": "Architecture",
    "section": "Dual Logging Architecture: Reporters vs. Observers",
    "text": "Dual Logging Architecture: Reporters vs. Observers\nContexts maintain two isolated logging channels: - Reporters (ctx.reporters): Receive progress messages via INFORM and output formatted status lines to stderr or custom IO streams. Throttled to prevent console flooding. - Observers (ctx.observers): Receive structural mutation events (:add!) via OBSERVE. Execute un-throttled callbacks that trigger persistence updates (CallbackLog).\n\n\n\n\n\nflowchart LR\n    Mutation[\"append_items! / index!\"] --&gt; INFORM[\"INFORM (ctx.reporters)\"]\n    Mutation --&gt; OBSERVE[\"OBSERVE :add! (ctx.observers)\"]\n    INFORM --&gt; Stderr[\"InformativeLog → stderr (throttled)\"]\n    OBSERVE --&gt; CallbackLog[\"CallbackLog → Persistence Trigger (un-throttled)\"]"
  },
  {
    "objectID": "architecture.html#soft-deletions-and-dataset-operations",
    "href": "architecture.html#soft-deletions-and-dataset-operations",
    "title": "Architecture",
    "section": "Soft Deletions and Dataset Operations",
    "text": "Soft Deletions and Dataset Operations\n\nSoft Deletions: The delete_item! function registers document identifiers in deleted_ids. The underlying index structure is preserved without requiring costly reorganization. Search queries evaluate candidates and attach an explicit deleted::Bool flag in SearchResult.\nWhole-Dataset Metric Operations: Functions allknn, fft, closestpairs, and bichromatic_kclosestpairs execute directly on the in-memory graph index, utilizing multithreaded batch routines to perform comprehensive metric analyses across the entire collection."
  },
  {
    "objectID": "architecture.html#failure-reporting-typed-errors-and-their-boundaries",
    "href": "architecture.html#failure-reporting-typed-errors-and-their-boundaries",
    "title": "Architecture",
    "section": "Failure Reporting: Typed Errors and Their Boundaries",
    "text": "Failure Reporting: Typed Errors and Their Boundaries\nEvery failure the engine raises deliberately is an EngineError, and the type is the contract. Four abstract categories sit beneath it, and a concrete type beneath each:\n\n\n\n\n\n\n\n\ncategory\nmeaning\nconcrete types\n\n\n\n\nInvalidRequest\nthe call cannot be honoured as written; retrying it unchanged fails again\nPayloadMismatch, WrongDimension, UnknownBackend, InvalidOption, UnsupportedOperation\n\n\nNotFound\nsomething the call named does not exist\nProfileNotInstalled\n\n\nConflictingState\nthe request is well formed but the project is not in a state that can serve it; the same call succeeds later\nPendingBacklog, NothingStaged, NoIndex, NotTrained, EmptyProject\n\n\nStorageFailure\nwhat was read back cannot be interpreted\nCorruptedStorage\n\n\n\nConcrete types carry structured detail where a caller can use it: PendingBacklog reports the staged and connected counts, WrongDimension the expected and received lengths, InvalidOption the keyword at fault.\nMapping lives at each boundary, never in the engine. The engine does not know what an HTTP status code or an exit code is. Its consumers translate:\ntry\n    search(handle, query, 10)\ncatch e\n    e isa ConflictingState && return retry_after_indexing()\n    e isa NotFound && return not_found()\n    rethrow()\nend\nSimilaritySearchServer maps categories to status codes (NotFound to 404, ConflictingState to 409, InvalidRequest to 400, anything else to 500) and its CLIs map the same categories to exit codes (4, 5, 2, 70), so a shell script can branch on the reason without reading output.\nThe alternative — bare error(\"...\") — forces every consumer to classify by message text, which breaks silently the first time the wording improves. That had already happened here: this engine’s own concurrency test recognized a refused whole-dataset operation with occursin(\"requires no pending backlog\", e.msg), and now matches PendingBacklog instead."
  },
  {
    "objectID": "tutorials/getting-started.html#handling-failures",
    "href": "tutorials/getting-started.html#handling-failures",
    "title": "Getting Started",
    "section": "Handling Failures",
    "text": "Handling Failures\nThe engine raises typed errors, and the type says why a call failed. Four categories carry the meaning, and a program acts on those rather than on the message text:\nusing SimilaritySearchEngine\n\ntry\n    searchbatch(h, queries, 10)\ncatch e\n    if e isa PendingBacklog\n        # Items were appended but not indexed yet. The engine reports both counts.\n        @info \"indexing $(e.staged - e.connected) pending item(s) first\"\n        index!(h)\n        searchbatch(h, queries, 10)\n    elseif e isa ConflictingState\n        rethrow()          # some other state problem: not trained, empty, no index\n    else\n        rethrow()\n    end\nend\nThe categories, and when each one appears:\n\n\n\n\n\n\n\n\ncategory\nmeaning\ntypical cause\n\n\n\n\nInvalidRequest\nthe call cannot be honoured as written\na DenseItem appended to a text project, a query of the wrong dimension, dimension on a dense project\n\n\nNotFound\nsomething the call named does not exist\nDefaultProfile(:es) with no profile installed\n\n\nConflictingState\nthe project is not in a state that can serve the request\nwhole-dataset operations against a staged backlog, ftsearch before the first index!\n\n\nStorageFailure\nstored data this version cannot interpret\na project written by a newer layout\n\n\n\nMatching a category catches every specific type beneath it, which is what makes this stable: a future EngineError added under ConflictingState is already handled by code that matches the category. Matching a concrete type (PendingBacklog, WrongDimension, InvalidOption) is for when the distinction matters, and those carry the detail — counts, expected dimension, the offending keyword."
  }
]